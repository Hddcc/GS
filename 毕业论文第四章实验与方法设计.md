# GaussianSR 任意尺度人脸超分第四章实验与方法设计

本文档是第四章方法实现、训练、测试、消融和论文撰写的唯一执行依据。数据准备已经结束，不再重新筛选、对齐、缩放、生成关键点或覆盖最终数据文件。

版本与记录规则：每次代码、配置、数据协议、训练、测试、指标、诊断、创新方向或结论出现新进展，都必须在同一次工作和 Git 提交中更新 `项目进展记录.md`。短程探针、准入分析和正式结果必须分开标记；没有通过准入的方案不得提前写成论文最终创新。

## 一、研究目标与模型关系

第四章使用 GaussianSR 作为 Baseline，研究方向高频信息与边缘结构监督对任意尺度人脸超分的作用，最终采用 A/B/C 三阶段关系：

```text
模型 A：原始 GaussianSR Baseline
→ 模型 B：方向高频查询残差融合
→ 模型 C：模型 B + 边缘感知加权重建（最终提出方法）
```

| 编号 | 模型 | 方向高频查询残差 | 边缘感知加权重建 |
|---|---|---:|---:|
| A | GaussianSR Baseline |  |  |
| B | Directional Frequency Residual | ✓ |  |
| C | Proposed Full Model | ✓ | ✓ |

比较关系：

```text
B - A：方向高频查询残差分支的贡献（5 epoch 准入分析）
C - B：边缘感知加权重建的贡献（5 epoch 准入分析）
C - A：完整提出方法的正式同种子对比
```

论文题目可采用：

> 基于频域建模与连续高斯表示的人脸超分辨率重建方法研究

第四章题目可采用：

> 基于方向高频查询残差与边缘感知优化的任意尺度人脸超分辨率重建

### 当前进度

- [x] 完成 FACE 数据准备和全量质量检查。
- [x] 完成模型 A 的 1,350 epoch 预实验训练。
- [x] 完成模型 A 在 CelebA、Helen 上的八倍率统一测试。
- [x] 完成 CelebA、Helen 的 Bicubic 八倍率统一测试。
- [x] 完成模型 B v1 的实现、冒烟测试和 1,350 epoch 预实验训练。
- [x] 完成模型 B v1 在 CelebA、Helen 上的八倍率统一测试。
- [x] 判定模型 B v1 提升不足，暂不进入模型 C。
- [x] 冻结并核验模型 B v1 的全部实验文件，九项 SHA-256 均为 `OK`。
- [x] 准备模型 B v1 参数诊断脚本 `diagnose_model_b.py`。
- [x] 在服务器完成旧随机协议与确定性协议的参数诊断，确认调制饱和且确定性评价下三个种子的 Gaussian 类别不一致率均为 0。
- [x] 使用确定性协议完成 CelebA 前 50 张图片的模型 A/B v1 ×2、×4、×8 复测，确认 B v1 仍无稳定优势。
- [x] 建立统一实验数据总账 `outputs/face_sr_experiment_ledger_20260725/GaussianSR人脸超分实验数据汇总.xlsx`。
- [x] 完成模型 B v2 代码、真实训练随机种子、倍率分组采样和明确的从头训练开关。
- [x] 在服务器完成模型 B v2 的 ×1.5、×4、×8 前向、反向和有限数值冒烟检查。
- [x] 完成模型 A/B v2 同协议 10 epoch 短实验，训练稳定且无 NaN、OOM 或运行异常。
- [x] 完成模型 B v2 的 10 epoch checkpoint 参数诊断，确认无饱和且评价确定，但调制残差和倍率响应仍接近零。
- [x] 完成 A/B 全量数据 5 epoch 探针；B v2 最终低 `0.0393 dB`，尚未通过正式训练准入。
- [x] 对全量探针 checkpoint 完成精简的单种子参数检查，停止无效的 B v2 几何调制版本。
- [x] 实现新的方向高频查询残差候选模型、探针/正式配置和专用冒烟脚本。
- [x] 在服务器完成新候选模型 ×1.5、×4、×8 前后向冒烟测试，残差梯度正常。
- [x] 完成方向高频查询残差模型 5 epoch 探针；最终低 Baseline `0.0831 dB`，结构单独使用未通过。
- [x] 完成边缘加权 L1 复测；最终仅低 `0.0012 dB`、整体持平，停止创新一结构迭代。
- [x] 使用原始 Baseline 完成连续倍率课程采样 5 epoch 探针；最终低 `0.0536 dB`，停止短探针试错。
- [x] 完成固定 Baseline 与边缘加权候选的 CelebA 50 图三倍率对照；候选 PSNR/SSIM 全升、LPIPS 全降，通过准入。
- [x] 正式训练固定种子 Baseline，再正式训练方向高频残差 + 边缘加权完整模型。
- [x] 已在服务器使用 `nohup` 启动固定种子正式 Baseline，运行名为 `face_gaussian_baseline_seed1_formal_v2protocol`。
- [x] 固定种子正式 Baseline 与正式创新模型均已完成 1,350 epoch 训练。
- [x] 完成 CelebA/Helen 八倍率 PSNR、SSIM、LPIPS 正式统一测试。
- [x] 将 48 条论文可用结果及逐倍率公式比较写入实验数据总账。
- [ ] 对两套正式 checkpoint、配置、日志、评价脚本、汇总 CSV 和逐图 CSV 执行 SHA-256 冻结。

旧运行 `face_gaussian_baseline_seed1` 和模型 B v1 虽然名称中带 `seed1`，实际没有固定训练随机种子，因此只能作为预实验。正式结论只使用 `face_gaussian_baseline_seed1_formal_v2protocol` 与 `face_gaussian_frequency_residual_edge_seed1_formal_v2protocol`。

## 二、数据集状态与使用限制

### 2.1 最终数据

本机最终数据集位于：

```text
D:\个人资料\项目\任意尺度\Dataset\FACE
```

服务器建议部署到：

```text
/root/userfolder_new/20260527GaussiSR/GaussianSR-main/Dataset/FACE
```

| 集合 | HR 图片 | 68 点 CSV | 用途 |
|---|---:|---:|---|
| CelebA train | 18,000 | 18,000 | 训练 |
| CelebA val | 500 | 500 | 选择 `epoch-best.pth` |
| CelebA test | 1,000 | 1,000 | 同数据集测试 |
| Helen test | 50 | 50 | 跨数据集测试 |

全部 19,550 张图片均为可解码的 128×128 RGB PNG；关键点 CSV 均为单行 68 点标注。最终质量报告满足 `errors=0`、`status=PASS`，图片内容重复组为 0。

### 2.2 最终目录

```text
FACE/
  CelebA/
    train/HR/*.png
    val/HR/*.png
    test/HR/*.png
  Helen/
    test/HR/*.png
  landmarks/
    CelebA/train/*.csv
    CelebA/val/*.csv
    CelebA/test/*.csv
    Helen/test/*.csv
  metadata/
    README.txt
    CelebA_train.txt
    CelebA_val.txt
    CelebA_test.txt
    Helen_test.txt
    checksums_sha256.txt
    source_files.tsv
    replacements.tsv
    duplicate_replacements.tsv
    landmark_failures.tsv
    qa_report.txt
    official/list_eval_partition.txt
```

### 2.3 数据使用规则

- 不重新运行 OpenFace。
- 不重新对齐、缩放、覆盖或筛选最终 PNG/CSV。
- 不按“前 N 张”重新生成训练、验证或测试划分。
- 固定划分以四个 TXT 清单为准。
- GaussianSR 从 HR 在线生成 Bicubic LR，不预生成 LR 文件。
- 关键点 CSV 不进入 A/B/C 的训练数据流，只在最终模型确定后用于 NME 评价。

服务器快速核验：

```bash
FACE_ROOT=/root/userfolder_new/20260527GaussiSR/GaussianSR-main/Dataset/FACE

find "$FACE_ROOT/CelebA/train/HR" -maxdepth 1 -type f -name '*.png' | wc -l  # 18000
find "$FACE_ROOT/CelebA/val/HR"   -maxdepth 1 -type f -name '*.png' | wc -l  # 500
find "$FACE_ROOT/CelebA/test/HR"  -maxdepth 1 -type f -name '*.png' | wc -l  # 1000
find "$FACE_ROOT/Helen/test/HR"   -maxdepth 1 -type f -name '*.png' | wc -l  # 50
find "$FACE_ROOT/landmarks" -type f -name '*.csv' | wc -l                    # 19550
grep -E '^(errors=0|status=PASS)$' "$FACE_ROOT/metadata/qa_report.txt"
```

## 三、禁止变化的统一实验协议

后续正式模型 A、B、C 必须保持以下条件一致，除非该项本身就是正在验证的变量。

### 3.1 训练配置

| 配置项 | 固定值 |
|---|---|
| 训练集 | `Dataset/FACE/CelebA/train/HR`，18,000 张 |
| 验证集 | `Dataset/FACE/CelebA/val/HR`，500 张 |
| 训练倍率 | `[1.5, 8]` 连续均匀采样 |
| `inp_size` | 16 |
| `sample_q` | 512 |
| `batch_size` | 12，三张 GPU |
| 优化器 | Adam |
| 初始学习率 | `1e-4` |
| `epoch_max` | 1350 |
| `epoch_val` | 10 |
| `epoch_save` | 50 |
| 学习率里程碑 | `[135,270,405,540,675,810,945,1080,1215]` |
| 学习率衰减 | `gamma: 0.5` |
| 验证倍率 | ×4 |
| checkpoint | 统一使用验证 ×4 PSNR 选择 `epoch-best.pth` |

训练配置中的核心字段：

```yaml
train_dataset:
  dataset:
    name: image-folder
    args:
      root_path: /root/userfolder_new/20260527GaussiSR/GaussianSR-main/Dataset/FACE/CelebA/train/HR
      repeat: 1
      cache: in_memory
  wrapper:
    name: sr-implicit-downsampled
    args:
      inp_size: 16
      scale_min: 1.5
      scale_max: 8
      augment: true
      sample_q: 512
      batch_per_gpu: 4
  batch_size: 12

val_dataset:
  dataset:
    name: image-folder
    args:
      root_path: /root/userfolder_new/20260527GaussiSR/GaussianSR-main/Dataset/FACE/CelebA/val/HR
      cache: in_memory
  wrapper:
    name: sr-implicit-downsampled
    args:
      inp_size: null
      scale_min: 4
      scale_max: 4
      augment: false
      sample_q: null
  batch_size: 1
```

### 3.2 初始化与随机种子

正式实验统一采用从头训练：

- 给 EDSR 模型工厂增加明确的 `use_pretrained: false`。
- 不再依赖 `models/weights/edsr_baseline_x2-1bc95232.pt` 恰好缺失。
- A、B、C 都不从前一个模型 checkpoint 继续训练。
- 给训练入口增加真实 `--seed` 或等价 YAML 字段。
- 固定 Python、NumPy、PyTorch、CUDA、DataLoader worker 和 shuffle 的随机状态。
- 固定 GPU 数量、PyTorch/CUDA/cuDNN 版本并记录环境。
- 共同结构在相同种子下保持相同初始化顺序；新增层按相同规则初始化。

若资源允许，使用 3 个种子报告均值和标准差；毕业论文最低要求是正式 A/B/C 使用同一个真实、可记录的种子完成公平对比。

日志中的：

```text
File not found: models/weights/edsr_baseline_x2-1bc95232.pt
```

表示 EDSR 预训练文件未加载，代码捕获异常后继续随机初始化训练。它不是数据路径错误，但也不能替代正式的 `use_pretrained: false` 配置开关。

### 3.3 测试协议

测试集固定为：

- CelebA test：1,000 张。
- Helen test：50 张。

测试倍率固定为：

```text
1.5、2、2.5、3.5、4、5.5、7.5、8
```

主要指标固定为：

- Y-PSNR，越高越好。
- Y-SSIM，越高越好。
- LPIPS-Alex，越低越好。

其他规则：

- 非整数倍率裁边固定为 `ceil(scale)`。
- 所有方法统一使用 `test_face_metrics.py`。
- 同时保存汇总 CSV 和逐图 CSV。
- 不混用原始 `test.py`、DIV2K 测试 YAML 或其他裁边规则。
- 最终模型确定后再统一增加 ArcFace 身份相似度和关键点 NME。
- VIF 仅在论文确实需要且所有方法可以统一计算时加入。

## 四、已完成的模型 A 与 Bicubic 预实验

### 4.1 模型 A

- [x] 使用 CelebA train 18,000 张训练 1,350 epoch。
- [x] 使用 CelebA val 500 张、×4 选择最佳 checkpoint。
- [x] 在 CelebA 和 Helen 上完成八倍率 Y-PSNR、Y-SSIM、LPIPS。
- [x] 冻结配置、日志和 Baseline 校验清单。

冻结目录不得覆盖：

```text
save/face_gaussian_baseline_seed1
results/face_baseline
```

由于训练入口没有真实设置 seed，模型 A 当前结果是预实验。它可以用于判断代码和数据流程，但修正模型 B 后必须用真实相同种子重新训练正式模型 A。

### 4.2 Bicubic

- [x] 完成 CelebA 八倍率统一指标。
- [x] 完成 Helen 八倍率统一指标。
- [x] 使用与模型相同的退化、裁边、颜色空间和 LPIPS 实现。

Bicubic 没有训练随机性，当前结果可以保留用于最终表格；若评价脚本发生实质变化，则所有方法必须统一重算。

## 五、创新一：方向高频查询残差融合及其结构探索

### 5.1 原始 GaussianSR 的局限

原始流程为：

```text
LR
→ EDSR 提取特征和 Gaussian logits
→ 从高斯参数库中选择参数
→ 二维高斯泼溅构建连续特征场
→ 隐式解码器根据查询坐标输出 RGB
```

原始实现会对展开后的不同图像和局部块的高斯参数取平均，使不同人脸区域共享相近的高斯几何。眼睛、眉毛、嘴唇、皮肤和头发具有不同方向频率特征，统一几何表达不利于局部结构建模。

### 5.2 模型 B 的结构

当前实现文件：

```text
models/gaussian_local_freq_scale.py
models/__init__.py
configs/train/face/train_face_gaussian_local_freq_scale.yaml
```

模型 B 包含四部分：

1. 保留每个局部块的 Gaussian logits，不再跨 batch/局部块平均参数。
2. 从局部 EDSR 特征提取水平、垂直、对角方向高频及总能量。
3. 将 `s、1/s、log(s)` 映射为连续倍率嵌入。
4. 融合方向频率和倍率特征，以有界残差调制 `sigma_x、sigma_y、rho、opacity`。

参数约束：

- `sigma_x、sigma_y` 使用正值下限和指数残差。
- `rho` 使用 `tanh` 和范围裁剪。
- `opacity` 在 logit 空间残差调制后使用 `sigmoid`。
- 最后一层零初始化或小值初始化，使初始行为接近 Baseline。

预期行为：

- 高频区域倾向于更小、方向性更强的高斯。
- 平滑区域倾向于更大、接近各向同性的高斯。
- 同一区域在不同倍率下的参数连续变化，而不是与倍率无关。

### 5.3 模型 B v1 已完成工作

- [x] 新增独立模型注册名，未覆盖原始 `gaussian-splatter`。
- [x] 调整 rasterization 张量维度，避免不同 batch 样本混合。
- [x] 完成 ×1.5、×4、×8 前向测试。
- [x] 完成反向传播和有限数值检查。
- [x] 完成 5 epoch 冒烟训练，无 NaN、Inf 或 OOM。
- [x] 完成三张 GPU、`batch_size: 12` 的 1,350 epoch 训练。
- [x] 最后阶段 loss 稳定在约 0.048，最终验证 PSNR 为 29.6960。
- [x] 完成 CelebA 和 Helen 八倍率测试。

模型 B v1 实际运行目录：

```text
save/face_gaussian_local_freq_scale_seed1_direct
results/face_model_b_direct
```

### 5.4 模型 B v1 结果结论

与模型 A 相比：

| 数据集 | 平均 PSNR 变化 | 平均 SSIM 变化 | 平均 LPIPS 变化 |
|---|---:|---:|---:|
| CelebA | 约 `+0.0021 dB` | 约 `+0.000073` | 约 `+0.000007`，略差 |
| Helen | 约 `+0.0090 dB` | 约 `+0.000112` | 约 `+0.000212`，变差 |

代表倍率：

- CelebA ×4 PSNR 约提高 `0.0041 dB`。
- CelebA ×8 PSNR 约下降 `0.0017 dB`。
- Helen ×4 PSNR 约提高 `0.0019 dB`。
- Helen ×8 PSNR 约提高 `0.0229 dB`，但 LPIPS 变差。

结论：模型 B v1 没有形成足以排除随机初始化影响的稳定优势，不能在论文中宣称创新一显著有效。当前不得用创新二掩盖创新一的问题。

### 5.5 模型 B v2：尺度-方向解耦局部高斯场

模型 B v2 不覆盖 B v1，新增文件和注册名为：

```text
models/gaussian_local_freq_scale_v2.py
gaussian-splatter-local-frequency-scale-v2
```

针对 B v1 的饱和与尺度主导问题，B v2 进行以下结构性调整：

1. 不再先对 8 个 EDSR 特征通道求平均，而是逐通道计算水平、垂直、对角 Haar 响应，再用 RMS 聚合，减少通道抵消。
2. 使用方向能量比例和局部归一化总能量构造频率描述，使局部方向信息不被全局尺度嵌入的数值幅度压制。
3. 将频率分支与尺度分支解耦：尺度分支预测整体 Gaussian 尺寸和四类频率调制增益，频率分支预测局部尺寸修正、各向异性、方向相关性和 opacity。
4. 将 `sigma_x/sigma_y` 重参数化为共同尺寸残差与正负对称的各向异性残差，避免两个轴由同一融合头无约束地同时饱和。
5. 使用 `softsign` 有界残差、较小的几何范围和四个可学习门控；所有输出头零初始化，使初始输出严格回到未调制的局部 Gaussian 参数。
6. 尺度输入改为 `[1.5,8]` 内归一化的对数倍率、平方项和正余弦项，保持连续倍率变化。

训练协议同时增加：

- `train_gaussian.py` 要求 YAML 或命令行提供真实 seed。
- checkpoint 保存并恢复 Python、NumPy、PyTorch、CUDA 和数据生成器状态。
- 模型初始化后重新对齐训练期随机状态，避免 A/B 参数数量不同导致 Gumbel 随机序列错位。
- 训练批次使用显式倍率分组，同一张 GPU 接收的连续 `batch_per_gpu=4` 个样本共享倍率。
- EDSR 编码器显式使用 `use_pretrained: false`，不再依赖权重文件恰好缺失。

正式与短实验配置：

```text
configs/train/face/train_face_gaussian_baseline_seed1.yaml
configs/train/face/train_face_gaussian_local_freq_scale_v2_seed1.yaml
configs/train/face/debug_face_gaussian_baseline_seed1.yaml
configs/train/face/debug_face_gaussian_local_freq_scale_v2_seed1.yaml
```

### 5.6 模型 B v2 的最终判定

全量 18,000 张训练图的 A/B 5 epoch 探针中，B v2 五个验证点相对 Baseline 分别为 `+0.0277、-0.0202、+0.0204、+0.0045、-0.0393 dB`，平均差 `-0.0014 dB`，没有稳定优势。

最后 checkpoint 的单种子诊断显示：`|delta_sigma|` 增长到约 `0.0009～0.0015`，说明分支能够更新；但频率能量与有效大小、各向异性的相关性接近 0，固定特征下倍率变化对 sigma 的影响仅约 `0.0001～0.0003`，对 rho 和 opacity 的影响接近 `1e-8`。因此 B v2 主要学到弱小的整体尺寸偏置，没有形成目标中的局部频率驱动几何。该版本停止，不进入 1,350 epoch 正式训练。

### 5.7 最终候选创新一：方向高频查询残差融合

为避免频率信号经过 Gaussian 几何约束后被削弱，新候选模型将高频信息直接接入连续 RGB 查询端，注册名为：

```text
gaussian-splatter-face-frequency-residual
```

实现文件和配置：

```text
models/gaussian_face_frequency_residual.py
configs/train/face/probe_face_gaussian_frequency_residual_seed1.yaml
configs/train/face/train_face_gaussian_frequency_residual_seed1.yaml
```

模块包括以下完整步骤：

1. 从 LR 人脸亮度计算水平、垂直及两条对角方向的固定高频响应。
2. 使用方向比例和局部归一化总能量构造稳定的方向频率描述。
3. 将频率描述与 EDSR/GaussianSR 编码特征融合，形成局部高频特征场。
4. 对每个连续查询坐标采样高频特征，并融合相对坐标、cell 和连续对数倍率编码。
5. 通过轻量 MLP 直接预测有界 RGB 残差，与原 GaussianSR 输出相加。
6. 输出层使用小值初始化并设置可学习残差门控，使初始行为接近 Baseline，同时从第一轮起允许梯度进入高频编码器。

该设计不是替换某个函数，而是新增“方向频率提取、连续倍率编码、查询级融合、有界 RGB 校正”完整路径。论文中创新一应以该最终候选为准；B v1/B v2 只作为实验设计过程记录。

正式模型中该结构与第七节的边缘感知加权重建共同使用。模型文件为 `models/gaussian_face_frequency_residual.py`，正式配置为 `configs/train/face/train_face_gaussian_frequency_residual_edge_seed1.yaml`。

## 六、候选结构筛选、诊断与准入过程

### 6.1 冻结模型 B v1

必须保留：

- `epoch-best.pth`：统一测试和最佳验证模型。
- `epoch-last.pth`：训练终点、恢复和收敛核验。
- `config.yaml`：实际运行配置快照。
- `log.txt`：逐 epoch 训练和验证记录。
- CelebA/Helen 汇总 CSV。
- CelebA/Helen 逐图 CSV。
- `test_face_metrics.py`。
- SHA-256 清单。

这些文件不是冗余文件，不得删除。周期 checkpoint 是否精简要等正式模型完成并确认不再恢复训练后另行决定；当前不删除。

在把确定性评价修正同步到服务器前，还要复制 B v1 使用的旧代码。checkpoint 只保存参数，不保存 Python 类实现；若直接覆盖 `models/edsr.py` 和评价脚本，将无法完整还原旧的随机评价行为。

```bash
cp models/edsr.py \
  "$OUT/frozen/models_edsr_stochastic_eval_v1.py"
cp models/gaussian.py \
  "$OUT/frozen/models_gaussian_v1.py"
cp models/gaussian_local_freq_scale.py \
  "$OUT/frozen/models_gaussian_local_freq_scale_v1.py"
cp datasets/wrappers.py \
  "$OUT/frozen/datasets_wrappers_v1.py"
cp test_face_metrics.py \
  "$OUT/frozen/test_face_metrics_stochastic_v1.py"

sha256sum \
  "$OUT/frozen/models_edsr_stochastic_eval_v1.py" \
  "$OUT/frozen/models_gaussian_v1.py" \
  "$OUT/frozen/models_gaussian_local_freq_scale_v1.py" \
  "$OUT/frozen/datasets_wrappers_v1.py" \
  "$OUT/frozen/test_face_metrics_stochastic_v1.py" \
  > "$OUT/frozen/model_b_v1_code_snapshot.sha256"
```

服务器冻结命令：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_local_freq_scale_seed1_direct
OUT=results/face_model_b_direct

mkdir -p "$OUT/frozen"

cp "$RUN/config.yaml" "$OUT/frozen/model_b_v1_config.yaml"
cp "$RUN/log.txt" "$OUT/frozen/model_b_v1_log.txt"

if [ -f "$OUT/train_console.log" ]; then
  cp "$OUT/train_console.log" "$OUT/frozen/model_b_v1_train_console.log"
fi

sha256sum \
  "$RUN/epoch-best.pth" \
  "$RUN/epoch-last.pth" \
  "$RUN/config.yaml" \
  "$RUN/log.txt" \
  test_face_metrics.py \
  "$OUT/celeba_metrics.csv" \
  "$OUT/celeba_metrics_per_image.csv" \
  "$OUT/helen_metrics.csv" \
  "$OUT/helen_metrics_per_image.csv" \
  > "$OUT/frozen/model_b_v1_manifest.sha256"
```

### 6.2 参数诊断目标

诊断脚本：

```text
diagnose_model_b.py
```

脚本只读取冻结 checkpoint 和测试图片，在指定结果目录新增诊断 CSV，不修改 checkpoint、数据集或既有评价结果。除高斯调制外，它还使用多个固定种子检查 `eval()` 状态下硬 Gumbel-Softmax 的 Gaussian 类别选择是否存在明显随机波动。

模型 B v1 的首次诊断已经完成：

- [x] 在固定 CelebA 图片上测试 ×2、×4、×8。
- [x] 分别记录基础参数、最终参数和调制残差。
- [x] 统计四类残差的幅度和饱和比例。
- [x] 统计频率能量与高斯有效大小、各向异性的相关性。
- [x] 隔离输入特征，检查尺度编码对 ×2、×4、×8 残差的影响。
- [x] 使用三个种子检查 `eval()` 下 Gaussian 类别选择波动。

首次只诊断 CelebA 中均匀抽取的 5 张图片：

```bash
mkdir -p results/face_model_b_direct/diagnostics

python diagnose_model_b.py \
  --dataset-root Dataset/FACE/CelebA/test/HR \
  --dataset-name CelebA \
  --model save/face_gaussian_local_freq_scale_seed1_direct/epoch-best.pth \
  --config save/face_gaussian_local_freq_scale_seed1_direct/config.yaml \
  --scales 2 4 8 \
  --reference-scale 4 \
  --seeds 1 2 3 \
  --num-images 5 \
  --gpu 3 \
  --output results/face_model_b_direct/diagnostics/celeba_model_b_v1.csv \
  | tee results/face_model_b_direct/diagnostics/celeba_model_b_v1.log
```

诊断结论：

- 调制分支并未接近零，而是明显过强。
- ×4、×8 的 `sigma_x/sigma_y` 约 85.7% 落入 `tanh` 饱和区。
- `rho` 在 ×2、×8 约有 73% 至 75% 饱和，opacity 约有 51% 至 61% 饱和。
- 频率能量与有效高斯大小只有弱负相关，与各向异性有弱到中等正相关，方向基本正确但不足以主导几何。
- 固定 ×4 输入特征时，×2 与 ×4 的残差差异很大，而 ×4 与 ×8 的 sigma 差异很小，说明尺度调制呈现分段饱和，不是理想的连续变化。
- 不同种子下 Gaussian 类别选择约有 27% 至 55% 不一致；调制残差不变，但基础高斯参数发生变化。

根因包括两部分：模型 B 的尺度调制压过局部频率并饱和；原始 EDSR 在 `eval()` 下仍调用硬 Gumbel-Softmax，导致测试随机。已将测试阶段改为 logits 的确定性 argmax one-hot，训练阶段仍保留软 Gumbel-Softmax，并给 `test_face_metrics.py` 增加 `--seed`。旧 checkpoint 不变，但 A/B v1 必须先按确定性协议进行小规模复测。

确定性修正后已使用相同 5 张 CelebA 图片、×2/×4/×8 和种子 1/2/3 重新诊断。所有调制残差范围、最终 Gaussian 参数范围及 Gaussian 类别不一致率均为 0，证明评价阶段的随机类别选择问题已经消除。该结果只证明评价可重复，不改变“模型 B v1 调制过强且提升不足”的结论。

随后在 CelebA 前 50 张图片上使用确定性协议复测模型 A 与模型 B v1：

| 倍率 | B-A PSNR-Y | B-A SSIM-Y | B-A LPIPS | 判定 |
|---:|---:|---:|---:|---|
| ×2 | -0.0116 | +0.000084 | +0.000411 | PSNR、LPIPS 退化 |
| ×4 | -0.0088 | -0.000105 | -0.000952 | 仅 LPIPS 改善 |
| ×8 | +0.0407 | +0.001235 | -0.000035 | 三项小幅改善 |

LPIPS 越低越好。虽然三个倍率的简单平均为 `ΔPSNR=+0.0068 dB`、`ΔSSIM=+0.000405`、`ΔLPIPS=-0.000192`，但平均值主要由 ×8 的小幅提升贡献，×2、×4 未形成一致优势。结合调制饱和诊断，模型 B v1 判定为不通过，不能进入模型 C，也不值得按当前结构重新进行 1,350 epoch 正式训练。

### 6.3 模型 B v2 的准入结果

- [x] 使用新的模型注册名、配置名和运行名，保留 B v1。
- [x] 实现真实随机种子、倍率分组采样和 `use_pretrained: false`。
- [x] 完成单图 ×1.5、×4、×8 前向测试。
- [x] 完成一次反向传播，所有 loss 和参数为有限值。
- [x] 完成 10 epoch checkpoint 参数诊断；频率方向相关性合理，但调制幅度和倍率响应过小，尚未证明新增分支有效参与预测。
- [x] 完成 A/B 同协议 10 epoch 冒烟训练，无 NaN、OOM 或运行异常。
- [x] 使用完整 18,000 张训练图完成 A/B 各 5 epoch 探针，达到约 7,500 次更新。
- [x] 使用一个种子和 5 张固定图片检查探针后的调制幅度及倍率响应。
- [x] 判定 B v2 未形成稳定提升且局部频率调制未达到设计目标。
- [x] 停止 B v2，不启动其 1,350 epoch 正式训练。

### 6.4 方向高频查询残差模型的准入结果

- [x] 通过 ×1.5、×4、×8 前向、反向和残差梯度冒烟测试。
- [x] 只训练该候选模型 5 epoch，与已完成的同协议 Baseline 探针比较；结构单独使用未通过。
- [x] 仅增加可选的边缘加权 L1 后复测一次，×4 验证 PSNR 与 Baseline 基本持平。
- [x] 在 CelebA 前 50 张的 ×2、×4、×8 上完成 PSNR/SSIM/LPIPS 统一评价。
- [x] 三种倍率 PSNR、SSIM 全部提高且 LPIPS 全部降低，批准进入正式训练。
- [x] 冻结最终创新一结构和损失，不再继续修改模块。
- [x] 固定种子 Baseline 和最终候选均完成 1,350 epoch 正式训练。
- [x] 完成 CelebA 1,000 图与 Helen 50 图的八倍率统一正式测试。

正式结果表明，最终候选的改进幅度较小但总体有效：CelebA 的 PSNR、SSIM、LPIPS 分别在 7、6、7 个倍率改善；Helen 分别在 6、8、5 个倍率改善。提升主要集中在中高倍率，Helen 的 LPIPS 结果存在混合，因此论文不得写成“所有数据集、所有倍率和所有指标全面领先”。

## 七、创新二：边缘感知加权重建目标

人脸超分中的眼睛、眉毛、嘴唇、鼻翼、轮廓和头发等区域只占部分像素，但对结构辨识和视觉质量更重要。普通像素 L1 对所有位置使用相同权重，容易让大面积平滑区域主导优化。为此，最终模型在不增加测试开销的前提下，引入仅在训练阶段使用的边缘感知加权 L1。

### 7.1 边缘权重构造

1. 将 HR RGB 图像按 `0.299R + 0.587G + 0.114B` 转换为亮度。
2. 计算水平和垂直一阶绝对差分，并组合为局部梯度能量。
3. 使用该图像的平均梯度能量进行归一化，并将异常大值截断到 4.0。
4. 令 `w = 1 + λe * e_norm`，再除以权重均值，使整张图的平均权重保持为 1。
5. 按训练查询坐标同步采样权重，确保 `pred、gt、weight` 一一对应。

正式配置固定 `edge_weight: 0.25`，没有继续搜索多个权重，避免扩大毕业论文实验量。

### 7.2 加权重建损失

```text
L_edge = mean(w * |I_pred - I_gt|)
```

当 `edge_weight=0` 时，数据封装器不返回 `weight`，训练自动退化为原始普通 L1。验证和测试阶段不使用边缘权重，因此不会改变评价协议，也不会增加模型参数量或推理时间。

### 7.3 实现位置与准入证据

```text
datasets/wrappers.py
train_gaussian.py
configs/train/face/train_face_gaussian_frequency_residual_edge_seed1.yaml
```

- 冒烟与 5 epoch 探针中权重张量形状为 `(512, 1)`，训练无 NaN、OOM 或运行错误。
- 方向高频残差单独使用时，5 epoch 最终验证 PSNR 比 Baseline 低 `0.0831 dB`。
- 加入边缘加权后，差距缩小至 `-0.0012 dB`；CelebA 前 50 图的 ×2、×4、×8 准入测试中，PSNR/SSIM 全部提高且 LPIPS 全部降低。
- 最终正式模型同时包含方向高频查询残差和边缘感知加权重建，已完成 1,350 epoch 训练与八倍率测试。

此前计划的“跨倍率高斯几何一致性”没有进入最终代码和正式实验，不再作为本文创新点，也不再启动新的 1,350 epoch 训练。

## 八、统一测试命令

CelebA：

```bash
python test_face_metrics.py \
  --method model \
  --dataset-root Dataset/FACE/CelebA/test/HR \
  --dataset-name CelebA \
  --model save/运行名称/epoch-best.pth \
  --config save/运行名称/config.yaml \
  --scales 1.5 2 2.5 3.5 4 5.5 7.5 8 \
  --gpu 3 \
  --output results/结果目录/celeba_metrics.csv
```

Helen：

```bash
python test_face_metrics.py \
  --method model \
  --dataset-root Dataset/FACE/Helen/test/HR \
  --dataset-name Helen \
  --model save/运行名称/epoch-best.pth \
  --config save/运行名称/config.yaml \
  --scales 1.5 2 2.5 3.5 4 5.5 7.5 8 \
  --gpu 3 \
  --output results/结果目录/helen_metrics.csv
```

每次测试后保存：汇总 CSV、逐图 CSV、终端日志、配置、checkpoint 哈希和评价脚本哈希。

## 九、最终实验与论文呈现

### 9.1 主对比方法

候选方法包括：

1. Bicubic。
2. LIIF。
3. LTE 或 ITSRN，选择一个可完整复现的方法。
4. GaussianSR Baseline。
5. 提出的完整模型 C。

其他论文结果只有在数据集、退化方式、颜色空间和裁边规则一致时才能直接引用；否则应统一协议重新测试，或在表中明确标注不可直接横向比较。

### 9.2 表格组织

论文正文建议使用三张核心结果表：

1. CelebA 主结果表：正文保留 ×2、×4、×8，连续倍率曲线或附表保留全部八倍率。
2. Helen 跨数据集结果表：正文保留 ×2、×4、×8，连续倍率曲线或附表保留全部八倍率。
3. 组件分析表：使用同协议 5 epoch 探针说明方向高频残差与边缘加权的筛选过程，明确标注为准入分析，不与 1,350 epoch 正式结果混排。

若后续完成一个可复现的外部任意尺度方法，再把它加入前两张主表；参数量和推理时间可以放在一张小表中，不必为了增加表数重复八个倍率。

#### 9.2.1 CelebA 正式八倍率结果

| Scale | Baseline PSNR | Proposed PSNR | ΔPSNR | Baseline SSIM | Proposed SSIM | ΔSSIM | Baseline LPIPS | Proposed LPIPS | ΔLPIPS |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1.5 | 41.4028 | 41.4025 | -0.0002 | 0.986933 | 0.986965 | +0.000032 | 0.005654 | 0.005651 | -0.000003 |
| 2.0 | 37.5225 | 37.5243 | +0.0018 | 0.970110 | 0.970201 | +0.000090 | 0.025976 | 0.025921 | -0.000055 |
| 2.5 | 35.1462 | 35.1489 | +0.0027 | 0.950803 | 0.950967 | +0.000164 | 0.046747 | 0.046249 | -0.000498 |
| 3.5 | 32.3548 | 32.3594 | +0.0046 | 0.913172 | 0.913377 | +0.000204 | 0.087112 | 0.086426 | -0.000685 |
| 4.0 | 31.3718 | 31.3817 | +0.0099 | 0.894269 | 0.894453 | +0.000184 | 0.108320 | 0.107704 | -0.000616 |
| 5.5 | 29.2378 | 29.2498 | +0.0119 | 0.845757 | 0.845816 | +0.000059 | 0.145636 | 0.144804 | -0.000833 |
| 7.5 | 27.1493 | 27.1613 | +0.0120 | 0.784833 | 0.784517 | -0.000316 | 0.208237 | 0.208670 | +0.000433 |
| 8.0 | 26.7214 | 26.7430 | +0.0216 | 0.770188 | 0.770118 | -0.000070 | 0.220357 | 0.220196 | -0.000161 |
| 八倍率均值差 |  |  | **+0.0080** |  |  | **+0.000044** |  |  | **-0.000302** |

CelebA 上 PSNR、SSIM、LPIPS 分别在 7、6、7 个倍率改善。PSNR 提升随倍率增大整体更明显，说明最终候选对较困难的中高倍率更有帮助；×7.5 和 ×8 的 SSIM 有极小回落，因此只表述为“整体改善”，不表述为全面领先。

#### 9.2.2 Helen 正式八倍率结果

| Scale | Baseline PSNR | Proposed PSNR | ΔPSNR | Baseline SSIM | Proposed SSIM | ΔSSIM | Baseline LPIPS | Proposed LPIPS | ΔLPIPS |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1.5 | 42.4254 | 42.4107 | -0.0147 | 0.989877 | 0.989888 | +0.000011 | 0.004127 | 0.004080 | -0.000047 |
| 2.0 | 38.5584 | 38.5306 | -0.0278 | 0.977821 | 0.977825 | +0.000004 | 0.019333 | 0.018861 | -0.000472 |
| 2.5 | 35.9657 | 36.0235 | +0.0579 | 0.963394 | 0.963698 | +0.000305 | 0.039328 | 0.038634 | -0.000694 |
| 3.5 | 32.9502 | 32.9934 | +0.0432 | 0.933290 | 0.933826 | +0.000536 | 0.084297 | 0.084113 | -0.000184 |
| 4.0 | 31.9708 | 32.0081 | +0.0373 | 0.917930 | 0.918488 | +0.000557 | 0.101366 | 0.101703 | +0.000336 |
| 5.5 | 29.9248 | 29.9315 | +0.0067 | 0.879278 | 0.879363 | +0.000085 | 0.144962 | 0.146769 | +0.001806 |
| 7.5 | 27.6639 | 27.7087 | +0.0448 | 0.823353 | 0.823996 | +0.000644 | 0.199765 | 0.201139 | +0.001373 |
| 8.0 | 27.3190 | 27.3361 | +0.0171 | 0.811328 | 0.812222 | +0.000894 | 0.213716 | 0.211815 | -0.001901 |
| 八倍率均值差 |  |  | **+0.0206** |  |  | **+0.000380** |  |  | **+0.000027** |

Helen 上 PSNR 在 6 个倍率提高、SSIM 在 8 个倍率全部提高、LPIPS 在 5 个倍率降低。×1.5 和 ×2 的 PSNR 有小幅下降，Helen 的平均 LPIPS 基本持平且略差 `0.000027`，因此结论限定为“结构相似度更稳定，中高倍率重建总体改善”。

### 9.3 消融表

| 模型 | 方向高频查询残差 | 边缘感知加权重建 | 训练性质 | 结论 |
|---|---:|---:|---|---|
| A |  |  | 5 epoch 同协议探针 | 最终验证 PSNR 28.1793 |
| B | ✓ |  | 5 epoch 同协议探针 | 28.0962，比 A 低 0.0831 dB |
| C | ✓ | ✓ | 5 epoch 同协议探针 | 28.1781，比 A 低 0.0012 dB；50 图三倍率准入通过 |

该表用于说明组件筛选过程，不冒充正式 1,350 epoch 消融。正式论文主结论来自 C-A 的全量同种子测试；同一数据集、同一倍率、同一指标内标粗最佳值，不在 CelebA 和 Helen 之间比较最佳值。

### 9.4 可视化和人脸指标

- 从 CelebA 和 Helen 分别固定选择 5 至 10 张测试图。
- 重点展示 ×4、×8，必要时增加非整数 ×3.5。
- 保存 LR、Bicubic、A、B、C、GT。
- 使用相同裁剪坐标放大眼睛、眉毛、鼻翼、嘴唇、牙齿和头发。
- 最终模型完成后统一增加 ArcFace 身份相似度。
- 使用现有 68 点 CSV 统一计算关键点 NME。

### 9.5 实验数据统一归档

实验指标、训练摘要、诊断结果和文件来源统一登记在：

```text
outputs/face_sr_experiment_ledger_20260725/GaussianSR人脸超分实验数据汇总.xlsx
```

该工作簿是本项目当前实验数据总账。`Paper Results` 已收录 16 条 Bicubic、16 条固定种子正式 Baseline 和 16 条正式提出方法指标，共 48 条论文可用数据；`Formal Comparison` 使用公式计算逐倍率差值、平均差值和改善倍率数。其余工作表保存必要的预实验、诊断、来源和结构草稿，并在标题或状态列明确标为分析用。模型 A/B v1、CelebA 50 图准入复测、机制诊断和早期 PSNR 均不得直接替代正式结果。

归档规则：

- 原始 CSV、逐图 CSV、日志、配置和 checkpoint 保持原位，不用工作簿替代或覆盖。
- 工作簿只做整理、公式比较、论文表筛选和来源索引；原始测量值不得手工改写。
- 论文撰写时只从 `Paper Results` 取数；分析用工作表中的数值不得作为最终实验结果。
- 每次新训练或测试完成后，先保存原始文件和 SHA-256，再向工作簿新增记录。
- 正式实验与预实验必须分开标记；当前模型 A/B v1 因训练种子未真实固定，均标记为 `Pre-experiment`。
- 若评价协议或脚本发生实质变化，必须对参与横向比较的全部模型统一重测，并保留旧记录而不是覆盖。

## 十、当前下一步

严格按以下顺序执行：

1. [x] 冻结并核验模型 B v1 的 checkpoint、配置、日志、CSV 和 SHA-256。
2. [x] 编写 `diagnose_model_b.py`。
3. [x] 完成 CelebA 5 张固定图片的 ×2、×4、×8、多种子诊断并解释结果。
4. [x] 修正测试阶段的随机 Gumbel-Softmax，增加统一评价种子。
5. [x] 重新运行诊断脚本，确认三个种子的 Gaussian 类别不一致率降为 0。
6. [x] 使用确定性协议在 CelebA 前 50 张上复测 A/B v1 的 ×2、×4、×8，确认 B v1 仅在 ×8 小幅改善，整体不通过。
7. [x] 根据确定性复测和饱和诊断设计并实现模型 B v2，不盲目叠加模块。
8. [x] 实现训练随机种子、倍率分组采样和明确的 `use_pretrained: false`。
9. [x] 完成模型 B v2 参数诊断；三倍率前向、反向、有限数值检查及 A/B 10 epoch 冒烟测试已通过，但约 100 次更新不足以激活零初始化调制分支。
10. [x] 使用完整 18,000 张训练图完成 A/B 各 5 epoch、约 7,500 次更新的中期探针；B v2 未形成稳定优势。
11. [x] 对 B v2 探针 checkpoint 完成单种子精简诊断，确认其未学习到有效局部频率调制并停止该版本。
12. [x] 实现方向高频查询残差候选模型，不再间接微调 Gaussian 几何参数。
13. [x] 在服务器只训练新模型 5 epoch 与已完成的 Baseline 探针对比；最终低 `0.0831 dB`，未通过。
14. [x] 保持网络不变，只增加边缘加权 L1 完成最后一次 5 epoch 复测；最终仅低 `0.0012 dB`，进入 50 图三倍率准入评价。
15. [x] 在原始 Baseline 上完成连续倍率课程采样 5 epoch 探针；五个验证点均低于 Baseline，停止该策略。
16. [x] 使用已有 checkpoint 完成 Baseline 与边缘加权候选的 50 图三倍率对照；候选三项指标在三个倍率上一致改善。
17. [x] 正式训练固定种子 Baseline，再正式训练方向高频残差 + 边缘加权完整模型。
18. [x] 完成 CelebA/Helen 八倍率统一正式测试，确认最终模型小幅但总体有效。
19. [x] 将正式原始指标、公式差值和限定性结论写入实验数据总账与本文档。
20. [ ] 在服务器冻结正式 checkpoint、配置、日志、评价脚本、汇总 CSV 和逐图 CSV，并验证 SHA-256。
21. [ ] 固定选择少量 CelebA/Helen 样本制作 Bicubic、Baseline、Proposed、GT 的 ×4/×8 可视化。
22. [ ] 完成毕业论文主结果、组件分析和可视化撰写；外部方法复现和效率表按时间选择性补充。

当前不再修改网络或损失，也不启动新的 1,350 epoch 训练。下一步只冻结正式实验文件和制作论文可视化。

### 10.1 当前服务器操作：冻结正式实验

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

OUT=results/face_formal_v2protocol
BASE=save/face_gaussian_baseline_seed1_formal_v2protocol
CAND=save/face_gaussian_frequency_residual_edge_seed1_formal_v2protocol

mkdir -p "$OUT/frozen"

cp "$BASE/config.yaml" "$OUT/frozen/baseline_config.yaml"
cp "$BASE/log.txt" "$OUT/frozen/baseline_log.txt"
cp "$CAND/config.yaml" "$OUT/frozen/proposed_config.yaml"
cp "$CAND/log.txt" "$OUT/frozen/proposed_log.txt"

sha256sum \
  "$BASE/epoch-best.pth" \
  "$BASE/epoch-last.pth" \
  "$BASE/config.yaml" \
  "$BASE/log.txt" \
  "$CAND/epoch-best.pth" \
  "$CAND/epoch-last.pth" \
  "$CAND/config.yaml" \
  "$CAND/log.txt" \
  train_gaussian.py \
  test_face_metrics.py \
  datasets/wrappers.py \
  models/gaussian_face_frequency_residual.py \
  "$OUT/baseline_console.log" \
  "$OUT/candidate_console.log" \
  "$OUT/metrics_console.log" \
  "$OUT"/metrics/*.csv \
  > "$OUT/frozen/formal_manifest.sha256"

sha256sum -c "$OUT/frozen/formal_manifest.sha256"
```

最后一条命令必须全部显示 `OK`。它只新增冻结副本和清单，不删除或覆盖任何训练结果。完成后反馈 `sha256sum -c` 的输出摘要，以及 `ls -lh "$OUT/frozen"`。

### 10.2 历史服务器操作记录：B v2 预检查与短实验

本机已生成保持相对目录结构的服务器补丁包：

```text
outputs/server_patches/GaussianSR_Bv2_server_patch_20260725.zip
SHA-256: 327B7DAE6C0766C2C522ECEFD91B8EC0DB3D2F6FFDA944CD3BDAD0DCEE52D65D
```

使用 Xftp 将该 ZIP 上传到服务器项目根目录：

```text
/root/userfolder_new/20260527GaussiSR/GaussianSR-main
```

先在服务器复制备份将被覆盖的七个旧文件，不删除任何文件：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

BACKUP="server_backups/before_bv2_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$BACKUP"
cp --parents \
  train_gaussian.py \
  datasets/wrappers.py \
  models/__init__.py \
  models/edsr.py \
  models/gaussian.py \
  diagnose_model_b.py \
  smoke_test_model_b.py \
  "$BACKUP/"
```

校验并查看 ZIP 内容：

```bash
sha256sum GaussianSR_Bv2_server_patch_20260725.zip
unzip -l GaussianSR_Bv2_server_patch_20260725.zip
```

哈希必须为：

```text
327B7DAE6C0766C2C522ECEFD91B8EC0DB3D2F6FFDA944CD3BDAD0DCEE52D65D
```

确认 ZIP 显示 13 个文件后，在项目根目录解压。`-o` 只覆盖补丁中同名代码，不删除其他文件：

```bash
unzip -o GaussianSR_Bv2_server_patch_20260725.zip
```

补丁包含以下新增或修改文件：

```text
train_gaussian.py
datasets/wrappers.py
models/__init__.py
models/edsr.py
models/gaussian.py
models/gaussian_local_freq_scale_v2.py
diagnose_model_b.py
smoke_test_model_b.py
validate_face_training_configs.py
configs/train/face/train_face_gaussian_baseline_seed1.yaml
configs/train/face/train_face_gaussian_local_freq_scale_v2_seed1.yaml
configs/train/face/debug_face_gaussian_baseline_seed1.yaml
configs/train/face/debug_face_gaussian_local_freq_scale_v2_seed1.yaml
```

在服务器项目根目录先运行静态编译：

```bash
python -m py_compile \
  train_gaussian.py \
  datasets/wrappers.py \
  models/edsr.py \
  models/gaussian.py \
  models/gaussian_local_freq_scale_v2.py \
  diagnose_model_b.py \
  smoke_test_model_b.py \
  validate_face_training_configs.py
```

验证短实验和正式实验的 A/B 协议分别一致：

```bash
python validate_face_training_configs.py \
  --baseline configs/train/face/debug_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/debug_face_gaussian_local_freq_scale_v2_seed1.yaml

python validate_face_training_configs.py \
  --baseline configs/train/face/train_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/train_face_gaussian_local_freq_scale_v2_seed1.yaml
```

使用空闲 GPU 3 完成 B v2 单模型前向和反向冒烟：

```bash
python smoke_test_model_b.py \
  --config configs/train/face/debug_face_gaussian_local_freq_scale_v2_seed1.yaml \
  --gpu 3 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器已完成该检查：参数量为 `1,853,276`，×1.5、×4、×8 均成功完成前向和反向传播，最终输出 `SMOKE TEST PASSED`。×4、×8 的调制器梯度按六位小数显示为 `0.000000`，实际为有限且非零的小量；因此代码可进入 10 epoch A/B 短实验，但在准入正式训练前必须结合短期验证轨迹和参数诊断判断调制分支是否有效学习。

只有出现 `SMOKE TEST PASSED` 后，才依次运行同协议的 A/B 10 epoch 短实验：

```bash
mkdir -p results/face_model_b_v2_smoke

python -u train_gaussian.py \
  --config configs/train/face/debug_face_gaussian_baseline_seed1.yaml \
  --name face_gaussian_baseline_seed1_smoke_v2protocol \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_model_b_v2_smoke/baseline_console.log

python -u train_gaussian.py \
  --config configs/train/face/debug_face_gaussian_local_freq_scale_v2_seed1.yaml \
  --name face_gaussian_local_freq_scale_v2_seed1_smoke \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_model_b_v2_smoke/model_b_v2_console.log
```

该 A/B 10 epoch 短实验已完成。它只使用 120 张训练图和 10 张验证图，属于结构准入分析，不作为论文最终结果：

| Epoch | Baseline PSNR | B v2 PSNR | B v2 - Baseline |
|---:|---:|---:|---:|
| 1 | 11.4360 | 11.4359 | -0.0001 |
| 2 | 13.7399 | 13.3593 | -0.3806 |
| 3 | 15.7999 | 15.8247 | +0.0248 |
| 4 | 17.7570 | 17.6553 | -0.1017 |
| 5 | 17.6443 | 17.6156 | -0.0287 |
| 6 | 18.7139 | 18.9171 | +0.2032 |
| 7 | 19.8594 | 19.5318 | -0.3276 |
| 8 | 19.8654 | 20.0548 | +0.1894 |
| 9 | 20.0561 | 20.0263 | -0.0298 |
| 10 | 20.4374 | 20.4740 | +0.0366 |

两者均正常收敛，B v2 最终高 `0.0366 dB`，但逐 epoch 领先关系不稳定，十个验证点平均仍低 `0.0415 dB`。因此该结果仅构成轻微正向信号，不能单独批准正式训练；下一步必须检查残差幅度、饱和率、频率相关性和尺度敏感性。

短实验完成后反馈完整的 10 epoch 验证轨迹和异常扫描结果，暂不运行正式配置：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_baseline_seed1_smoke_v2protocol/log.txt

grep -nE "epoch .*val:" \
  save/face_gaussian_local_freq_scale_v2_seed1_smoke/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_model_b_v2_smoke/baseline_console.log \
  results/face_model_b_v2_smoke/model_b_v2_console.log \
  || true
```

完成短实验后，使用 B v2 的最佳 checkpoint 在固定 CelebA 测试图上执行参数诊断。该命令只新增过程分析文件，不修改任何已有文件：

```bash
mkdir -p results/face_model_b_v2_smoke/diagnostics

python diagnose_model_b.py \
  --dataset-root Dataset/FACE/CelebA/test/HR \
  --dataset-name CelebA \
  --model save/face_gaussian_local_freq_scale_v2_seed1_smoke/epoch-best.pth \
  --config save/face_gaussian_local_freq_scale_v2_seed1_smoke/config.yaml \
  --scales 1.5 4 8 \
  --reference-scale 4 \
  --seeds 1 2 3 \
  --num-images 5 \
  --gpu 3 \
  --output results/face_model_b_v2_smoke/diagnostics/celeba_model_b_v2_10epoch.csv \
  | tee results/face_model_b_v2_smoke/diagnostics/celeba_model_b_v2_10epoch.log
```

诊断完成后反馈三个汇总文件：

```bash
cat results/face_model_b_v2_smoke/diagnostics/celeba_model_b_v2_10epoch.csv
cat results/face_model_b_v2_smoke/diagnostics/celeba_model_b_v2_10epoch_scale_only.csv
cat results/face_model_b_v2_smoke/diagnostics/celeba_model_b_v2_10epoch_seed_variation.csv
```

该诊断已经完成，属于过程分析，不进入论文最终指标表。主要结论如下：

- 四类残差饱和率均为 0，B v1 的饱和问题已经消除。
- `|delta_sigma|` 约为 `5.2e-5` 至 `1.55e-4`，`|delta_rho|` 约为 `1.76e-5`，`|delta_opacity|` 约为 `2.17e-4`，相对允许范围接近零。
- 固定 ×4 输入特征，仅改变目标倍率时，×1.5 与 ×4 的 sigma 差异约 `2.07e-6`，×8 与 ×4 约 `9.24e-6`；rho 和 opacity 的尺度响应近乎为 0。
- 三个评价种子的所有参数范围及 Gaussian 类别不一致率均为 0，确定性评价生效。
- 频率能量与最终有效大小呈 `-0.18` 至 `-0.30` 的相关、与各向异性呈约 `+0.24` 的相关，方向符合预期；但由于调制残差过小，该相关性主要可能来自基础 Gaussian 选择，不能证明新增分支有效。

短实验每个模型只有约 100 次参数更新，不能据此直接修改结构，也不能批准 1,350 epoch。下一步使用完整 18,000 张训练图做 5 epoch 探针，每个模型约 7,500 次更新。两个探针配置与正式配置只在 `epoch_max、epoch_val、epoch_save` 三项上不同，学习率里程碑保持从 epoch 135 开始，探针期间不降学习率。

本机探针配置补丁：

```text
outputs/server_patches/GaussianSR_Bv2_full_data_probe_configs_20260725.zip
SHA-256: 39FC4C8DF9DDDDE8C8FB27F7051C201E8F5F22DA39BC5F4B668F920F57948F4D
```

使用 Xftp 上传到服务器项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_Bv2_full_data_probe_configs_20260725.zip
unzip -l GaussianSR_Bv2_full_data_probe_configs_20260725.zip
unzip -o GaussianSR_Bv2_full_data_probe_configs_20260725.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_local_freq_scale_v2_seed1.yaml
```

确认哈希一致、ZIP 只有两个配置且显示 `CONFIG VALIDATION PASSED` 后，使用 GPU 0、1、2 顺序运行：

```bash
(
  set -euo pipefail

  for p in \
    save/face_gaussian_baseline_seed1_probe_full5 \
    save/face_gaussian_local_freq_scale_v2_seed1_probe_full5 \
    results/face_model_b_v2_full5_probe/baseline_console.log \
    results/face_model_b_v2_full5_probe/model_b_v2_console.log
  do
    if [ -e "$p" ]; then
      echo "STOP: 路径已经存在，未覆盖：$p"
      exit 1
    fi
  done

  mkdir -p results/face_model_b_v2_full5_probe

  python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
    --name face_gaussian_baseline_seed1_probe_full5 \
    --gpu 0,1,2 \
    --seed 1 \
    2>&1 | tee results/face_model_b_v2_full5_probe/baseline_console.log

  python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_local_freq_scale_v2_seed1.yaml \
    --name face_gaussian_local_freq_scale_v2_seed1_probe_full5 \
    --gpu 0,1,2 \
    --seed 1 \
    2>&1 | tee results/face_model_b_v2_full5_probe/model_b_v2_console.log
)
```

完成后反馈：

```bash
grep -nE "epoch .*val:" save/face_gaussian_baseline_seed1_probe_full5/log.txt
grep -nE "epoch .*val:" save/face_gaussian_local_freq_scale_v2_seed1_probe_full5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_model_b_v2_full5_probe/baseline_console.log \
  results/face_model_b_v2_full5_probe/model_b_v2_console.log \
  || true
```

全量数据 5 epoch 探针已正常完成，无需写入论文主结果表：

| Epoch | Baseline PSNR | B v2 PSNR | B v2 - Baseline |
|---:|---:|---:|---:|
| 1 | 26.9929 | 27.0206 | +0.0277 |
| 2 | 27.4621 | 27.4419 | -0.0202 |
| 3 | 27.7418 | 27.7622 | +0.0204 |
| 4 | 27.9767 | 27.9812 | +0.0045 |
| 5 | 28.1793 | 28.1400 | -0.0393 |

五个验证点的平均差为 `-0.0014 dB`，可视为基本持平；最终点 B v2 低 `0.0393 dB`，没有形成稳定优势。为控制毕业论文工作量，后续不再重复多种子和多数据集诊断，只对 5 epoch 的最后 checkpoint 做一次单种子检查：

```bash
mkdir -p results/face_model_b_v2_full5_probe/diagnostics

python diagnose_model_b.py \
  --dataset-root Dataset/FACE/CelebA/test/HR \
  --dataset-name CelebA \
  --model save/face_gaussian_local_freq_scale_v2_seed1_probe_full5/epoch-last.pth \
  --config save/face_gaussian_local_freq_scale_v2_seed1_probe_full5/config.yaml \
  --scales 1.5 4 8 \
  --reference-scale 4 \
  --seeds 1 \
  --num-images 5 \
  --gpu 3 \
  --output results/face_model_b_v2_full5_probe/diagnostics/celeba_model_b_v2_full5.csv \
  | tee results/face_model_b_v2_full5_probe/diagnostics/celeba_model_b_v2_full5.log

sed -n '/Summary:/,$p' \
  results/face_model_b_v2_full5_probe/diagnostics/celeba_model_b_v2_full5.log

cat results/face_model_b_v2_full5_probe/diagnostics/celeba_model_b_v2_full5_scale_only.csv
```

根据这一次输出直接二选一：若残差仍接近零，则只调整调制头初始化和门控强度后复测 B v2；若残差已明显增大但指标仍不提升，则停止 B v2 当前结构，改用更直接的轻量人脸高频残差分支。两种情况都只再做一次 B 单模型 5 epoch 复测，不重跑 Baseline 探针。

上述诊断已经完成，实际属于第二种情况，因此停止 B v2，执行新的方向高频查询残差候选模型。服务器补丁为：

```text
outputs/server_patches/GaussianSR_face_frequency_residual_patch_20260726.zip
SHA-256: 29C0E30DA2F2A4DA802D3C774C0CC246AADF83311B3E4132C47301430CA9896C
```

使用 Xftp 上传到服务器项目根目录。补丁只覆盖 `models/__init__.py`，其余四个文件均为新增文件。先备份被覆盖文件并校验补丁：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

BACKUP="server_backups/before_frequency_residual_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$BACKUP"
cp --parents models/__init__.py "$BACKUP/"

sha256sum GaussianSR_face_frequency_residual_patch_20260726.zip
unzip -l GaussianSR_face_frequency_residual_patch_20260726.zip
unzip -o GaussianSR_face_frequency_residual_patch_20260726.zip

python -m py_compile \
  models/gaussian_face_frequency_residual.py \
  smoke_test_face_frequency_residual.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_frequency_residual_seed1.yaml \
  --expected-model-b gaussian-splatter-face-frequency-residual
```

首次运行时发现旧校验器将 B v2 注册名硬编码在脚本中，新候选配置因此被正确读取但被旧名称规则拒绝；这不是模型或配置错误。兼容性修复补丁为：

```text
outputs/server_patches/GaussianSR_config_validator_fix_20260726.zip
SHA-256: A379F3561DEA6C34D757DBA6AAA04F114D1853084AC2B6A72B30C01A1731B4E1
```

修复后可通过 `--expected-model-b` 指定候选注册名，不传该参数时仍默认校验 B v2。

确认哈希一致、ZIP 只有 5 个文件、静态编译无输出且配置显示 `CONFIG VALIDATION PASSED` 后，在 GPU 3 冒烟：

```bash
python smoke_test_face_frequency_residual.py \
  --config configs/train/face/probe_face_gaussian_frequency_residual_seed1.yaml \
  --gpu 3 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器冒烟已通过：模型参数量为 `1,896,803`，初始残差门控为 `0.5`；×1.5、×4、×8 的 RGB 残差绝对均值分别为 `2.511e-5、1.967e-5、1.339e-5`，残差分支梯度范数分别为 `0.06169、0.06099、0.06135`。初始修正足够小且三种倍率均有明确非零梯度，可以进入单模型 5 epoch 探针。

只有显示 `SMOKE TEST PASSED` 才运行一次新模型 5 epoch 探针；不重跑 Baseline：

```bash
mkdir -p results/face_frequency_residual_full5_probe

python -u train_gaussian.py \
  --config configs/train/face/probe_face_gaussian_frequency_residual_seed1.yaml \
  --name face_gaussian_frequency_residual_seed1_probe_full5 \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_frequency_residual_full5_probe/train_console.log
```

完成后只反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_frequency_residual_seed1_probe_full5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_frequency_residual_full5_probe/train_console.log \
  || true
```

当前不要运行 `train_face_gaussian_frequency_residual_seed1.yaml` 的 1,350 epoch 正式配置。

方向高频查询残差模型的全量 5 epoch 探针已完成，属于分析数据：

| Epoch | Baseline PSNR | 方向残差 PSNR | 差值 |
|---:|---:|---:|---:|
| 1 | 26.9929 | 26.9997 | +0.0068 |
| 2 | 27.4621 | 27.3893 | -0.0728 |
| 3 | 27.7418 | 27.7543 | +0.0125 |
| 4 | 27.9767 | 27.9870 | +0.0103 |
| 5 | 28.1793 | 28.0962 | -0.0831 |

模型训练稳定但最终未提升，因此不启动正式训练。为避免继续堆叠网络，只进行最后一次训练损失简化：根据 HR 人脸像素的局部梯度生成均值归一化权重，训练 L1 中令眼睛、眉毛、嘴唇、轮廓和头发等高频位置获得略高权重。权重强度固定为 `0.25`，旧配置默认关闭。

服务器补丁：

```text
outputs/server_patches/GaussianSR_edge_weighted_probe_patch_20260726.zip
SHA-256: 09A03527D662719CA4A66A48E9C3232C2B893C8D0A580221CA1E5E01293B32A6
```

补丁只包含 `train_gaussian.py、datasets/wrappers.py` 和一个新探针配置。上传到服务器项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

BACKUP="server_backups/before_edge_weighted_loss_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$BACKUP"
cp --parents train_gaussian.py datasets/wrappers.py "$BACKUP/"

sha256sum GaussianSR_edge_weighted_probe_patch_20260726.zip
unzip -l GaussianSR_edge_weighted_probe_patch_20260726.zip
unzip -o GaussianSR_edge_weighted_probe_patch_20260726.zip

python -m py_compile train_gaussian.py datasets/wrappers.py
```

运行一次单样本权重检查：

```bash
python - <<'PY'
import copy
import torch
import yaml
import datasets

with open(
    'configs/train/face/probe_face_gaussian_frequency_residual_edge_seed1.yaml',
    'r', encoding='utf-8'
) as f:
    config = yaml.load(f, Loader=yaml.FullLoader)

dataset_spec = copy.deepcopy(config['train_dataset']['dataset'])
dataset_spec['args']['first_k'] = 1
dataset_spec['args']['cache'] = 'none'
base = datasets.make(dataset_spec)
wrapped = datasets.make(
    config['train_dataset']['wrapper'], args={'dataset': base}
)
sample = wrapped[(0, 4.0)]
weight = sample['weight']
assert weight.shape == (512, 1)
assert torch.isfinite(weight).all() and (weight > 0).all()
print('EDGE WEIGHT CHECK PASSED')
print('shape:', tuple(weight.shape))
print('min/mean/max:', weight.min().item(), weight.mean().item(), weight.max().item())
PY
```

检查通过后只训练最后一次候选，不重跑 Baseline：

```bash
mkdir -p results/face_frequency_residual_edge_full5_probe

python -u train_gaussian.py \
  --config configs/train/face/probe_face_gaussian_frequency_residual_edge_seed1.yaml \
  --name face_gaussian_frequency_residual_edge_seed1_probe_full5 \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_frequency_residual_edge_full5_probe/train_console.log
```

完成后只反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_frequency_residual_edge_seed1_probe_full5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_frequency_residual_edge_full5_probe/train_console.log \
  || true
```

边缘加权候选已经完成，权重张量形状正确且训练无异常：

| Epoch | Baseline PSNR | 边缘加权候选 PSNR | 差值 |
|---:|---:|---:|---:|
| 1 | 26.9929 | 27.0555 | +0.0626 |
| 2 | 27.4621 | 27.4216 | -0.0405 |
| 3 | 27.7418 | 27.7273 | -0.0145 |
| 4 | 27.9767 | 28.0021 | +0.0254 |
| 5 | 28.1793 | 28.1781 | -0.0012 |

五点平均高 `0.0064 dB`，最终低 `0.0012 dB`，属于数值持平而非可靠提升。至此停止频率残差网络和边缘损失迭代。

下一步将原定复杂的双倍率一致性压缩为“连续倍率课程采样”：模型保持原始 `gaussian-splatter`，前五个探针 epoch 的训练倍率上限依次为 `4、5、6、7、8`，倍率下限始终为 `1.5`。它不增加参数、第二次前向或额外损失，训练耗时基本不变。

服务器补丁：

```text
outputs/server_patches/GaussianSR_scale_curriculum_probe_patch_20260726.zip
SHA-256: 5E77C02C6DF8931D5080098E0245691CBC6F7FFA9EC1AA2BC3A2FB928D48A2D0
```

上传到服务器项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

BACKUP="server_backups/before_scale_curriculum_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$BACKUP"
cp --parents train_gaussian.py "$BACKUP/"

sha256sum GaussianSR_scale_curriculum_probe_patch_20260726.zip
unzip -l GaussianSR_scale_curriculum_probe_patch_20260726.zip
unzip -o GaussianSR_scale_curriculum_probe_patch_20260726.zip

python -m py_compile train_gaussian.py
```

然后只训练课程采样 Baseline：

```bash
mkdir -p results/face_scale_curriculum_full5_probe

python -u train_gaussian.py \
  --config configs/train/face/probe_face_gaussian_scale_curriculum_seed1.yaml \
  --name face_gaussian_scale_curriculum_seed1_probe_full5 \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_scale_curriculum_full5_probe/train_console.log
```

日志中五个 epoch 应依次显示 `scale_max=4.00、5.00、6.00、7.00、8.00`。完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_scale_curriculum_seed1_probe_full5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_curriculum_full5_probe/train_console.log \
  || true
```

课程采样按计划运行，但五个验证点均低于固定 Baseline：

| Epoch | Baseline PSNR | 课程采样 PSNR | 差值 |
|---:|---:|---:|---:|
| 1 | 26.9929 | 26.7718 | -0.2211 |
| 2 | 27.4621 | 27.2967 | -0.1654 |
| 3 | 27.7418 | 27.6076 | -0.1342 |
| 4 | 27.9767 | 27.9496 | -0.0271 |
| 5 | 28.1793 | 28.1257 | -0.0536 |

因此停止课程采样及新的 5 epoch 候选训练。边缘加权方向残差模型在 ×4 PSNR 上与 Baseline 几乎相同，而其目标偏向结构质量，最后只使用现有 checkpoint 做一次 50 图三倍率 PSNR/SSIM/LPIPS 对照，不再训练：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main
mkdir -p results/face_probe_final_compare

python test_face_metrics.py \
  --dataset-root Dataset/FACE/CelebA/test/HR \
  --dataset-name CelebA \
  --method model \
  --model save/face_gaussian_baseline_seed1_probe_full5/epoch-best.pth \
  --config save/face_gaussian_baseline_seed1_probe_full5/config.yaml \
  --scales 2 4 8 \
  --gpu 3 \
  --first-k 50 \
  --seed 1 \
  --output results/face_probe_final_compare/baseline_first50.csv

python test_face_metrics.py \
  --dataset-root Dataset/FACE/CelebA/test/HR \
  --dataset-name CelebA \
  --method model \
  --model save/face_gaussian_frequency_residual_edge_seed1_probe_full5/epoch-best.pth \
  --config save/face_gaussian_frequency_residual_edge_seed1_probe_full5/config.yaml \
  --scales 2 4 8 \
  --gpu 3 \
  --first-k 50 \
  --seed 1 \
  --output results/face_probe_final_compare/edge_candidate_first50.csv

cat results/face_probe_final_compare/baseline_first50.csv
cat results/face_probe_final_compare/edge_candidate_first50.csv
```

若候选仅数值持平或三项互有胜负，则不再为它运行 1,350 epoch；若 SSIM 提高且 LPIPS 稳定降低，才保留为结构感知候选。

最终 50 图三倍率结果满足准入条件：

| 倍率 | ΔPSNR-Y | ΔSSIM-Y | ΔLPIPS | 判定 |
|---:|---:|---:|---:|---|
| ×2 | +0.0246 | +0.000299 | -0.002086 | 三项改善 |
| ×4 | +0.0119 | +0.000623 | -0.004497 | 三项改善 |
| ×8 | +0.0294 | +0.001142 | -0.003067 | 三项改善 |

候选在三个倍率上均表现为 PSNR/SSIM 提高、LPIPS 降低，因此保留“方向高频查询残差 + 边缘加权 L1”为最终创新一候选并进入正式训练。该 50 图结果仍属于准入分析，论文最终表使用 1,350 epoch 正式 checkpoint 的全量测试结果。

正式候选配置补丁只包含一个新 YAML：

```text
outputs/server_patches/GaussianSR_frequency_edge_formal_config_20260726.zip
SHA-256: 0889EC789998B6D666F9FBDE5977BE6D3B2DA2903684CD6FE21FD0E615794072
```

使用 Xftp 上传并解压：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main
sha256sum GaussianSR_frequency_edge_formal_config_20260726.zip
unzip -l GaussianSR_frequency_edge_formal_config_20260726.zip
unzip -o GaussianSR_frequency_edge_formal_config_20260726.zip
```

正式训练必须先运行固定种子 Baseline。为避免 SSH 断开导致训练中止，使用 `nohup`：

```bash
mkdir -p results/face_formal_v2protocol

nohup python -u train_gaussian.py \
  --config configs/train/face/train_face_gaussian_baseline_seed1.yaml \
  --name face_gaussian_baseline_seed1_formal_v2protocol \
  --gpu 0,1,2 \
  --seed 1 \
  > results/face_formal_v2protocol/baseline_console.log 2>&1 &

echo $! | tee results/face_formal_v2protocol/baseline.pid
```

查看状态：

```bash
PID=$(cat results/face_formal_v2protocol/baseline.pid)
ps -fp "$PID"
tail -n 20 results/face_formal_v2protocol/baseline_console.log
```

当前只启动 Baseline。Baseline 完成并核验后，再启动正式创新模型，避免两个正式训练同时抢占 GPU。

正式 Baseline 已成功启动。启动检查显示训练集 18,000 张、验证集 500 张、模型参数约 1.8M、`seed=1`、`deterministic=True`、分组倍率采样已启用；后台 PID 为 `678561`。`nohup: ignoring input`、torchvision interpolation 和 `torch.meshgrid` 均为非致命提示。

两套正式训练随后均已完成。正式创新模型运行名为 `face_gaussian_frequency_residual_edge_seed1_formal_v2protocol`，完整训练 `1,350/1,350` epoch，耗时约 `65.7h`，最后一次验证 PSNR 为 `29.6962`。训练阶段至此冻结，不再修改网络、损失或采样策略；下一步只执行正式统一测试。
