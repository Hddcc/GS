# GaussianSR 人脸超分项目进展记录

本文件是代码版本、数据协议、训练运行、测试指标、创新方向和研究结论的持续记录。它记录实验事实，不等同于论文定稿。

## 强制记录规则

1. 每次出现新进展，都必须在同一次工作和 Git 提交中更新本文件。
2. 新进展包括代码、配置、数据集协议、训练命令、测试命令、指标、异常、诊断、创新方向、结论和下一步的变化。
3. 每条记录至少包含日期、分支、目标、改动文件、实验协议、命令或运行名、结果、结论和下一步。
4. 正式实验、短程探针、准入测试和失败方案必须明确区分，不能混写。
5. 原始 CSV、逐图 CSV、配置、日志和 checkpoint 保持在服务器或本地原位置；Markdown 只记录路径、摘要和校验状态。
6. `main` 只保存稳定可复现版本。新方案使用 `experiment/<topic>` 分支，修复使用 `fix/<topic>` 分支，文档整理使用 `docs/<topic>` 分支。
7. 未通过准入标准的方案不得合并为最终模型，也不得提前写成论文最终创新。
8. 禁止把数据集、checkpoint、训练输出、凭据、缓存和传输压缩包提交到 GitHub。

## 2026-07-31：建立 Git 版本管理与正式实验保底快照

### 版本目标

- Git 分支：`main`。
- 首次 Git 提交：`ade78d6`（`chore: snapshot GaussianSR face SR experiments`）。
- 远端目标：`https://github.com/Hddcc/GS.git`。
- 保存当前 GaussianSR 人脸超分代码、配置和实验文档。
- 当前正式候选仅作为保底版本，不认定为最终创新模型。
- 后续新方案在独立实验分支开发，避免覆盖可复现 Baseline 和正式候选。

### GitHub 备份状态

- `main` 已推送到 `origin/main`。
- `experiment/face-structure-experts` 已创建并推送，当前与首次稳定快照一致。
- 备份标签 `formal-v2protocol-backup-20260731` 已推送。
- 数据集、checkpoint、`save/`、`results/`、`outputs/`、缓存和压缩包均被 `.gitignore` 排除，没有进入 GitHub。

### 数据与统一协议

| 数据集划分 | 数量 | 用途 |
|---|---:|---|
| CelebA train | 18,000 | 训练 |
| CelebA val | 500 | 选择最佳 checkpoint |
| CelebA test | 1,000 | 同数据集正式测试 |
| Helen test | 50 | 跨数据集正式测试 |

- 训练倍率范围：`[1.5, 8]`。
- 正式测试倍率：`1.5、2、2.5、3.5、4、5.5、7.5、8`。
- 正式训练：1,350 epoch，batch size 12，GPU `0,1,2`，seed 1，确定性训练，倍率分组采样，从头随机初始化。
- 指标：PSNR-Y、SSIM-Y、LPIPS-Alex。

### 已完成的正式运行

| 角色 | 运行名 | 状态 |
|---|---|---|
| Baseline | `face_gaussian_baseline_seed1_formal_v2protocol` | 1,350 epoch 与八倍率测试完成 |
| 保底候选 | `face_gaussian_frequency_residual_edge_seed1_formal_v2protocol` | 1,350 epoch 与八倍率测试完成 |

保底候选由“方向高频查询残差”和 `edge_weight: 0.25` 的边缘加权 L1 组成。该组合已经完整训练和测试，但两部分均不是最终确定的论文创新点。

### 正式结果摘要

| 数据集 | 平均 ΔPSNR | 平均 ΔSSIM | 平均 ΔLPIPS | PSNR/SSIM/LPIPS 改善倍率数 |
|---|---:|---:|---:|---:|
| CelebA | +0.0080 dB | +0.000044 | -0.000302 | 7 / 6 / 7 |
| Helen | +0.0206 dB | +0.000380 | +0.000027 | 6 / 8 / 5 |

结果小幅但总体有效，主要改善中高倍率；Helen 的 LPIPS 表现混合。该结果足以作为毕业论文保底对照，但创新性和提升幅度尚不足以直接确定为最终方法。

详细逐倍率结果见 `毕业论文第四章实验与方法设计.md` 和本地实验数据总账。服务器正式文件位于：

```text
results/face_formal_v2protocol
save/face_gaussian_baseline_seed1_formal_v2protocol
save/face_gaussian_frequency_residual_edge_seed1_formal_v2protocol
```

### 当前冻结状态

- 正式配置和日志已复制到 `results/face_formal_v2protocol/frozen`。
- 已存在 Baseline、候选和正式训练 manifest。
- 仍需反馈并记录 `sha256sum -c` 的完整核验结果；仅有 `ls -lh` 不能证明所有哈希均为 `OK`。

### 下一创新方向筛选结论

原计划的“关键点监督人脸区域注意力”不再作为主要创新。近两年定向检索发现 ARASFSR（arXiv:2511.16341）已经使用局部频率、相对坐标、倍率和全局坐标调制，SwinIFS（arXiv:2601.01406）已经使用关键点 Gaussian heatmap 引导人脸超分，You Only Landmark Once（arXiv:2605.14166）已经使用检测热图构建语义加权损失。继续采用关键点热图或普通人脸区域注意力，和近期工作的重合风险较高。

新的实验方向确定为“连续倍率查询级局部专家路由”（Scale-Conditioned Query Mixture of Experts）。定向检索没有发现与“GaussianSR 查询接口 + 连续倍率先验 + 每查询局部专家路由 + 防坍缩约束”直接同构的近期人脸超分方法，但该结论只表示当前检索未发现直接重复，不构成绝对无人做过的证明。

## 2026-07-31：连续倍率查询级局部专家路由 v1 实现与本地验证

### 分支与研究状态

- Git 分支：`experiment/face-structure-experts`。
- 本轮性质：新结构实现与本地冒烟验证，尚未进行服务器 5 epoch 探针，不属于正式论文结果。
- `main` 保持正式 Baseline 和保底候选的稳定快照，不直接修改。

### 方法设计

新模型注册名为 `gaussian-splatter-face-scale-moe`，保留原 GaussianSR 主干，在基础 RGB 预测后增加三个轻量查询级 RGB 残差专家：

1. 从 EDSR/GaussianSR 编码特征生成 48 维局部专家上下文。
2. 每个 HR 查询点使用局部上下文、绝对坐标、相对特征坐标、cell 和连续倍率编码独立路由。
3. 三个专家分别设置在对数归一化倍率轴的低、中、高尺度中心，尺度先验连续变化，不把倍率离散成三个固定任务。
4. 路由器在尺度先验上学习局部内容修正，三个独立 MLP 产生 RGB 残差并加权融合。
5. 使用全局 batch 专家负载均衡约束防止单专家坍缩，使用低熵约束鼓励每个查询形成明确偏好。
6. 三卡训练时由训练循环汇总各卡路由统计后计算辅助损失，避免只读取主卡临时状态造成错误。
7. 第一轮仅使用普通 L1，不加入旧候选的 `edge_weight: 0.25`，以单独判断新结构贡献。
8. 训练态返回路由统计后立即清空模型内部对计算图的临时引用，避免诊断数据跨 batch 滞留显存。

### 改动文件

```text
models/gaussian_face_scale_moe.py
models/__init__.py
train_gaussian.py
smoke_test_face_scale_moe.py
configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml
configs/train/face/probe30_face_gaussian_baseline_seed1.yaml
configs/train/face/probe30_face_gaussian_scale_moe_seed1.yaml
configs/train/face/train_face_gaussian_scale_moe_seed1.yaml
.gitignore
项目进展记录.md
```

`train_gaussian.py` 只在模型训练态返回路由诊断字典时增加辅助损失和日志；原 Baseline 仍返回单一预测张量，重建损失和默认行为保持不变。新增日志包括 reconstruction loss、auxiliary loss、router entropy、三个专家 usage 和 RGB residual 均值。

### 统一实验协议

- seed 1，确定性训练，从头随机初始化，`use_pretrained: false`。
- CelebA train 18,000，val 500；训练倍率 `[1.5, 8]`，验证固定 ×4。
- batch size 12，三卡每卡 4 张，倍率分组采样，`sample_q: 512`。
- 5、30、1,350 epoch 三组候选配置分别与同阶段 Baseline 逐字段对比通过。
- 准入顺序固定为：本地前后向 -> 服务器 GPU 冒烟 -> 5 epoch 候选探针 -> 30 epoch Baseline/候选对照 -> 满足标准后才允许 1,350 epoch。

### 已完成验证

Python 编译通过：

```text
models/gaussian_face_scale_moe.py
smoke_test_face_scale_moe.py
train_gaussian.py
models/__init__.py
validate_face_training_configs.py
```

配置校验通过：

```text
5 epoch: CONFIG VALIDATION PASSED
30 epoch: CONFIG VALIDATION PASSED
1350 epoch: CONFIG VALIDATION PASSED
```

本地环境为 CPU，使用 `inp_size=4`、`sample_q=16` 在 ×1.5、×4、×8 完成前向、反向和测试态接口检查。模型参数量为 1,919,164。结果如下：

| 倍率 | 专家使用率（低/中/高） | 路由熵 | RGB 残差绝对均值 | 梯度检查 |
|---:|---|---:|---:|---|
| 1.5 | 0.5156 / 0.3608 / 0.1236 | 0.967725 | 0.00001460 | 上下文、路由、三个专家、门控均非零 |
| 4 | 0.2568 / 0.4150 / 0.3283 | 1.079759 | 0.00001336 | 上下文、路由、三个专家、门控均非零 |
| 8 | 0.1236 / 0.3608 / 0.5156 | 0.967725 | 0.00001452 | 上下文、路由、三个专家、门控均非零 |

测试态输出为 `(1, 16, 3)` 单一张量，兼容现有 `test.py` 和 `test_face_metrics.py` 的评测调用。所有预测、损失和梯度均为有限值，`SMOKE TEST PASSED`。

兼容性补测确认原 Baseline 在训练态仍返回 `(1, 4, 3)` 单一张量；新 MoE 的 123 项 `state_dict` 可使用 `strict=True` 完整加载，无缺失键或多余键。

本地 Anaconda 环境缺少 `tensorboardX`，因此本次仅在验证进程内注入未落盘的空日志类以完成模型计算检查；服务器 GS 环境已有训练依赖，不需要据此修改项目代码或安装包。

### 服务器传输包

- 文件：`GaussianSR_face_scale_moe_v1_patch_20260731.zip`。
- 本地路径：仓库根目录。
- SHA-256：`5FD2C9168B2640F3BD809B90D52899FB62BCD093F3B0E8D611B801639642B1D9`。
- 状态：仅用于 XFTP 传输，已被 `*.zip` 忽略，不提交 GitHub。
- 内容：新模型、模型注册、训练循环、GPU/CPU 冒烟脚本和 5/30/1,350 epoch 配置，共 8 个文件，已确认保留 `models/` 与 `configs/train/face/` 目录层级。

### 服务器部署与 GPU 冒烟命令

先用 XFTP 把 `GaussianSR_face_scale_moe_v1_patch_20260731.zip` 上传到服务器仓库根目录，然后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_scale_moe_v1_patch_20260731.zip

mkdir -p server_backups/pre_scale_moe_v1_20260731/models
cp train_gaussian.py server_backups/pre_scale_moe_v1_20260731/train_gaussian.py
cp models/__init__.py server_backups/pre_scale_moe_v1_20260731/models/__init__.py

unzip -o GaussianSR_face_scale_moe_v1_patch_20260731.zip

python -m py_compile \
  models/gaussian_face_scale_moe.py \
  models/__init__.py \
  train_gaussian.py \
  smoke_test_face_scale_moe.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --expected-model-b gaussian-splatter-face-scale-moe

python smoke_test_face_scale_moe.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --device cuda \
  --gpu 3 \
  --scales 1.5 4 8 \
  --seed 1
```

上传包的服务器 `sha256sum` 必须等于：

```text
5fd2c9168b2640f3bd809b90d52899fb62bcd093f3b0e8d611b801639642b1d9
```

GPU 冒烟通过后，待执行的 5 epoch 候选探针命令为：

```bash
mkdir -p results/face_scale_moe_v1_probe5

python -u train_gaussian.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --name face_gaussian_scale_moe_seed1_probe5 \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_scale_moe_v1_probe5/train_console.log
```

当前先执行到 GPU 冒烟并反馈完整输出；5 epoch 命令只有在冒烟输出确认无误后才执行。

### Git 状态

- 功能提交：`b384cdd`（`feat: add scale-conditioned query experts`）。
- 已推送：`origin/experiment/face-structure-experts`。
- 传输 ZIP、数据、checkpoint、训练结果和文献检索 XML 均未进入 Git。

### 准入标准与下一步

1. 上传本轮补丁后，先在服务器 GPU 3 运行 ×1.5、×4、×8 冒烟测试。
2. GPU 冒烟必须无 NaN、OOM、Traceback，三个专家均有非零使用率和梯度，测试态输出检查通过。
3. 随后只训练候选 5 epoch，检查训练日志中的 loss、entropy、usage 和 residual；这一步只验证可训练性，不根据 5 epoch 的单点 PSNR下正式结论。
4. 5 epoch 正常后，才运行同协议 30 epoch Baseline 和 MoE 对照。
5. 30 epoch 候选后期验证 PSNR相对 Baseline 至少呈约 `+0.03 dB` 趋势，并在 CelebA 前 50 图 ×2/×4/×8 的多数 PSNR/SSIM 上改善且 LPIPS不明显恶化，才允许启动 1,350 epoch。
6. 未通过准入标准时保留记录并调整或放弃本方向，不合并到 `main`，也不写成论文最终创新。

## 2026-07-31：服务器首次 GPU 冒烟被占用显存阻断

### 已完成项

- 补丁在服务器成功解压，8 个文件进入预期的 `models/`、仓库根目录和 `configs/train/face/` 路径。
- `models/gaussian_face_scale_moe.py`、`models/__init__.py`、`train_gaussian.py` 和 `smoke_test_face_scale_moe.py` 均通过 `py_compile`。
- 5 epoch Baseline/MoE 配置对比输出 `CONFIG VALIDATION PASSED`，确认 seed 1、epoch 5、batch size 12 和模型注册名正确。

### 阻断现象与结论

使用物理 GPU 3 执行冒烟测试时，在以下阶段报错：

```text
model = models.make(config['model']).to(device).train()
RuntimeError: CUDA error: out of memory
```

错误发生在模型转移到 GPU 时，尚未创建冒烟输入、查询坐标或执行模型前向，因此不能归因于输入尺寸、`sample_q`、路由计算或模型数值不稳定。新模型只有约 1.919M 参数，单纯权重转移所需显存很小；该现象表示 GPU 3 当时已被其他进程占用到几乎没有可用显存。

本次状态记为“环境资源阻断，GPU 冒烟尚未完成”，不是方案失败，也不是准入通过。下一步先查看各卡空闲显存，选择空闲显存最多且不承担训练任务的 GPU 重试；不得为了本测试终止来源不明或不属于用户的进程。

### 下一条服务器命令

```bash
nvidia-smi --query-gpu=index,name,memory.total,memory.used,memory.free,utilization.gpu \
  --format=csv,noheader,nounits

nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_memory \
  --format=csv,noheader,nounits
```

根据第一条输出选择空闲显存最多的卡，将下面的 `FREE_GPU` 替换成卡号：

```bash
FREE_GPU=0

python smoke_test_face_scale_moe.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --device cuda \
  --gpu "$FREE_GPU" \
  --scales 1.5 4 8 \
  --seed 1
```

需要反馈两条 `nvidia-smi` 输出和重试后的完整冒烟输出。5 epoch 训练继续保持未执行状态。

## 2026-07-31：服务器 GPU 冒烟通过

### 运行条件

- 服务器环境：Conda `GS`。
- 使用 GPU：物理 GPU 0；用户确认 GPU 0、1、2 均空闲，可用于后续三卡探针。
- 配置：`configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml`。
- 输入：`inp_size=16`，每次 512 个查询，倍率 ×1.5、×4、×8，seed 1。
- 模型参数量：1,919,164，专家数：3。

### GPU 冒烟结果

| 倍率 | 专家使用率（低/中/高） | 辅助损失 | 路由熵 | RGB 残差绝对均值 |
|---:|---|---:|---:|---:|
| 1.5 | 0.5156 / 0.3608 / 0.1236 | 0.003307 | 0.967725 | 0.00001537 |
| 4 | 0.2568 / 0.4150 / 0.3283 | 0.001456 | 1.079759 | 0.00001327 |
| 8 | 0.1236 / 0.3608 / 0.5156 | 0.003307 | 0.967725 | 0.00001465 |

- ×1.5 时低倍率专家占比最高，×4 时中倍率专家占比最高，×8 时高倍率专家占比最高，连续倍率先验行为符合设计。
- 上下文编码器、路由器、三个专家和残差门控在三个倍率下均有有限且非零的梯度。
- 三个专家均有非零使用率，没有初始单专家坍缩。
- 测试态输出为 `(1, 512, 3)` 单一张量，`SMOKE TEST PASSED`。
- `torch.meshgrid` 输出为原项目已有的弃用预警，不是运行错误。

结论：服务器 GPU 功能准入通过，可以进入 5 epoch 候选可训练性探针；该结果仍不是图像质量提升结论。

### 待执行的 5 epoch 探针

为避免覆盖已有运行，命令先检查目标路径；若路径已经存在则拒绝启动，不执行删除：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_scale_moe_seed1_probe5
OUT=results/face_scale_moe_v1_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: probe path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
    --name face_gaussian_scale_moe_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

启动后检查：

```bash
PID=$(cat results/face_scale_moe_v1_probe5/train.pid)
ps -fp "$PID"
tail -n 30 results/face_scale_moe_v1_probe5/train_console.log
```

完成后需要反馈：

```bash
tail -n 30 results/face_scale_moe_v1_probe5/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v1_probe5/train_console.log \
  || true
```

## 2026-08-01：Scale MoE v1 完成 5 epoch 可训练性探针

### 运行与结果

- 运行名：`face_gaussian_scale_moe_seed1_probe5`。
- 结果目录：`results/face_scale_moe_v1_probe5`。
- GPU：0、1、2；seed 1；确定性训练；统一倍率分组协议。
- 状态：5 epoch 正常完成，控制台未出现 NaN、OOM、Traceback 或 RuntimeError。

| Epoch | 总训练损失 | 重建损失 | 辅助损失 | 路由熵 | 专家使用率（低/中/高） | RGB 残差均值 | Val PSNR |
|---:|---:|---:|---:|---:|---|---:|---:|
| 1 | 0.0897 | 0.0884 | 0.00131 | 1.0405 | 0.326 / 0.343 / 0.331 | 0.007370 | 27.0065 |
| 2 | 0.0666 | 0.0656 | 0.00105 | 0.9214 | 0.336 / 0.331 / 0.333 | 0.020696 | 27.4245 |
| 3 | 0.0638 | 0.0631 | 0.00068 | 0.3550 | 0.335 / 0.330 / 0.335 | 0.025601 | 27.7515 |
| 4 | 0.0623 | 0.0618 | 0.00043 | 0.1190 | 0.334 / 0.332 / 0.334 | 0.027185 | 27.9691 |
| 5 | 0.0600 | 0.0597 | 0.00025 | 0.0855 | 0.334 / 0.333 / 0.334 | 0.023135 | 28.1037 |

### 与同协议 5 epoch Baseline 的初步比较

此前同协议 Baseline 的验证 PSNR 为 `26.9929、27.4621、27.7418、27.9767、28.1793`。MoE 相对差值为：

```text
epoch 1: +0.0136 dB
epoch 2: -0.0376 dB
epoch 3: +0.0097 dB
epoch 4: -0.0076 dB
epoch 5: -0.0756 dB
五个 epoch 平均: -0.0195 dB
```

结论分两部分：

- 工程准入通过：训练稳定，三个专家使用率非零且总体均衡，辅助损失正常下降，残差分支确实参与优化。
- 效果准入尚未通过：第 5 epoch 和五轮平均均未优于 Baseline，不能据此启动 1,350 epoch，也不能写成最终创新结果。

路由熵由 1.0405 快速降至 0.0855，表示查询路由已接近硬选择；专家总使用率仍约为三等分，因此不是单专家坍缩。该现象需在 30 epoch 对照中继续观察。考虑到新模型参数略多、可能存在前期收敛较慢，本轮不立即调参，仅进行一次既定的 30 epoch 同协议判定；若仍无明确提升则停止 v1 正式训练并调整方案。

### 下一步：先运行 30 epoch Baseline

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_baseline_seed1_probe30_scale_moe_compare
OUT=results/face_scale_moe_v1_probe30_baseline

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: baseline probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
    --name face_gaussian_baseline_seed1_probe30_scale_moe_compare \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

运行状态与完成结果：

```bash
PID=$(cat results/face_scale_moe_v1_probe30_baseline/train.pid)
ps -fp "$PID"
tail -n 20 results/face_scale_moe_v1_probe30_baseline/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_baseline_seed1_probe30_scale_moe_compare/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v1_probe30_baseline/train_console.log \
  || true
```

Baseline 完成并确认无异常后，再启动 30 epoch MoE；当前不并行运行两者，也不启动 1,350 epoch。

## 2026-08-01：完成 30 epoch 配对 Baseline

### 运行信息

- 运行名：`face_gaussian_baseline_seed1_probe30_scale_moe_compare`。
- 配置：`configs/train/face/probe30_face_gaussian_baseline_seed1.yaml`。
- 结果目录：`results/face_scale_moe_v1_probe30_baseline`。
- GPU 0、1、2；seed 1；30 epoch；每个 epoch 固定 ×4 验证。
- 状态：正常完成；错误检索没有输出，未发现 NaN、OOM、Traceback 或 RuntimeError。

### 基准摘要

| 项目 | PSNR |
|---|---:|
| Epoch 1 | 26.9884 dB |
| Epoch 5 | 28.1628 dB |
| Epoch 10 | 28.5576 dB |
| Epoch 20 | 28.8427 dB |
| Epoch 25 | 28.9117 dB |
| Epoch 30（最终/最佳） | 29.0128 dB |
| Epoch 26-30 平均 | 28.9738 dB |
| Epoch 21-30 平均 | 28.9379 dB |

该运行是过程性 30 epoch 准入基准，不属于论文最终数据。完整逐 epoch 数据保留在服务器 `save/face_gaussian_baseline_seed1_probe30_scale_moe_compare/log.txt`。

预先固定的候选判定参考：MoE 最终 PSNR 若要达到约 `+0.03 dB`，需接近或超过 `29.0428 dB`；Epoch 26-30 平均需接近或超过 `29.0038 dB`，并且不能依靠单个偶然峰值下结论。

### 下一步：运行 30 epoch Scale MoE v1

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_scale_moe_seed1_probe30
OUT=results/face_scale_moe_v1_probe30_candidate

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: candidate probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_scale_moe_seed1.yaml \
    --name face_gaussian_scale_moe_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

状态与完成结果检查：

```bash
PID=$(cat results/face_scale_moe_v1_probe30_candidate/train.pid)
ps -fp "$PID"
tail -n 20 results/face_scale_moe_v1_probe30_candidate/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v1_probe30_candidate/train_console.log \
  || true
```

候选完成后比较最终值、最后 5 轮均值、最后 10 轮均值、路由熵和专家使用率，再决定做前 50 图测试、调整 v1，或停止该方向。当前不得启动正式 1,350 epoch。

## 2026-08-01：Scale MoE v1 未通过 30 epoch 效果准入

### 候选运行

- 运行名：`face_gaussian_scale_moe_seed1_probe30`。
- 配置：`configs/train/face/probe30_face_gaussian_scale_moe_seed1.yaml`。
- 结果目录：`results/face_scale_moe_v1_probe30_candidate`。
- GPU 0、1、2；seed 1；30 epoch；每个 epoch 固定 ×4 验证。
- 状态：正常完成；错误检索没有输出，未发现 NaN、OOM、Traceback 或 RuntimeError。

### 配对结论

| 统计项 | Baseline | Scale MoE v1 | 差值 |
|---|---:|---:|---:|
| Epoch 30 最终/最佳 | 29.0128 | 29.0030 | -0.0098 dB |
| Epoch 26-30 平均 | 28.9738 | 28.9748 | +0.0010 dB |
| Epoch 21-30 平均 | 28.9379 | 28.9412 | +0.0033 dB |

预设目标为后期约 `+0.03 dB`。候选最终值更低，后期均值仅有千分之几 dB 的差异，可视为与 Baseline 持平，不能证明有效提升。

### 路由诊断

- 路由熵从 Epoch 1 的 `1.0286` 降至 Epoch 30 的 `0.0043`，已接近完全硬路由。
- 三个专家总使用率长期稳定在约 `0.333 / 0.333 / 0.333`。
- RGB 残差绝对均值从中期约 `0.031` 回落到 Epoch 30 的 `0.009569`。
- 该组合强烈表明“均匀负载均衡 + 最小熵”把查询推向了近乎一对一的硬分区，而不是保持连续倍率先验下的柔性局部融合。
- v1 专家自身也接收完整倍率编码，使每个专家仍可覆盖全部倍率，削弱了通过路由形成尺度专长的必要性。

### 决策

1. Scale MoE v1 停止，不进行 CelebA 前 50 图测试，不启动 1,350 epoch，不合并到 `main`，不作为论文最终创新。
2. 保留代码、配置、checkpoint 和日志作为失败方案证据，不执行删除。
3. 下一版仍保留“连续倍率查询级专家”主创新，但修正训练机制：
   - 删除鼓励硬路由的熵最小化项；
   - 不再把总使用率强制成均匀三等分；
   - 用路由分布与连续倍率先验之间的轻量 KL 一致性替代原防坍缩项；
   - 对局部内容路由修正做有界限制，防止短期完全覆盖尺度先验；
   - 专家不再直接接收倍率编码，迫使低、中、高倍率专长通过路由形成。
4. 新版本单独命名为 Scale MoE v2，重新执行 CPU/GPU 冒烟和 5 epoch 探针；通过后才考虑 30 epoch。

## 2026-08-01：实现 Scale MoE v2 并通过本地冒烟

### v2 针对性改动

- 新模型注册名：`gaussian-splatter-face-scale-moe-v2`。
- 保留 v1 查询级三专家和低、中、高连续倍率中心，不改 GaussianSR 主干。
- 将局部内容路由 logits 限制为 `0.75 * tanh(logits)`，防止短期完全覆盖倍率先验。
- 尺度先验强度由 1.0 调整为 2.0，使低、中、高倍率初始专长更明确。
- `balance_loss_weight` 和 `entropy_loss_weight` 均设为 0，删除“强制三等分 + 强制硬选择”的失效组合。
- 新增路由分布相对连续倍率先验的 KL 一致性，`prior_loss_weight: 0.001`。
- 专家 MLP 不再接收四维显式倍率编码；路由器仍接收倍率编码。专家保留必要的 cell 几何信息，但必须主要依靠路由形成尺度专长。
- 日志新增 `prior_kl` 和 `prior=[低/中/高]`，可直接比较实际路由总使用率与倍率先验使用率。
- v1 默认参数、参数形状和 registry 名保持兼容，原 v1 配置和 checkpoint 不需要修改。

### 文件

```text
models/gaussian_face_scale_moe.py
train_gaussian.py
smoke_test_face_scale_moe.py
configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml
项目进展记录.md
```

当前只创建 v2 的 5 epoch 配置，不创建 30 或 1,350 epoch v2 配置，避免在可训练性和早期效果未知时提前启动更长实验。

### 本地验证

- Python 编译通过。
- v2 与 5 epoch Baseline 协议逐字段校验输出 `CONFIG VALIDATION PASSED`。
- 参数量：1,918,396；三个专家。
- CPU 使用 `inp_size=4`、16 个查询，在 ×1.5、×4、×8 完成前向、反向和测试态接口检查。

| 倍率 | 实际路由使用率 | 倍率先验使用率 | 路由熵 | prior KL | RGB 残差均值 |
|---:|---|---|---:|---:|---:|
| 1.5 | 0.6464 / 0.3165 / 0.0371 | 0.6464 / 0.3165 / 0.0371 | 0.768411 | 0.000000 | 0.00001301 |
| 4 | 0.1907 / 0.4978 / 0.3115 | 0.1907 / 0.4978 / 0.3115 | 1.026529 | 0.000000 | 0.00000914 |
| 8 | 0.0371 / 0.3165 / 0.6464 | 0.0371 / 0.3165 / 0.6464 | 0.768411 | 0.000000 | 0.00000977 |

三个倍率下上下文编码器、路由器、三个专家和残差门控均有有限且非零梯度，测试态返回 `(1, 16, 3)` 单一张量，`SMOKE TEST PASSED`。初始 KL 为 0 是预期行为，因为局部路由器末层零初始化，实际路由从倍率先验开始；重建损失仍向路由器提供非零梯度。

v1 兼容性补测：原 v1 模型仍有 123 个状态项，`strict=True` 状态加载无缺失或多余键，新增诊断字段均为有限值。

### v2 服务器补丁

- 文件：`GaussianSR_face_scale_moe_v2_patch_20260801.zip`。
- SHA-256：`DE37F3C1675ABC94E4274F95A6B88B7B591319D0A1A575D14E3B943A5656833B`。
- 大小：11,389 bytes。
- 内容：4 个服务器必需文件，已确认保留 `models/` 和 `configs/train/face/` 目录层级。
- ZIP 为 XFTP 传输文件，受 `.gitignore` 排除，不提交 GitHub。

### 服务器部署与 GPU 冒烟

用 XFTP 将补丁上传到服务器仓库根目录，然后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_scale_moe_v2_patch_20260801.zip

mkdir -p server_backups/pre_scale_moe_v2_20260801/models
cp models/gaussian_face_scale_moe.py \
  server_backups/pre_scale_moe_v2_20260801/models/gaussian_face_scale_moe.py
cp train_gaussian.py \
  server_backups/pre_scale_moe_v2_20260801/train_gaussian.py
cp smoke_test_face_scale_moe.py \
  server_backups/pre_scale_moe_v2_20260801/smoke_test_face_scale_moe.py

unzip -o GaussianSR_face_scale_moe_v2_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_scale_moe.py \
  train_gaussian.py \
  smoke_test_face_scale_moe.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml \
  --expected-model-b gaussian-splatter-face-scale-moe-v2

python smoke_test_face_scale_moe.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器 SHA-256 必须为：

```text
de37f3c1675abc94e4274f95a6b88b7b591319d0a1a575d14e3b943a5656833b
```

当前只执行到 GPU 冒烟并反馈完整输出，不启动 v2 的 5 epoch 训练。

## 2026-08-01：Scale MoE v2 服务器 GPU 冒烟通过

### 服务器验证状态

- 补丁成功解压，Python 编译通过。
- v2 与 5 epoch Baseline 配置协议校验输出 `CONFIG VALIDATION PASSED`。
- GPU 0，CUDA，`inp_size=16`，512 个查询，seed 1。
- 模型参数量 1,918,396，三个专家。

| 倍率 | 实际路由使用率 | 倍率先验使用率 | 路由熵 | prior KL | RGB 残差均值 |
|---:|---|---|---:|---:|---:|
| 1.5 | 0.6464 / 0.3165 / 0.0371 | 0.6464 / 0.3165 / 0.0371 | 0.768411 | 0.000000 | 0.00001267 |
| 4 | 0.1907 / 0.4978 / 0.3115 | 0.1907 / 0.4978 / 0.3115 | 1.026529 | 0.000000 | 0.00000957 |
| 8 | 0.0371 / 0.3165 / 0.6464 | 0.0371 / 0.3165 / 0.6464 | 0.768411 | 0.000000 | 0.00000966 |

- 三个倍率下上下文、路由器、三个专家和门控均通过非零梯度断言。
- ×4 门控梯度在六位小数格式中显示为 `0.000000`，但真实值大于 0；否则冒烟脚本会抛出异常。
- 测试态输出 `(1, 512, 3)`，`SMOKE TEST PASSED`。
- 初始辅助损失和 prior KL 为 0 符合零初始化路由器从倍率先验开始的设计，训练后应出现小幅非零 KL。

结论：v2 服务器功能准入通过，可以进入 5 epoch 可训练性和早期趋势探针；仍不能视为效果提升证据。

### 下一步：5 epoch v2 探针

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_scale_moe_v2_seed1_probe5
OUT=results/face_scale_moe_v2_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: v2 probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml \
    --name face_gaussian_scale_moe_v2_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

检查与完成反馈：

```bash
PID=$(cat results/face_scale_moe_v2_probe5/train.pid)
ps -fp "$PID"
tail -n 30 results/face_scale_moe_v2_probe5/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_v2_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v2_probe5/train_console.log \
  || true
```

重点检查五项：Val PSNR、entropy 是否保持明显大于 0、prior KL 是否有限且较小、usage 是否大致跟随 prior、残差幅度是否稳定。当前不创建或运行 v2 的 30/1,350 epoch 配置。

## 2026-08-01：Scale MoE v2 完成 5 epoch 探针

### 运行结果

- 运行名：`face_gaussian_scale_moe_v2_seed1_probe5`。
- 结果目录：`results/face_scale_moe_v2_probe5`。
- GPU 0、1、2；seed 1；5 epoch；统一倍率分组协议。
- 状态：正常完成；错误检索没有输出，未发现 NaN、OOM、Traceback 或 RuntimeError。

| Epoch | 重建/总损失 | 路由熵 | prior KL | 实际 usage | 先验 usage | RGB 残差均值 | Val PSNR |
|---:|---:|---:|---:|---|---|---:|---:|
| 1 | 0.0883 | 0.9245 | 0.00209 | 0.191 / 0.440 / 0.369 | 0.201 / 0.428 / 0.370 | 0.008993 | 27.0448 |
| 2 | 0.0656 | 0.9285 | 0.00188 | 0.192 / 0.435 / 0.373 | 0.200 / 0.430 / 0.370 | 0.020773 | 27.4480 |
| 3 | 0.0630 | 0.9335 | 0.00033 | 0.198 / 0.429 / 0.372 | 0.200 / 0.429 / 0.371 | 0.021197 | 27.6967 |
| 4 | 0.0617 | 0.9300 | 0.00018 | 0.194 / 0.429 / 0.377 | 0.196 / 0.427 / 0.377 | 0.021226 | 27.9929 |
| 5 | 0.0597 | 0.9317 | 0.00010 | 0.200 / 0.428 / 0.371 | 0.202 / 0.428 / 0.370 | 0.021249 | 28.1544 |

### 机制与效果判定

- v2 路由熵始终约 0.93，没有重现 v1 从约 1 降至 0.0043 的硬路由退化。
- 实际 usage 始终贴近倍率 prior；KL 有限并由 0.00209 降至 0.00010。
- RGB 残差在约 0.021 稳定，分支持续参与训练。
- 与本轮已有的 30 epoch 配对 Baseline 前 5 轮比较，v2 各轮差值为 `+0.0564、-0.0128、-0.0692、+0.0048、-0.0084 dB`，五轮平均差约 `-0.0058 dB`。
- 第 5 轮 v2 为 28.1544，配对 Baseline 为 28.1628，差 `-0.0084 dB`，属于基本持平，机制准入通过但效果优势尚未建立。

决策：利用已经完成的 30 epoch Baseline，只追加一次 v2 30 epoch 候选运行。若最终和后期均值仍未达到约 `+0.03 dB`，停止 v2，不做前 50 图测试和正式训练。当前仍不创建 1,350 epoch 配置。

### 新增 30 epoch 配置

```text
configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml
```

除 `epoch_max: 30`、`epoch_save: 10` 和 v2 模型结构外，其数据、随机种子、优化器、batch、倍率和验证协议与 `probe30_face_gaussian_baseline_seed1.yaml` 一致。

### v2 30 epoch 配置传输与运行

- 传输包：`GaussianSR_face_scale_moe_v2_probe30_config_20260801.zip`。
- SHA-256：`0A5BDEE5A6C876589B7E4C74CAE872F657330786C08E737DBB594000EA0FED86`。
- 内容：仅 `configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml`。
- ZIP 为传输文件，不提交 GitHub。

上传到服务器仓库根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_scale_moe_v2_probe30_config_20260801.zip

CONFIG=configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml
if [ -e "$CONFIG" ]; then
  echo "STOP: v2 probe30 config already exists; do not overwrite"
else
  unzip GaussianSR_face_scale_moe_v2_probe30_config_20260801.zip
fi

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml \
  --expected-model-b gaussian-splatter-face-scale-moe-v2

RUN=save/face_gaussian_scale_moe_v2_seed1_probe30
OUT=results/face_scale_moe_v2_probe30_candidate

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: v2 probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml \
    --name face_gaussian_scale_moe_v2_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

状态与完成结果：

```bash
PID=$(cat results/face_scale_moe_v2_probe30_candidate/train.pid)
ps -fp "$PID"
tail -n 20 results/face_scale_moe_v2_probe30_candidate/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_v2_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v2_probe30_candidate/train_console.log \
  || true
```

## 2026-08-01：Scale MoE v2 30 epoch 候选已启动

- 运行名：`face_gaussian_scale_moe_v2_seed1_probe30`。
- 结果目录：`results/face_scale_moe_v2_probe30_candidate`。
- GPU：0、1、2；后台 PID：`679438`。
- 启动命令正常返回后台任务号，路径防覆盖检查未触发 STOP。
- 启动后立即执行验证日志 `grep` 尚无输出，是因为第一个约 3.2 分钟的 epoch 尚未完成。
- 异常检索为空，启动阶段未发现 NaN、OOM、Traceback 或 RuntimeError。
- 当前状态：运行中，预计总时长约 1.6 小时；该状态不是实验结果。

运行状态检查：

```bash
PID=679438
ps -fp "$PID"
tail -n 30 results/face_scale_moe_v2_probe30_candidate/train_console.log
```

持续查看时使用：

```bash
tail -f results/face_scale_moe_v2_probe30_candidate/train_console.log
```

`Ctrl+C` 只退出 `tail -f`，不会停止后台训练。进程结束后再执行逐 epoch 和异常检索命令。

## 2026-08-01：Scale MoE v2 未通过 30 epoch 效果准入

### 完成状态

- 运行名：`face_gaussian_scale_moe_v2_seed1_probe30`。
- 结果目录：`results/face_scale_moe_v2_probe30_candidate`。
- GPU 0、1、2；seed 1；30 epoch；固定 ×4 验证。
- 训练正常完成，未报告 NaN、OOM、Traceback 或 RuntimeError。

### 与配对 Baseline 的最终比较

| 统计项 | Baseline | Scale MoE v2 | 差值 |
|---|---:|---:|---:|
| Epoch 30 最终/最佳 | 29.0128 | 29.0093 | -0.0035 dB |
| Epoch 26-30 平均 | 28.9738 | 28.9849 | +0.0110 dB |
| Epoch 21-30 平均 | 28.9379 | 28.9453 | +0.0074 dB |

预设准入目标为后期约 `+0.03 dB`。v2 最终值低于 Baseline，最后 5/10 轮均值虽略高，但只有 `0.007–0.011 dB`，仍属于训练波动范围，不能证明有效提升。

### 机制诊断

- Epoch 30 路由熵为 `0.9314`，没有 v1 的硬路由退化。
- Epoch 30 prior KL 为 `0.00007`，实际 usage `0.201 / 0.428 / 0.372` 与 prior `0.201 / 0.428 / 0.371` 基本一致。
- RGB 残差均值稳定在约 `0.0133`，分支没有消失。
- v2 成功修复了路由训练机制，但重建损失和最终 PSNR 与 Baseline 几乎完全一致，表明问题不再是路由坍缩，而是“基础 RGB 输出后追加轻量专家残差”这一作用位置不足以带来有意义提升。

### 最终决策

1. 严格执行预先固定的准入规则，不因看到结果后再增加前 50 图测试来寻找局部优势。
2. Scale MoE v2 停止，不进行 CelebA 前 50 图测试，不创建或运行 1,350 epoch 配置，不作为论文最终创新。
3. Scale MoE v1/v2 的代码、配置、checkpoint 和日志全部保留，不执行删除，作为失败方案与研究过程证据。
4. 停止继续调整 MoE 的熵、均衡、KL、专家数或 residual gate；两轮结果已说明继续微调该 RGB 残差路线收益不高。
5. 下一创新必须改变 GaussianSR 的编码特征或高斯表示本身。暂定调研“遮挡置信度控制的双侧人脸对应特征融合”：在 Gaussian splatting 之前利用对齐人脸的左右对应关系补充局部结构，同时用可学习置信度避免胡须、发型、姿态和遮挡区域被强制对称。该方向不使用关键点 heatmap，必须先检查近两年人脸超分和对称先验工作后再实现。

本节中的 30 epoch 数据是方案准入过程数据，不进入论文最终主表或实验数据总账的正式结果区。

## 2026-08-01：启动置信度双侧人脸特征融合方向

### 分支与研究状态

- 分支：`experiment/face-bilateral-fusion`，从已完整记录 MoE 失败结论的提交创建。
- 当前性质：文献定向查重、结构实现和本地冒烟，尚未进行服务器训练，不属于论文结果。

### 定向查重结论

检索关键词覆盖 `face super-resolution / face hallucination` 与 `symmetry / bilateral / mirror / occlusion / correspondence / feature fusion` 的组合，并通过 ArXiv API、OpenAlex 和 Semantic Scholar（后者部分请求受到 429 限流）核对。相关工作主要分为：

- FSRNet（CVPR 2018）和后续工作使用关键点、解析图等显式人脸先验。
- FishFSRNet（2023）使用解析图注意力与多尺度特征融合。
- SwinIFS（arXiv:2601.01406）使用关键点 Gaussian heatmap 引导人脸超分。
- FASR（arXiv:2408.15386）及 FASR++（arXiv:2607.05702）利用多张辅助低质量人脸的特征条件和扩散模型，属于多参考输入。
- JDSR-GAN、FCSR-GAN 等较早工作处理口罩或遮挡补全，并非单张正常人脸的镜像对应特征融合。
- 传统人脸重建中“对称性”并非新概念，不能将普通水平翻转或对称约束本身表述为创新。

当前未发现与“单张 LR 输入、无需关键点或外部参考、在 Gaussian splatting 前通过成对镜像特征差异和 LR 不对称度估计置信度，再做残差特征融合”直接同构的近两年人脸超分方案。该结论只表示定向检索未发现直接重复，不是绝对无人做过的证明。

### 方法边界与结构

新模型注册名：`gaussian-splatter-face-bilateral-fusion`。创新表述限定为“遮挡/不对称置信度控制的双侧对应特征融合”，而不是泛称人脸对称先验。

1. EDSR 编码后，将每个 LR 特征与其水平镜像对应特征配对。
2. 使用原特征、镜像特征和绝对差构造双侧上下文。
3. 由 LR RGB 与镜像 RGB 的差异生成固定不对称先验；发型、姿态、遮挡和单侧细节差异越大，先验置信越低。
4. 学习置信头补充特征层判断，并强制一对镜像位置共享同一置信度。
5. 融合头预测特征残差，经可学习全局门控和局部置信度缩放后，在 Gaussian splatting 之前加入编码特征。
6. 融合末层采用小幅初始化，初始模型接近 Baseline；不使用关键点、解析图、额外标注、MoE、频域残差或边缘损失。

### 新增文件

```text
models/gaussian_face_bilateral_fusion.py
smoke_test_face_bilateral_fusion.py
configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml
```

修改 `models/__init__.py` 完成注册。训练循环保持不变，新模型训练态和测试态都返回原有预测张量接口。

### 本地验证

- Python 编译通过。
- 与 5 epoch Baseline 的数据、seed、batch、倍率、优化器和验证协议逐字段校验通过。
- 参数量：1,943,345。
- 对称输入平均置信度 `0.500000`；加入单侧扰动后降至 `0.295514`；镜像配对置信度最大误差 `0.00000000`。

| 倍率 | 平均置信度 | 平均不对称先验 | 特征残差绝对均值 | confidence/fusion/gate 梯度 |
|---:|---:|---:|---:|---|
| 1.5 | 0.193046 | 0.386093 | 0.00007912 | 2.577e-8 / 5.171e-5 / 2.846e-8 |
| 4 | 0.106413 | 0.212826 | 0.00005311 | 2.631e-8 / 5.510e-5 / 1.997e-8 |
| 8 | 0.139368 | 0.278735 | 0.00006637 | 1.659e-8 / 4.482e-5 / 1.990e-8 |

三个倍率预测、置信、残差和梯度均为有限值且关键分支梯度非零；测试态输出 `(1, 16, 3)`，`SMOKE TEST PASSED`。

### 服务器补丁与准入顺序

- 补丁：`GaussianSR_face_bilateral_fusion_patch_20260801.zip`。
- SHA-256：`E5DDDBE769C5455CF17C76A7FE0C9939DF0FDE23227AD1FE931501255EC78C5C`。
- 大小：4,768 bytes；包含 4 个文件且保留目录结构；ZIP 不提交 GitHub。
- 准入顺序：服务器 GPU 冒烟 -> 5 epoch 探针 -> 结果接近或优于配对 Baseline后才创建 30 epoch 配置。当前不创建 30/1,350 epoch 配置。

### 服务器部署与 GPU 冒烟命令

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_bilateral_fusion_patch_20260801.zip

mkdir -p server_backups/pre_bilateral_fusion_20260801/models
cp models/__init__.py \
  server_backups/pre_bilateral_fusion_20260801/models/__init__.py

unzip -o GaussianSR_face_bilateral_fusion_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_bilateral_fusion.py \
  models/__init__.py \
  smoke_test_face_bilateral_fusion.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml \
  --expected-model-b gaussian-splatter-face-bilateral-fusion

python smoke_test_face_bilateral_fusion.py \
  --config configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器 ZIP 哈希必须为：

```text
e5dddbe769c5455cf17c76a7fe0c9939df0fde23227ad1fe931501255ec78c5c
```

当前只执行到 GPU 冒烟并反馈完整输出，不启动 5 epoch 训练。

## 2026-08-01：双侧置信特征融合服务器 GPU 冒烟通过

### 服务器结果

- 配置协议校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12，模型注册名正确。
- GPU 0，CUDA，`inp_size=16`，512 查询；模型参数量 1,943,345。
- 完全对称输入平均置信度 `0.500000`，加入单侧扰动后降至 `0.289013`，镜像配对误差 `0.00000000`。

| 倍率 | 平均置信度 | 平均不对称先验 | 特征残差绝对均值 | confidence/fusion/gate 梯度 |
|---:|---:|---:|---:|---|
| 1.5 | 0.153412 | 0.306824 | 0.00010056 | 8.616e-8 / 6.448e-5 / 7.562e-8 |
| 4 | 0.145639 | 0.291278 | 0.00009241 | 5.616e-8 / 6.556e-5 / 5.003e-8 |
| 8 | 0.149634 | 0.299269 | 0.00009548 | 5.098e-8 / 6.206e-5 / 4.506e-8 |

三个倍率的预测、置信度、残差和梯度均为有限值；关键分支梯度均非零；测试态输出 `(1, 512, 3)`，`SMOKE TEST PASSED`。原项目的 `torch.meshgrid` 弃用提示不是运行错误。

结论：服务器功能准入通过，可以进行 5 epoch 可训练性与早期趋势探针；仍不代表效果提升。

### 下一步：5 epoch 双侧融合探针

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_bilateral_fusion_seed1_probe5
OUT=results/face_bilateral_fusion_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: bilateral probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml \
    --name face_gaussian_bilateral_fusion_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

启动和完成检查：

```bash
PID=$(cat results/face_bilateral_fusion_probe5/train.pid)
ps -fp "$PID"
tail -n 30 results/face_bilateral_fusion_probe5/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_bilateral_fusion_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_bilateral_fusion_probe5/train_console.log \
  || true
```

5 epoch 仅判断训练稳定性和早期 PSNR 趋势；没有通过前不创建 30/1,350 epoch 配置。

## 2026-08-01：双侧置信特征融合 5 epoch 探针完成

### 训练结果

- 训练进程已正常结束，5 个 epoch 全部完成。
- 未出现 NaN、显存溢出或运行时异常；日志中的 torchvision 和 `torch.meshgrid` 信息仍是弃用提示。
- 与同为 seed 1、相同数据和训练协议的 Baseline 前 5 epoch 对比：

| Epoch | Baseline PSNR | 双侧融合 PSNR | 差值（双侧融合 - Baseline） |
|---:|---:|---:|---:|
| 1 | 26.9884 | 26.9886 | +0.0002 |
| 2 | 27.4608 | 27.4644 | +0.0036 |
| 3 | 27.7659 | 27.7542 | -0.0117 |
| 4 | 27.9881 | 27.9899 | +0.0018 |
| 5 | 28.1628 | 28.1576 | -0.0052 |

5 次验证差值均值为 `-0.0023 dB`。当前结果与 Baseline 基本持平，说明训练稳定、没有明显退化，但尚无提升证据。考虑到 30 epoch 约需 1.5 小时，保留一次 30 epoch 终筛；不直接进行 1,350 epoch 正式训练。

### 30 epoch 终筛配置与判定

- 新增配置：`configs/train/face/probe30_face_gaussian_bilateral_fusion_seed1.yaml`。
- 服务器补丁：`GaussianSR_face_bilateral_fusion_probe30_patch_20260801.zip`，仅包含上述配置并保留目录结构。
- 补丁 SHA-256：`44a8305bf397a5952a3ec7c3ba398f9aa8bfa9efc292c450d06ecb2af799c178`。
- Baseline 继续使用已经完成的 `face_gaussian_baseline_seed1_probe30_scale_moe_compare`，不重复训练。
- 主要判据：双侧融合第 26 至 30 epoch 的验证 PSNR 均值相对 Baseline 同期均值至少提高 `0.03 dB`。
- 若未达到 `+0.03 dB`，停止该方向，不进行 1,350 epoch；若达到，再进行正式训练前确认。

服务器在上传新增的 30 epoch 配置后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_bilateral_fusion_probe30_patch_20260801.zip
unzip -o GaussianSR_face_bilateral_fusion_probe30_patch_20260801.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_bilateral_fusion_seed1.yaml \
  --expected-model-b gaussian-splatter-face-bilateral-fusion

RUN=save/face_gaussian_bilateral_fusion_seed1_probe30
OUT=results/face_bilateral_fusion_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: bilateral probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_bilateral_fusion_seed1.yaml \
    --name face_gaussian_bilateral_fusion_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后检查：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_bilateral_fusion_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_bilateral_fusion_probe30/train_console.log \
  || true
```

## 2026-08-01：双侧置信特征融合未通过 30 epoch 效果准入

### 服务器结果

- 运行名：`face_gaussian_bilateral_fusion_seed1_probe30`。
- 30 个 epoch 全部完成，训练日志未检出 NaN、显存溢出、Traceback 或 RuntimeError。
- 训练损失由 `0.0890` 下降至 `0.0528`，验证 PSNR 由 `26.9548` 上升至 `29.0146`，训练过程稳定。
- 对照为相同 seed、数据、batch、倍率范围和验证协议的 `face_gaussian_baseline_seed1_probe30_scale_moe_compare`。

| 统计项 | Baseline | 双侧置信融合 | 差值 |
|---|---:|---:|---:|
| Epoch 30 PSNR | 29.0128 | 29.0146 | +0.0018 |
| Epoch 26-30 平均 PSNR | 28.9738 | 28.9805 | +0.0067 |
| Epoch 21-30 平均 PSNR | 28.9379 | 28.9501 | +0.0122 |

末 5 次验证值为 `28.9618、28.9452、28.9778、29.0032、29.0146`。虽然末 10 次平均略高于 Baseline，但主要判据要求末 5 次平均至少提高 `0.03 dB`，实际仅提高 `0.0067 dB`，未通过效果准入。

### 结论与下一步

1. 双侧置信特征融合停止，不创建或运行 1,350 epoch 配置，不做 CelebA/Helen 全倍率正式测试，不作为论文最终创新。
2. 代码、配置、服务器 checkpoint 和日志全部保留，不删除，作为研究过程和失败消融记录；30 epoch 探针数据只保存在本进展文档，不进入论文正式结果表。
3. 该结果说明单纯在 EDSR 编码后注入镜像对应特征，对当前 GaussianSR 的重建上限影响很小；继续调整置信度温度、隐藏维度或 gate 初始化属于低收益微调，停止继续试参。
4. 下一方向必须直接改变高斯参数生成或高斯渲染所表达的局部结构，并保持不依赖关键点、解析图或额外标注。开始新方向前先完成结构设计和近期工作边界检查，再单独建立实验分支。

## 2026-08-01：启动守恒式成对子高斯分裂方向

### 分支与目标

- 分支：`experiment/face-conservative-gaussian-split`。
- 模型注册名：`gaussian-splatter-face-conservative-split`。
- 目标：直接改变 GaussianSR 的高斯中心与局部特征表达，使一个居中的父高斯能够按局部结构分裂为一对亚像素子高斯，表达眼睑、眉毛、鼻翼和唇线等单高斯难以描述的双边结构。
- 不使用关键点、解析图、身份网络、外部参考、额外标注、频域 RGB 残差、边缘加权损失或专家路由。

### 近期工作边界检查

通过 OpenAlex、Crossref 和论文 DOI 做了 2024-2026 定向检索。相关工作包括：

- GaussianSR，AAAI 2025，DOI `10.1609/aaai.v39i4.32369`：2D Gaussian splatting 任意尺度超分 Baseline。
- Gaussian Splatting Based on Mamba Interaction for Arbitrary Scale Image Super Resolution，IEEE SPL 2025，DOI `10.1109/LSP.2025.3615079`：Mamba 特征交互与 Gaussian SR。
- S3R-GS，Remote Sensing 2026，DOI `10.3390/rs18101585`：显著性编码、离散语义 Gaussian 路由和显著性加权训练。
- TADSR，Optics & Laser Technology 2026，DOI `10.1016/j.optlastec.2026.115812`：纹理感知动态 Gaussian splatting。
- Efficient Arbitrary-Scale Super-Resolution With Compact Gaussian Splatting，TCSVT 2026，DOI `10.1109/TCSVT.2026.3675508`：紧凑 Gaussian 表示。

因此不能将“动态高斯”“边缘感知高斯”“纹理高斯”或“双分支”本身作为创新表述。当前公开索引和可获得摘要中未发现与以下完整约束直接同构的方法：单个 2D 父高斯保留，同时生成中心偏移互为相反数、混合权重守恒、特征残差互为正负的成对子高斯，并用于无需额外先验的人脸任意尺度超分。该结论仅表示本次定向检索未发现直接重复，不构成绝对无人做过的证明。

### 实现内容

新增文件：

```text
models/gaussian_face_conservative_split.py
smoke_test_face_conservative_split.py
configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml
```

修改 `models/__init__.py` 完成模型注册。核心结构为：

1. 从每个局部编码特征计算 Sobel 结构方向，并与连续倍率编码共同送入分裂预测器。
2. 每个父高斯保留在原中心，同时产生偏移 `+d` 和 `-d` 的两个子高斯。
3. 三个分量权重为 `1-a、a/2、a/2`，总权重保持为 1，中心加权偏移为 0。
4. 两个子高斯特征为 `f+Δf` 和 `f-Δf`，配对特征平均仍为 `f`，避免无约束改变整体特征。
5. 分裂距离、局部混合权重、方向修正和配对特征残差由网络联合学习；全局 gate 初始较小，使模型从接近父高斯的状态开始。
6. 在 Gaussian rasterization 内同时渲染父分量和两个子分量，再进入原 GaussianSR 的连续坐标解码器。

专用冒烟脚本必须检查：三个倍率预测有限、中心守恒误差、特征守恒误差、分裂权重范围、新预测器梯度、全局 gate 梯度和测试态输出。

### 补丁与服务器准入

- 补丁：`GaussianSR_face_conservative_split_patch_20260801.zip`。
- SHA-256：`763b8935c33767306e79237070f2dd5c2b6cb7d8b5186f261999e3fae47eb5bb`。
- ZIP 包含 4 个文件并保留目录结构；ZIP 不提交 GitHub。

服务器将补丁上传到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_conservative_split_patch_20260801.zip

mkdir -p server_backups/pre_conservative_split_20260801/models
cp models/__init__.py \
  server_backups/pre_conservative_split_20260801/models/__init__.py

unzip -o GaussianSR_face_conservative_split_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_conservative_split.py \
  models/__init__.py \
  smoke_test_face_conservative_split.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --expected-model-b gaussian-splatter-face-conservative-split

python smoke_test_face_conservative_split.py \
  --config configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

当前只执行到配置校验和 GPU 冒烟并反馈完整输出。冒烟通过前不启动 5/30/1,350 epoch 训练。

## 2026-08-01：守恒式成对子高斯 v1 功能通过但梯度准入失败

### 服务器冒烟结果

- 模型参数量：1,866,060；CUDA、seed 1、输入 16、查询 512。
- ×1.5、×4、×8 均输出 `(1, 512, 3)`，中心与特征守恒误差均为 0，测试态输出正常，脚本打印 `SMOKE TEST PASSED`。
- 三个倍率的初始偏移均为 `0.20000002`，混合权重均为 `0.07500000`，配对子特征残差均为 `0.00000000`。
- split predictor 梯度依次为 `2.081e-8、2.458e-9、1.174e-9`；gate 梯度依次为 `6.780e-9、8.191e-10、3.143e-10`。

结论：功能、显存、数值和守恒约束均通过，但特征残差初始为零，×4/×8 的新分支梯度过弱。该状态不启动 5 epoch，避免再次出现结构存在但实际学不动的情况。

### v2 初始化修正

不改变创新结构和实验协议，只修正训练信号：

1. 初始子高斯偏移由约 `0.2` 提高到约 `1.0`，让 5×5 Gaussian kernel 上的正负子分量产生可分辨差异。
2. 初始总分裂 gate 由 `0.15` 提高到 `0.30`，局部初始混合比例由约 `0.075` 提高到约 `0.15`。
3. 配对子特征残差输出层使用小幅非零权重初始化；`f+Δf` 与 `f-Δf` 的守恒关系不变，但预测器前层可从第一步收到梯度。
4. 冒烟新增准入：平均配对特征残差必须大于 `1e-5`，split predictor 梯度不得低于 `1e-8`，gate 梯度不得低于 `1e-9`。

v2 仍先重新执行语法检查、配置校验和 GPU 冒烟，不直接训练。

v2 服务器补丁：

- 文件：`GaussianSR_face_conservative_split_v2_patch_20260801.zip`。
- SHA-256：`69104720b5c2fd158f5e54eabfcff48deb250aca02e38afea9294e39ccd5bd0f`。
- ZIP 包含模型注册、模型、5 epoch 配置和冒烟脚本共 4 个文件；ZIP 不提交 GitHub。

服务器上传 v2 补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_conservative_split_v2_patch_20260801.zip
unzip -o GaussianSR_face_conservative_split_v2_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_conservative_split.py \
  models/__init__.py \
  smoke_test_face_conservative_split.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --expected-model-b gaussian-splatter-face-conservative-split

python smoke_test_face_conservative_split.py \
  --config configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

## 2026-08-01：守恒式成对子高斯 v2 GPU 冒烟通过

### 服务器结果

- 模型参数量仍为 1,866,060，说明 v2 只修改初始化和准入检查，没有增加结构或改变参数规模。
- ×1.5、×4、×8 均输出 `(1, 512, 3)`，测试态输出正常，脚本打印 `SMOKE TEST PASSED`。
- 三个倍率的中心守恒误差和特征守恒误差均为 0。

| 倍率 | 平均偏移 | 平均混合权重 | 平均配对特征残差 | split predictor 梯度 | gate 梯度 |
|---:|---:|---:|---:|---:|---:|
| 1.5 | 1.00000000 | 0.15000001 | 0.00255400 | 6.993e-7 | 3.177e-7 |
| 4 | 1.00000000 | 0.15000001 | 0.00223322 | 9.600e-8 | 4.156e-8 |
| 8 | 1.00000000 | 0.15000001 | 0.00209698 | 3.902e-8 | 1.541e-8 |

相较 v1，split predictor 梯度提高约 33-39 倍，gate 梯度提高约 47-51 倍；配对子特征残差由严格为零变为非零，且没有破坏守恒关系。v2 通过功能、数值、显存、守恒和训练信号准入，可以进行 5 epoch 早期趋势探针，仍不代表效果提升。

### 下一步：v2 运行 5 epoch

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_conservative_split_v2_seed1_probe5
OUT=results/face_conservative_split_v2_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: conservative split v2 probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
    --name face_gaussian_conservative_split_v2_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_conservative_split_v2_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_conservative_split_v2_probe5/train_console.log \
  || true
```

同协议 Baseline 前 5 epoch PSNR 为 `26.9884、27.4608、27.7659、27.9881、28.1628`。v2 结果完成前不创建或运行 30/1,350 epoch 配置。

## 2026-08-02：守恒式成对子高斯 v2 完成 5 epoch 探针

### 训练结果

- 运行名：`face_gaussian_conservative_split_v2_seed1_probe5`。
- 5 个 epoch 全部完成；错误检索未发现 NaN、显存溢出、Traceback 或 RuntimeError。
- 训练损失由 `0.0884` 下降至 `0.0596`，训练过程稳定。

| Epoch | Baseline PSNR | 守恒分裂 v2 PSNR | 差值（v2 - Baseline） |
|---:|---:|---:|---:|
| 1 | 26.9884 | 26.9979 | +0.0095 |
| 2 | 27.4608 | 27.4570 | -0.0038 |
| 3 | 27.7659 | 27.7617 | -0.0042 |
| 4 | 27.9881 | 27.9515 | -0.0366 |
| 5 | 28.1628 | 28.1609 | -0.0019 |

5 次验证均值为 `27.6658 dB`，Baseline 为 `27.6732 dB`，差值 `-0.0074 dB`。最终值仅低 `0.0019 dB`，当前属于基本持平而非效果提升。

### 判定与下一步

该方向直接改变核心高斯表示，且训练稳定、最终值未明显退化。保留一次约 1.5 小时的 30 epoch 终筛，不在 5 epoch 结果上调参，也不直接启动 1,350 epoch。

- 新增配置：`configs/train/face/probe30_face_gaussian_conservative_split_seed1.yaml`。
- Baseline 使用现有 `face_gaussian_baseline_seed1_probe30_scale_moe_compare`，不重复训练。
- 准入标准：Epoch 26-30 平均 PSNR 至少达到 `29.0038 dB`，即相对 Baseline 的 `28.9738 dB` 提高至少 `0.03 dB`。
- 未达到门槛则停止该方向，不进行正式训练和全倍率测试。

30 epoch 配置补丁：

- 文件：`GaussianSR_face_conservative_split_probe30_patch_20260802.zip`。
- SHA-256：`cc6c1dd19d760667767e13a586842e04d19fd6bd2beedd9caca4d7d7507da7aa`。
- ZIP 仅包含 30 epoch 配置并保留目录结构；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_conservative_split_probe30_patch_20260802.zip
unzip -o GaussianSR_face_conservative_split_probe30_patch_20260802.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_conservative_split_seed1.yaml \
  --expected-model-b gaussian-splatter-face-conservative-split

RUN=save/face_gaussian_conservative_split_v2_seed1_probe30
OUT=results/face_conservative_split_v2_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: conservative split v2 probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_conservative_split_seed1.yaml \
    --name face_gaussian_conservative_split_v2_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_conservative_split_v2_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_conservative_split_v2_probe30/train_console.log \
  || true
```

## 2026-08-02：守恒式成对子高斯 v2 未通过 30 epoch 效果准入

### 服务器结果

- 运行名：`face_gaussian_conservative_split_v2_seed1_probe30`。
- 30 个 epoch 全部完成，训练损失由 `0.0883` 下降至 `0.0528`。
- 错误检索未发现 NaN、显存溢出、Traceback 或 RuntimeError。
- 结果与相同 seed、数据、batch、倍率范围和验证协议的已有 Baseline 30 epoch 运行比较。

| 统计项 | Baseline | 守恒分裂 v2 | 差值 |
|---|---:|---:|---:|
| Epoch 30 PSNR | 29.0128 | 29.0080 | -0.0048 |
| Epoch 26-30 平均 PSNR | 28.9738 | 28.9711 | -0.0027 |
| Epoch 21-30 平均 PSNR | 28.9379 | 28.9397 | +0.0018 |

候选 Epoch 26-30 为 `28.9484、28.9637、28.9347、29.0009、29.0080`，平均 `28.9711 dB`。预设准入线为 `29.0038 dB`，实际低 `0.0327 dB`；相对 Baseline 同期均值没有提升。

### 结论

1. 守恒式成对子高斯分裂停止，不创建或运行 1,350 epoch 配置，不进行 CelebA/Helen 全倍率正式测试，不作为论文最终创新。
2. 服务器 checkpoint、日志和本地代码全部保留，不删除；探针结果只进入本进展文档，不进入论文正式结果表。
3. v2 已证明结构、守恒约束和训练信号均实际工作，但单纯增加成对高斯中心和特征自由度没有改善当前 GaussianSR 的 PSNR 上限；不继续调整 offset、gate 或 feature delta。
4. 下一方向不再继续修改单个高斯核、路由、镜像融合或 RGB 小残差。优先回到已经获得小幅正式提升的频率残差与边缘训练保底版本，在其基础上寻找能够扩大提升、且可形成完整方法描述的跨层特征重建机制。

## 2026-08-02：启动人脸高斯几何-外观职责解耦方向

### 分支与研究边界

- 分支：`experiment/face-geometry-appearance-decoupling`。
- 新模型注册名：`gaussian-splatter-face-geometry-appearance`。
- 新编码器注册名：`edsr-face-geometry-appearance`。
- 第一轮使用普通 L1，不加入 `edge_weight: 0.25`，以单独判断新结构贡献；频率查询残差作为已有保底重建端保留。

2024-2026 定向检索覆盖“geometry/appearance decoupling、structure/appearance Gaussian、shallow/deep Gaussian SR”等组合。相关边界包括：

- GStex，arXiv 2024，DOI `10.48550/arXiv.2409.12954`：在 2D Gaussian splatting 视图合成中使用逐 primitive 纹理实现几何与外观解耦。
- SuperGS，arXiv 2024，DOI `10.48550/arXiv.2410.02571`：面向 3D Gaussian 场景超分的残差特征与不确定性学习。
- GaussianSR，AAAI 2025，DOI `10.1609/aaai.v39i4.32369`：当前 2D Gaussian 任意尺度图像超分 Baseline。
- Mamba Interaction Gaussian SR，IEEE SPL 2025，DOI `10.1109/LSP.2025.3615079`：Gaussian SR 的 Mamba 特征交互。
- S3R-GS，Remote Sensing 2026，DOI `10.3390/rs18101585`：显著性编码、语义 Gaussian 路由与加权训练。

几何/外观解耦本身已存在于 Gaussian view synthesis，不能泛称为新概念。本方向的限定表述是：在人脸单图任意尺度 2D Gaussian SR 中，将 EDSR 浅中层结构特征专用于 Gaussian 参数概率，将深层特征与门控跨层残差专用于 Gaussian appearance，再连接倍率感知的查询级方向频率残差。当前公开索引未发现这一完整职责划分的直接同构工作；该表述不等于绝对无人做过。

### 实现内容

新增：

```text
models/edsr_geometry_appearance.py
models/gaussian_face_geometry_appearance.py
smoke_test_face_geometry_appearance.py
configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml
```

修改：

```text
models/__init__.py
validate_face_training_configs.py
```

结构分工：

1. EDSR 第 4 个残差块输出作为浅层轮廓特征，第 10 个残差块输出作为中层部件结构特征，第 16 个残差块与尾卷积输出作为深层外观特征。
2. 浅层和中层各自投影后只进入 geometry head，产生 100 类 Gaussian 参数概率，不直接承担最终外观融合。
3. 浅层、中层和深层特征经过独立 appearance fusion，使用小幅初始化和可学习 gate 作为深层外观残差。
4. Gaussian rasterization 使用解耦后的 geometry 概率与 appearance 特征；连续坐标解码后再使用已有倍率感知方向频率残差修正 RGB。
5. 第一轮不使用边缘权重，保证与普通 L1 Baseline 的训练协议一致。

配置校验器新增可选参数 `--expected-encoder-b`。默认行为不变；只有显式传入时才允许候选 encoder spec 不同，同时仍校验候选编码器名称、`no_upsampling`、数据、batch、优化器、epoch、decoder 和 Gaussian kernel 协议。

### 补丁与服务器准入

- 补丁：`GaussianSR_face_geometry_appearance_patch_20260802.zip`。
- SHA-256：`c3c1271a0a36a3a4931dc566a10ea432f6b4f0d5bf1cc689f06bacfa5d16d432`。
- ZIP 包含 7 个文件并保留目录结构；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_geometry_appearance_patch_20260802.zip

mkdir -p server_backups/pre_geometry_appearance_20260802/models
cp models/__init__.py \
  server_backups/pre_geometry_appearance_20260802/models/__init__.py
cp models/gaussian_face_frequency_residual.py \
  server_backups/pre_geometry_appearance_20260802/models/gaussian_face_frequency_residual.py
cp validate_face_training_configs.py \
  server_backups/pre_geometry_appearance_20260802/validate_face_training_configs.py

unzip -o GaussianSR_face_geometry_appearance_patch_20260802.zip

python -m py_compile \
  models/edsr_geometry_appearance.py \
  models/gaussian_face_geometry_appearance.py \
  models/gaussian_face_frequency_residual.py \
  models/__init__.py \
  validate_face_training_configs.py \
  smoke_test_face_geometry_appearance.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --expected-model-b gaussian-splatter-face-geometry-appearance \
  --expected-encoder-b edsr-face-geometry-appearance

python smoke_test_face_geometry_appearance.py \
  --config configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

当前只执行配置校验和 GPU 冒烟。完整输出反馈前不启动 5/30/1,350 epoch 训练。

## 2026-08-02：几何-外观职责解耦服务器 GPU 冒烟通过

### 服务器结果

- 配置输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、普通 L1，模型和候选编码器注册名正确。
- 模型参数量 2,024,100；输入 16、查询 512；appearance gate 初值 0.25，频率残差 gate 初值 0.50。
- ×1.5、×4、×8 的预测与全部诊断值有限，测试态输出 `(1, 512, 3)`，脚本输出 `SMOKE TEST PASSED`。

| 倍率 | geometry 熵 | 概率和误差 | appearance 残差 | RGB 残差 | geometry/appearance/gate/residual 梯度 |
|---:|---:|---:|---:|---:|---|
| 1.5 | 2.912414 | 2.38e-7 | 0.00016807 | 0.00002932 | 7.076e-7 / 2.066e-4 / 3.758e-8 / 6.166e-2 |
| 4 | 2.988026 | 1.79e-7 | 0.00016635 | 0.00002495 | 1.289e-7 / 1.956e-4 / 2.805e-8 / 6.039e-2 |
| 8 | 2.997806 | 2.38e-7 | 0.00016166 | 0.00002234 | 3.933e-8 / 2.031e-4 / 8.620e-9 / 6.108e-2 |

geometry 概率和误差仅为 `10^-7` 量级；geometry、appearance、gate 和查询频率残差四部分梯度均非零。appearance 与查询残差分支梯度充足，geometry 在高倍率仍高于此前守恒分裂设置的最低训练信号参考。功能、数值、显存、职责分支和训练信号准入通过，可以进行普通 L1 的 5 epoch 早期趋势探针，仍不代表效果提升。

### 下一步：普通 L1 5 epoch 探针

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_geometry_appearance_seed1_probe5
OUT=results/face_geometry_appearance_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry appearance probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --name face_gaussian_geometry_appearance_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_appearance_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe5/train_console.log \
  || true
```

主要比较同协议 Baseline 前 5 epoch：`26.9884、27.4608、27.7659、27.9881、28.1628`。结果完成前不加入 edge weight，不创建或运行 30/1,350 epoch 配置。

## 2026-08-02：几何-外观职责解耦完成 5 epoch 探针

### 训练结果

- 运行名：`face_gaussian_geometry_appearance_seed1_probe5`。
- 5 个 epoch 全部完成，训练损失由 `0.0867` 下降至 `0.0593`；进程状态为 `Done`，日志尾部没有异常信息。
- 第一轮为普通 L1，不包含 `edge_weight`。

| Epoch | Baseline PSNR | 几何-外观候选 PSNR | 差值（候选 - Baseline） |
|---:|---:|---:|---:|
| 1 | 26.9884 | 27.0180 | +0.0296 |
| 2 | 27.4608 | 27.4557 | -0.0051 |
| 3 | 27.7659 | 27.7460 | -0.0199 |
| 4 | 27.9881 | 28.0584 | +0.0703 |
| 5 | 28.1628 | 28.2235 | +0.0607 |

候选 5 次验证均值为 `27.7003 dB`，Baseline 为 `27.6732 dB`，平均提高 `+0.0271 dB`；最终提高 `+0.0607 dB`。训练损失末值也由 Baseline 的约 `0.0597` 降至 `0.0593`。这是当前新结构中第一个在 5 epoch 同时出现末期持续提升和有意义均值提升的候选。

### 判定与下一步

1. 通过 5 epoch 效果准入，进行普通 L1 的 30 epoch 终筛。
2. 不修改层数、gate、geometry 维度或残差上限，不加入边缘权重，避免在正向结果上追参。
3. 新增 `configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml`；Baseline 继续使用已有同协议 30 epoch 结果，不重复训练。
4. 30 epoch 主要准入：Epoch 26-30 平均至少达到 `29.0038 dB`，即相对 Baseline 同期平均提高至少 `0.03 dB`。
5. 通过前不创建或运行 1,350 epoch 配置，不做全倍率测试。

30 epoch 配置补丁：

- 文件：`GaussianSR_face_geometry_appearance_probe30_patch_20260802.zip`。
- SHA-256：`3e78b8f81d644165d71fc3e6f810d1be9650b66505c96a303ed63304f036ee95`。
- ZIP 仅包含 30 epoch 配置并保留目录结构；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe5/train_console.log \
  || true

sha256sum GaussianSR_face_geometry_appearance_probe30_patch_20260802.zip
unzip -o GaussianSR_face_geometry_appearance_probe30_patch_20260802.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml \
  --expected-model-b gaussian-splatter-face-geometry-appearance \
  --expected-encoder-b edsr-face-geometry-appearance

RUN=save/face_gaussian_geometry_appearance_seed1_probe30
OUT=results/face_geometry_appearance_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry appearance probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml \
    --name face_gaussian_geometry_appearance_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_appearance_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe30/train_console.log \
  || true
```

## 2026-08-02：几何-外观 30 epoch 配置服务器准入通过

- 服务器补丁 SHA-256 与本地记录一致：`3e78b8f81d644165d71fc3e6f810d1be9650b66505c96a303ed63304f036ee95`。
- ZIP 正确解压到 `configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml`。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、30 epoch、batch size 12、Baseline 与候选模型名正确，候选编码器为 `edsr-face-geometry-appearance`。
- 可以使用既定运行名 `face_gaussian_geometry_appearance_seed1_probe30` 启动 30 epoch；不得修改配置或覆盖已有路径。

### 2026-08-02：30 epoch 配置已部署到服务器

- 服务器计算 SHA-256 为 `3e78b8f81d644165d71fc3e6f810d1be9650b66505c96a303ed63304f036ee95`，与本地记录完全一致。
- ZIP 已成功解压，目标文件为 `configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml`。
- 下一步先运行配置协议校验；通过后启动 `face_gaussian_geometry_appearance_seed1_probe30`，不得跳过校验或覆盖已有路径。
## 2026-09-08：操作建议 2 / 低显存渲染与通道预算重估预注册（修订）

### 状态与边界

- 从干净基线 `4d0f59bbd0a8dd8cc1f583ae85076f3ab4271728` 新建 `experiment/face-gather-channel-budget`，不继承连续 SPD、ESC 或其他失败候选代码；基线文件保持不变，新模型使用独立注册名。
- 对 GSASR 正式实现和 GaussianSR 解码路径复核后，撤销直接实施 `K=3` query gather 的原预案。该预案的 `/100` 只近似 x4、边界 clamp 会重复 primitive，且 query 特征不能直接穿过现有两个 3x3 `coef/freq` 卷积；按原伪代码实现会同时改变几何、边界质量和解码器接口。
- 阶段 1 改为数值等价的 primitive-chunk raster：只把 49 个 primitive 分成每组 7 个逐组累加，删除完整 `[B*L,49,C,HRh,HRw]` layer 张量；保留 Gaussian 通道 `Cg=8`、离散 bank、Gumbel/argmax、patch padding/fold/resize 和 Fourier 解码器的全部语义。
- 方向 4 的 `8/16/32/64` 通道预算只在等价重构通过 CUDA 输出/梯度/显存门禁后实施。query-only gather 作为后续独立候选，必须先实现 3x3 HR 邻域渲染以严格保留 `coef/freq` 卷积，不能与本阶段混合。
- 这是机制与效率准入，不是训练结果或正式论文结果。

### 固定数学与实现协议

- 保留原 100 类 Gaussian bank、训练态 Gumbel / 测试态 argmax、8/56 双流、核采样 `[-5,5]`、归一化峰值核、opacity、patch padding/fold/bicubic resize、Fourier `coef/freq/phase/dec`、L1、数据和优化协议。
- `primitive_chunk_size=7` 是纯内存调度参数，不新增可学习参数，不改变 state-dict；baseline epoch-5 checkpoint 可以严格加载到新注册模型。
- 每组先执行与 baseline 相同的 `affine_grid/grid_sample`，再对组内 primitive 求和并累加；最终 clamp、fold 和 decoder 顺序不变。
- 不在本阶段删除原 `weighted_gaussian_parameters()` 的跨 batch/patch 平均，因为那会变成质量候选而破坏等价对照。

### 阶段门禁

1. CPU/CUDA 等价测试覆盖非方形输入、train/eval 两条路径、chunk `1/7/49`、输出与参数梯度；输出误差不超过 `3e-6`，梯度误差不超过 `5e-6`。
2. 使用同一个 seed-1 baseline epoch-5 checkpoint，在 x8、16x24 合成输入、512 queries 上分别实例化 baseline 和候选；固定 Gumbel 随机种子后执行完整训练前向与反向，最大输出误差不超过 `2e-5`，`torch.cuda.max_memory_allocated()` 至少降低 30%，同时报告 median train-step wall-clock，不把 allocator reserved memory 混入指标。
3. 由于候选与 baseline 数学和参数完全一致，通过 1–2 后不重复做 5 epoch 质量训练，直接进入 `Cg=8/16/32/64` 的 seed-1 消融；这能省去一轮没有信息增量的约 20 分钟训练。
4. 通道预算实验保持总 encoder 与 decoder 输入通道 64 不变，只改变 Gaussian/bicubic 分流比例。先用 seed 1 的 x2/x4/x8 held-out 平均选预算，再用 seed 2/3 复核；不以单倍率最好值挑选。
5. 服务器使用现有可用卡中的单卡，默认物理 GPU 4，可由 `PHYSICAL_GPU=0/2/4` 显式覆盖。所有交付使用一个包含代码、配置、安装、准入、清单和说明的 ZIP；收到 CUDA 准入反馈前不生成或启动通道预算长训练。

### 本地实现与 CPU 机制准入

- 新增独立注册模型 `gaussian-splatter-memory-efficient`；基线 `models/gaussian.py` 未修改，候选不增加参数，state-dict 键完全一致。
- 非方形 `6x9 -> 9x14` 的 CPU 测试已通过。chunk `1/7/49` 相对 baseline 的 eval 最大误差分别为 `7.45e-9 / 7.45e-9 / 0`；固定 Gumbel 随机数后的 train 输出最大误差 `7.45e-9`，29 组有效参数梯度的最大误差 `7.45e-9`。
- 测试中的最大 primitive-layer 元素数由 `156800` 降为 `22400`，恰好是 baseline 的 `1/7`；这只是张量规模核算，真实 CUDA 峰值仍以服务器准入输出为准。
- 分块训练路径使用无随机操作的 activation checkpoint 重算每组 `grid_sample`，否则 autograd 会同时保留所有组的 translated layers，推理显存下降不能证明训练显存下降。CUDA 门禁因此固定测量完整前向与反向，而不是仅测 `no_grad` 推理。
- activation checkpoint 的闭包显式接收每个 chunk 自己的 affine grid，避免反向重算错误捕获循环末尾 grid；修正后重新执行 CPU 等价测试，train 输出与 29 组有效参数梯度最大误差均恢复为 `7.45e-9`。

### 操作建议 2 / CUDA 准入单包

- 最终单文件为 `D:/个人资料/项目/任意尺度/GaussianSR_memory_efficient_raster_operation2_bundle_v3_20260908.zip`，大小 `13771` bytes，SHA-256 为 `2a62c495a73eee4004a15b9d314df19c7796f9d6934e5de5b73453aa5c97fb46`。`v3` 只修正包内 README 的归档名和解压目录名，模型、测试、benchmark、安装逻辑与已验证的 `v2` 完全相同；旧的无版本、`v2` 包均不再使用。
- 包内包含模型、seed-1 协议配置、CPU/CUDA 等价测试、完整训练前向+反向显存/耗时 benchmark、安装脚本、双 SHA-256 清单和 README；安装器默认物理 GPU 4，也只接受当前可用的 `0/2/4`，且不会启动训练。
- 模型 payload 已在 detached `4d0f59b` 验证副本通过 `py_compile`、配置校验和 CPU 等价测试；`v3` 归档另在全新目录解压并逐项复核 `PAYLOAD_SHA256`，5/5 文件均为 `OK`，README 也只引用 `v3` 路径。服务器仍需报告真实 CUDA peak allocated memory 与 train-step 时间，达到 30% 门禁前不创建通道预算训练。

服务器只上传该 ZIP，并执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main
unzip GaussianSR_memory_efficient_raster_operation2_bundle_v3_20260908.zip
PHYSICAL_GPU=4 bash \
  memory_efficient_raster_operation2_v3_20260908/install_face_memory_efficient_raster_operation2_20260908.sh
```

### 2025--2026 CCF-A/B 并行备选筛选

- 本轮并行复核了已记录方向并排除重复项。若操作建议 2 的训练显存门禁失败，下一架构候选首选 CVPR 2026 ShiftLUT 的 Learnable Spatial Shift：论文在多种深度/通道设置的 Set5 x4 上报告不低于 `+0.30 dB`；只迁移每通道二维 latent shift 与 pointwise fusion，不迁 LUT 推理结构，也不复制无许可证代码。它移动 latent 采样而不是 Gaussian primitive，因此不与已失败 center drift 重复。
- 第二备选为 TCSVT 2025 MAT 的 MSConvStar：官方仓库为 MIT，模块使用 `1/3/5/7` 多尺度 depthwise convolution 与乘法门控，Urban100 x2 独立消融为 `32.98 -> 33.22 dB`；只替换 EDSR 后部少量 residual transform，不迁 NATTEN 注意力。它与已失败 DGFN 都有门控，但主增益来源是多尺度空间卷积。
- 第三备选为 CVPR 2026 IAFMNet 的 soft-only IDM+ARM，论文 Urban100/Manga109 x2 消融总增益约 `+0.22/+0.13 dB`；由于截至当前未见作者官方代码且原训练很长，只在前两项失败后考虑。
- 不再优先 attention、Mamba、diffusion、blind-degradation dropout 或大型整网替换；这些方向与既有失败类别重叠、短程收敛证据弱，或超出 1--2 模块边界。备选研究不代表 GaussianSR 上已取得增益，仍须独立预注册和分阶段 probe。
- Windows 本地 PyTorch 报告 NumPy 1.x/2.x ABI 警告，但测试退出码为 0，测试未调用 Tensor/NumPy 转换。当前尚未取得服务器 CUDA 峰值与时间，不能表述为已经获得实际效率提升。

### 2026-09-08：MAT / MSConvStar 实现级复核

- 已完整核对 TCSVT 2025 MAT 论文中与 MSConvStar 有关的结构图、方法说明、实验协议与 Table I/IV，并与作者 MIT 许可仓库固定提交 `fc0344ebff881c665117ad7b652adf716280ddbe` 的实现交叉验证；本阶段只做研究记录，不修改操作建议 2 的代码或交付包。
- 官方默认 `mlp_ratio=2`。对输入通道 `C`，先用 `1x1 Conv` 扩到 `2C`，按连续通道四分后分别执行 `1x1/3x3/5x5/7x7` depthwise convolution，拼接并与扩维特征相加；随后二分为两支，只对第一支做 GELU，再逐元素相乘并用 `1x1 Conv` 投影回 `C`。模块内部没有 LayerNorm 和输入 residual，所有卷积含 bias。
- Table IV 的 Urban100 x2 累计消融为：vanilla FFN `32.98`、ConvFFN `33.06`、ConvStar `33.19`、MSConvStar `33.22 dB`。因此完整 MSConvStar 相对 vanilla FFN 是 `+0.24 dB`，Star gating 相对 ConvFFN 是 `+0.13 dB`，而多尺度 `1/3/5/7` 相对单一 `5x5` ConvStar 的独立贡献只有 `+0.03 dB`；后续不能把完整增益归因于多尺度卷积。
- 若进入该方向，首轮采用论文默认的完整 MSConvStar，而不是单独添加多尺度 depthwise convolution；但不再一对一替换全部 16 个 EDSR residual transform，只固定替换第 8、16 个块，保留其余 14 块、外层 `y=x+R(x)`、head、body-tail、Gaussian renderer、数据、损失、seed 和训练计划，不增加 LayerNorm、不零初始化、不迁移 MAT attention。
- `C=64` 时每个 MSConvStar 为 `15,296` 参数，原 EDSR 两层 `3x3` residual transform 为 `73,856` 参数；两处替换合计减少 `117,120` 参数，候选约为 `1,721,039`，相对完整 baseline 下降约 `6.37%`。它仍同时改变结构和容量，但已限制在两个位置；若下降不能据此否定全主干替换或多尺度机制本身。
- 当前候选顺序修订为：先完成操作建议 2 的 CUDA 等价/显存门禁；通过后按原计划做 Gaussian 通道预算。只有通道预算未超过噪声时，才预注册单位置的 ShiftLUT-inspired static LSS；MSConvStar 两处替换列在其后，避免先进行大范围主干重构。

### 2026-09-08：CVPR 2026 UCAN / LKD 补充材料复核

- 已完整核对 UCAN 主论文中 LKD 的结构、训练协议和 Table 3，并逐页检查补充材料中的 LKD 公式、有效感受野推导和 Table 5。主论文文件为 `D:/个人资料/项目/任意尺度/ucan_cvpr2026.pdf`，SHA-256 `697c8f19dd8165369a3fe29ea2cbec2cbb1f5475f164b071bf2a8547d0de69ea`；补充材料为 `D:/个人资料/项目/任意尺度/ucan_cvpr2026_supplemental.pdf`，SHA-256 `b770d8977092ad7193a57169e89a081c8fabb73057be56ff5b533399b4c21ece`。作者 Apache-2.0 仓库固定在提交 `b812c0adb8ce70d442973db5c87bab7d1e8c3333`。
- LKD 将 `Cfg=max(C/4,16)` 个细粒度通道送入三分支 TFE：通道调制、`1x1 -> 3x3 -> 1x1` 局部分支和由 depthwise/dilated separable convolution 构成的大核分支；其余通道直接 bypass，最后拼接。该结构可作为单个 EDSR residual 插件，但论文没有给出相同网络的 `w/o LKD` 对照。
- 补充 Table 5 只证明 LKD 内部加深或扩大有效核的累计收益：Urban100 x2 的 LKD depth `1 -> 4` 为 `32.99 -> 33.12 dB`，kernel size `5 -> 53 -> 65` 为 `33.12 -> 33.18 -> 33.19 dB`。主论文 Table 3 同样只比较 `5x5` 与 `47x47`，不能把完整 UCAN 的 `33.22 dB` 与某个无 LKD baseline 作因果对照。
- 这些消融训练 400K iterations，完整 x2 模型训练 800K iterations；当前 GaussianSR 的 5 epoch 短程门禁不能据此假定单个 LKD 会充分学习。结论是 UCAN-LKD 暂不实施、不打包、不占用下一轮 GPU；只有前置通道预算和证据更强的轻量候选都失败时，才重新评估它。

### 2026-09-08：CVPR 2026 ShiftLUT / LSS 深度复核与轻量化边界

- 已完整核对 ShiftLUT 正文的 LSS 方法、两阶段训练、Figure 8 和补充材料 Table 1/2。正文文件 `D:/个人资料/项目/任意尺度/shiftlut_cvpr2026.pdf` 的 SHA-256 为 `96940655d18f09304af180041b19f1fa56c8d7bb0797402d8807663d339a1e29`；补充材料 `D:/个人资料/项目/任意尺度/shiftlut_cvpr2026_supplemental.pdf` 为 `0270140dbdbbdebe455192eb2ff17bc003c126c2be23539a25b805beb80e6c2e`。官方仓库固定提交 `c5db332fa168386296a1b05fd443a70d0ccae184` 未提供许可证，后续只能按论文公式独立实现。
- Figure 8 在 ShiftLUT、Set5 x4 内给出明确的 `w/o LSS` 对照：4/5/6/7 个块分别提高 `+0.37/+0.46/+0.43/+0.41 dB`，4/8/12/16 通道分别提高 `+0.30/+0.43/+0.43/+0.41 dB`。但跨主干补充实验并非全正：SPLUT 五数据集平均仅约 `+0.014 dB`，其中 B100 为 `-0.03 dB`；TinyLUT 平均约 `+0.082 dB`。不能把 Set5 上的 `+0.30 dB` 直接外推到 GaussianSR。
- 官方公开训练实际为连续偏移 Stage 1 约 200K 步，再固定整数偏移继续 Stage 2 约 200K 步；当前五轮约 7,500 步不足以忠实复现。官方 64 通道 offset predictor 单块约 `338,464` 参数，也不符合 1--2 个小模块的范围。补充 Table 1 显示连续到整数的平均 PSNR 损失约 `0.0175 dB`，STE 与 Gumbel 离散都更差，因此不采用训练中硬取整。
- 正文报告最终偏移的跨样本方差多数低于 `1e-3`；发布的 x4 Stage-2 checkpoint 中偏移也全部落在 `{-1,0,+1}`，且只有 `21.875%` 通道为非零。这支持更保守的 GaussianSR 适配：保留全部 EDSR ResBlock，只在第 8 块前加入一个零初始化、`tanh(raw)*1` 的静态连续通道位移，新增严格 `2x64=128` 参数，使用 bilinear/border/`align_corners=True`。
- 该适配只能称为 `LSS-inspired static shift`，不是 ShiftLUT 复现。首轮只做一个位置、seed 1；除连续态 x2/x4/x8 聚合质量外，必须报告取整态质量、连续到整数跌幅、非零取整通道比例、越界采样比例、饱和率和 offset 梯度。若全部取整为 0 或连续到整数下降超过 `0.03 dB`，判定五轮机制未成熟，不追加第二位置或 seed 2/3。

### 同步文献复核与后续质量路线

- 已逐页复核 ICCV 2025（CCF-A）GSASR 正文 Table 2、补充材料 Table 10 与 Table 12，并核对官方代码 commit `9d2eb64a51303ce7a22fd672197185488b38e10a`。其 EDSR 对照在 DIV2K x4 从 GaussianSR `31.49` 提到 `32.01 dB`，x8 从 `27.77` 提到 `28.25 dB`；完整模型变化很多，不能把差值归因于单一模块。
- 最有信息量的受控证据是 Gaussian 数量比例 `m=1 -> 4`：DIV2K x4 `30.61 -> 30.80 dB`（`+0.19`），x8 `27.00 -> 27.14 dB`（`+0.14`）；`m=4 -> 9` 在 x4 已饱和，说明后续若以涨点为优先，`m=4` 是有证据的固定预算，没必要扫描大量取值。
- GSASR 补充材料 Table 10 报告其 CUDA renderer 相对 GaussianSR renderer：x4 `249 ms / 2685 MB -> 168 ms / 172 MB`，x8 `234 ms / 2650 MB -> 71 ms / 135 MB`。这证明局部截断与稀疏累加可显著降低渲染资源，但不证明纯 PyTorch query gather 会提高 PSNR。
- GSASR 的 `m=4` 结果只保留为效应量与容量诊断证据，不再列为可执行 fallback。主项目记录已经把均匀/自适应多 Gaussian 分配列为文献重合排除项，且本项目已完成守恒式成对子 Gaussian、support-adaptive allocation、per-patch geometry 与 center drift 等相邻方向的否定实验；继续做四 Gaussian/像素会违反“不重复已研究方向”的边界。
- 因此固定后续顺序：先完成当前等价内存重构；若 CUDA 门禁通过，做 `Cg=8/16/32/64` 的单因素消融。若通道预算不能产生高于种子噪声的增益，则停止 renderer/geometry 容量追试，转入尚未在主项目记录中实施的 MSConvStar encoder transform；不再把 `m=4`、位置自由度或 Gaussian 分配作为性能保底。
- 无法保证任何新结构在 CelebA 协议下必然提升；本轮用正式论文的 `+0.14~+0.19 dB` 受控效应量作为候选下限，明显高于本项目约 `0.02~0.03 dB` 的种子噪声，从而减少继续验证弱候选的浪费。

## 2026-09-08：操作建议 2 / CUDA 准入通过与通道预算 seed-1 屏

### CUDA 准入实测

- 服务器物理 GPU 4 已完成 `v2` bundle 的 CPU/CUDA 等价与完整训练前向、反向显存屏；随后 `v3` bundle 的 SHA-256 `2a62c495a73eee4004a15b9d314df19c7796f9d6934e5de5b73453aa5c97fb46` 也核验正确，并通过已存在的 admission stamp 确认同一实现已经准入。
- 非方形 `6x9 -> 9x14` CUDA 测试中，chunk `1/7/49` 的 eval 最大误差为 `7.451e-9 / 7.451e-9 / 0`，训练输出误差 `7.451e-9`，29 组有效参数梯度最大误差 `1.775e-9`。
- seed-1 baseline epoch-5 checkpoint、合成 `16x24` 输入、x8、512 queries 的完整训练步实测：最大输出误差 `0`；peak allocated 从 `266.74 MiB` 降到 `127.66 MiB`，下降 `52.14%`，超过预注册的 30% 门禁；median train step 从 `0.0415 s` 变为 `0.0487 s`，候选/基线耗时比 `1.173`。
- 因此数值等价的 memory-efficient raster 正式通过机制/效率准入。它没有改变 PSNR 数学，不将该结果表述为质量提升；下一阶段按预注册计划只研究 Gaussian/bicubic 通道分流比例。

### `Cg=16/32/64` seed-1 屏协议

- `Cg=8` 与原 baseline 数学等价，直接复用 `save/face_gaussian_baseline_seed1_probe_full5`，不重复训练。三个新增候选只把 Gaussian 通道改为 `16/32/64`，总 encoder/decoder 通道保持 64，参数量、初始化、数据、L1、优化器、5 epoch、seed 1 和 x2/x4/x8 100 张 held-out 评测完全不变。
- 三个候选使用 physical GPU `0/2/4` 并行，每个候选采用已经验证的 `microbatch 4 x 3 = effective batch 12`；预算分别绑定为 `Cg16 -> GPU0`、`Cg32 -> GPU2`、`Cg64 -> GPU4`，以缩短墙钟时间而不改变单个候选的更新。
- 排名只按三个 held-out scale delta 的算术平均，若完全并列优先较小 `Cg`；不按单倍率最好值挑选。seed-1 放宽为聚合筛选：选中预算要求训练五轮均值/末轮 delta 不低于 `-0.030/-0.050 dB`、held-out mean 严格大于 `0`、任一倍率不低于 `-0.100 dB`。这允许少数倍率小幅回退，但不允许用单倍率大幅退化换取微弱平均收益。
- 本轮只运行 seed 1 并在分析后停止。只有屏通过才另做选中预算的 seed 2/3；最终判据仍要求跨 seed 聚合训练和 held-out 指标为正，而不是把 seed-1 probe 当正式结果。
- `Cg=64` 会产生 0 通道 bicubic bypass，模型现显式跳过对空张量的 bicubic interpolation；CPU 机制测试已覆盖 `Cg=16/32/64` 的前向、反向、有限输出、有效梯度与参数量不变。

## 2026-09-08：并行论文复核 / ASID 与 ESC 不进入候选队列

- AAAI 2025 ASID 的 Table 3 在 Urban100 x2 中从 baseline `32.43` 到 `+ID 32.70 dB`，但论文将该收益归因于 self-attention 层数翻倍，参数/FLOPs 也从 `288K/62.323G` 增到 `401K/87.344G`；attention-sharing 与 channel split 后分别回落到 `32.62/32.53 dB`。其 IDB 还耦合 local module、三级 SAM、CAM 和 ESA，没有 local module 单独消融；正式比较训练 1000 epochs，不能为本项目 1--2 个纯局部 block 提供因果证据。原文为 `D:/个人资料/项目/任意尺度/easidtf_aaai2025.pdf`，SHA-256 `46282a67645cd24d71c1378b2101f720c6e4c307acefb76cf689b2ca7bc22e69`；官方仓库固定 commit `b248b34508d347bd59a1d003f923177ca906d804`，该提交树无许可证。
- ICCV 2025 ESC 正是本项目此前已经实现并完成 CUDA admission 的 encoder 路线，不重复。论文 Table 3 的 Urban100 x2 为 full `33.46`、Only-ConvAttns `32.91`、Only-Self-Attentions `33.23 dB`；证据支持 self-attention、多个 ConvAttn、共享大核和动态核的整体组合，不支持只向 EDSR 插 1--2 个 ConvAttn。正文/补充材料稳定保存为 `D:/个人资料/项目/任意尺度/esc_iccv2025.pdf` 和 `esc_iccv2025_supplemental.pdf`，SHA-256 分别为 `ca1952fbc2b106f0f62b04ef2f3ef196c1628eb2b90b8c12886c84c536313243`、`99a7d26d56a4edb6c7ee4702c147c103fe59c5b66c21e7da137e35966bd5ba1d`；官方仓库 commit `f22c712a57c664360f9af3a77cfc527c414067c4`，MIT。
- 两篇均不新增可执行 fallback；保持顺序为通道预算、单位置 static LSS、两处 MSConvStar，不让已失败 attention/ESC 类路线重复消耗 GPU。

### 通道预算 seed-1 单包交付

- 实验实现固定在 commit `004d2d3260e308dfda82441d85805df2e8594ff8`，安装/启动入口固定在后续 commit `ec854ad`；两个 commit 均位于 `experiment/face-gather-channel-budget`，工作树未混入结果、checkpoint 或归档。
- 最终只需传输 `D:/个人资料/项目/任意尺度/GaussianSR_face_gaussian_channel_budget_seed1_bundle_20260908.zip`，大小 `28558` bytes，SHA-256 `74e83329fdfae7ce4c99e7cde812539913ed32ce303f13d1fca495d8d7a9e2df`。
- 单包包含 12 个 payload 文件：memory-efficient 模型、已验证的 microbatch 训练器、三个预算配置、配置校验、held-out 评测、预算分析器、三个 CPU 测试和三卡并行 runner；另含安装并启动入口、README、payload/installed 双 SHA-256 清单。
- 已在全新目录解压验证：18 个 ZIP entry 无绝对路径、反斜杠或 `../`，两个清单均 `12/12 OK`，两个 shell 文件均为 LF 行尾。Windows 本机没有 Bash/WSL，故 `bash -n` 固定由服务器安装入口在复制和启动前执行；Python compile、原 renderer 等价测试、预算机制测试、microbatch 等价测试与分析器正反例均已在本地通过。

### 通道预算 seed-1 服务器结果与结论

- 三个候选已在 physical GPU `0/2/4` 并行完成，无 OOM、NaN 或评测异常。`Cg=16` 的五轮 train mean/final delta 为 `-0.0022/-0.0214 dB`，held-out x2/x4/x8 为 `-0.0665/-0.0169/-0.0096 dB`，平均 `-0.0310 dB`。
- `Cg=32` 的 train mean/final 为 `-0.1841/-0.2202 dB`，held-out 平均 `-0.3184 dB`；`Cg=64` 分别为 `-3.8609/-3.4301 dB` 与 `-4.9335 dB`。预算越大退化越显著，说明当前 Gaussian raster 支路不能替代多数 bicubic bypass 特征；这不是多 seed 噪声能够解释的趋势。
- 按预注册排名选出的 `Cg=16` 仍未达到 held-out mean `>0`，故 `GAUSSIAN CHANNEL-BUDGET SEED1 SCREEN FAILED`。停止 seed 2/3，不把最接近 baseline 的负结果包装成提升；操作建议 2 只保留其已证实的 `52.14%` 峰值显存下降，不产生质量配置变更。
- 下一候选按固定顺序切换到单位置 `LSS-inspired static shift`。它只移动 encoder latent 的每通道空间采样，不再扩大 Gaussian 通道或修改 primitive 几何；首轮仍只做 seed 1 的连续/取整双态门禁。

## 2026-09-09：操作建议 C / 独立四 primitive 致密化预注册

### 启动理由与论文边界

- 后续独立分支上的 static LSS 与两位置 MSConvStar seed-1 已相继失败：前者 held-out mean `-0.0297 dB` 且所有位移取整为 0，后者 x2/x4/x8 全部下降并得到 held-out mean `-0.0754 dB`。按用户要求继续候选文档中的下一条操作建议，回到建议 C 的低显存 renderer + 多高斯容量路线。
- 证据固定为 ICCV 2025（CCF-A）GSASR。正文 `D:/个人资料/项目/任意尺度/literature_cache_20260811/2501.06838.pdf` 的 SHA-256 为 `1a3734aec741ff76cac936b5c5ebe7c3af5affca355e3a7f9f603072fcce5a4b`；Apache-2.0 官方仓库固定 commit `9d2eb64a51303ce7a22fd672197185488b38e10a`。
- GSASR 补充 Table 12 的受控 Gaussian 密度消融为 `m=1 -> 4`：DIV2K x4 `30.61 -> 30.80 dB`（`+0.19`），x8 `27.00 -> 27.14 dB`（`+0.14`）；x4 在 `m=4 -> 9` 已基本饱和。因此只固定 `m=4`，不扫描更多密度。
- 该方向与 GSASR 已有工作重合，只作为 GaussianSR latent/Fourier 架构中的性能适配和容量诊断，不声称多 Gaussian 致密化本身是新贡献。代码按本项目接口独立实现，不复制 GSASR Transformer 或 CUDA renderer。

### 固定实现

- 从已完成低显存 renderer 和通道预算终态 `cfdd7fc` 新建独立分支 `experiment/face-multi-gaussian-densification`；不修改脏主工作树，也不继承 static LSS 或 MSConvStar 源码。
- 保持 Gaussian 通道数 `Cg=8` 和 bicubic bypass `56` 不变。每个 LR 位置固定产生 4 个 primitive，初始中心位于 `(-0.25,-0.25)`、`(-0.25,+0.25)`、`(+0.25,-0.25)`、`(+0.25,+0.25)` LR pixel 的 2x2 子格；每支可学习的残差位移为 `0.25*tanh(delta)`，故中心始终位于原 LR cell 的 `[-0.5,+0.5]` 范围。
- 一个 `1x1 Conv(64, 4x8)` 从完整 encoder feature 预测四支独立的 8 通道颜色残差，使用 `0.1*tanh` 限幅；权重用小随机值初始化而不是零初始化。四支另有独立的 x/y covariance log-scale 和 opacity logit；covariance multiplier 为 `exp(0.25*tanh(raw_scale))`，opacity multiplier 为独立 sigmoid，初始化为 0.25 但不通过 softmax 强制总和守恒。
- 四支共享原 100 类 bank 产生的基础 covariance，只在其上施加独立尺度；不增加 attention、额外 encoder、连续 SPD 头或 Gaussian 通道，不修改数据、L1、Adam、Fourier decoder和 baseline checkpoint。
- 渲染继续使用已验证的 primitive-chunk checkpointing。primitive 数量从每 patch 49 增至 196，但单次 chunk 仍固定 7；这是性能容量实验，不再要求与 m=1 数值等价。

### seed-1 门禁

- 只训练 seed 1、5 epoch，physical GPU `0,2,4` DataParallel、每卡 4、总 batch 12。使用同一 baseline checkpoint 在 100 张 held-out 图像上配对评测 x2/x4/x8。
- 质量门禁固定为 train mean/final delta 不低于 `-0.030/-0.050 dB`，held-out mean 严格大于 0，任一倍率不低于 `-0.100 dB`。允许单倍率小幅回退，但总体必须为正。
- 机制门禁要求四支颜色残差、中心分离、offset gradient、covariance scale gradient 和 opacity gradient 均非零且有限；四支实际 opacity 均大于 `1e-3`，颜色残差跨支 diversity 大于 `0.01`。
- 通过后才创建 seed 2/3；失败则停止，不改 m、不增加训练轮数，也不把本方向表述为创新提升。

### 本地机制准入

- 完整 candidate 参数量为 `1,840,259`，相对 baseline `1,838,159` 只增加 `2,100`；`1,838,159` 个共享参数逐元素初始化最大误差为 0。
- 初始中心严格为四个预注册 2x2 子格位置，covariance multiplier 全为 1，四支独立 opacity multiplier 全为 0.25。四支颜色响应为 `0.000213/0.000220/0.000189/0.000210`，normalized diversity 非零。
- 小尺寸 x1.5/x4/x8 初始输出相对单 primitive baseline 的 mean absolute difference 约为 `0.9e-5/1.1e-5/0.9e-5`；既不要求严格等价，也没有产生大幅初始漂移。
- feature head、offset、covariance scale、opacity 的平均绝对梯度分别为 `1.329e-9/2.997e-8/5.125e-9/8.962e-9`，均有限且非零。primitive chunk `1/7` 最大输出误差 `7.45e-9`，repeat error 为 0。
- 配置校验、Python compile、分析器正反例测试、独立机制测试和 CPU full-model smoke 均通过；这只证明实现可训练，不代表 CelebA PSNR 已提升。

### 独立四 primitive seed-1 单包交付

- 实验实现固定在 commit `4407bf02fb613bc0abe2b924dc1895e2b89dca0a`，安装/启动入口固定在 commit `d1d061bce48081b27a23742d93a350bf9c909211`；均位于 `experiment/face-multi-gaussian-densification`，不包含服务器结果或 checkpoint。
- 最终只需传输 `D:/个人资料/项目/任意尺度/GaussianSR_face_multi_primitive_m4_seed1_bundle_20260909.zip`，大小 `34993` bytes，SHA-256 `87fd54606ae2c75e6c49396cf57b0317e3f283c082a0f0689e22d56b00353007`。
- 单包顶层目录为 `face_multi_primitive_m4_seed1_bundle_20260909/`，包含安装并启动入口、README、13 个完整 payload 文件和 payload/installed 双 SHA-256 清单。服务器入口固定使用 physical GPU `0,2,4` 训练，GPU 4 做 CUDA admission 和 held-out evaluation。
- 已在全新目录解压并覆盖到从当前提交导出的干净代码树验证：ZIP 共 19 个 entry，无绝对路径、反斜杠或 `../`；15/15 包内哈希与 13/13 安装后哈希通过，两个 shell 文件均为 LF。
- 解包后的 Python compile、配置校验、分析器正反例、microbatch 等价、独立机制和完整模型 CPU smoke 均通过。Windows 本地 PyTorch 报告 NumPy 2.x ABI 警告但所有测试退出码为 0；本机无 Bash/CUDA，因此 `bash -n` 和 CUDA full-model admission 由服务器入口在训练前强制执行。
- 本包只包含 `m=4`、seed 1。只有 x2/x4/x8 held-out 聚合均值严格为正并满足预注册训练与机制门禁后，才会另行制作 seed 2/3；否则立即停止该方向。

### 2026-09-10：独立四 primitive seed-1 屏拒绝

- 服务器在 physical GPU `0,2,4` 上完成 `face_gaussian_multi_primitive_m4_seed1_probe5`；五轮验证 PSNR 为 `26.9410/27.4469/27.7061/28.0004/28.1744 dB`，相对同 seed baseline 的五轮均值/末轮差为 `-0.0168/-0.0049 dB`，通过训练侧 floor `-0.030/-0.050 dB`。
- 固定 100 张 held-out 的 x2/x4/x8 差为 `-0.0141/-0.0040/+0.0019 dB`，三倍率平均为 `-0.0054 dB`，未达到预注册的严格正值要求。虽然 x8 略有提升，但不能用单倍率微小正值掩盖聚合退化。
- 四个颜色分支响应为 `0.005406/0.004911/0.005093/0.004658`，归一化多样性 `0.062750`、中心间距 `0.491716`、最小 opacity `0.248451`、未截断 renderer 响应 `0.019343`；所有机制门禁均通过，说明失败不是分支未激活或对称性未打破，而是新增容量没有转化为 CelebA 重建质量。
- 判定为 `MULTI-PRIMITIVE SEED1 SCREEN FAILED`。停止 seed 2/3，不扫描 `m=2/9/16`，不调整位移、opacity、颜色残差或门禁，也不把接近零的 held-out 结果表述为提升。
- 至此候选文档的建议 C 终止：低显存 renderer 只保留已证实的 `52.14%` 峰值显存下降，多 Gaussian 致密化不进入最终质量模型。

## 2026-09-13：操作建议 D / 原公式拒绝与 oracle 前置门禁

- 已按一手来源复核 WACV 2024《Best of Both Worlds》正文与官方 commit `85493094990b3744e4176d94131a413893e62063`。论文实际把隐式退化向量用于 EDSR 特征调制，把显式 LR kernel 仅用于训练期 cycle consistency；推理时移除显式 kernel estimator。论文和代码均没有把输入 blur covariance 直接加到 Gaussian renderer。
- 原候选公式 `Sigma_render' = Sigma_geo + Sigma_blur / r^2` 不直接实施：正号对应进一步卷积扩散，不天然是逆退化；GaussianSR covariance 又定义在固定 `[-5,5]` 的 5x5 kernel 采样坐标，经 padding、affine sampling、fold 与 resize 后才形成输出，和 HR/LR 像素 covariance 量纲不同。
- WACV 不属于 CCF-A/B，官方仓库没有开源许可证；不得复制或重分发其代码。正文消融只支持其“隐式调制 + cycle”整体方案在 DIV2K x3 上有效，不能推出 Gaussian 人脸 SPD 直接加法会有 `0.3--1 dB` 收益。
- 因此建议 D 不进入新的 5-epoch 训练。唯一保留的低成本入口是免训练 oracle sign/scale/unit admission：在同一 baseline checkpoint 和固定 blind held-out manifest 上比较不调制、正号、PSD-safe 负号以及 HR/LR covariance 映射。若 oracle 总体、mild/medium blur 和 x2/x4/x8 不能同时为正，则永久停止建议 D，不开发 degradation encoder。
- 完整依据、论文页码、官方代码行、许可/专利边界和公平训练条件记录于 `建议D_盲人脸SPD退化补偿_研究复核_20260913.md`。该研究结论不是训练结果。

## 2026-09-13：操作建议 D / blind-SPD oracle 门禁实现

- Git 分支：`experiment/face-blind-spd-oracle`；从研究决策 commit `974400d` 建立独立 worktree。当前状态是免训练 oracle 准入实现，不是盲超分训练结果或论文正式结果。
- `models/gaussian.py` 新增默认关闭的 evaluation-only 2x2 covariance override。默认 `None` 路径不增加参数且必须逐元素复现原输出；正号路径保持 SPD，负号路径通过 `torch.linalg.eigh` 和 `1e-4` 特征值下限执行 PSD-safe subtraction，并记录 clamp 比例和特征值范围。
- 新增独立的各向异性 Gaussian 退化、离散二阶矩、LR 有效核近似和 renderer impulse-response 标定。标定在固定 5x5 renderer 核、参考 variance `2.25` 附近用中心差分估计“内部 covariance 单位 / 输出核像素 variance”，不再把 `/r^2` 冒充完整单位换算。
- 固定协议文件 `configs/eval/face_blind_spd_oracle_protocol_20260913.json`：noise-free 21x21 blur；mild 为 `sigma=0.8/0.5`，medium 为 `1.6/0.8`，角度 `0/45/90`；候选是 HR/LR 映射、加/减号和 `alpha=0.25/0.5/1.0` 的 12 个预注册组合。
- 为减少 GPU 浪费，先在 CelebA val 索引 `100--117` 的 18 张调参子集上筛选；只有总体、mild、medium 均为正、x2/x4/x8 均非负、clamp 不超过 10% 且 covariance 有限 SPD 的候选才能进入确认。按总体 paired delta 只选最优合格候选，再在不重叠的 CelebA val `0--99` 上确认；CelebA 通过后才在 Helen test 50 张上做同门禁外域复核。x1.5 只在确认阶段报告，但不替代 x2/x4/x8 门禁。
- `evaluate_face_blind_spd_oracle.py` 对同一退化 LR 输入和同一 baseline checkpoint 逐样本配对比较 A0 与 oracle；保存源图文件名、SHA-256、实际尺寸和完整 JSON 结果。`analyze_face_blind_spd_oracle.py` 独立重算预注册门禁并拒绝不一致状态。
- 运行只复用 `save/face_gaussian_baseline_seed1_probe_full5/epoch-5.pth`，不训练、不跑 seed 2/3、不复制 WACV 无许可证代码。若任一阶段失败，建议 D 永久停止；只有 CelebA 与 Helen 都通过才允许后续设计公平的 blind baseline 与条件分支训练。
- 单文件交付采用 `face_blind_spd_oracle_bundle_20260913.zip`。包内包含实现、固定协议、测试、研究复核、README、runner、installer、`PAYLOAD_SHA256` 与 `INSTALLED_SHA256`；安装器默认仅占物理 GPU 4，也允许 0 或 2，校验原始 `models/gaussian.py`/`models/edsr.py` 哈希、备份被覆盖文件、运行 CPU/CUDA admission 后后台启动。服务器输出固定为 `results/face_blind_spd_oracle_admission_orchestrator.log`，不覆盖已有结果。
- 实现 commit 为 `4466f93725dc6846edc3797a577488f0c5baf1c9`，单包入口 commit 为 `e19a061`。最终 ZIP 位于仓库外 `D:\个人资料\项目\任意尺度\face_blind_spd_oracle_bundle_20260913.zip`，SHA-256 为 `f871cdf8b31de441363e7d5a6a108834e5da3b451968936663880d92b91f2eb1`，大小 `36137` bytes；ZIP 不进入 Git。
- 最终包已在全新目录 `_verify_blind_spd_oracle_bundle_20260913_v2` 解压验证：17 个 ZIP 条目均无绝对路径、反斜杠、`../`、`__pycache__` 或 `.pyc`；`PAYLOAD_SHA256` 14/14、`INSTALLED_SHA256` 12/12、shell LF 2/2；解压副本的 analyzer 测试、退化/标定数学测试和 `py_compile` 均通过。本地 Windows Torch 因 NumPy 2 ABI 产生已知 warning 但测试退出码为 0；真实 GS 依赖与 CUDA smoke 由服务器 installer 在启动前强制执行。

## 2026-09-13：操作建议 D / blind-SPD oracle sweep 拒绝

- 服务器完成免训练 sweep，结果文件为 `results/face_blind_spd_oracle_admission/oracle_result.json`，终态为 `ORACLE RESULT: sweep_failed`、`selected variant: None` 和 `BLIND-SPD ORACLE PROBE COMPLETED`。这表示 12 个预注册 oracle 组合没有任何一个同时通过总体、mild/medium、x2/x4/x8 与 SPD 门禁，因此没有启动 CelebA 100 张和 Helen 50 张确认阶段。
- 用户回传日志中最后一个候选 `lr-subtract-a1` 的最大 clamp fraction 为 `1.00000000`，调整后 covariance 特征值范围为 `0.00010000 .. 8.80831242`，门禁失败；100% clamp 说明该负向补偿强度已经落在 PSD 边界，不能继续扩大系数并称为更强锐化。
- 建议 D 永久停止：不训练 degradation encoder，不扩展 alpha、blur 桶或 seed，不把 oracle 结果表述为 blind SR 结果。下一候选转向 CVPR 2026 IAFMNet 的 soft-only `IDM + ARM`，并继续使用独立 seed-1 staged screen。

## 2026-09-13：建议 E / IAFM soft IDM + ARM seed-1 实现

- Git 分支：`experiment/face-iafm-idm-arm`，已 fast-forward 继承 blind-SPD oracle 至终态 commit `3159492`，确保下一包与服务器当前文件状态一致；oracle covariance hook 默认关闭，不参与本候选训练或评估。
- 已核对 CVPR 2026 IAFMNet 主文 Eq.3--9、Table 3--5 与补充材料。仅选择 Table 5 有受控收益的 soft IDM-guided ARM，不迁移 IGRA sparse convolution 或整网结构。Urban100/Manga109 x2 的无 AR/无 IDM到 AR+IDM为 `32.30->32.52/39.19->39.32 dB`；该证据来自论文原任务，不保证 GaussianSR 增益。
- 新 encoder `edsr-face-iafm-idm-arm` 完整保留 baseline EDSR 与 Gaussian segmentation head，在 shallow feature 上用两组 `Conv+GDN` 预测 mean/IDM scale，按离散 Gaussian CDF 计算 information entropy；在第 4/8/12/16 个 ResBlock 后加入 Eq.8--9 的 ARM，并以固定 `0.1` residual scale 接回。
- 候选增加 `151,040` 参数。额外模块构造后恢复 CPU RNG，使所有同名 baseline 参数逐元素初始化一致；量化噪声采样后恢复每卡 RNG，使随后的 Gumbel 随机流与 baseline 对齐。训练器只在收到 `information_entropy_loss` diagnostics 时加入论文最优 `1e-4` auxiliary，原 baseline/MoE 行为不变。
- 本地 CPU 机制测试确认 `431,439` 个共享参数最大初始化误差 0，GDN/ARM 方程误差均为 0，information entropy `6.278120`、IDM spatial std `0.087014`，四处 scaled ARM response 为 `0.001448/0.001654/0.001671/0.001775`；IDE mean/scale、ARM、原 encoder 梯度均有限非零，eval repeat error 为 0。配置校验、analyzer 正反例、microbatch 回归和 Python compile 均通过；服务器安装器还必须通过三卡 DataParallel diagnostics gather/backward smoke。这些只证明实现和机制，不是质量结果。
- seed-1 使用 5 epoch、普通 L1、相同 CelebA、batch 12、physical GPU `0,2,4` DataParallel 和固定 100 张 x2/x4/x8 held-out。质量门禁保持 train mean/final `>=-0.010/-0.020 dB`、held-out mean `>0`、各倍率 `>=-0.030 dB`；机制门禁要求 entropy、IDM spatial std、四个 ARM raw/scaled response 与最终 output response 全部 `>1e-4`。只有通过才制作 seed 2/3。
- 完整论文证据、迁移边界、训练动态与许可状态记录于 `建议E_IAFM_IDM_ARM迁移复核_20260913.md`。当前阶段是短程准入，不是论文正式结果。

### 建议 E / IAFM soft IDM + ARM seed-1 单包交付

- 实验实现固定在 commit `e4b637e584605e68828018330201a4b1d064a0d1`，安装与交付入口固定在 commit `cbaf564`；分支为 `experiment/face-iafm-idm-arm`，不包含服务器结果、checkpoint 或 ZIP。
- 最终只需传输 `D:/个人资料/项目/任意尺度/face_iafm_idm_arm_seed1_bundle_20260913.zip`，大小 `41421` bytes，SHA-256 为 `7cf2c42345c5fa0e3e64a254d7edd4d011c354a09ab80631b9a4cd5892532eed`。
- ZIP 顶层目录为 `face_iafm_idm_arm_seed1_bundle_20260913/`，共 20 个 entry：安装并启动入口、README、16 个完整 payload 文件和 payload/installed 双 SHA-256 清单。默认使用 physical GPU `0,2,4` 做 DataParallel 训练，GPU 4 做单卡 CUDA admission 与 held-out evaluation。
- 安装器要求 blind-SPD 日志同时包含失败和完成终态，要求 baseline 的 `log.txt` 与 `epoch-5.pth` 存在，并校验服务器当前 `models/__init__.py`、`models/gaussian.py`、`train_gaussian.py` 与 `models/edsr.py` 的固定前置哈希。三个覆盖文件先备份；若上次安装在准入中途停止，也只接受与本包逐字节相同的部分安装后重跑。
- 已在全新目录解压验证：20 个 ZIP entry 无绝对路径、反斜杠、`../`、`__pycache__` 或 `.pyc`；包内清单 `18/18`、安装后清单 `16/16`，两个 shell 均为 LF。另从 blind-SPD 终态 commit `3159492` 导出干净代码树，四个前置哈希全部命中，再覆盖本包 payload，Python compile、配置校验、分析器正反例、microbatch 等价和 IAFM CPU full-model 机制测试全部通过。
- 本机没有 Bash/CUDA，所以 `bash -n`、单 GPU CUDA mechanism test 和三 GPU DataParallel diagnostics gather/backward smoke 由服务器 installer 在后台训练启动前强制执行。Windows 本地 Torch 仍报告已知 NumPy 2 ABI warning，但所有测试退出码为 0。
- 本包只包含 seed 1。只有 train mean/final、机制门禁以及 100 张配对 held-out 的 x2/x4/x8 聚合门禁全部通过，才另行制作 seed 2/3；失败立即停止建议 E，不延长 epoch、不调松门禁。

### 建议 E / 服务器累积模型注册兼容修复 v2

- v1 在写入前被安全停止：服务器 `models/__init__.py` 的实际 SHA-256 为 `25b730714c4afff8626ca3e5b3d946c6bb201ec13ab5a77a5a1e3018f77cd887`，而不是干净 blind-SPD 分支的预期值。用户回传内容确认差异来自此前 ESC、continuous-SPD、static-LSS、MSConvStar 和 multi-primitive 等实验累积的合法注册项；`models/gaussian.py`、`train_gaussian.py`、`models/edsr.py` 与 microbatch 测试文件则全部命中预期哈希。v1 没有复制文件、创建结果或启动训练。
- 兼容修复 commit 为 `59aa22e`。v2 不再从 payload 覆盖整个 `models/__init__.py`：安装器要求去除零或一条 IAFM 注册后的文件严格对应上述服务器哈希，备份原文件后只追加一次 `from . import edsr_face_iafm_idm_arm`，并再次验证无未知改动或重复注册。这样完整保留服务器已有模型注册，也支持安装完成前中断后的安全重跑。
- 新单包为 `D:/个人资料/项目/任意尺度/face_iafm_idm_arm_seed1_bundle_v2_20260913.zip`，大小 `41451` bytes，SHA-256 为 `4da2a275e20e6fbc4120618f7f0196d7306eaf4393ddf763aa146ced5b3c89ee`。ZIP 顶层目录为同名无扩展名目录，共 19 个 entry；全新解压后包内清单 `17/17`、安装后 payload 清单 `15/15`，无绝对路径、反斜杠、`../`、`__pycache__` 或 `.pyc`，两个 shell 均为 LF。

## 2026-09-15：建议 E / IAFM soft IDM + ARM seed-1 屏拒绝

- 服务器在 physical GPU `0,2,4` 上完成 `face_gaussian_iafm_idm_arm_seed1_probe5`。五轮验证 PSNR 为 `26.9807/27.4593/27.6755/27.9799/28.1465 dB`；相对同 seed baseline 的 train mean/final delta 为 `-0.0222/-0.0328 dB`，均未达到预注册 floor `-0.010/-0.020 dB`。
- 固定 100 张 held-out 的 x2/x4/x8 delta 为 `-0.0915/-0.0291/-0.0156 dB`，三倍率平均 `-0.0454 dB`。三项均为负，其中 x2 还低于单倍率 floor `-0.030 dB`；因此不是仅由某个倍率的微小噪声造成。
- information entropy `0.080936`、IDM spatial std `0.019958`，四处 ARM raw response 最小 `0.010740`、scaled response 最小 `0.001074`，candidate-baseline output response `0.004361`，全部通过 `1e-4` 机制门禁。训练期 entropy 从 `0.5476` 降到 `0.1387`、scaled ARM response 从 `0.002576` 降到 `0.001181`，模块保持活跃且发生了优化；失败不能归因于未注册、零梯度或旁路塌缩。
- 判定为 `IAFM IDM+ARM SEED1 SCREEN FAILED`。停止 seed 2/3，不扫描 entropy weight、ARM 数量或 residual scale，不延长训练轮数，也不把 x4 接近 floor 表述为正信号。CVPR 2026 的 soft IDM+ARM 在其原始固定倍率自然图像任务上的消融收益没有迁移到本项目的人脸任意尺度 GaussianSR 设置。

## 2026-09-15：建议 F / 完整 GSASR 零训练跨域准入实现

- 从研究决策 commit `44102d9` 新建独立分支 `experiment/face-gsasr-zero-train`。本阶段只评测 ICCV 2025 GSASR 官方 paper-version EDSR/DIV2K checkpoint，不训练、不微调、不做 TTA 或后处理，也不是论文正式结果。
- 固定官方代码 commit 为 `9d2eb64a51303ce7a22fd672197185488b38e10a`，仅复制 Apache-2.0 许可下的 EDSR、Fea2GS 与 dmax CUDA rasterizer 最小子集。兼容修改只包括删除未使用 import、用等价 `reshape/permute` 替代 `einops.rearrange`，以及避开一处本机 NumPy 2 ABI 的 `torch.from_numpy` 转换；权重严格加载与参数量检查通过，encoder/decoder 参数量为 `1,220,416/19,224,381`。
- 官方 paper EDSR 权重固定为 encoder SHA-256 `a61351983470a6bbda0691980cfcba0d3b75a07305c2a14000ce663a49e54847`、decoder SHA-256 `b96999739e6d5f6a81ef9aa432ee2350ed21b205bc0e6b98e3a40613f6898d87`。权重不进入 Git，只进入服务器传输 ZIP，并由包清单与分析器双重校验。
- 评测使用同一 CelebA val 前 100 张、同一裁剪尺寸和 x2/x4/x8。GaussianSR baseline 与 GSASR candidate 均使用固定 `ToTensor -> ToPILImage -> torchvision Resize(Image.BICUBIC) -> ToTensor` LR 路径；每条记录保存原图 SHA-256、LR 字节 SHA-256、文件名、HR/LR 尺寸和倍率，分析器拒绝任何非配对输入。
- 指标固定为未量化 float RGB PSNR，与此前 GaussianSR face held-out 协议一致。分析器同时要求 baseline 三倍率均值复现 `34.08013660/28.04536727/23.67010000 dB`，误差不超过 `5e-6`，从而防止数据顺序、缩放或 checkpoint 漂移。
- 准入门禁预注册为三倍率 paired delta 平均严格大于 0、任一倍率不低于 `-0.030 dB`、每倍率重复推理最大误差不超过 `1e-6`。通过只允许后续完整迁移/人脸微调设计；失败则停止 GSASR 方向，不做 dmax、checkpoint、预处理或参数扫描。
- 运行器先在 physical GPU 4 生成 baseline 配对清单，再把 GSASR x2/x4/x8 分配到 physical GPU `0/2/4` 并行评测；三路进度带倍率前缀实时汇入 orchestrator，并各自保留独立日志。任一 CUDA 进程、配对检查或分析器失败都会写出固定 `GSASR ZERO-TRAIN ADMISSION FAILED` 与 `GSASR ZERO-TRAIN ADMISSION COMPLETED` 终态。
- 本地已通过 Python compile、分析器正例/质量失败/LR 篡改拒绝测试、官方权重严格加载和兼容 tensor 排布回归。Windows 本机 PyTorch 2.3 会报告已知 NumPy 2.x ABI warning，但测试退出码为 0；真实服务器 Python/PyTorch/CUDA 兼容性必须由安装时原地编译官方 extension 和单 GPU CUDA smoke 决定，不能预先宣称兼容。

### 建议 F / GSASR 零训练单包交付

- 实现、预注册协议与实时三路进度输出固定在 commit `95c077f7689a5f33ba604cd20552a3cac5e9e5ac`，单包安装/启动入口固定在 commit `9f495a3ff27ea54852690531a22e89cf7041142f`；均位于 `experiment/face-gsasr-zero-train`，Git 不包含官方 checkpoint、生成结果、CUDA 编译产物或传输 ZIP。
- 最终只需传输 `D:/个人资料/项目/任意尺度/face_gsasr_zero_train_admission_bundle_20260915.zip`，大小 `152,212,063` bytes，SHA-256 `0e572044d6e8222b5bc548b48332871205a7cfc7e4e90fb0265e0155e76450d1`。包已携带 encoder/decoder 权重，不依赖服务器联网下载。
- ZIP 顶层目录为 `face_gsasr_zero_train_admission_bundle_20260915/`，共 34 个 entry；其中包内哈希清单 `25/25`、安装后哈希清单 `23/23`。全新解压复验确认无绝对路径、反斜杠、`../`、`__pycache__` 或 `.pyc`，两份 shell 均为 LF，权重 SHA-256 精确命中预注册值。
- 从全新解压副本再次运行 Python compile、分析器正例/质量失败/LR 元数据篡改拒绝测试、官方 encoder/decoder 严格加载和参数量检查，全部退出码为 0。本机无 CUDA/Bash，故 `bash -n`、官方 CUDA extension 编译及单 GPU 重复推理 smoke 由服务器 installer 强制完成；任一步失败都不会启动三卡评测。
- installer 只新增独立 GSASR 评测文件与 `third_party/gsasr_paper_9d2eb64`，不覆盖 `models/__init__.py`、`models/gaussian.py`、`train_gaussian.py` 或 conda 环境；要求 IAFM 日志同时包含失败和完成终态，且拒绝覆盖任何已有结果目录、orchestrator 日志、PID 或 admission stamp。

### 建议 F / 双 GPU 0、1 调度兼容包 v2

- 因服务器当前只提供 physical GPU `0,1`，三卡 runner 改为支持两个或三个互不重复的 GPU id。双卡固定分波次执行：baseline 在 GPU 1；第一波 x2→GPU 0、x4→GPU 1 并行；两者均成功后第二波 x8→GPU 0。不同倍率不会同时占用同一张卡，模型、权重、输入、指标和准入门禁均不变。
- 双卡调度实现 commit 为 `bdec36133ba92c91d7dcf4d096192808117afc46`，默认双卡安装入口 commit 为 `168c79e9134e1ae418ca71b05d272927f26294e4`。三卡模式仍兼容，但本次交付默认 `PHYSICAL_GPUS=0,1`、`ADMISSION_GPU=1`。
- 新单包为 `D:/个人资料/项目/任意尺度/face_gsasr_zero_train_admission_bundle_v2_20260915.zip`，大小 `152,212,505` bytes，SHA-256 `32a733587df95f4db30a241ca21a3b6fe002b2fdf4d69d97bdbca301346389c9`。旧的三卡包不再用于当前服务器资源状态。
- v2 已在新的独立目录完整解压复验：34 个 ZIP entry 无不安全路径或缓存文件；包内 `PAYLOAD_SHA256` 25/25、安装后 `INSTALLED_SHA256` 23/23 全部命中；两个 shell 为 LF。解压副本的 Python compile、分析器正反例/篡改拒绝和官方权重严格加载测试均通过。

### 建议 F / manifest CRLF 安全停止与 v3 修复

- 服务器首次运行 v2 时在最前置 `sha256sum -c PAYLOAD_SHA256` 安全停止；所有 25 个路径末尾均显示 `$'\r'`。根因是 Windows PowerShell 生成的两个 manifest 使用 CRLF，Linux `sha256sum` 将 CR 解释为文件名字符。停止发生在任何 payload 复制、CUDA 编译、stamp/result 创建或后台启动之前，不存在半安装或实验结果污染。
- 模型、双卡 runner 和安装器逻辑无需修改。v3 仅将 `PAYLOAD_SHA256` 与 `INSTALLED_SHA256` 明确生成为 UTF-8 无 BOM、LF 行尾，并把 README 指向 v3 文件名；文档修复 commit 为 `cb84a5e6be36dc9bc41c235c41f4caeb83776f2c`。
- 修正单包为 `D:/个人资料/项目/任意尺度/face_gsasr_zero_train_admission_bundle_v3_20260915.zip`，大小 `152,212,488` bytes，SHA-256 `496c2980b45d46cee2bff8394739509f2107a6e1d9ccee4cc94a699e440c6d41`。全新解压检查为 34 个 entry、0 个不安全路径，两个 manifest 分别 25/25 与 23/23 命中，CR 数均为 0。
- 已上传的 v2 也可在服务器解压目录内对两个 manifest 执行 `sed -i 's/\r$//'` 后安全重试；由于第一次失败尚未安装任何文件，该方式与 v3 payload 等价，无需重新上传 152 MB。v3 是此后应保留的正式交付包，v2 仅保留作问题追溯。

### 建议 F / 中文 ZIP 路径兼容与全 ASCII v4

- v2 清单转为 LF 后，服务器 24 个代码/权重/ASCII 文档全部通过 payload 校验，仅可选研究说明 `建议F_完整GSASR迁移与零训练门禁复核_20260915.md` 因 Windows ZIP 中文文件名编码与 Linux unzip 解码不一致而找不到；安装仍停在第一道校验，未复制文件或启动任务。
- 该研究文档不参与模型、协议、测试或运行。服务器上的 v2 可从两个 manifest 与 installer targets 中删除唯一匹配 `GSASR.*20260915.md` 的行后等价继续；代码和权重哈希保持不变。
- 长期交付 v4 完全移除该可选中文路径，所有 ZIP entry 和 manifest path 均限制为 ASCII。对应构建提交为 `6c6ad0ed6bd1ba7a93c4a8b3f27ece86389bdb41`。
- 最终包 `D:/个人资料/项目/任意尺度/face_gsasr_zero_train_admission_bundle_v4_20260915.zip` 大小 `152,202,553` bytes，SHA-256 `98ca8c8d8a5fab6f297da342e1acc086bb5cc3e58ff64620c9836807c3162ca0`。全新解压验证：33 个 entry，非 ASCII/绝对/反斜杠/父目录/缓存路径共 0；`PAYLOAD_SHA256` 24/24、`INSTALLED_SHA256` 22/22，两个清单 CR 数均为 0；解压副本 compile、分析器测试与官方权重严格加载通过。

### 2026-09-18：服务器 Toolkit 前置阻塞与交付偏好

- 用户修复 v2 中文研究文档文件名后，25 个 payload 校验全部通过；随后在 `command -v nvcc` 处停止，尚未复制 payload、编译或启动评测，因此 orchestrator 日志不存在是预期状态。当前证据仅支持 nvcc 不在 PATH，不能断言 Toolkit 未安装；先检查 nvcc 所在目录、PyTorch 编译 CUDA 版本及 CUDA_HOME，再决定是否配置路径或安装匹配工具链。
- 当前可用物理卡改为 `3,4`。现有双卡 runner 支持任意两个不同的非负 GPU id，无需代码更改；使用 `PHYSICAL_GPUS=3,4 ADMISSION_GPU=4`，baseline 在 GPU 4，第一波 x2/x4 在 GPU 3/4，第二波 x8 在 GPU 3。
- 用户要求后续服务器上传包只包含运行所需文件，所有命名必须为 ASCII，不再携带研究或进展 Markdown。研究文档与版本记录继续保留本地 Git；必要的第三方 LICENSE/许可通知仍须随再分发代码保留，可使用 ASCII 的 NOTICE 文件名。当前不重复制作大包，优先排查服务器工具链。

### 2026-09-18：nvcc 路径已定位，编译兼容性待实测

- 服务器回传：`command -v nvcc` 无输出，shell CUDA_HOME 为空；Torch 为 `1.13.1+cu117`、编译 CUDA 11.7，cpp_extension 自动探测 CUDA_HOME 为 `/usr/local/cuda`；实际 nvcc 文件位于 `/usr/local/cuda-11.3/bin/nvcc`。当前失败原因是 PATH 未配置，不是已证实的 Toolkit 缺失。
- 下一步只在当前 shell 设置 `CUDA_HOME=/usr/local/cuda-11.3` 并将其 bin 目录前置到 PATH，用 GPU `3,4` 重试现有 installer；不修改系统 symlink、conda 包或 PyTorch。CUDA 11.3/11.7 的次版本差异与官方 PyTorch >=2.0 要求是兼容风险，不提前声称已通过；由真实 extension 编译和 CUDA smoke 决定。若失败，需要完整编译输出再选择匹配工具链，不绕过版本或确定性门禁。

### 2026-09-19：正式高频残差＋边缘损失的逐图配对复核准备

- 用户要求推送尺度门控失败结论后继续下一步：`cb89371` 已成功 push，另建 `experiment/face-formal-paired-audit` 独立 worktree；稳定 `main` 和主工作区未改动。
- 范围：不新建质量模块、不重训，读取服务器 `results/face_formal_v2protocol/metrics` 中 Baseline 与正式候选的 CelebA test1000、Helen test50 逐图 CSV；先核对配对、身份、倍率、裁边和指标有限性，再输出八倍率完整结果及预先固定的 x2/x2.5/x3.5/x4 子范围。
- 新增 `audit_face_formal_paired.py`（仅标准库）、测试、校验启动脚本和单 ZIP 构建脚本；目标文件为 `results/face_formal_v2protocol/paired_audit_20260919.json`，若存在则拒绝覆盖。运行者不必占用 GPU，原始 CSV/checkpoint 不进 Git 或传输包。
- 图像 bootstrap 按配对图片而非倍率独立抽样，LPIPS 低为优。该不确定性分析不涵盖训练 seed，不能替代多 seed 重复；真实 CSV 不在本地，尚未有新的实验数字。详见 `face_formal_paired_audit_protocol_20260919.md`。
- 本地合成 CSV 单元测试覆盖完整报告、重复图片、缺行、dataset 错误与歧义文件名；最终打包前还需验证一次。
- `python -m unittest -q test_audit_face_formal_paired.py` 五项全通过；`compileall`、`bash -n`、`git diff --check` 通过；LPIPS 改善占比按负差值计数有回归断言。实现 `9b05e90d92135089cffefa6b536b2167b5fe5366` 已 push。
- 单文件服务器包 `D:/个人资料/项目/任意尺度/face_formal_paired_audit_bundle_20260919.zip` 为 4,157 bytes，SHA-256 `345b8c3155ef635322a0ec3b5c653469fe8f03fa0e481574fb9ab4b40acb3091`。解压后 3 项 SHA 清单、bash 语法、Python 编译均通过；包内无中文命名、研究 MD、CSV 或权重。
- 服务器首次真实 CSV 配对复核尚待用户上传运行和回传；先不追加多 seed 重训练，也不根据已知正式汇总表推断 bootstrap CI。
- 用户按文档执行 `sha256sum -c results/face_formal_v2protocol/frozen/formal_manifest.sha256`，返回 `No such file or directory`。因此正式冻结清单当前不存在，旧记录中的“已存在正式 manifest”不应视为已核验事实；下一步先只读盘点 `results/face_formal_v2protocol`、`save/face_gaussian_*_formal_v2protocol` 和现有 metrics 文件，不用当前工作树代码重建历史清单。

### 2026-09-19：正式高频残差＋边缘损失逐图配对复核完成

- 用户回传单包运行结果：CelebA test1000、Helen test50 的 Baseline/候选逐图 CSV 均完成配对校验；八个原始倍率和预先固定的 x2/x2.5/x3.5/x4 子范围均输出，终态为 `AUDIT COMPLETED`，JSON 位于服务器 `results/face_formal_v2protocol/paired_audit_20260919.json`。
- x2--x4 四倍率聚合配对差（候选减 Baseline）为：CelebA PSNR-Y `+0.004754 dB`，image-bootstrap CI95 `[+0.000292,+0.009456]`；SSIM-Y `+0.000161`，`[+0.000112,+0.000210]`；LPIPS `-0.000464`，`[-0.000569,-0.000357]`。Helen PSNR-Y `+0.027651 dB`，`[+0.003355,+0.053191]`；SSIM-Y `+0.000350`，`[+0.000152,+0.000550]`；LPIPS `-0.000253`，`[-0.000771,+0.000260]`。
- 重点倍率：Helen x2.5 PSNR-Y `+0.057851`，CI `[+0.025112,+0.092786]`；x3.5 `+0.043244`，CI `[-0.001660,+0.090456]`；x4 `+0.037274`，CI `[-0.000915,+0.075713]`。CelebA x4 PSNR-Y `+0.009895`，CI `[+0.001218,+0.018239]`。CelebA x2.5/x3.5/x4 的 SSIM-Y 和 LPIPS 也均为正向改善。
- 低倍率限制：Helen x2 PSNR-Y `-0.027765`，CI `[-0.066560,+0.010817]`；Helen x1.5 也为 `-0.014703`。因此论文可以把主结论限定为“中倍率、尤其非整数 x2.5/x3.5 和 x4，整体改善”，不能声称每个倍率或所有数据集均优于 Baseline。
- 统计限制：CI 是逐图 paired bootstrap，不包括训练 seed 不确定性；候选仍是 seed 1。结果足以把该方案作为当前论文保底模型和主线，但不是多 seed 显著性证明。下一步停止新增网络探索，冻结正式 checkpoint/配置/代码/CSV manifest，并制作 x2.5、x3.5、x4 的 CelebA/Helen 可视化；不根据 CI 重新挑 checkpoint。

### 2026-09-19：尺度门控高频残差五倍率探针完成，质量筛选未通过

- 分支 `experiment/face-scale-gated-frequency`，实现 `00dfce1`。用户回传服务器 `face_scale_gated_frequency_bundle_20260918/runtime/results/orchestrator.log`：control/residual/gated 均 DONE，100/100 张评测完成，终态 `SCALE-GATED FREQUENCY SEED1 PROBE COMPLETED`。gated 第 30 轮 x4 训练过程验证 PSNR `29.6824`；该值与下列固定逐图配对评测不是同一汇总协议。
- 固定协议为同一个 seed-1 正式 baseline 权重、三组各 30 轮微调、CelebA val100、float RGB PSNR、固定 epoch-last、x2/x2.5/x3/x3.5/x4。逐倍率 `original/control/residual/gated` 分别为：x2 `35.46734/35.48512/35.48451/35.48445`；x2.5 `33.18996/33.20755/33.20694/33.20694`；x3 `30.34996/30.36137/30.36026/30.36056`；x3.5 `29.57435/29.58107/29.58034/29.58060`；x4 `29.57397/29.57672/29.57705/29.57711` dB。
- gated 减同预算 control 分别为 `-0.00067/-0.00061/-0.00081/-0.00048/+0.00039 dB`，五倍率平均 `-0.00044 dB`，逐图 bootstrap 95% CI `[-0.0006129,-0.0002712]`。gated 减无门控 residual 为 `-0.00006/+0.00000/+0.00030/+0.00026/+0.00006 dB`，平均 `+0.00011 dB`，图像 bootstrap 95% CI `[+0.0000488,+0.0001727]`；此区间不包含训练 seed/重新训练不确定性，不能证明稳健收益。
- 结论：相对同预算微调 control，门控模型仅 x4 微弱为正，x2–x4 预定范围均值为负；相对 residual 的增量约万分之一 dB，不能作为论文质量优势。所有微调组相对 original 的正数主要归于额外训练，不能归功新模块。首轮 seed-1 探针按质量目标判为未通过；不合并 main、不在 CelebA test/Helen 上选择性挑倍率、不继续调门控超参。下一步等待用户选择回到已有正式频率残差＋边缘损失方案或另立预注册方向。
- 原始资料留在服务器 `face_scale_gated_frequency_bundle_20260918/runtime/results/scale_gated_frequency_evaluation.json`、`results/evaluation.log`、三组 `results/*/train_console.log` 和 `save/face_scale_gated_frequency_*_seed1/epoch-last.pth`；这些生成物未提交 Git，日志给出的 JSON 相对路径以 bundle 的 `runtime/` 为根。

### 2026-09-19：尺度门控高频残差两组微调完成

- 分支 `experiment/face-scale-gated-frequency`，实现 `00dfce1`；用户回传服务器运行目录 `face_scale_gated_frequency_bundle_20260918/runtime/results/{control,residual}/train_console.log`，两组均达到固定的 epoch 30/30，未收到 gated 或最终五倍率评测日志。
- 协议：同一正式 seed-1 baseline checkpoint 初始化、CelebA train、连续倍率 `[2,4]`、30 轮、effective batch 12；control 全程 joint，residual 前 3 轮 residual-only 后 27 轮 joint；每轮验证为 x4，非最终五倍率配对评测。
- control 末轮 train loss `0.0337`、val PSNR `29.6824`；residual 末轮 train loss `0.0337`、val PSNR `29.6823`，相差 `-0.0001 dB`（残差减 control）。两组各自 x4 最高值均在第 23 轮，为 `29.6841` 与 `29.6840`；本协议固定比较 epoch-last，不能改选最高轮。
- 目前只能判断无门控残差在本次 x4 验证几乎与同预算 control 持平，既不能称其改善，也不能预测 gated 在其他倍率的效果。按原脚本流程先核对总日志是否出现 `DONE: control`、`DONE: residual`、`START: gated`，等待 gated 结束及完整 x2/x2.5/x3/x3.5/x4 配对 JSON；不重启已有任务或依据单一倍率修改门禁。

### 2026-09-18：尺度门控高频残差服务器 CUDA 准入通过

- 用户回传单包 23 项 SHA-256 全部 OK；CUDA 上有/无尺度门控模型在 x2/x2.5/x3/x3.5/x4/x8 的初始误差全部为 0，机制测试通过。
- 正式来源配置准备通过，后台任务 pid `128734`。总日志已确认 `START: control GPU=3` 与 `START: residual GPU=4`；尚未收到 epoch 训练指标，不能据此称训练完成。
- gated 组按固定协议等待前两组完成后再用 GPU 4 启动；目前其 train_console.log 可能尚不存在。总日志只在阶段变化时更新，当前 epoch 进度应读取 control/residual 的独立训练日志。
- 此次仅更新准入与启动记录，不改代码/压缩包，不自动继续其他方向；继续等待用户反馈训练与完整五倍率评测结果。

### 2026-09-18：尺度门控高频残差独立实验实现

- 用户将目标明确为部分倍率/数据上的可信改善，并授权代码修改、版本提交/push 和 Markdown 更新；不要求全倍率领先。
- 新建 `experiment/face-scale-gated-frequency` / `GaussianSR-clean-scale-gated-frequency-20260918`，从 `7a5532d` 继承稳定训练设施，不修改主工作区的未提交历史实验。
- 新模型为原 GaussianSR baseline 加零初始化方向性 query RGB 残差和尺度/局部特征 sigmoid 门控，仅改两个模块，不替换编码器或 renderer。
- 固定三组：同预算 baseline 微调、固定门控残差、学习尺度门控残差；从同一个 seed-1 正式 baseline checkpoint 初始化，30 轮、连续倍率 `[2,4]`、L1、effective batch 12。
- 控制组主干训练 30 轮；两候选前 3 轮冻结主干、后 27 轮联合微调。该训练策略差异明确保留，门控增量通过 residual/gated 两组同策略消融判定。
- 固定 x2/x2.5/x3/x3.5/x4 CelebA val100，比较最终 checkpoint，完整报告每图/每倍率 RGB PSNR 和配对图像 bootstrap；不据此直接声称正式论文提升。
- CPU 测试已通过：六倍率初始输出最大误差 0、对齐 Gumbel 随机状态的训练前向等价、新模块梯度、主干冻结/解冻、恢复优化器下一步一致与 query chunk 一致。诊断差分流程确认最初等价测试失败是未对齐训练模式随机采样，而不是共享权重或零初始化失败，未改原编码器。
- 本地独立验证环境 `.venv-scale-gated-frequency` 使用 CPU PyTorch 2.3、torchvision 0.18、NumPy 1.26；不修改服务器 GS/CUDA 环境。CUDA 与人脸收益仍待服务器验证。
- 训练 prepare/resume 集成、错误恢复协议拒绝、合成人脸图配置生成/四模型五倍率评测的端到端测试通过；原 microbatch test 通过（loss error `1.987e-08`，参数更新误差 0）；Python 编译、shell 语法与 diff 检查通过。合成测试仅用于流程验证，不是实验质量结果。
- 运行包采用英文文件名、LF 校验清单、隔离 runtime，不上传研究 Markdown，不覆盖服务器原 models/train/save/results。详见 `scale_gated_frequency_protocol_20260918.md`；单包在实现提交/push 后生成并单独核验。
- 实现提交 `00dfce19d8c61941b6b25fddeb098e9cd92c2a20`：`feat: 新增尺度门控高频残差与微调对照`，已 push 到 `origin/experiment/face-scale-gated-frequency` 后才生成压缩包。
- 单包路径：`D:/个人资料/项目/任意尺度/face_scale_gated_frequency_bundle_20260918.zip`，41,654 bytes，SHA-256 `ad6dae2d536b077682f0c211afdfc594a908e593f9746b708e8a59026a98f502`；包记录的是上述实现 commit，后续交付文档提交不改变包内代码。
- 已解压至 `_verify_scale_gated_frequency_bundle_20260918`；23 项 SHA-256 全部通过，两个 shell 语法检查通过，精简注册入口下独立 CPU 机制/prepare-resume 测试通过。包内无 Markdown、中文路径、数据集或 checkpoint。
- 当前只完成本地交付，没有连接服务器或启动人脸训练；下一步由用户上传一个 ZIP 并回传 CUDA admission/训练/评测输出，暂不继续其他模块方向。

### 2026-09-18：GSASR 零训练跨域评测完成，x8 floor 拒绝

- 用户回传完整安装及评测日志：GS 环境 Torch `1.13.1+cu117` 配合 CUDA Toolkit 11.3 已成功编译官方 gscuda extension；CUDA smoke 输出 `(1,3,24,24)`，repeat max error `1.19209290e-7`。次版本差异及缺少 ninja 仅产生 warning，未阻断本次实测。此结论仅限当前安装与推理路径，不代表官方训练流程的全面兼容。
- 使用 physical GPU `3,4`，baseline 先生成，x2/x4 第一波并行，x8 第二波完成。100 张 CelebA 配对 RGB PSNR：x2 baseline/candidate `34.08013660/35.27818947`，delta `+1.19805286 dB`；x4 `28.04536727/28.62190261`，delta `+0.57653534 dB`；x8 `23.67010000/23.54361327`，delta `-0.12648673 dB`。三倍率平均 delta `+0.54936716 dB`。
- 三倍率 repeat error 分别 `4.17232513e-7/2.38418579e-7/2.98023224e-7`，均通过 `1e-6`；baseline 数值及原图/LR 配对检查通过。终态为 `GSASR ZERO-TRAIN ADMISSION FAILED`、`GSASR ZERO-TRAIN PROBE FAILED`、`GSASR ZERO-TRAIN ADMISSION COMPLETED`，并非 CUDA、输入配对或进程异常。
- 拒绝原因仅为 x8 delta 低于预注册单倍率 floor `-0.030 dB`。x2/x4 的明显正增益是值得保留的观察，但不能替代任意尺度全倍率准入；不放宽 floor、不扫 dmax/权重/预处理，不自动启动人脸微调或完整迁移。保留全部服务器 JSON、日志、stamp 与 extension，不重复运行 installer 覆盖结果。
- 本次比较是官方 DIV2K 预训练 GSASR（encoder/decoder 共 `20,444,797` 参数）对本项目 5-epoch face GaussianSR（`1,838,159` 参数）的跨域候选准入，训练来源、预算与参数量不匹配。因此不能表述为同预算架构创新收益或论文公平对照；零训练 x8 退化也不证明 GSASR 经过公平人脸训练后必然无效。按当前预注册策略终止建议 F，后续改变实验目标须另立协议并取得用户指示。
