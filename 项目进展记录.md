# GaussianSR 人脸超分项目进展记录

本文件是代码版本、数据协议、训练运行、测试指标、创新方向和研究结论的持续记录。它记录实验事实，不等同于论文定稿。

## 强制记录规则

1. 每次出现新的研究事实，都必须及时更新本文件；记录 Markdown 与 GitHub 推送采用不同门槛。
2. 失败方向、未通过准入的探针、异常和诊断只需记录到本地 Markdown，不要求为每次失败单独提交或推送。只有方法通过预定准入、产生可复现实验资产、形成有效指标提升或论文可用结论等实质研究进展时，才提交并推送；届时一并包含此前累计但尚未推送的记录。
3. 每条记录至少包含日期、分支、目标、改动文件、实验协议、命令或运行名、结果、结论和下一步。
4. 正式实验、短程探针、准入测试和失败方案必须明确区分，不能混写。
5. 原始 CSV、逐图 CSV、配置、日志和 checkpoint 保持在服务器或本地原位置；Markdown 只记录路径、摘要和校验状态。
6. `main` 只保存稳定可复现版本。新方案使用 `experiment/<topic>` 分支，修复使用 `fix/<topic>` 分支，文档整理使用 `docs/<topic>` 分支。
7. 未通过准入标准的方案不得合并为最终模型，也不得提前写成论文最终创新。
8. 禁止把数据集、checkpoint、训练输出、凭据、缓存和传输压缩包提交到 GitHub。

以上提交与推送规则自 2026-08-02 起按用户要求执行，取代此前“每次变化都立即提交并推送”的约定。

## 2026-07-31：建立 Git 版本管理与正式实验保底快照

### 版本目标

- Git 分支：`main`。
- 首次 Git 提交：`ade78d6`（`chore: snapshot GaussianSR face SR experiments`）。
- 远端目标：`https://github.com/Hddcc/GS.git`。
- 保存当前 GaussianSR 人脸超分代码、配置和实验文档。
- 当前正式候选仅作为保底版本，不认定为最终创新模型。
- 后续新方案在独立实验分支开发，避免覆盖可复现 Baseline 和正式候选。

### GitHub 备份状态

- `main` 已推送到 `origin/main`。
- `experiment/face-structure-experts` 已创建并推送，当前与首次稳定快照一致。
- 备份标签 `formal-v2protocol-backup-20260731` 已推送。
- 数据集、checkpoint、`save/`、`results/`、`outputs/`、缓存和压缩包均被 `.gitignore` 排除，没有进入 GitHub。

### 数据与统一协议

| 数据集划分 | 数量 | 用途 |
|---|---:|---|
| CelebA train | 18,000 | 训练 |
| CelebA val | 500 | 选择最佳 checkpoint |
| CelebA test | 1,000 | 同数据集正式测试 |
| Helen test | 50 | 跨数据集正式测试 |

- 训练倍率范围：`[1.5, 8]`。
- 正式测试倍率：`1.5、2、2.5、3.5、4、5.5、7.5、8`。
- 正式训练：1,350 epoch，batch size 12，GPU `0,1,2`，seed 1，确定性训练，倍率分组采样，从头随机初始化。
- 指标：PSNR-Y、SSIM-Y、LPIPS-Alex。

### 已完成的正式运行

| 角色 | 运行名 | 状态 |
|---|---|---|
| Baseline | `face_gaussian_baseline_seed1_formal_v2protocol` | 1,350 epoch 与八倍率测试完成 |
| 保底候选 | `face_gaussian_frequency_residual_edge_seed1_formal_v2protocol` | 1,350 epoch 与八倍率测试完成 |

保底候选由“方向高频查询残差”和 `edge_weight: 0.25` 的边缘加权 L1 组成。该组合已经完整训练和测试，但两部分均不是最终确定的论文创新点。

### 正式结果摘要

| 数据集 | 平均 ΔPSNR | 平均 ΔSSIM | 平均 ΔLPIPS | PSNR/SSIM/LPIPS 改善倍率数 |
|---|---:|---:|---:|---:|
| CelebA | +0.0080 dB | +0.000044 | -0.000302 | 7 / 6 / 7 |
| Helen | +0.0206 dB | +0.000380 | +0.000027 | 6 / 8 / 5 |

结果小幅但总体有效，主要改善中高倍率；Helen 的 LPIPS 表现混合。该结果足以作为毕业论文保底对照，但创新性和提升幅度尚不足以直接确定为最终方法。

详细逐倍率结果见 `毕业论文第四章实验与方法设计.md` 和本地实验数据总账。服务器正式文件位于：

```text
results/face_formal_v2protocol
save/face_gaussian_baseline_seed1_formal_v2protocol
save/face_gaussian_frequency_residual_edge_seed1_formal_v2protocol
```

### 当前冻结状态

- 正式配置和日志已复制到 `results/face_formal_v2protocol/frozen`。
- 已存在 Baseline、候选和正式训练 manifest。
- 仍需反馈并记录 `sha256sum -c` 的完整核验结果；仅有 `ls -lh` 不能证明所有哈希均为 `OK`。

### 下一创新方向筛选结论

原计划的“关键点监督人脸区域注意力”不再作为主要创新。近两年定向检索发现 ARASFSR（arXiv:2511.16341）已经使用局部频率、相对坐标、倍率和全局坐标调制，SwinIFS（arXiv:2601.01406）已经使用关键点 Gaussian heatmap 引导人脸超分，You Only Landmark Once（arXiv:2605.14166）已经使用检测热图构建语义加权损失。继续采用关键点热图或普通人脸区域注意力，和近期工作的重合风险较高。

新的实验方向确定为“连续倍率查询级局部专家路由”（Scale-Conditioned Query Mixture of Experts）。定向检索没有发现与“GaussianSR 查询接口 + 连续倍率先验 + 每查询局部专家路由 + 防坍缩约束”直接同构的近期人脸超分方法，但该结论只表示当前检索未发现直接重复，不构成绝对无人做过的证明。

## 2026-07-31：连续倍率查询级局部专家路由 v1 实现与本地验证

### 分支与研究状态

- Git 分支：`experiment/face-structure-experts`。
- 本轮性质：新结构实现与本地冒烟验证，尚未进行服务器 5 epoch 探针，不属于正式论文结果。
- `main` 保持正式 Baseline 和保底候选的稳定快照，不直接修改。

### 方法设计

新模型注册名为 `gaussian-splatter-face-scale-moe`，保留原 GaussianSR 主干，在基础 RGB 预测后增加三个轻量查询级 RGB 残差专家：

1. 从 EDSR/GaussianSR 编码特征生成 48 维局部专家上下文。
2. 每个 HR 查询点使用局部上下文、绝对坐标、相对特征坐标、cell 和连续倍率编码独立路由。
3. 三个专家分别设置在对数归一化倍率轴的低、中、高尺度中心，尺度先验连续变化，不把倍率离散成三个固定任务。
4. 路由器在尺度先验上学习局部内容修正，三个独立 MLP 产生 RGB 残差并加权融合。
5. 使用全局 batch 专家负载均衡约束防止单专家坍缩，使用低熵约束鼓励每个查询形成明确偏好。
6. 三卡训练时由训练循环汇总各卡路由统计后计算辅助损失，避免只读取主卡临时状态造成错误。
7. 第一轮仅使用普通 L1，不加入旧候选的 `edge_weight: 0.25`，以单独判断新结构贡献。
8. 训练态返回路由统计后立即清空模型内部对计算图的临时引用，避免诊断数据跨 batch 滞留显存。

### 改动文件

```text
models/gaussian_face_scale_moe.py
models/__init__.py
train_gaussian.py
smoke_test_face_scale_moe.py
configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml
configs/train/face/probe30_face_gaussian_baseline_seed1.yaml
configs/train/face/probe30_face_gaussian_scale_moe_seed1.yaml
configs/train/face/train_face_gaussian_scale_moe_seed1.yaml
.gitignore
项目进展记录.md
```

`train_gaussian.py` 只在模型训练态返回路由诊断字典时增加辅助损失和日志；原 Baseline 仍返回单一预测张量，重建损失和默认行为保持不变。新增日志包括 reconstruction loss、auxiliary loss、router entropy、三个专家 usage 和 RGB residual 均值。

### 统一实验协议

- seed 1，确定性训练，从头随机初始化，`use_pretrained: false`。
- CelebA train 18,000，val 500；训练倍率 `[1.5, 8]`，验证固定 ×4。
- batch size 12，三卡每卡 4 张，倍率分组采样，`sample_q: 512`。
- 5、30、1,350 epoch 三组候选配置分别与同阶段 Baseline 逐字段对比通过。
- 准入顺序固定为：本地前后向 -> 服务器 GPU 冒烟 -> 5 epoch 候选探针 -> 30 epoch Baseline/候选对照 -> 满足标准后才允许 1,350 epoch。

### 已完成验证

Python 编译通过：

```text
models/gaussian_face_scale_moe.py
smoke_test_face_scale_moe.py
train_gaussian.py
models/__init__.py
validate_face_training_configs.py
```

配置校验通过：

```text
5 epoch: CONFIG VALIDATION PASSED
30 epoch: CONFIG VALIDATION PASSED
1350 epoch: CONFIG VALIDATION PASSED
```

本地环境为 CPU，使用 `inp_size=4`、`sample_q=16` 在 ×1.5、×4、×8 完成前向、反向和测试态接口检查。模型参数量为 1,919,164。结果如下：

| 倍率 | 专家使用率（低/中/高） | 路由熵 | RGB 残差绝对均值 | 梯度检查 |
|---:|---|---:|---:|---|
| 1.5 | 0.5156 / 0.3608 / 0.1236 | 0.967725 | 0.00001460 | 上下文、路由、三个专家、门控均非零 |
| 4 | 0.2568 / 0.4150 / 0.3283 | 1.079759 | 0.00001336 | 上下文、路由、三个专家、门控均非零 |
| 8 | 0.1236 / 0.3608 / 0.5156 | 0.967725 | 0.00001452 | 上下文、路由、三个专家、门控均非零 |

测试态输出为 `(1, 16, 3)` 单一张量，兼容现有 `test.py` 和 `test_face_metrics.py` 的评测调用。所有预测、损失和梯度均为有限值，`SMOKE TEST PASSED`。

兼容性补测确认原 Baseline 在训练态仍返回 `(1, 4, 3)` 单一张量；新 MoE 的 123 项 `state_dict` 可使用 `strict=True` 完整加载，无缺失键或多余键。

本地 Anaconda 环境缺少 `tensorboardX`，因此本次仅在验证进程内注入未落盘的空日志类以完成模型计算检查；服务器 GS 环境已有训练依赖，不需要据此修改项目代码或安装包。

### 服务器传输包

- 文件：`GaussianSR_face_scale_moe_v1_patch_20260731.zip`。
- 本地路径：仓库根目录。
- SHA-256：`5FD2C9168B2640F3BD809B90D52899FB62BCD093F3B0E8D611B801639642B1D9`。
- 状态：仅用于 XFTP 传输，已被 `*.zip` 忽略，不提交 GitHub。
- 内容：新模型、模型注册、训练循环、GPU/CPU 冒烟脚本和 5/30/1,350 epoch 配置，共 8 个文件，已确认保留 `models/` 与 `configs/train/face/` 目录层级。

### 服务器部署与 GPU 冒烟命令

先用 XFTP 把 `GaussianSR_face_scale_moe_v1_patch_20260731.zip` 上传到服务器仓库根目录，然后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_scale_moe_v1_patch_20260731.zip

mkdir -p server_backups/pre_scale_moe_v1_20260731/models
cp train_gaussian.py server_backups/pre_scale_moe_v1_20260731/train_gaussian.py
cp models/__init__.py server_backups/pre_scale_moe_v1_20260731/models/__init__.py

unzip -o GaussianSR_face_scale_moe_v1_patch_20260731.zip

python -m py_compile \
  models/gaussian_face_scale_moe.py \
  models/__init__.py \
  train_gaussian.py \
  smoke_test_face_scale_moe.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --expected-model-b gaussian-splatter-face-scale-moe

python smoke_test_face_scale_moe.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --device cuda \
  --gpu 3 \
  --scales 1.5 4 8 \
  --seed 1
```

上传包的服务器 `sha256sum` 必须等于：

```text
5fd2c9168b2640f3bd809b90d52899fb62bcd093f3b0e8d611b801639642b1d9
```

GPU 冒烟通过后，待执行的 5 epoch 候选探针命令为：

```bash
mkdir -p results/face_scale_moe_v1_probe5

python -u train_gaussian.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --name face_gaussian_scale_moe_seed1_probe5 \
  --gpu 0,1,2 \
  --seed 1 \
  2>&1 | tee results/face_scale_moe_v1_probe5/train_console.log
```

当前先执行到 GPU 冒烟并反馈完整输出；5 epoch 命令只有在冒烟输出确认无误后才执行。

### Git 状态

- 功能提交：`b384cdd`（`feat: add scale-conditioned query experts`）。
- 已推送：`origin/experiment/face-structure-experts`。
- 传输 ZIP、数据、checkpoint、训练结果和文献检索 XML 均未进入 Git。

### 准入标准与下一步

1. 上传本轮补丁后，先在服务器 GPU 3 运行 ×1.5、×4、×8 冒烟测试。
2. GPU 冒烟必须无 NaN、OOM、Traceback，三个专家均有非零使用率和梯度，测试态输出检查通过。
3. 随后只训练候选 5 epoch，检查训练日志中的 loss、entropy、usage 和 residual；这一步只验证可训练性，不根据 5 epoch 的单点 PSNR下正式结论。
4. 5 epoch 正常后，才运行同协议 30 epoch Baseline 和 MoE 对照。
5. 30 epoch 候选后期验证 PSNR相对 Baseline 至少呈约 `+0.03 dB` 趋势，并在 CelebA 前 50 图 ×2/×4/×8 的多数 PSNR/SSIM 上改善且 LPIPS不明显恶化，才允许启动 1,350 epoch。
6. 未通过准入标准时保留记录并调整或放弃本方向，不合并到 `main`，也不写成论文最终创新。

## 2026-07-31：服务器首次 GPU 冒烟被占用显存阻断

### 已完成项

- 补丁在服务器成功解压，8 个文件进入预期的 `models/`、仓库根目录和 `configs/train/face/` 路径。
- `models/gaussian_face_scale_moe.py`、`models/__init__.py`、`train_gaussian.py` 和 `smoke_test_face_scale_moe.py` 均通过 `py_compile`。
- 5 epoch Baseline/MoE 配置对比输出 `CONFIG VALIDATION PASSED`，确认 seed 1、epoch 5、batch size 12 和模型注册名正确。

### 阻断现象与结论

使用物理 GPU 3 执行冒烟测试时，在以下阶段报错：

```text
model = models.make(config['model']).to(device).train()
RuntimeError: CUDA error: out of memory
```

错误发生在模型转移到 GPU 时，尚未创建冒烟输入、查询坐标或执行模型前向，因此不能归因于输入尺寸、`sample_q`、路由计算或模型数值不稳定。新模型只有约 1.919M 参数，单纯权重转移所需显存很小；该现象表示 GPU 3 当时已被其他进程占用到几乎没有可用显存。

本次状态记为“环境资源阻断，GPU 冒烟尚未完成”，不是方案失败，也不是准入通过。下一步先查看各卡空闲显存，选择空闲显存最多且不承担训练任务的 GPU 重试；不得为了本测试终止来源不明或不属于用户的进程。

### 下一条服务器命令

```bash
nvidia-smi --query-gpu=index,name,memory.total,memory.used,memory.free,utilization.gpu \
  --format=csv,noheader,nounits

nvidia-smi --query-compute-apps=gpu_uuid,pid,process_name,used_memory \
  --format=csv,noheader,nounits
```

根据第一条输出选择空闲显存最多的卡，将下面的 `FREE_GPU` 替换成卡号：

```bash
FREE_GPU=0

python smoke_test_face_scale_moe.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
  --device cuda \
  --gpu "$FREE_GPU" \
  --scales 1.5 4 8 \
  --seed 1
```

需要反馈两条 `nvidia-smi` 输出和重试后的完整冒烟输出。5 epoch 训练继续保持未执行状态。

## 2026-07-31：服务器 GPU 冒烟通过

### 运行条件

- 服务器环境：Conda `GS`。
- 使用 GPU：物理 GPU 0；用户确认 GPU 0、1、2 均空闲，可用于后续三卡探针。
- 配置：`configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml`。
- 输入：`inp_size=16`，每次 512 个查询，倍率 ×1.5、×4、×8，seed 1。
- 模型参数量：1,919,164，专家数：3。

### GPU 冒烟结果

| 倍率 | 专家使用率（低/中/高） | 辅助损失 | 路由熵 | RGB 残差绝对均值 |
|---:|---|---:|---:|---:|
| 1.5 | 0.5156 / 0.3608 / 0.1236 | 0.003307 | 0.967725 | 0.00001537 |
| 4 | 0.2568 / 0.4150 / 0.3283 | 0.001456 | 1.079759 | 0.00001327 |
| 8 | 0.1236 / 0.3608 / 0.5156 | 0.003307 | 0.967725 | 0.00001465 |

- ×1.5 时低倍率专家占比最高，×4 时中倍率专家占比最高，×8 时高倍率专家占比最高，连续倍率先验行为符合设计。
- 上下文编码器、路由器、三个专家和残差门控在三个倍率下均有有限且非零的梯度。
- 三个专家均有非零使用率，没有初始单专家坍缩。
- 测试态输出为 `(1, 512, 3)` 单一张量，`SMOKE TEST PASSED`。
- `torch.meshgrid` 输出为原项目已有的弃用预警，不是运行错误。

结论：服务器 GPU 功能准入通过，可以进入 5 epoch 候选可训练性探针；该结果仍不是图像质量提升结论。

### 待执行的 5 epoch 探针

为避免覆盖已有运行，命令先检查目标路径；若路径已经存在则拒绝启动，不执行删除：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_scale_moe_seed1_probe5
OUT=results/face_scale_moe_v1_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: probe path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_scale_moe_seed1.yaml \
    --name face_gaussian_scale_moe_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

启动后检查：

```bash
PID=$(cat results/face_scale_moe_v1_probe5/train.pid)
ps -fp "$PID"
tail -n 30 results/face_scale_moe_v1_probe5/train_console.log
```

完成后需要反馈：

```bash
tail -n 30 results/face_scale_moe_v1_probe5/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v1_probe5/train_console.log \
  || true
```

## 2026-08-01：Scale MoE v1 完成 5 epoch 可训练性探针

### 运行与结果

- 运行名：`face_gaussian_scale_moe_seed1_probe5`。
- 结果目录：`results/face_scale_moe_v1_probe5`。
- GPU：0、1、2；seed 1；确定性训练；统一倍率分组协议。
- 状态：5 epoch 正常完成，控制台未出现 NaN、OOM、Traceback 或 RuntimeError。

| Epoch | 总训练损失 | 重建损失 | 辅助损失 | 路由熵 | 专家使用率（低/中/高） | RGB 残差均值 | Val PSNR |
|---:|---:|---:|---:|---:|---|---:|---:|
| 1 | 0.0897 | 0.0884 | 0.00131 | 1.0405 | 0.326 / 0.343 / 0.331 | 0.007370 | 27.0065 |
| 2 | 0.0666 | 0.0656 | 0.00105 | 0.9214 | 0.336 / 0.331 / 0.333 | 0.020696 | 27.4245 |
| 3 | 0.0638 | 0.0631 | 0.00068 | 0.3550 | 0.335 / 0.330 / 0.335 | 0.025601 | 27.7515 |
| 4 | 0.0623 | 0.0618 | 0.00043 | 0.1190 | 0.334 / 0.332 / 0.334 | 0.027185 | 27.9691 |
| 5 | 0.0600 | 0.0597 | 0.00025 | 0.0855 | 0.334 / 0.333 / 0.334 | 0.023135 | 28.1037 |

### 与同协议 5 epoch Baseline 的初步比较

此前同协议 Baseline 的验证 PSNR 为 `26.9929、27.4621、27.7418、27.9767、28.1793`。MoE 相对差值为：

```text
epoch 1: +0.0136 dB
epoch 2: -0.0376 dB
epoch 3: +0.0097 dB
epoch 4: -0.0076 dB
epoch 5: -0.0756 dB
五个 epoch 平均: -0.0195 dB
```

结论分两部分：

- 工程准入通过：训练稳定，三个专家使用率非零且总体均衡，辅助损失正常下降，残差分支确实参与优化。
- 效果准入尚未通过：第 5 epoch 和五轮平均均未优于 Baseline，不能据此启动 1,350 epoch，也不能写成最终创新结果。

路由熵由 1.0405 快速降至 0.0855，表示查询路由已接近硬选择；专家总使用率仍约为三等分，因此不是单专家坍缩。该现象需在 30 epoch 对照中继续观察。考虑到新模型参数略多、可能存在前期收敛较慢，本轮不立即调参，仅进行一次既定的 30 epoch 同协议判定；若仍无明确提升则停止 v1 正式训练并调整方案。

### 下一步：先运行 30 epoch Baseline

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_baseline_seed1_probe30_scale_moe_compare
OUT=results/face_scale_moe_v1_probe30_baseline

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: baseline probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
    --name face_gaussian_baseline_seed1_probe30_scale_moe_compare \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

运行状态与完成结果：

```bash
PID=$(cat results/face_scale_moe_v1_probe30_baseline/train.pid)
ps -fp "$PID"
tail -n 20 results/face_scale_moe_v1_probe30_baseline/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_baseline_seed1_probe30_scale_moe_compare/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v1_probe30_baseline/train_console.log \
  || true
```

Baseline 完成并确认无异常后，再启动 30 epoch MoE；当前不并行运行两者，也不启动 1,350 epoch。

## 2026-08-01：完成 30 epoch 配对 Baseline

### 运行信息

- 运行名：`face_gaussian_baseline_seed1_probe30_scale_moe_compare`。
- 配置：`configs/train/face/probe30_face_gaussian_baseline_seed1.yaml`。
- 结果目录：`results/face_scale_moe_v1_probe30_baseline`。
- GPU 0、1、2；seed 1；30 epoch；每个 epoch 固定 ×4 验证。
- 状态：正常完成；错误检索没有输出，未发现 NaN、OOM、Traceback 或 RuntimeError。

### 基准摘要

| 项目 | PSNR |
|---|---:|
| Epoch 1 | 26.9884 dB |
| Epoch 5 | 28.1628 dB |
| Epoch 10 | 28.5576 dB |
| Epoch 20 | 28.8427 dB |
| Epoch 25 | 28.9117 dB |
| Epoch 30（最终/最佳） | 29.0128 dB |
| Epoch 26-30 平均 | 28.9738 dB |
| Epoch 21-30 平均 | 28.9379 dB |

该运行是过程性 30 epoch 准入基准，不属于论文最终数据。完整逐 epoch 数据保留在服务器 `save/face_gaussian_baseline_seed1_probe30_scale_moe_compare/log.txt`。

预先固定的候选判定参考：MoE 最终 PSNR 若要达到约 `+0.03 dB`，需接近或超过 `29.0428 dB`；Epoch 26-30 平均需接近或超过 `29.0038 dB`，并且不能依靠单个偶然峰值下结论。

### 下一步：运行 30 epoch Scale MoE v1

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_scale_moe_seed1_probe30
OUT=results/face_scale_moe_v1_probe30_candidate

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: candidate probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_scale_moe_seed1.yaml \
    --name face_gaussian_scale_moe_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

状态与完成结果检查：

```bash
PID=$(cat results/face_scale_moe_v1_probe30_candidate/train.pid)
ps -fp "$PID"
tail -n 20 results/face_scale_moe_v1_probe30_candidate/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v1_probe30_candidate/train_console.log \
  || true
```

候选完成后比较最终值、最后 5 轮均值、最后 10 轮均值、路由熵和专家使用率，再决定做前 50 图测试、调整 v1，或停止该方向。当前不得启动正式 1,350 epoch。

## 2026-08-01：Scale MoE v1 未通过 30 epoch 效果准入

### 候选运行

- 运行名：`face_gaussian_scale_moe_seed1_probe30`。
- 配置：`configs/train/face/probe30_face_gaussian_scale_moe_seed1.yaml`。
- 结果目录：`results/face_scale_moe_v1_probe30_candidate`。
- GPU 0、1、2；seed 1；30 epoch；每个 epoch 固定 ×4 验证。
- 状态：正常完成；错误检索没有输出，未发现 NaN、OOM、Traceback 或 RuntimeError。

### 配对结论

| 统计项 | Baseline | Scale MoE v1 | 差值 |
|---|---:|---:|---:|
| Epoch 30 最终/最佳 | 29.0128 | 29.0030 | -0.0098 dB |
| Epoch 26-30 平均 | 28.9738 | 28.9748 | +0.0010 dB |
| Epoch 21-30 平均 | 28.9379 | 28.9412 | +0.0033 dB |

预设目标为后期约 `+0.03 dB`。候选最终值更低，后期均值仅有千分之几 dB 的差异，可视为与 Baseline 持平，不能证明有效提升。

### 路由诊断

- 路由熵从 Epoch 1 的 `1.0286` 降至 Epoch 30 的 `0.0043`，已接近完全硬路由。
- 三个专家总使用率长期稳定在约 `0.333 / 0.333 / 0.333`。
- RGB 残差绝对均值从中期约 `0.031` 回落到 Epoch 30 的 `0.009569`。
- 该组合强烈表明“均匀负载均衡 + 最小熵”把查询推向了近乎一对一的硬分区，而不是保持连续倍率先验下的柔性局部融合。
- v1 专家自身也接收完整倍率编码，使每个专家仍可覆盖全部倍率，削弱了通过路由形成尺度专长的必要性。

### 决策

1. Scale MoE v1 停止，不进行 CelebA 前 50 图测试，不启动 1,350 epoch，不合并到 `main`，不作为论文最终创新。
2. 保留代码、配置、checkpoint 和日志作为失败方案证据，不执行删除。
3. 下一版仍保留“连续倍率查询级专家”主创新，但修正训练机制：
   - 删除鼓励硬路由的熵最小化项；
   - 不再把总使用率强制成均匀三等分；
   - 用路由分布与连续倍率先验之间的轻量 KL 一致性替代原防坍缩项；
   - 对局部内容路由修正做有界限制，防止短期完全覆盖尺度先验；
   - 专家不再直接接收倍率编码，迫使低、中、高倍率专长通过路由形成。
4. 新版本单独命名为 Scale MoE v2，重新执行 CPU/GPU 冒烟和 5 epoch 探针；通过后才考虑 30 epoch。

## 2026-08-01：实现 Scale MoE v2 并通过本地冒烟

### v2 针对性改动

- 新模型注册名：`gaussian-splatter-face-scale-moe-v2`。
- 保留 v1 查询级三专家和低、中、高连续倍率中心，不改 GaussianSR 主干。
- 将局部内容路由 logits 限制为 `0.75 * tanh(logits)`，防止短期完全覆盖倍率先验。
- 尺度先验强度由 1.0 调整为 2.0，使低、中、高倍率初始专长更明确。
- `balance_loss_weight` 和 `entropy_loss_weight` 均设为 0，删除“强制三等分 + 强制硬选择”的失效组合。
- 新增路由分布相对连续倍率先验的 KL 一致性，`prior_loss_weight: 0.001`。
- 专家 MLP 不再接收四维显式倍率编码；路由器仍接收倍率编码。专家保留必要的 cell 几何信息，但必须主要依靠路由形成尺度专长。
- 日志新增 `prior_kl` 和 `prior=[低/中/高]`，可直接比较实际路由总使用率与倍率先验使用率。
- v1 默认参数、参数形状和 registry 名保持兼容，原 v1 配置和 checkpoint 不需要修改。

### 文件

```text
models/gaussian_face_scale_moe.py
train_gaussian.py
smoke_test_face_scale_moe.py
configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml
项目进展记录.md
```

当前只创建 v2 的 5 epoch 配置，不创建 30 或 1,350 epoch v2 配置，避免在可训练性和早期效果未知时提前启动更长实验。

### 本地验证

- Python 编译通过。
- v2 与 5 epoch Baseline 协议逐字段校验输出 `CONFIG VALIDATION PASSED`。
- 参数量：1,918,396；三个专家。
- CPU 使用 `inp_size=4`、16 个查询，在 ×1.5、×4、×8 完成前向、反向和测试态接口检查。

| 倍率 | 实际路由使用率 | 倍率先验使用率 | 路由熵 | prior KL | RGB 残差均值 |
|---:|---|---|---:|---:|---:|
| 1.5 | 0.6464 / 0.3165 / 0.0371 | 0.6464 / 0.3165 / 0.0371 | 0.768411 | 0.000000 | 0.00001301 |
| 4 | 0.1907 / 0.4978 / 0.3115 | 0.1907 / 0.4978 / 0.3115 | 1.026529 | 0.000000 | 0.00000914 |
| 8 | 0.0371 / 0.3165 / 0.6464 | 0.0371 / 0.3165 / 0.6464 | 0.768411 | 0.000000 | 0.00000977 |

三个倍率下上下文编码器、路由器、三个专家和残差门控均有有限且非零梯度，测试态返回 `(1, 16, 3)` 单一张量，`SMOKE TEST PASSED`。初始 KL 为 0 是预期行为，因为局部路由器末层零初始化，实际路由从倍率先验开始；重建损失仍向路由器提供非零梯度。

v1 兼容性补测：原 v1 模型仍有 123 个状态项，`strict=True` 状态加载无缺失或多余键，新增诊断字段均为有限值。

### v2 服务器补丁

- 文件：`GaussianSR_face_scale_moe_v2_patch_20260801.zip`。
- SHA-256：`DE37F3C1675ABC94E4274F95A6B88B7B591319D0A1A575D14E3B943A5656833B`。
- 大小：11,389 bytes。
- 内容：4 个服务器必需文件，已确认保留 `models/` 和 `configs/train/face/` 目录层级。
- ZIP 为 XFTP 传输文件，受 `.gitignore` 排除，不提交 GitHub。

### 服务器部署与 GPU 冒烟

用 XFTP 将补丁上传到服务器仓库根目录，然后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_scale_moe_v2_patch_20260801.zip

mkdir -p server_backups/pre_scale_moe_v2_20260801/models
cp models/gaussian_face_scale_moe.py \
  server_backups/pre_scale_moe_v2_20260801/models/gaussian_face_scale_moe.py
cp train_gaussian.py \
  server_backups/pre_scale_moe_v2_20260801/train_gaussian.py
cp smoke_test_face_scale_moe.py \
  server_backups/pre_scale_moe_v2_20260801/smoke_test_face_scale_moe.py

unzip -o GaussianSR_face_scale_moe_v2_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_scale_moe.py \
  train_gaussian.py \
  smoke_test_face_scale_moe.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml \
  --expected-model-b gaussian-splatter-face-scale-moe-v2

python smoke_test_face_scale_moe.py \
  --config configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器 SHA-256 必须为：

```text
de37f3c1675abc94e4274f95a6b88b7b591319d0a1a575d14e3b943a5656833b
```

当前只执行到 GPU 冒烟并反馈完整输出，不启动 v2 的 5 epoch 训练。

## 2026-08-01：Scale MoE v2 服务器 GPU 冒烟通过

### 服务器验证状态

- 补丁成功解压，Python 编译通过。
- v2 与 5 epoch Baseline 配置协议校验输出 `CONFIG VALIDATION PASSED`。
- GPU 0，CUDA，`inp_size=16`，512 个查询，seed 1。
- 模型参数量 1,918,396，三个专家。

| 倍率 | 实际路由使用率 | 倍率先验使用率 | 路由熵 | prior KL | RGB 残差均值 |
|---:|---|---|---:|---:|---:|
| 1.5 | 0.6464 / 0.3165 / 0.0371 | 0.6464 / 0.3165 / 0.0371 | 0.768411 | 0.000000 | 0.00001267 |
| 4 | 0.1907 / 0.4978 / 0.3115 | 0.1907 / 0.4978 / 0.3115 | 1.026529 | 0.000000 | 0.00000957 |
| 8 | 0.0371 / 0.3165 / 0.6464 | 0.0371 / 0.3165 / 0.6464 | 0.768411 | 0.000000 | 0.00000966 |

- 三个倍率下上下文、路由器、三个专家和门控均通过非零梯度断言。
- ×4 门控梯度在六位小数格式中显示为 `0.000000`，但真实值大于 0；否则冒烟脚本会抛出异常。
- 测试态输出 `(1, 512, 3)`，`SMOKE TEST PASSED`。
- 初始辅助损失和 prior KL 为 0 符合零初始化路由器从倍率先验开始的设计，训练后应出现小幅非零 KL。

结论：v2 服务器功能准入通过，可以进入 5 epoch 可训练性和早期趋势探针；仍不能视为效果提升证据。

### 下一步：5 epoch v2 探针

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_scale_moe_v2_seed1_probe5
OUT=results/face_scale_moe_v2_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: v2 probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_scale_moe_v2_seed1.yaml \
    --name face_gaussian_scale_moe_v2_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

检查与完成反馈：

```bash
PID=$(cat results/face_scale_moe_v2_probe5/train.pid)
ps -fp "$PID"
tail -n 30 results/face_scale_moe_v2_probe5/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_v2_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v2_probe5/train_console.log \
  || true
```

重点检查五项：Val PSNR、entropy 是否保持明显大于 0、prior KL 是否有限且较小、usage 是否大致跟随 prior、残差幅度是否稳定。当前不创建或运行 v2 的 30/1,350 epoch 配置。

## 2026-08-01：Scale MoE v2 完成 5 epoch 探针

### 运行结果

- 运行名：`face_gaussian_scale_moe_v2_seed1_probe5`。
- 结果目录：`results/face_scale_moe_v2_probe5`。
- GPU 0、1、2；seed 1；5 epoch；统一倍率分组协议。
- 状态：正常完成；错误检索没有输出，未发现 NaN、OOM、Traceback 或 RuntimeError。

| Epoch | 重建/总损失 | 路由熵 | prior KL | 实际 usage | 先验 usage | RGB 残差均值 | Val PSNR |
|---:|---:|---:|---:|---|---|---:|---:|
| 1 | 0.0883 | 0.9245 | 0.00209 | 0.191 / 0.440 / 0.369 | 0.201 / 0.428 / 0.370 | 0.008993 | 27.0448 |
| 2 | 0.0656 | 0.9285 | 0.00188 | 0.192 / 0.435 / 0.373 | 0.200 / 0.430 / 0.370 | 0.020773 | 27.4480 |
| 3 | 0.0630 | 0.9335 | 0.00033 | 0.198 / 0.429 / 0.372 | 0.200 / 0.429 / 0.371 | 0.021197 | 27.6967 |
| 4 | 0.0617 | 0.9300 | 0.00018 | 0.194 / 0.429 / 0.377 | 0.196 / 0.427 / 0.377 | 0.021226 | 27.9929 |
| 5 | 0.0597 | 0.9317 | 0.00010 | 0.200 / 0.428 / 0.371 | 0.202 / 0.428 / 0.370 | 0.021249 | 28.1544 |

### 机制与效果判定

- v2 路由熵始终约 0.93，没有重现 v1 从约 1 降至 0.0043 的硬路由退化。
- 实际 usage 始终贴近倍率 prior；KL 有限并由 0.00209 降至 0.00010。
- RGB 残差在约 0.021 稳定，分支持续参与训练。
- 与本轮已有的 30 epoch 配对 Baseline 前 5 轮比较，v2 各轮差值为 `+0.0564、-0.0128、-0.0692、+0.0048、-0.0084 dB`，五轮平均差约 `-0.0058 dB`。
- 第 5 轮 v2 为 28.1544，配对 Baseline 为 28.1628，差 `-0.0084 dB`，属于基本持平，机制准入通过但效果优势尚未建立。

决策：利用已经完成的 30 epoch Baseline，只追加一次 v2 30 epoch 候选运行。若最终和后期均值仍未达到约 `+0.03 dB`，停止 v2，不做前 50 图测试和正式训练。当前仍不创建 1,350 epoch 配置。

### 新增 30 epoch 配置

```text
configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml
```

除 `epoch_max: 30`、`epoch_save: 10` 和 v2 模型结构外，其数据、随机种子、优化器、batch、倍率和验证协议与 `probe30_face_gaussian_baseline_seed1.yaml` 一致。

### v2 30 epoch 配置传输与运行

- 传输包：`GaussianSR_face_scale_moe_v2_probe30_config_20260801.zip`。
- SHA-256：`0A5BDEE5A6C876589B7E4C74CAE872F657330786C08E737DBB594000EA0FED86`。
- 内容：仅 `configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml`。
- ZIP 为传输文件，不提交 GitHub。

上传到服务器仓库根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_scale_moe_v2_probe30_config_20260801.zip

CONFIG=configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml
if [ -e "$CONFIG" ]; then
  echo "STOP: v2 probe30 config already exists; do not overwrite"
else
  unzip GaussianSR_face_scale_moe_v2_probe30_config_20260801.zip
fi

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml \
  --expected-model-b gaussian-splatter-face-scale-moe-v2

RUN=save/face_gaussian_scale_moe_v2_seed1_probe30
OUT=results/face_scale_moe_v2_probe30_candidate

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: v2 probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_scale_moe_v2_seed1.yaml \
    --name face_gaussian_scale_moe_v2_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

状态与完成结果：

```bash
PID=$(cat results/face_scale_moe_v2_probe30_candidate/train.pid)
ps -fp "$PID"
tail -n 20 results/face_scale_moe_v2_probe30_candidate/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_scale_moe_v2_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_moe_v2_probe30_candidate/train_console.log \
  || true
```

## 2026-08-01：Scale MoE v2 30 epoch 候选已启动

- 运行名：`face_gaussian_scale_moe_v2_seed1_probe30`。
- 结果目录：`results/face_scale_moe_v2_probe30_candidate`。
- GPU：0、1、2；后台 PID：`679438`。
- 启动命令正常返回后台任务号，路径防覆盖检查未触发 STOP。
- 启动后立即执行验证日志 `grep` 尚无输出，是因为第一个约 3.2 分钟的 epoch 尚未完成。
- 异常检索为空，启动阶段未发现 NaN、OOM、Traceback 或 RuntimeError。
- 当前状态：运行中，预计总时长约 1.6 小时；该状态不是实验结果。

运行状态检查：

```bash
PID=679438
ps -fp "$PID"
tail -n 30 results/face_scale_moe_v2_probe30_candidate/train_console.log
```

持续查看时使用：

```bash
tail -f results/face_scale_moe_v2_probe30_candidate/train_console.log
```

`Ctrl+C` 只退出 `tail -f`，不会停止后台训练。进程结束后再执行逐 epoch 和异常检索命令。

## 2026-08-01：Scale MoE v2 未通过 30 epoch 效果准入

### 完成状态

- 运行名：`face_gaussian_scale_moe_v2_seed1_probe30`。
- 结果目录：`results/face_scale_moe_v2_probe30_candidate`。
- GPU 0、1、2；seed 1；30 epoch；固定 ×4 验证。
- 训练正常完成，未报告 NaN、OOM、Traceback 或 RuntimeError。

### 与配对 Baseline 的最终比较

| 统计项 | Baseline | Scale MoE v2 | 差值 |
|---|---:|---:|---:|
| Epoch 30 最终/最佳 | 29.0128 | 29.0093 | -0.0035 dB |
| Epoch 26-30 平均 | 28.9738 | 28.9849 | +0.0110 dB |
| Epoch 21-30 平均 | 28.9379 | 28.9453 | +0.0074 dB |

预设准入目标为后期约 `+0.03 dB`。v2 最终值低于 Baseline，最后 5/10 轮均值虽略高，但只有 `0.007–0.011 dB`，仍属于训练波动范围，不能证明有效提升。

### 机制诊断

- Epoch 30 路由熵为 `0.9314`，没有 v1 的硬路由退化。
- Epoch 30 prior KL 为 `0.00007`，实际 usage `0.201 / 0.428 / 0.372` 与 prior `0.201 / 0.428 / 0.371` 基本一致。
- RGB 残差均值稳定在约 `0.0133`，分支没有消失。
- v2 成功修复了路由训练机制，但重建损失和最终 PSNR 与 Baseline 几乎完全一致，表明问题不再是路由坍缩，而是“基础 RGB 输出后追加轻量专家残差”这一作用位置不足以带来有意义提升。

### 最终决策

1. 严格执行预先固定的准入规则，不因看到结果后再增加前 50 图测试来寻找局部优势。
2. Scale MoE v2 停止，不进行 CelebA 前 50 图测试，不创建或运行 1,350 epoch 配置，不作为论文最终创新。
3. Scale MoE v1/v2 的代码、配置、checkpoint 和日志全部保留，不执行删除，作为失败方案与研究过程证据。
4. 停止继续调整 MoE 的熵、均衡、KL、专家数或 residual gate；两轮结果已说明继续微调该 RGB 残差路线收益不高。
5. 下一创新必须改变 GaussianSR 的编码特征或高斯表示本身。暂定调研“遮挡置信度控制的双侧人脸对应特征融合”：在 Gaussian splatting 之前利用对齐人脸的左右对应关系补充局部结构，同时用可学习置信度避免胡须、发型、姿态和遮挡区域被强制对称。该方向不使用关键点 heatmap，必须先检查近两年人脸超分和对称先验工作后再实现。

本节中的 30 epoch 数据是方案准入过程数据，不进入论文最终主表或实验数据总账的正式结果区。

## 2026-08-01：启动置信度双侧人脸特征融合方向

### 分支与研究状态

- 分支：`experiment/face-bilateral-fusion`，从已完整记录 MoE 失败结论的提交创建。
- 当前性质：文献定向查重、结构实现和本地冒烟，尚未进行服务器训练，不属于论文结果。

### 定向查重结论

检索关键词覆盖 `face super-resolution / face hallucination` 与 `symmetry / bilateral / mirror / occlusion / correspondence / feature fusion` 的组合，并通过 ArXiv API、OpenAlex 和 Semantic Scholar（后者部分请求受到 429 限流）核对。相关工作主要分为：

- FSRNet（CVPR 2018）和后续工作使用关键点、解析图等显式人脸先验。
- FishFSRNet（2023）使用解析图注意力与多尺度特征融合。
- SwinIFS（arXiv:2601.01406）使用关键点 Gaussian heatmap 引导人脸超分。
- FASR（arXiv:2408.15386）及 FASR++（arXiv:2607.05702）利用多张辅助低质量人脸的特征条件和扩散模型，属于多参考输入。
- JDSR-GAN、FCSR-GAN 等较早工作处理口罩或遮挡补全，并非单张正常人脸的镜像对应特征融合。
- 传统人脸重建中“对称性”并非新概念，不能将普通水平翻转或对称约束本身表述为创新。

当前未发现与“单张 LR 输入、无需关键点或外部参考、在 Gaussian splatting 前通过成对镜像特征差异和 LR 不对称度估计置信度，再做残差特征融合”直接同构的近两年人脸超分方案。该结论只表示定向检索未发现直接重复，不是绝对无人做过的证明。

### 方法边界与结构

新模型注册名：`gaussian-splatter-face-bilateral-fusion`。创新表述限定为“遮挡/不对称置信度控制的双侧对应特征融合”，而不是泛称人脸对称先验。

1. EDSR 编码后，将每个 LR 特征与其水平镜像对应特征配对。
2. 使用原特征、镜像特征和绝对差构造双侧上下文。
3. 由 LR RGB 与镜像 RGB 的差异生成固定不对称先验；发型、姿态、遮挡和单侧细节差异越大，先验置信越低。
4. 学习置信头补充特征层判断，并强制一对镜像位置共享同一置信度。
5. 融合头预测特征残差，经可学习全局门控和局部置信度缩放后，在 Gaussian splatting 之前加入编码特征。
6. 融合末层采用小幅初始化，初始模型接近 Baseline；不使用关键点、解析图、额外标注、MoE、频域残差或边缘损失。

### 新增文件

```text
models/gaussian_face_bilateral_fusion.py
smoke_test_face_bilateral_fusion.py
configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml
```

修改 `models/__init__.py` 完成注册。训练循环保持不变，新模型训练态和测试态都返回原有预测张量接口。

### 本地验证

- Python 编译通过。
- 与 5 epoch Baseline 的数据、seed、batch、倍率、优化器和验证协议逐字段校验通过。
- 参数量：1,943,345。
- 对称输入平均置信度 `0.500000`；加入单侧扰动后降至 `0.295514`；镜像配对置信度最大误差 `0.00000000`。

| 倍率 | 平均置信度 | 平均不对称先验 | 特征残差绝对均值 | confidence/fusion/gate 梯度 |
|---:|---:|---:|---:|---|
| 1.5 | 0.193046 | 0.386093 | 0.00007912 | 2.577e-8 / 5.171e-5 / 2.846e-8 |
| 4 | 0.106413 | 0.212826 | 0.00005311 | 2.631e-8 / 5.510e-5 / 1.997e-8 |
| 8 | 0.139368 | 0.278735 | 0.00006637 | 1.659e-8 / 4.482e-5 / 1.990e-8 |

三个倍率预测、置信、残差和梯度均为有限值且关键分支梯度非零；测试态输出 `(1, 16, 3)`，`SMOKE TEST PASSED`。

### 服务器补丁与准入顺序

- 补丁：`GaussianSR_face_bilateral_fusion_patch_20260801.zip`。
- SHA-256：`E5DDDBE769C5455CF17C76A7FE0C9939DF0FDE23227AD1FE931501255EC78C5C`。
- 大小：4,768 bytes；包含 4 个文件且保留目录结构；ZIP 不提交 GitHub。
- 准入顺序：服务器 GPU 冒烟 -> 5 epoch 探针 -> 结果接近或优于配对 Baseline后才创建 30 epoch 配置。当前不创建 30/1,350 epoch 配置。

### 服务器部署与 GPU 冒烟命令

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_bilateral_fusion_patch_20260801.zip

mkdir -p server_backups/pre_bilateral_fusion_20260801/models
cp models/__init__.py \
  server_backups/pre_bilateral_fusion_20260801/models/__init__.py

unzip -o GaussianSR_face_bilateral_fusion_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_bilateral_fusion.py \
  models/__init__.py \
  smoke_test_face_bilateral_fusion.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml \
  --expected-model-b gaussian-splatter-face-bilateral-fusion

python smoke_test_face_bilateral_fusion.py \
  --config configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器 ZIP 哈希必须为：

```text
e5dddbe769c5455cf17c76a7fe0c9939df0fde23227ad1fe931501255ec78c5c
```

当前只执行到 GPU 冒烟并反馈完整输出，不启动 5 epoch 训练。

## 2026-08-01：双侧置信特征融合服务器 GPU 冒烟通过

### 服务器结果

- 配置协议校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12，模型注册名正确。
- GPU 0，CUDA，`inp_size=16`，512 查询；模型参数量 1,943,345。
- 完全对称输入平均置信度 `0.500000`，加入单侧扰动后降至 `0.289013`，镜像配对误差 `0.00000000`。

| 倍率 | 平均置信度 | 平均不对称先验 | 特征残差绝对均值 | confidence/fusion/gate 梯度 |
|---:|---:|---:|---:|---|
| 1.5 | 0.153412 | 0.306824 | 0.00010056 | 8.616e-8 / 6.448e-5 / 7.562e-8 |
| 4 | 0.145639 | 0.291278 | 0.00009241 | 5.616e-8 / 6.556e-5 / 5.003e-8 |
| 8 | 0.149634 | 0.299269 | 0.00009548 | 5.098e-8 / 6.206e-5 / 4.506e-8 |

三个倍率的预测、置信度、残差和梯度均为有限值；关键分支梯度均非零；测试态输出 `(1, 512, 3)`，`SMOKE TEST PASSED`。原项目的 `torch.meshgrid` 弃用提示不是运行错误。

结论：服务器功能准入通过，可以进行 5 epoch 可训练性与早期趋势探针；仍不代表效果提升。

### 下一步：5 epoch 双侧融合探针

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_bilateral_fusion_seed1_probe5
OUT=results/face_bilateral_fusion_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: bilateral probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_bilateral_fusion_seed1.yaml \
    --name face_gaussian_bilateral_fusion_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

启动和完成检查：

```bash
PID=$(cat results/face_bilateral_fusion_probe5/train.pid)
ps -fp "$PID"
tail -n 30 results/face_bilateral_fusion_probe5/train_console.log

grep -nE "epoch .*val:" \
  save/face_gaussian_bilateral_fusion_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_bilateral_fusion_probe5/train_console.log \
  || true
```

5 epoch 仅判断训练稳定性和早期 PSNR 趋势；没有通过前不创建 30/1,350 epoch 配置。

## 2026-08-01：双侧置信特征融合 5 epoch 探针完成

### 训练结果

- 训练进程已正常结束，5 个 epoch 全部完成。
- 未出现 NaN、显存溢出或运行时异常；日志中的 torchvision 和 `torch.meshgrid` 信息仍是弃用提示。
- 与同为 seed 1、相同数据和训练协议的 Baseline 前 5 epoch 对比：

| Epoch | Baseline PSNR | 双侧融合 PSNR | 差值（双侧融合 - Baseline） |
|---:|---:|---:|---:|
| 1 | 26.9884 | 26.9886 | +0.0002 |
| 2 | 27.4608 | 27.4644 | +0.0036 |
| 3 | 27.7659 | 27.7542 | -0.0117 |
| 4 | 27.9881 | 27.9899 | +0.0018 |
| 5 | 28.1628 | 28.1576 | -0.0052 |

5 次验证差值均值为 `-0.0023 dB`。当前结果与 Baseline 基本持平，说明训练稳定、没有明显退化，但尚无提升证据。考虑到 30 epoch 约需 1.5 小时，保留一次 30 epoch 终筛；不直接进行 1,350 epoch 正式训练。

### 30 epoch 终筛配置与判定

- 新增配置：`configs/train/face/probe30_face_gaussian_bilateral_fusion_seed1.yaml`。
- 服务器补丁：`GaussianSR_face_bilateral_fusion_probe30_patch_20260801.zip`，仅包含上述配置并保留目录结构。
- 补丁 SHA-256：`44a8305bf397a5952a3ec7c3ba398f9aa8bfa9efc292c450d06ecb2af799c178`。
- Baseline 继续使用已经完成的 `face_gaussian_baseline_seed1_probe30_scale_moe_compare`，不重复训练。
- 主要判据：双侧融合第 26 至 30 epoch 的验证 PSNR 均值相对 Baseline 同期均值至少提高 `0.03 dB`。
- 若未达到 `+0.03 dB`，停止该方向，不进行 1,350 epoch；若达到，再进行正式训练前确认。

服务器在上传新增的 30 epoch 配置后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_bilateral_fusion_probe30_patch_20260801.zip
unzip -o GaussianSR_face_bilateral_fusion_probe30_patch_20260801.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_bilateral_fusion_seed1.yaml \
  --expected-model-b gaussian-splatter-face-bilateral-fusion

RUN=save/face_gaussian_bilateral_fusion_seed1_probe30
OUT=results/face_bilateral_fusion_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: bilateral probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_bilateral_fusion_seed1.yaml \
    --name face_gaussian_bilateral_fusion_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后检查：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_bilateral_fusion_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_bilateral_fusion_probe30/train_console.log \
  || true
```

## 2026-08-01：双侧置信特征融合未通过 30 epoch 效果准入

### 服务器结果

- 运行名：`face_gaussian_bilateral_fusion_seed1_probe30`。
- 30 个 epoch 全部完成，训练日志未检出 NaN、显存溢出、Traceback 或 RuntimeError。
- 训练损失由 `0.0890` 下降至 `0.0528`，验证 PSNR 由 `26.9548` 上升至 `29.0146`，训练过程稳定。
- 对照为相同 seed、数据、batch、倍率范围和验证协议的 `face_gaussian_baseline_seed1_probe30_scale_moe_compare`。

| 统计项 | Baseline | 双侧置信融合 | 差值 |
|---|---:|---:|---:|
| Epoch 30 PSNR | 29.0128 | 29.0146 | +0.0018 |
| Epoch 26-30 平均 PSNR | 28.9738 | 28.9805 | +0.0067 |
| Epoch 21-30 平均 PSNR | 28.9379 | 28.9501 | +0.0122 |

末 5 次验证值为 `28.9618、28.9452、28.9778、29.0032、29.0146`。虽然末 10 次平均略高于 Baseline，但主要判据要求末 5 次平均至少提高 `0.03 dB`，实际仅提高 `0.0067 dB`，未通过效果准入。

### 结论与下一步

1. 双侧置信特征融合停止，不创建或运行 1,350 epoch 配置，不做 CelebA/Helen 全倍率正式测试，不作为论文最终创新。
2. 代码、配置、服务器 checkpoint 和日志全部保留，不删除，作为研究过程和失败消融记录；30 epoch 探针数据只保存在本进展文档，不进入论文正式结果表。
3. 该结果说明单纯在 EDSR 编码后注入镜像对应特征，对当前 GaussianSR 的重建上限影响很小；继续调整置信度温度、隐藏维度或 gate 初始化属于低收益微调，停止继续试参。
4. 下一方向必须直接改变高斯参数生成或高斯渲染所表达的局部结构，并保持不依赖关键点、解析图或额外标注。开始新方向前先完成结构设计和近期工作边界检查，再单独建立实验分支。

## 2026-08-01：启动守恒式成对子高斯分裂方向

### 分支与目标

- 分支：`experiment/face-conservative-gaussian-split`。
- 模型注册名：`gaussian-splatter-face-conservative-split`。
- 目标：直接改变 GaussianSR 的高斯中心与局部特征表达，使一个居中的父高斯能够按局部结构分裂为一对亚像素子高斯，表达眼睑、眉毛、鼻翼和唇线等单高斯难以描述的双边结构。
- 不使用关键点、解析图、身份网络、外部参考、额外标注、频域 RGB 残差、边缘加权损失或专家路由。

### 近期工作边界检查

通过 OpenAlex、Crossref 和论文 DOI 做了 2024-2026 定向检索。相关工作包括：

- GaussianSR，AAAI 2025，DOI `10.1609/aaai.v39i4.32369`：2D Gaussian splatting 任意尺度超分 Baseline。
- Gaussian Splatting Based on Mamba Interaction for Arbitrary Scale Image Super Resolution，IEEE SPL 2025，DOI `10.1109/LSP.2025.3615079`：Mamba 特征交互与 Gaussian SR。
- S3R-GS，Remote Sensing 2026，DOI `10.3390/rs18101585`：显著性编码、离散语义 Gaussian 路由和显著性加权训练。
- TADSR，Optics & Laser Technology 2026，DOI `10.1016/j.optlastec.2026.115812`：纹理感知动态 Gaussian splatting。
- Efficient Arbitrary-Scale Super-Resolution With Compact Gaussian Splatting，TCSVT 2026，DOI `10.1109/TCSVT.2026.3675508`：紧凑 Gaussian 表示。

因此不能将“动态高斯”“边缘感知高斯”“纹理高斯”或“双分支”本身作为创新表述。当前公开索引和可获得摘要中未发现与以下完整约束直接同构的方法：单个 2D 父高斯保留，同时生成中心偏移互为相反数、混合权重守恒、特征残差互为正负的成对子高斯，并用于无需额外先验的人脸任意尺度超分。该结论仅表示本次定向检索未发现直接重复，不构成绝对无人做过的证明。

### 实现内容

新增文件：

```text
models/gaussian_face_conservative_split.py
smoke_test_face_conservative_split.py
configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml
```

修改 `models/__init__.py` 完成模型注册。核心结构为：

1. 从每个局部编码特征计算 Sobel 结构方向，并与连续倍率编码共同送入分裂预测器。
2. 每个父高斯保留在原中心，同时产生偏移 `+d` 和 `-d` 的两个子高斯。
3. 三个分量权重为 `1-a、a/2、a/2`，总权重保持为 1，中心加权偏移为 0。
4. 两个子高斯特征为 `f+Δf` 和 `f-Δf`，配对特征平均仍为 `f`，避免无约束改变整体特征。
5. 分裂距离、局部混合权重、方向修正和配对特征残差由网络联合学习；全局 gate 初始较小，使模型从接近父高斯的状态开始。
6. 在 Gaussian rasterization 内同时渲染父分量和两个子分量，再进入原 GaussianSR 的连续坐标解码器。

专用冒烟脚本必须检查：三个倍率预测有限、中心守恒误差、特征守恒误差、分裂权重范围、新预测器梯度、全局 gate 梯度和测试态输出。

### 补丁与服务器准入

- 补丁：`GaussianSR_face_conservative_split_patch_20260801.zip`。
- SHA-256：`763b8935c33767306e79237070f2dd5c2b6cb7d8b5186f261999e3fae47eb5bb`。
- ZIP 包含 4 个文件并保留目录结构；ZIP 不提交 GitHub。

服务器将补丁上传到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_conservative_split_patch_20260801.zip

mkdir -p server_backups/pre_conservative_split_20260801/models
cp models/__init__.py \
  server_backups/pre_conservative_split_20260801/models/__init__.py

unzip -o GaussianSR_face_conservative_split_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_conservative_split.py \
  models/__init__.py \
  smoke_test_face_conservative_split.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --expected-model-b gaussian-splatter-face-conservative-split

python smoke_test_face_conservative_split.py \
  --config configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

当前只执行到配置校验和 GPU 冒烟并反馈完整输出。冒烟通过前不启动 5/30/1,350 epoch 训练。

## 2026-08-01：守恒式成对子高斯 v1 功能通过但梯度准入失败

### 服务器冒烟结果

- 模型参数量：1,866,060；CUDA、seed 1、输入 16、查询 512。
- ×1.5、×4、×8 均输出 `(1, 512, 3)`，中心与特征守恒误差均为 0，测试态输出正常，脚本打印 `SMOKE TEST PASSED`。
- 三个倍率的初始偏移均为 `0.20000002`，混合权重均为 `0.07500000`，配对子特征残差均为 `0.00000000`。
- split predictor 梯度依次为 `2.081e-8、2.458e-9、1.174e-9`；gate 梯度依次为 `6.780e-9、8.191e-10、3.143e-10`。

结论：功能、显存、数值和守恒约束均通过，但特征残差初始为零，×4/×8 的新分支梯度过弱。该状态不启动 5 epoch，避免再次出现结构存在但实际学不动的情况。

### v2 初始化修正

不改变创新结构和实验协议，只修正训练信号：

1. 初始子高斯偏移由约 `0.2` 提高到约 `1.0`，让 5×5 Gaussian kernel 上的正负子分量产生可分辨差异。
2. 初始总分裂 gate 由 `0.15` 提高到 `0.30`，局部初始混合比例由约 `0.075` 提高到约 `0.15`。
3. 配对子特征残差输出层使用小幅非零权重初始化；`f+Δf` 与 `f-Δf` 的守恒关系不变，但预测器前层可从第一步收到梯度。
4. 冒烟新增准入：平均配对特征残差必须大于 `1e-5`，split predictor 梯度不得低于 `1e-8`，gate 梯度不得低于 `1e-9`。

v2 仍先重新执行语法检查、配置校验和 GPU 冒烟，不直接训练。

v2 服务器补丁：

- 文件：`GaussianSR_face_conservative_split_v2_patch_20260801.zip`。
- SHA-256：`69104720b5c2fd158f5e54eabfcff48deb250aca02e38afea9294e39ccd5bd0f`。
- ZIP 包含模型注册、模型、5 epoch 配置和冒烟脚本共 4 个文件；ZIP 不提交 GitHub。

服务器上传 v2 补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_conservative_split_v2_patch_20260801.zip
unzip -o GaussianSR_face_conservative_split_v2_patch_20260801.zip

python -m py_compile \
  models/gaussian_face_conservative_split.py \
  models/__init__.py \
  smoke_test_face_conservative_split.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --expected-model-b gaussian-splatter-face-conservative-split

python smoke_test_face_conservative_split.py \
  --config configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

## 2026-08-01：守恒式成对子高斯 v2 GPU 冒烟通过

### 服务器结果

- 模型参数量仍为 1,866,060，说明 v2 只修改初始化和准入检查，没有增加结构或改变参数规模。
- ×1.5、×4、×8 均输出 `(1, 512, 3)`，测试态输出正常，脚本打印 `SMOKE TEST PASSED`。
- 三个倍率的中心守恒误差和特征守恒误差均为 0。

| 倍率 | 平均偏移 | 平均混合权重 | 平均配对特征残差 | split predictor 梯度 | gate 梯度 |
|---:|---:|---:|---:|---:|---:|
| 1.5 | 1.00000000 | 0.15000001 | 0.00255400 | 6.993e-7 | 3.177e-7 |
| 4 | 1.00000000 | 0.15000001 | 0.00223322 | 9.600e-8 | 4.156e-8 |
| 8 | 1.00000000 | 0.15000001 | 0.00209698 | 3.902e-8 | 1.541e-8 |

相较 v1，split predictor 梯度提高约 33-39 倍，gate 梯度提高约 47-51 倍；配对子特征残差由严格为零变为非零，且没有破坏守恒关系。v2 通过功能、数值、显存、守恒和训练信号准入，可以进行 5 epoch 早期趋势探针，仍不代表效果提升。

### 下一步：v2 运行 5 epoch

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_conservative_split_v2_seed1_probe5
OUT=results/face_conservative_split_v2_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: conservative split v2 probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_conservative_split_seed1.yaml \
    --name face_gaussian_conservative_split_v2_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_conservative_split_v2_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_conservative_split_v2_probe5/train_console.log \
  || true
```

同协议 Baseline 前 5 epoch PSNR 为 `26.9884、27.4608、27.7659、27.9881、28.1628`。v2 结果完成前不创建或运行 30/1,350 epoch 配置。

## 2026-08-02：守恒式成对子高斯 v2 完成 5 epoch 探针

### 训练结果

- 运行名：`face_gaussian_conservative_split_v2_seed1_probe5`。
- 5 个 epoch 全部完成；错误检索未发现 NaN、显存溢出、Traceback 或 RuntimeError。
- 训练损失由 `0.0884` 下降至 `0.0596`，训练过程稳定。

| Epoch | Baseline PSNR | 守恒分裂 v2 PSNR | 差值（v2 - Baseline） |
|---:|---:|---:|---:|
| 1 | 26.9884 | 26.9979 | +0.0095 |
| 2 | 27.4608 | 27.4570 | -0.0038 |
| 3 | 27.7659 | 27.7617 | -0.0042 |
| 4 | 27.9881 | 27.9515 | -0.0366 |
| 5 | 28.1628 | 28.1609 | -0.0019 |

5 次验证均值为 `27.6658 dB`，Baseline 为 `27.6732 dB`，差值 `-0.0074 dB`。最终值仅低 `0.0019 dB`，当前属于基本持平而非效果提升。

### 判定与下一步

该方向直接改变核心高斯表示，且训练稳定、最终值未明显退化。保留一次约 1.5 小时的 30 epoch 终筛，不在 5 epoch 结果上调参，也不直接启动 1,350 epoch。

- 新增配置：`configs/train/face/probe30_face_gaussian_conservative_split_seed1.yaml`。
- Baseline 使用现有 `face_gaussian_baseline_seed1_probe30_scale_moe_compare`，不重复训练。
- 准入标准：Epoch 26-30 平均 PSNR 至少达到 `29.0038 dB`，即相对 Baseline 的 `28.9738 dB` 提高至少 `0.03 dB`。
- 未达到门槛则停止该方向，不进行正式训练和全倍率测试。

30 epoch 配置补丁：

- 文件：`GaussianSR_face_conservative_split_probe30_patch_20260802.zip`。
- SHA-256：`cc6c1dd19d760667767e13a586842e04d19fd6bd2beedd9caca4d7d7507da7aa`。
- ZIP 仅包含 30 epoch 配置并保留目录结构；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_conservative_split_probe30_patch_20260802.zip
unzip -o GaussianSR_face_conservative_split_probe30_patch_20260802.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_conservative_split_seed1.yaml \
  --expected-model-b gaussian-splatter-face-conservative-split

RUN=save/face_gaussian_conservative_split_v2_seed1_probe30
OUT=results/face_conservative_split_v2_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: conservative split v2 probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_conservative_split_seed1.yaml \
    --name face_gaussian_conservative_split_v2_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_conservative_split_v2_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_conservative_split_v2_probe30/train_console.log \
  || true
```

## 2026-08-02：守恒式成对子高斯 v2 未通过 30 epoch 效果准入

### 服务器结果

- 运行名：`face_gaussian_conservative_split_v2_seed1_probe30`。
- 30 个 epoch 全部完成，训练损失由 `0.0883` 下降至 `0.0528`。
- 错误检索未发现 NaN、显存溢出、Traceback 或 RuntimeError。
- 结果与相同 seed、数据、batch、倍率范围和验证协议的已有 Baseline 30 epoch 运行比较。

| 统计项 | Baseline | 守恒分裂 v2 | 差值 |
|---|---:|---:|---:|
| Epoch 30 PSNR | 29.0128 | 29.0080 | -0.0048 |
| Epoch 26-30 平均 PSNR | 28.9738 | 28.9711 | -0.0027 |
| Epoch 21-30 平均 PSNR | 28.9379 | 28.9397 | +0.0018 |

候选 Epoch 26-30 为 `28.9484、28.9637、28.9347、29.0009、29.0080`，平均 `28.9711 dB`。预设准入线为 `29.0038 dB`，实际低 `0.0327 dB`；相对 Baseline 同期均值没有提升。

### 结论

1. 守恒式成对子高斯分裂停止，不创建或运行 1,350 epoch 配置，不进行 CelebA/Helen 全倍率正式测试，不作为论文最终创新。
2. 服务器 checkpoint、日志和本地代码全部保留，不删除；探针结果只进入本进展文档，不进入论文正式结果表。
3. v2 已证明结构、守恒约束和训练信号均实际工作，但单纯增加成对高斯中心和特征自由度没有改善当前 GaussianSR 的 PSNR 上限；不继续调整 offset、gate 或 feature delta。
4. 下一方向不再继续修改单个高斯核、路由、镜像融合或 RGB 小残差。优先回到已经获得小幅正式提升的频率残差与边缘训练保底版本，在其基础上寻找能够扩大提升、且可形成完整方法描述的跨层特征重建机制。

## 2026-08-02：启动人脸高斯几何-外观职责解耦方向

### 分支与研究边界

- 分支：`experiment/face-geometry-appearance-decoupling`。
- 新模型注册名：`gaussian-splatter-face-geometry-appearance`。
- 新编码器注册名：`edsr-face-geometry-appearance`。
- 第一轮使用普通 L1，不加入 `edge_weight: 0.25`，以单独判断新结构贡献；频率查询残差作为已有保底重建端保留。

2024-2026 定向检索覆盖“geometry/appearance decoupling、structure/appearance Gaussian、shallow/deep Gaussian SR”等组合。相关边界包括：

- GStex，arXiv 2024，DOI `10.48550/arXiv.2409.12954`：在 2D Gaussian splatting 视图合成中使用逐 primitive 纹理实现几何与外观解耦。
- SuperGS，arXiv 2024，DOI `10.48550/arXiv.2410.02571`：面向 3D Gaussian 场景超分的残差特征与不确定性学习。
- GaussianSR，AAAI 2025，DOI `10.1609/aaai.v39i4.32369`：当前 2D Gaussian 任意尺度图像超分 Baseline。
- Mamba Interaction Gaussian SR，IEEE SPL 2025，DOI `10.1109/LSP.2025.3615079`：Gaussian SR 的 Mamba 特征交互。
- S3R-GS，Remote Sensing 2026，DOI `10.3390/rs18101585`：显著性编码、语义 Gaussian 路由与加权训练。

几何/外观解耦本身已存在于 Gaussian view synthesis，不能泛称为新概念。本方向的限定表述是：在人脸单图任意尺度 2D Gaussian SR 中，将 EDSR 浅中层结构特征专用于 Gaussian 参数概率，将深层特征与门控跨层残差专用于 Gaussian appearance，再连接倍率感知的查询级方向频率残差。当前公开索引未发现这一完整职责划分的直接同构工作；该表述不等于绝对无人做过。

### 实现内容

新增：

```text
models/edsr_geometry_appearance.py
models/gaussian_face_geometry_appearance.py
smoke_test_face_geometry_appearance.py
configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml
```

修改：

```text
models/__init__.py
validate_face_training_configs.py
```

结构分工：

1. EDSR 第 4 个残差块输出作为浅层轮廓特征，第 10 个残差块输出作为中层部件结构特征，第 16 个残差块与尾卷积输出作为深层外观特征。
2. 浅层和中层各自投影后只进入 geometry head，产生 100 类 Gaussian 参数概率，不直接承担最终外观融合。
3. 浅层、中层和深层特征经过独立 appearance fusion，使用小幅初始化和可学习 gate 作为深层外观残差。
4. Gaussian rasterization 使用解耦后的 geometry 概率与 appearance 特征；连续坐标解码后再使用已有倍率感知方向频率残差修正 RGB。
5. 第一轮不使用边缘权重，保证与普通 L1 Baseline 的训练协议一致。

配置校验器新增可选参数 `--expected-encoder-b`。默认行为不变；只有显式传入时才允许候选 encoder spec 不同，同时仍校验候选编码器名称、`no_upsampling`、数据、batch、优化器、epoch、decoder 和 Gaussian kernel 协议。

### 补丁与服务器准入

- 补丁：`GaussianSR_face_geometry_appearance_patch_20260802.zip`。
- SHA-256：`c3c1271a0a36a3a4931dc566a10ea432f6b4f0d5bf1cc689f06bacfa5d16d432`。
- ZIP 包含 7 个文件并保留目录结构；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_geometry_appearance_patch_20260802.zip

mkdir -p server_backups/pre_geometry_appearance_20260802/models
cp models/__init__.py \
  server_backups/pre_geometry_appearance_20260802/models/__init__.py
cp models/gaussian_face_frequency_residual.py \
  server_backups/pre_geometry_appearance_20260802/models/gaussian_face_frequency_residual.py
cp validate_face_training_configs.py \
  server_backups/pre_geometry_appearance_20260802/validate_face_training_configs.py

unzip -o GaussianSR_face_geometry_appearance_patch_20260802.zip

python -m py_compile \
  models/edsr_geometry_appearance.py \
  models/gaussian_face_geometry_appearance.py \
  models/gaussian_face_frequency_residual.py \
  models/__init__.py \
  validate_face_training_configs.py \
  smoke_test_face_geometry_appearance.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --expected-model-b gaussian-splatter-face-geometry-appearance \
  --expected-encoder-b edsr-face-geometry-appearance

python smoke_test_face_geometry_appearance.py \
  --config configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

当前只执行配置校验和 GPU 冒烟。完整输出反馈前不启动 5/30/1,350 epoch 训练。

## 2026-08-02：几何-外观职责解耦服务器 GPU 冒烟通过

### 服务器结果

- 配置输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、普通 L1，模型和候选编码器注册名正确。
- 模型参数量 2,024,100；输入 16、查询 512；appearance gate 初值 0.25，频率残差 gate 初值 0.50。
- ×1.5、×4、×8 的预测与全部诊断值有限，测试态输出 `(1, 512, 3)`，脚本输出 `SMOKE TEST PASSED`。

| 倍率 | geometry 熵 | 概率和误差 | appearance 残差 | RGB 残差 | geometry/appearance/gate/residual 梯度 |
|---:|---:|---:|---:|---:|---|
| 1.5 | 2.912414 | 2.38e-7 | 0.00016807 | 0.00002932 | 7.076e-7 / 2.066e-4 / 3.758e-8 / 6.166e-2 |
| 4 | 2.988026 | 1.79e-7 | 0.00016635 | 0.00002495 | 1.289e-7 / 1.956e-4 / 2.805e-8 / 6.039e-2 |
| 8 | 2.997806 | 2.38e-7 | 0.00016166 | 0.00002234 | 3.933e-8 / 2.031e-4 / 8.620e-9 / 6.108e-2 |

geometry 概率和误差仅为 `10^-7` 量级；geometry、appearance、gate 和查询频率残差四部分梯度均非零。appearance 与查询残差分支梯度充足，geometry 在高倍率仍高于此前守恒分裂设置的最低训练信号参考。功能、数值、显存、职责分支和训练信号准入通过，可以进行普通 L1 的 5 epoch 早期趋势探针，仍不代表效果提升。

### 下一步：普通 L1 5 epoch 探针

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

RUN=save/face_gaussian_geometry_appearance_seed1_probe5
OUT=results/face_geometry_appearance_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry appearance probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --name face_gaussian_geometry_appearance_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_appearance_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe5/train_console.log \
  || true
```

主要比较同协议 Baseline 前 5 epoch：`26.9884、27.4608、27.7659、27.9881、28.1628`。结果完成前不加入 edge weight，不创建或运行 30/1,350 epoch 配置。

## 2026-08-02：几何-外观职责解耦完成 5 epoch 探针

### 训练结果

- 运行名：`face_gaussian_geometry_appearance_seed1_probe5`。
- 5 个 epoch 全部完成，训练损失由 `0.0867` 下降至 `0.0593`；进程状态为 `Done`，日志尾部没有异常信息。
- 第一轮为普通 L1，不包含 `edge_weight`。

| Epoch | Baseline PSNR | 几何-外观候选 PSNR | 差值（候选 - Baseline） |
|---:|---:|---:|---:|
| 1 | 26.9884 | 27.0180 | +0.0296 |
| 2 | 27.4608 | 27.4557 | -0.0051 |
| 3 | 27.7659 | 27.7460 | -0.0199 |
| 4 | 27.9881 | 28.0584 | +0.0703 |
| 5 | 28.1628 | 28.2235 | +0.0607 |

候选 5 次验证均值为 `27.7003 dB`，Baseline 为 `27.6732 dB`，平均提高 `+0.0271 dB`；最终提高 `+0.0607 dB`。训练损失末值也由 Baseline 的约 `0.0597` 降至 `0.0593`。这是当前新结构中第一个在 5 epoch 同时出现末期持续提升和有意义均值提升的候选。

### 判定与下一步

1. 通过 5 epoch 效果准入，进行普通 L1 的 30 epoch 终筛。
2. 不修改层数、gate、geometry 维度或残差上限，不加入边缘权重，避免在正向结果上追参。
3. 新增 `configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml`；Baseline 继续使用已有同协议 30 epoch 结果，不重复训练。
4. 30 epoch 主要准入：Epoch 26-30 平均至少达到 `29.0038 dB`，即相对 Baseline 同期平均提高至少 `0.03 dB`。
5. 通过前不创建或运行 1,350 epoch 配置，不做全倍率测试。

30 epoch 配置补丁：

- 文件：`GaussianSR_face_geometry_appearance_probe30_patch_20260802.zip`。
- SHA-256：`3e78b8f81d644165d71fc3e6f810d1be9650b66505c96a303ed63304f036ee95`。
- ZIP 仅包含 30 epoch 配置并保留目录结构；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe5/train_console.log \
  || true

sha256sum GaussianSR_face_geometry_appearance_probe30_patch_20260802.zip
unzip -o GaussianSR_face_geometry_appearance_probe30_patch_20260802.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_baseline_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml \
  --expected-model-b gaussian-splatter-face-geometry-appearance \
  --expected-encoder-b edsr-face-geometry-appearance

RUN=save/face_gaussian_geometry_appearance_seed1_probe30
OUT=results/face_geometry_appearance_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry appearance probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml \
    --name face_gaussian_geometry_appearance_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_appearance_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe30/train_console.log \
  || true
```

## 2026-08-02：几何-外观 30 epoch 配置服务器准入通过

- 服务器补丁 SHA-256 与本地记录一致：`3e78b8f81d644165d71fc3e6f810d1be9650b66505c96a303ed63304f036ee95`。
- ZIP 正确解压到 `configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml`。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、30 epoch、batch size 12、Baseline 与候选模型名正确，候选编码器为 `edsr-face-geometry-appearance`。
- 可以使用既定运行名 `face_gaussian_geometry_appearance_seed1_probe30` 启动 30 epoch；不得修改配置或覆盖已有路径。

### 2026-08-02：30 epoch 配置已部署到服务器

- 服务器计算 SHA-256 为 `3e78b8f81d644165d71fc3e6f810d1be9650b66505c96a303ed63304f036ee95`，与本地记录完全一致。
- ZIP 已成功解压，目标文件为 `configs/train/face/probe30_face_gaussian_geometry_appearance_seed1.yaml`。
- 下一步先运行配置协议校验；通过后启动 `face_gaussian_geometry_appearance_seed1_probe30`，不得跳过校验或覆盖已有路径。

## 2026-08-02：几何-外观普通 L1 30 epoch 保持小幅正提升

### 训练结果

- 运行名：`face_gaussian_geometry_appearance_seed1_probe30`。
- 30 个 epoch 全部完成，训练损失在 Epoch 30 降至 `0.0526`；进程状态为 `Done`，日志尾部没有异常信息。

| 统计项 | Baseline | 几何-外观候选 | 差值 |
|---|---:|---:|---:|
| Epoch 30 PSNR | 29.0128 | 29.0223 | +0.0095 |
| Epoch 26-30 平均 PSNR | 28.9738 | 28.9930 | +0.0191 |
| Epoch 21-30 平均 PSNR | 28.9379 | 28.9572 | +0.0193 |

候选 Epoch 26-30 为 `28.9753、28.9757、28.9824、29.0092、29.0223`，平均 `28.9930 dB`。它比 Baseline 同期稳定提高约 `0.019 dB`，但低于独立正式训练准入线 `29.0038 dB`，差 `0.0108 dB`。

### 判定

1. 普通 L1 几何-外观结构没有达到单独启动 1,350 epoch 的 `+0.03 dB` 门槛，当前不正式训练。
2. 与此前失败方向不同，本候选的末 10、末 5 和最终值均为正，且 5 epoch 也曾提高 `+0.0607 dB`，因此保留为组合基础，不淘汰结构。
3. 按预先分开的实验计划，只增加已有保底训练策略 `edge_weight: 0.25` 做一次 5 epoch 组合测试；模型结构和所有参数保持不变。
4. edge 组合使用已有 `probe_face_gaussian_frequency_residual_edge_seed1.yaml` 作为同损失参考，不重跑参考模型。其 5 epoch PSNR 为 `27.0555、27.4216、27.7273、28.0021、28.1781`。
5. 新增 `configs/train/face/probe_face_gaussian_geometry_appearance_edge_seed1.yaml`。候选 5 epoch 必须至少超过普通 L1 几何-外观的最终 `28.2235` 和均值 `27.7003`，否则停止 edge 组合。

配置校验器新增向后兼容的可选参数 `--expected-baseline-model`，默认仍为 `gaussian-splatter`。本次显式指定已有频率残差 edge 参考模型，从而校验两者的 edge loss、数据、batch、epoch、decoder、Gaussian kernel 和频率残差设置一致，只允许候选模型名与 encoder spec 不同。

edge 组合 5 epoch 补丁：

- 文件：`GaussianSR_face_geometry_appearance_edge_probe5_patch_20260802.zip`。
- SHA-256：`8872e0b91cff2620b8adb0dc0a63da596daef8cd423dbb4aeb771a6e9d1d2717`。
- ZIP 包含 edge 组合配置与新版配置校验器共 2 个文件；ZIP 不提交 GitHub。

服务器上传补丁到项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_probe30/train_console.log \
  || true

sha256sum GaussianSR_face_geometry_appearance_edge_probe5_patch_20260802.zip

mkdir -p server_backups/pre_geometry_appearance_edge_20260802
cp validate_face_training_configs.py \
  server_backups/pre_geometry_appearance_edge_20260802/validate_face_training_configs.py

unzip -o GaussianSR_face_geometry_appearance_edge_probe5_patch_20260802.zip
python -m py_compile validate_face_training_configs.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_frequency_residual_edge_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_geometry_appearance_edge_seed1.yaml \
  --expected-baseline-model gaussian-splatter-face-frequency-residual \
  --expected-model-b gaussian-splatter-face-geometry-appearance \
  --expected-encoder-b edsr-face-geometry-appearance

RUN=save/face_gaussian_geometry_appearance_edge_seed1_probe5
OUT=results/face_geometry_appearance_edge_probe5

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry appearance edge probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_geometry_appearance_edge_seed1.yaml \
    --name face_gaussian_geometry_appearance_edge_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

完成后反馈：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_appearance_edge_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_edge_probe5/train_console.log \
  || true
```

## 2026-08-02：几何-外观与 edge 组合通过 5 epoch 准入

### 训练结果

- 运行名：`face_gaussian_geometry_appearance_edge_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；当前回传日志未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练使用 `edge_weight: 0.25`，因此加权训练损失不能与普通 L1 的损失数值直接比较，准入只比较相同验证协议下的 PSNR。

| Epoch | 几何-外观普通 L1 | 几何-外观 + edge | 差值（edge - 普通 L1） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0453 | +0.0273 |
| 2 | 27.4557 | 27.5175 | +0.0618 |
| 3 | 27.7460 | 27.8125 | +0.0665 |
| 4 | 28.0584 | 28.0889 | +0.0305 |
| 5 | 28.2235 | 28.2355 | +0.0120 |

edge 候选 5 次验证均值为 `27.7399 dB`，普通 L1 几何-外观为 `27.7003 dB`，均值提高 `+0.0396 dB`，最终提高 `+0.0120 dB`。相对旧频率残差 edge 参考的均值 `27.6769 dB` 提高 `+0.0630 dB`，最终提高 `+0.0574 dB`。

### 判定与 30 epoch 终筛

1. 候选同时超过预先规定的普通 L1 几何-外观最终值与 5 次均值，正式通过 5 epoch 准入。
2. 新增 `probe30_face_gaussian_frequency_residual_edge_seed1.yaml`，它只用于确认相同 edge 训练协议，不运行、不重复训练旧参考模型。
3. 新增 `probe30_face_gaussian_geometry_appearance_edge_seed1.yaml`，仅将候选从 5 epoch 改为 30 epoch，并把 `epoch_save` 改为 10；其余数据、损失、模型与优化器设置不变。
4. 30 epoch 主要门槛：Epoch 26-30 平均 PSNR 至少达到 `29.0038 dB`；同时应比普通 L1 几何-外观末 5 平均 `28.9930 dB` 高约 `0.01 dB`。
5. 只有通过 30 epoch 终筛后才创建 1,350 epoch 正式配置；否则停止该 edge 组合，避免无效长训练。
6. 本次仍属于探针过程数据，只写入进展记录，不进入论文正式结果表。

长期记录规则保持不变：每次出现新的代码、配置、训练数据、实验方向、创新点或结论，都必须同步更新本 Markdown 文件，并提交、推送对应 Git 分支。

30 epoch 终筛补丁：

- 文件：`GaussianSR_face_geometry_appearance_edge_probe30_patch_20260802.zip`。
- SHA-256：`77c57ff4b657383f3c115e5fa61e95e45f0191e8ee149ac059cc94d75c1b0bff`。
- ZIP 仅包含两个 30 epoch 配置并保留 `configs/train/face/` 目录结构；ZIP 本身不提交 GitHub。
- 本地项目校验器已返回 `CONFIG VALIDATION PASSED`：seed 1、30 epoch、batch size 12、参考模型、候选模型及候选编码器均正确。

通过 XFTP 将 ZIP 上传到服务器项目根目录后执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_geometry_appearance_edge_probe30_patch_20260802.zip
unzip -o GaussianSR_face_geometry_appearance_edge_probe30_patch_20260802.zip

python validate_face_training_configs.py \
  --baseline configs/train/face/probe30_face_gaussian_frequency_residual_edge_seed1.yaml \
  --model-b configs/train/face/probe30_face_gaussian_geometry_appearance_edge_seed1.yaml \
  --expected-baseline-model gaussian-splatter-face-frequency-residual \
  --expected-model-b gaussian-splatter-face-geometry-appearance \
  --expected-encoder-b edsr-face-geometry-appearance

RUN=save/face_gaussian_geometry_appearance_edge_seed1_probe30
OUT=results/face_geometry_appearance_edge_probe30

if [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry appearance edge probe30 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe30_face_gaussian_geometry_appearance_edge_seed1.yaml \
    --name face_gaussian_geometry_appearance_edge_seed1_probe30 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

注意：`probe30_face_gaussian_frequency_residual_edge_seed1.yaml` 只用于协议校验，绝对不运行。实际只训练 `probe30_face_gaussian_geometry_appearance_edge_seed1.yaml`。

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_appearance_edge_seed1_probe30/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_appearance_edge_probe30/train_console.log \
  || true
```

## 2026-08-02：几何-外观与 edge 组合未通过 30 epoch 终筛

### 训练结果

- 运行名：`face_gaussian_geometry_appearance_edge_seed1_probe30`。
- 30 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- Epoch 30 的加权训练损失为 `0.0588`，验证 PSNR 为 `29.0180 dB`。加权损失不能与普通 L1 损失直接比较。

| 统计项 | 原始 Baseline | 几何-外观普通 L1 | 几何-外观 + edge |
|---|---:|---:|---:|
| Epoch 30 PSNR | 29.0128 | 29.0223 | 29.0180 |
| Epoch 26-30 平均 PSNR | 28.9738 | 28.9930 | 28.9772 |
| Epoch 21-30 平均 PSNR | 28.9379 | 28.9572 | 28.9518 |

edge 候选 Epoch 26-30 为 `28.9293、28.9830、28.9690、28.9868、29.0180`，平均 `28.9772 dB`。它只比原始 Baseline 同期平均高 `+0.0034 dB`，比几何-外观普通 L1 低 `-0.0158 dB`，并低于预设正式准入线 `29.0038 dB` 达 `0.0266 dB`。最终值相对 Baseline 为 `+0.0052 dB`，相对普通 L1 几何-外观为 `-0.0043 dB`。

### 判定

1. `edge_weight: 0.25` 在 5 epoch 的早期优势没有延续到 30 epoch，不能依据短探针结果启动长训练。
2. 该组合未通过终筛，停止此方向；不创建、不运行 1,350 epoch 正式配置，也不执行全倍率正式测试。
3. 本结果属于分析用探针数据，只保留在本进展记录中，不进入论文正式结果表。
4. 几何-外观普通 L1 仍是该结构分支上更好的基础版本：末 5 和末 10 均稳定超过 Baseline，但尚未达到独立正式训练门槛，因此暂不直接长训。
5. 下一版创新应继续以普通 L1 几何-外观模型为基础，针对其结构本身增强有效信息传递；不再叠加 edge loss，也不重复已失败的 MoE、双边融合或高斯分裂路线。

## 2026-08-02：启动几何引导外观融合候选

### 改动方向与创新边界

新候选命名为“几何引导的外观融合”，模型注册名为 `gaussian-splatter-face-geometry-guided-appearance`，编码器注册名为 `edsr-face-geometry-guided-appearance`。

普通几何-外观模型虽然将浅层/中层特征用于 Gaussian geometry、将深层与跨层特征用于 appearance，但两个分支彼此不交流。本候选保留原有职责划分，并新增以下结构连接：

1. 对 geometry logits 使用稳定 softmax，得到不受训练态 Gumbel 采样与评估态 hard selection 切换影响的几何分布。
2. 用两层轻量卷积把几何分布编码成空间几何嵌入。
3. 通过独立可学习门控，将几何嵌入直接注入浅层、中层、深层组成的外观融合模块，使 appearance residual 在不同人脸位置根据 Gaussian geometry 证据自适应恢复。
4. Gaussian 概率生成、方向频率 RGB 残差、训练损失和数据协议均保持不变。

创新表述必须限定为：在面向人脸任意尺度超分辨率的 2D GaussianSR 中，以稳定 Gaussian geometry distribution 引导跨层 appearance restoration。不能泛称“几何引导”或“几何-外观交互”概念本身为首次提出。

首版曾尝试只对外观残差做接近恒等的乘性调制。虽然 smoke test 能运行，但调制因子平均只偏离 1 约 `7.6e-5`，引导门梯度仅约 `1.1e-12`，实际作用过弱，因此在上传服务器前被本地诊断否决。最终版改为门控几何嵌入直接进入外观融合，不保留该无效首版。

### 本地验证

- 参数量：`2,038,629`；普通几何-外观模型为 `2,024,100`，增加 `14,529`，约 `0.72%`。
- Python 静态编译通过。
- 与普通 L1 几何-外观 5 epoch 配置的协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、数据、优化器和解码器协议一致。
- CPU 完整尺寸 smoke test 使用 `16×16` 输入、512 queries、x1.5/x4/x8，三种倍率均输出 `(1, 512, 3)`，训练态与评估态均通过。
- geometry probability 与 guidance distribution 的归一化误差均小于 `6e-7`；引导特征、外观残差和 RGB 残差均非零且有限。
- geometry、appearance、guidance、guidance gate 和 frequency residual 五条路径均获得非零有限梯度，最终输出 `SMOKE TEST PASSED`。
- Windows 本地环境出现 PyTorch 与 NumPy 版本 ABI 提示，但测试退出码为 0，模型计算未失败；该提示与服务器 GS 环境无关，不修改项目依赖。

本候选先只做 5 epoch。准入时与普通 L1 几何-外观的同协议结果比较：最终值必须超过 `28.2235 dB`，5 次验证均值必须超过 `27.7003 dB`；不满足则停止，不创建 30 epoch 配置。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_geometry_guided_appearance_probe5_patch_20260802.zip`。
- SHA-256：`af6159a5e91fac1ab9734ce06255728e7d73faf4dd3d1a0272305517296b5a77`。
- ZIP 只包含模型注册、新编码器、新模型类、独立 smoke test 和 5 epoch 配置共 5 个必要文件；ZIP 本身不提交 GitHub。

通过 XFTP 将 ZIP 上传到服务器项目根目录后，第一阶段只执行备份、解压、编译、协议校验和 smoke test：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_geometry_guided_appearance_probe5_patch_20260802.zip

mkdir -p server_backups/pre_geometry_guided_appearance_20260802
cp models/__init__.py \
  server_backups/pre_geometry_guided_appearance_20260802/models_init.py

unzip -o GaussianSR_face_geometry_guided_appearance_probe5_patch_20260802.zip

python -m py_compile \
  models/edsr_geometry_guided_appearance.py \
  models/gaussian_face_geometry_guided_appearance.py \
  models/__init__.py \
  smoke_test_face_geometry_guided_appearance.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_geometry_guided_appearance_seed1.yaml \
  --expected-baseline-model gaussian-splatter-face-geometry-appearance \
  --expected-model-b gaussian-splatter-face-geometry-guided-appearance \
  --expected-encoder-b edsr-face-geometry-guided-appearance

python smoke_test_face_geometry_guided_appearance.py \
  --config configs/train/face/probe_face_gaussian_geometry_guided_appearance_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器 smoke test 输出 `SMOKE TEST PASSED` 前不启动 5 epoch 训练。

## 2026-08-02：几何引导外观融合通过服务器 smoke 准入

- 服务器配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型、候选模型及候选编码器均正确。
- 服务器 CUDA smoke test 参数量为 `2,038,629`，与本地完全一致；输入为 `16×16`，query 数为 512。
- x1.5/x4/x8 的输出均为 `(1, 512, 3)`，评估态输出也为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- geometry probability 最大归一化误差不超过 `2.38e-7`，guidance distribution 最大误差不超过 `4.77e-7`。
- 三种倍率的 guidance feature 均约为 `0.004056`，appearance residual 与 RGB residual 均非零且有限。
- geometry、appearance、guidance、guidance gate 和 residual 的梯度均非零且有限；引导门梯度较小但稳定存在，可交由 Adam 在短探针中验证实际效果。
- `torch.meshgrid` 信息为已知版本兼容性 warning，不影响本次准入。
- 回传片段未显示 `sha256sum` 的输出，因此启动训练前必须再次自动核对补丁 SHA-256；不一致时禁止启动。

通过 smoke 准入后，只启动候选的 5 epoch 探针，不重复训练普通几何-外观参考模型：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=af6159a5e91fac1ab9734ce06255728e7d73faf4dd3d1a0272305517296b5a77
ACTUAL=$(sha256sum GaussianSR_face_geometry_guided_appearance_probe5_patch_20260802.zip | awk '{print $1}')
RUN=save/face_gaussian_geometry_guided_appearance_seed1_probe5
OUT=results/face_geometry_guided_appearance_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: geometry guided appearance probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_geometry_guided_appearance_seed1.yaml \
    --name face_gaussian_geometry_guided_appearance_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_geometry_guided_appearance_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_geometry_guided_appearance_probe5/train_console.log \
  || true
```

## 2026-08-02：几何引导外观融合未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_geometry_guided_appearance_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0875` 降至 `0.0591`，过程稳定。

| Epoch | 几何-外观参考 | 几何引导外观 | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0853 | +0.0673 |
| 2 | 27.4557 | 27.5095 | +0.0538 |
| 3 | 27.7460 | 27.7990 | +0.0530 |
| 4 | 28.0584 | 28.0563 | -0.0021 |
| 5 | 28.2235 | 28.1831 | -0.0404 |

候选 5 次验证均值为 `27.7266 dB`，参考为 `27.7003 dB`，均值提高 `+0.0263 dB`。但是优势从前三轮的约 `+0.05 dB` 持续收窄，Epoch 4 已基本消失，Epoch 5 反而落后 `0.0404 dB`。

### 判定

1. 候选只满足均值超过 `27.7003 dB`，没有满足最终值超过 `28.2235 dB`；按照训练前确定的双条件规则，未通过 5 epoch 准入。
2. 几何引导外观融合改善了早期收敛，但没有证明可提高收敛后的重建上限，不能依据前三轮优势宣称方法有效。
3. 停止该候选：不创建、不运行 30 epoch 或 1,350 epoch 配置，不执行全倍率正式测试，也不调整 `guidance_gate_init` 进行追参。
4. 本结果属于分析用探针数据，只保留在本进展记录中，不进入论文正式结果表。
5. 代码与独立分支保留为负结果证据；后续结构不再把 geometry distribution 直接注入 appearance fusion，以免重复同一路径。

## 2026-08-02：启动 Gaussian 渲染对齐特征路由候选

### 改动方向与创新边界

新候选命名为“Gaussian 渲染对齐特征路由”，模型注册名为 `gaussian-splatter-face-render-aligned`，编码器注册名为 `edsr-face-render-aligned`。

GaussianSR 基类在渲染时固定将编码特征的前 8 个通道送入 Gaussian splatting，并将其余 56 个通道通过双三次插值旁路恢复；此前的编码器却输出统一的 64 通道特征，没有显式约束前 8/后 56 通道承担不同职责。新候选将编码器输出与该固定执行路径对齐：

1. 浅层与中层结构特征继续生成 Gaussian geometry probability。
2. 同一结构特征与深层前 8 通道进入独立 render fusion，通过 render gate 只修正前 8 个 Gaussian splatting 通道。
3. 浅层、中层、深层跨层特征进入独立 appearance fusion，通过 appearance gate 只修正后 56 个双三次旁路通道。
4. 编码器最终严格按 `[8 render channels, 56 bypass channels]` 拼接，直接匹配 GaussianSR 基类的硬编码通道切分。
5. 方向频率 RGB residual、Gaussian kernel、数据、损失、优化器和训练协议保持不变。

该方向不再把 geometry distribution 注入 appearance fusion，与上一失败候选不同。创新表述必须限定为：在人脸任意尺度 2D GaussianSR 中，根据实际 Gaussian splatting/插值旁路执行路径，对编码特征通道进行职责对齐与双路残差路由。不能泛称通道分组、双路网络或特征解耦本身为首次提出。

### 本地验证

- 参数量：`2,065,637`；普通几何-外观模型为 `2,024,100`，增加 `41,537`，约 `2.05%`。
- Python 静态编译通过。
- 与普通 L1 几何-外观 5 epoch 配置的协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、数据、优化器和解码器协议一致。
- CPU 完整尺寸 smoke test 使用 `16×16` 输入、512 queries、x1.5/x4/x8，三种倍率及评估态均输出 `(1, 512, 3)`。
- 编码器输出职责严格为 8 个 render channels 和 56 个 bypass channels；两条残差均非零且有限。
- geometry、render fusion、bypass fusion、render gate、appearance gate 和 frequency residual 六条路径均获得独立非零有限梯度。
- geometry probability 最大归一化误差为 `2.38e-7`，最终输出 `SMOKE TEST PASSED`。
- Windows 本地的 PyTorch/NumPy ABI 提示不影响退出码和模型计算，不修改服务器 GS 环境依赖。

本候选先只做 5 epoch。仍与普通 L1 几何-外观同协议结果比较：最终值必须超过 `28.2235 dB`，5 次验证均值必须超过 `27.7003 dB`；两个条件必须同时满足，否则停止。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_render_aligned_probe5_patch_20260802.zip`。
- SHA-256：`ede60f1c120418203babd316a14ead13331e7025579c8ff658949ea20018e388`。
- ZIP 只包含模型导入、新编码器、新模型类、独立 smoke test 和 5 epoch 配置共 5 个必要文件；ZIP 本身不提交 GitHub。

通过 XFTP 将 ZIP 上传到服务器项目根目录后，第一阶段只执行备份、解压、编译、协议校验和 smoke test：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_render_aligned_probe5_patch_20260802.zip

mkdir -p server_backups/pre_render_aligned_20260802
cp models/__init__.py \
  server_backups/pre_render_aligned_20260802/models_init.py

unzip -o GaussianSR_face_render_aligned_probe5_patch_20260802.zip

python -m py_compile \
  models/edsr_render_aligned.py \
  models/gaussian_face_render_aligned.py \
  models/__init__.py \
  smoke_test_face_render_aligned.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_render_aligned_seed1.yaml \
  --expected-baseline-model gaussian-splatter-face-geometry-appearance \
  --expected-model-b gaussian-splatter-face-render-aligned \
  --expected-encoder-b edsr-face-render-aligned

python smoke_test_face_render_aligned.py \
  --config configs/train/face/probe_face_gaussian_render_aligned_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器输出 `CONFIG VALIDATION PASSED` 和 `SMOKE TEST PASSED` 前不启动 5 epoch 训练。

## 2026-08-02：Gaussian 渲染对齐路由通过服务器 smoke 准入

- 服务器补丁 SHA-256 为 `ede60f1c120418203babd316a14ead13331e7025579c8ff658949ea20018e388`，与本地记录完全一致。
- Python 静态编译无输出，四个目标文件均通过。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型、候选模型及候选编码器正确。
- 服务器 CUDA smoke test 参数量为 `2,065,637`，与本地一致；通道职责明确输出 `8 56`。
- x1.5/x4/x8 的训练态输出和 x4 评估态输出均为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- geometry probability 最大归一化误差不超过 `2.38e-7`。
- 三种倍率的 render residual 约为 `0.000255-0.000265`，bypass residual 约为 `0.000167-0.000173`，两条路径均非零且有限。
- geometry、render、bypass、render gate、appearance gate 和 frequency residual 六条路径均获得独立非零有限梯度。
- `torch.meshgrid` 信息为已知兼容性 warning，不影响准入。

通过服务器 smoke 后，只启动候选 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=ede60f1c120418203babd316a14ead13331e7025579c8ff658949ea20018e388
ACTUAL=$(sha256sum GaussianSR_face_render_aligned_probe5_patch_20260802.zip | awk '{print $1}')
RUN=save/face_gaussian_render_aligned_seed1_probe5
OUT=results/face_render_aligned_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: render aligned probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_render_aligned_seed1.yaml \
    --name face_gaussian_render_aligned_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_render_aligned_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_render_aligned_probe5/train_console.log \
  || true
```

## 2026-08-02：Gaussian 渲染对齐路由未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_render_aligned_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0863` 降至 `0.0593`，过程稳定。

| Epoch | 几何-外观参考 | 渲染对齐候选 | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0680 | +0.0500 |
| 2 | 27.4557 | 27.4997 | +0.0440 |
| 3 | 27.7460 | 27.7376 | -0.0084 |
| 4 | 28.0584 | 28.0437 | -0.0147 |
| 5 | 28.2235 | 28.2030 | -0.0205 |

候选 5 次验证均值为 `27.7104 dB`，参考为 `27.7003 dB`，均值提高 `+0.0101 dB`。但优势只存在于前两轮，从 Epoch 3 开始持续落后，Epoch 5 低 `0.0205 dB`。

### 判定

1. 候选只满足均值超过 `27.7003 dB`，没有满足最终值超过 `28.2235 dB`；按照训练前确定的双条件规则，未通过 5 epoch 准入。
2. 显式 8/56 通道职责对齐略微改善了早期收敛，但没有提高当前训练阶段的最终重建能力。
3. 停止该候选：不创建、不运行 30 epoch 或 1,350 epoch 配置，不执行全倍率正式测试，也不调整 render/appearance gate 追参。
4. 本结果属于分析用探针数据，只保留在本进展记录中，不进入论文正式结果表。
5. 后续不再继续拆分或重路由 Gaussian 渲染通道；下一方向必须改变有效表征本身，而不是再次调整已有分支之间的信息连接方式。

## 2026-08-02：启动多频带人脸细节表征候选

### 改动方向与创新边界

新候选命名为“多频带人脸细节表征”，模型注册名为 `gaussian-splatter-face-multiband-detail`，编码器注册名为 `edsr-face-multiband-detail`。

该候选保留普通几何-外观模型的浅层/中层 Gaussian geometry 分支和深层 appearance 主干，新增显式的输入细节表征：

1. 使用固定且归一化的 3×3 与 5×5 Gaussian 核得到两个低通结果。
2. 以 `input - blur3` 表示局部高频带，以 `blur3 - blur5` 表示中频轮廓带。
3. 将两个 RGB 频带拼接后通过轻量 detail encoder，生成 64 通道细节特征。
4. 细节特征与浅层、中层、深层 EDSR 特征共同进入 appearance fusion，补充眼眉、嘴唇、发丝、皮肤纹理和脸部轮廓等不同频率信息。
5. Gaussian geometry probability、方向频率 query-wise RGB residual、数据、损失和优化器协议不变。

它与已有方向频率残差的职责不同：多频带分支在 LR 编码阶段改变空间 appearance representation，已有残差在连续查询阶段修正最终 RGB。Laplacian/多频带分解本身不是新概念；创新表述只能限定为：在人脸任意尺度 2D GaussianSR 的几何-外观职责框架中，将固定双尺度细节带作为显式 appearance representation，并与连续查询频率残差协同使用。

### 本地验证

- 参数量：`2,048,452`；普通几何-外观模型为 `2,024,100`，增加 `24,352`，约 `1.20%`。
- Python 静态编译通过。
- 与普通 L1 几何-外观 5 epoch 配置的协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、数据、优化器和解码器协议一致。
- 两个固定 Gaussian 核的权重和误差为 `0.00e+00`。
- CPU 完整尺寸 smoke test 使用 `16×16` 输入、512 queries、x1.5/x4/x8，三种倍率及评估态均输出 `(1, 512, 3)`。
- 随机输入下高频带平均幅值约 `0.372-0.378`，中频带约 `0.062-0.069`，细节特征约 `0.0257`；各项均非零且有限。
- geometry、detail、appearance、appearance gate 和 frequency residual 五条路径均获得独立非零有限梯度；detail gradient 约为 `0.8e-7` 至 `1.1e-7`。
- geometry probability 最大归一化误差不超过 `3.58e-7`，最终输出 `SMOKE TEST PASSED`。

本候选先只做 5 epoch。仍与普通 L1 几何-外观同协议结果比较：最终值必须超过 `28.2235 dB`，5 次验证均值必须超过 `27.7003 dB`；两个条件必须同时满足，否则停止。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_multiband_detail_probe5_patch_20260802.zip`。
- SHA-256：`62048a1097173621ad76684a2a509cb2f80b5a41ed0f2f7e5fb2326860420190`。
- ZIP 只包含模型导入、新编码器、新模型类、独立 smoke test 和 5 epoch 配置共 5 个必要文件；ZIP 本身不提交 GitHub。

通过 XFTP 将 ZIP 上传到服务器项目根目录后，第一阶段只执行备份、解压、编译、协议校验和 smoke test：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

sha256sum GaussianSR_face_multiband_detail_probe5_patch_20260802.zip

mkdir -p server_backups/pre_multiband_detail_20260802
cp models/__init__.py \
  server_backups/pre_multiband_detail_20260802/models_init.py

unzip -o GaussianSR_face_multiband_detail_probe5_patch_20260802.zip

python -m py_compile \
  models/edsr_multiband_detail.py \
  models/gaussian_face_multiband_detail.py \
  models/__init__.py \
  smoke_test_face_multiband_detail.py

python validate_face_training_configs.py \
  --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
  --model-b configs/train/face/probe_face_gaussian_multiband_detail_seed1.yaml \
  --expected-baseline-model gaussian-splatter-face-geometry-appearance \
  --expected-model-b gaussian-splatter-face-multiband-detail \
  --expected-encoder-b edsr-face-multiband-detail

python smoke_test_face_multiband_detail.py \
  --config configs/train/face/probe_face_gaussian_multiband_detail_seed1.yaml \
  --device cuda \
  --gpu 0 \
  --scales 1.5 4 8 \
  --seed 1
```

服务器输出 `CONFIG VALIDATION PASSED` 和 `SMOKE TEST PASSED` 前不启动 5 epoch 训练。

## 2026-08-02：多频带人脸细节表征通过服务器 smoke 准入

- Python 静态编译无输出，四个目标文件均通过。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型、候选模型及候选编码器正确。
- 服务器 CUDA smoke test 参数量为 `2,048,452`，与本地一致；两个 Gaussian 核的归一化误差为 `0.00e+00`。
- x1.5/x4/x8 的训练态输出和 x4 评估态输出均为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- geometry probability 最大归一化误差不超过 `2.38e-7`。
- 三种倍率的高频带平均幅值约 `0.376-0.381`，中频带约 `0.0666-0.0679`，detail feature 约 `0.0257-0.0263`，均非零且有限。
- geometry、detail、appearance、appearance gate 和 frequency residual 五条路径均获得独立非零有限梯度；detail gradient 约 `0.79e-7` 至 `1.12e-7`。
- `torch.meshgrid` 信息为已知兼容性 warning，不影响准入。
- 回传片段未显示 `sha256sum` 输出，因此启动训练前再次自动核对补丁 SHA-256。

通过服务器 smoke 后，只启动候选 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=62048a1097173621ad76684a2a509cb2f80b5a41ed0f2f7e5fb2326860420190
ACTUAL=$(sha256sum GaussianSR_face_multiband_detail_probe5_patch_20260802.zip | awk '{print $1}')
RUN=save/face_gaussian_multiband_detail_seed1_probe5
OUT=results/face_multiband_detail_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: multiband detail probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_multiband_detail_seed1.yaml \
    --name face_gaussian_multiband_detail_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_multiband_detail_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_multiband_detail_probe5/train_console.log \
  || true
```

## 2026-08-02：多频带人脸细节表征未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_multiband_detail_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0877` 降至 `0.0595`，过程稳定。

| Epoch | 几何-外观参考 | 多频带候选 | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0536 | +0.0356 |
| 2 | 27.4557 | 27.4370 | -0.0187 |
| 3 | 27.7460 | 27.7267 | -0.0193 |
| 4 | 28.0584 | 28.0253 | -0.0331 |
| 5 | 28.2235 | 28.1602 | -0.0633 |

候选 5 次验证均值为 `27.6806 dB`，参考为 `27.7003 dB`，均值降低 `-0.0198 dB`。除 Epoch 1 外其余四轮均落后，且差距随训练扩大，Epoch 5 低 `0.0633 dB`。

### 判定

1. 候选既没有满足最终值超过 `28.2235 dB`，也没有满足均值超过 `27.7003 dB`，明确未通过 5 epoch 准入。
2. 固定双尺度细节带直接进入 appearance fusion 没有提高有效表征，反而逐步降低验证 PSNR。
3. 停止该候选：不创建、不运行 30 epoch 或 1,350 epoch 配置，不执行全倍率正式测试，不更换滤波核、频带数量或 `detail_dim` 追参。
4. 本结果属于分析用探针数据，只保留在本进展记录中，不进入论文正式结果表。
5. 后续不再重复显式 edge/frequency band 注入 appearance 的路线。连续候选均表现为早期短暂改善、随后优势消失，因此下一步先汇总失败模式，再判断最后一个结构方向，而不是立即堆叠新模块。

## 2026-08-02：完成热点调研并选定带符号局部 Gaussian moment 方向

- 新增独立调研文档：`文献调研_任意尺度SR热点_20260802.md`。
- 检索覆盖 2022-2026 年任意尺度 SR、2D Gaussian SR、连续隐式注意力、Mamba/状态空间、频率/小波 Transformer、跨层注意力和 2026 年自适应/高阶 Gaussian 预印本。
- 当前最相关的 2026 热点是 QuADA-GS 的自适应 Gaussian 分配和 RBS 的高阶带符号 Gaussian 表达。前者工程成本过高；后者提出的“普通 Gaussian 低通、局部边缘需要带符号模式”与当前瓶颈最吻合。
- 不复制 RBS 的 Brane primitive、Gaussian-Hermite rasterizer、mode 参数化或 culling，只参考其问题判断。
- 选定的本地改法是在现有 query-wise RGB residual 输入中增加 5 个固定解析局部 Gaussian moment，保持编码器、Gaussian primitive、rasterizer、损失和训练协议不变。
- 该方向只预计增加 residual MLP 第一层约 320 个参数，适合作为低成本最后候选；仍执行 smoke → 5 epoch 双条件准入，不直接长训。

## 2026-08-02：带符号局部 Gaussian moment 候选完成本地实现与验证

### 实现与公平性边界

- 新模型注册名：`gaussian-splatter-face-moment-residual`；编码器仍为 `edsr-face-geometry-appearance`。
- 在现有 query-wise RGB residual 的 `query feature + relative coordinate + relative cell + scale feature` 共 56 维输入后，加入 5 维固定解析局部 Gaussian moment：水平/垂直一阶项及水平、交叉、垂直二阶项。
- 5 个 moment 同时包含正负响应，并由局部 Gaussian envelope 约束作用范围；它们用于给 residual MLP 提供方向与曲率条件，不改变 Gaussian primitive、rasterizer、encoder、decoder、loss、数据或优化器。
- residual MLP 第一层从 56 维扩展到 61 维。扩展前先按原实现构造旧层，再逐项复制旧 56 列权重和 bias，仅新增 5 列 moment 权重；smoke 实测共享初始化最大误差为 `0.00e+00`。因此后续差异不会来自旧路径被重新随机初始化。
- 本项目没有复制 Resonant Brane Splatting 的 Brane primitive、Gaussian-Hermite rasterizer、mode 参数化、culling 或代码，只参考“平滑 Gaussian 聚合需要带符号局部表达补充”的问题判断。详细来源和取舍见 `文献调研_任意尺度SR热点_20260802.md`。

### 本地验证

- Python 静态编译通过。
- 与普通 L1 几何-外观 5 epoch 配置的协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、数据、优化器、编码器和解码器协议一致。
- 参数量为 `2,024,420`；普通几何-外观模型为 `2,024,100`，只增加 `320` 个参数，约 `0.016%`。
- CPU smoke 使用 `16x16` 输入、512 queries、x1.5/x4/x8；三个训练态输出及 x4 评估态输出均为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- moment 值范围分别为 x1.5 `[-0.427454, 0.427455]`、x4 `[-0.584101, 0.429757]`、x8 `[-0.880700, 0.554684]`，确认各倍率都有非零带符号响应。
- 新增 5 列权重的梯度范数分别为 x1.5 `1.294e-5`、x4 `1.274e-5`、x8 `9.344e-6`；geometry、appearance 和完整 frequency residual 路径也均获得非零有限梯度。
- 本机旧 PyTorch 扩展会提示 NumPy 2.x ABI 兼容 warning，但进程退出码为 0，前向、反向、数值断言和评估态断言均完成。服务器 CUDA smoke 仍是训练准入的正式依据。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_moment_residual_probe5_patch_20260802.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_moment_residual_probe5_patch_20260802.zip`。
- 大小：`5,331` bytes。
- SHA-256：`9a698603b76f516aae461a99cdb49f6f879e8b02a33d55dec0d2efc9bea710b3`。
- ZIP 只包含模型注册、模型、独立 smoke test 和 5 epoch 配置共 4 个必要文件；ZIP 本身受 `.gitignore` 排除，不提交 GitHub。

通过 XFTP 将 ZIP 上传到服务器项目根目录后，第一阶段只执行哈希校验、备份、解压、编译、协议校验和 CUDA smoke：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=9a698603b76f516aae461a99cdb49f6f879e8b02a33d55dec0d2efc9bea710b3
ACTUAL=$(sha256sum GaussianSR_face_moment_residual_probe5_patch_20260802.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_moment_residual_20260802
  cp models/__init__.py \
    server_backups/pre_moment_residual_20260802/models_init.py

  unzip -o GaussianSR_face_moment_residual_probe5_patch_20260802.zip

  python -m py_compile \
    models/gaussian_face_moment_residual.py \
    models/__init__.py \
    smoke_test_face_moment_residual.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_moment_residual_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-moment-residual

  python smoke_test_face_moment_residual.py \
    --config configs/train/face/probe_face_gaussian_moment_residual_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时输出 `CONFIG VALIDATION PASSED`、`shared initialization error: 0.00e+00` 和 `SMOKE TEST PASSED`，且三个倍率的 moment gradient 均非零有限，才允许启动 5 epoch。训练效果仍采用预先固定的双条件门禁：最终 PSNR 必须超过 `28.2235 dB`，5 次验证均值必须超过 `27.7003 dB`；两个条件必须同时满足，否则停止，不创建 30 epoch 或正式长训配置。探针结果只写入本进展记录，不进入论文正式结果表。

## 2026-08-02：带符号局部 Gaussian moment 通过服务器 smoke 准入

- 补丁成功解压，模型注册、模型、smoke test 和 5 epoch 配置均写入预期路径。
- Python 静态编译无输出，三个目标文件通过。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型和候选模型正确。
- 服务器 CUDA smoke 参数量为 `2,024,420`，residual 输入为基础 56 维加 moment 5 维，共 61 维；与本地一致。
- 旧 residual 第一层权重和 bias 的共享初始化最大误差为 `0.00e+00`。
- x1.5/x4/x8 的 moment 值均同时包含正负响应，范围分别为 `[-0.427454, 0.427455]`、`[-0.584101, 0.429757]`、`[-0.880700, 0.554684]`。
- 三种倍率新增 moment 输入列的梯度范数分别为 `1.294e-5`、`1.274e-5`、`9.337e-6`；geometry、appearance 和完整 residual 路径也均获得非零有限梯度。
- 三个训练态输出及 x4 评估态输出均为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- `torch.meshgrid` 信息是已知兼容性 warning，不影响准入。

服务器 smoke 准入条件全部满足，只启动候选 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=9a698603b76f516aae461a99cdb49f6f879e8b02a33d55dec0d2efc9bea710b3
ACTUAL=$(sha256sum GaussianSR_face_moment_residual_probe5_patch_20260802.zip | awk '{print $1}')
RUN=save/face_gaussian_moment_residual_seed1_probe5
OUT=results/face_moment_residual_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: moment residual probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_moment_residual_seed1.yaml \
    --name face_gaussian_moment_residual_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_moment_residual_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_moment_residual_probe5/train_console.log \
  || true
```

本轮仍严格执行双条件门禁：最终 PSNR 必须高于 `28.2235 dB`，5 次验证均值必须高于 `27.7003 dB`。未同时满足时停止该方向，不通过换 `moment_scale`、moment 数量或 gate 继续追参。

## 2026-08-02：带符号局部 Gaussian moment 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_moment_residual_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0868` 降至 `0.0593`，训练过程稳定。

| Epoch | 几何-外观参考 | moment 候选 | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0036 | -0.0144 |
| 2 | 27.4557 | 27.4522 | -0.0035 |
| 3 | 27.7460 | 27.7464 | +0.0004 |
| 4 | 28.0584 | 28.0381 | -0.0203 |
| 5 | 28.2235 | 28.2179 | -0.0056 |

候选五次验证均值为 `27.6916 dB`，参考为 `27.7003 dB`，均值降低 `-0.0087 dB`。只有 Epoch 3 高 `0.0004 dB`，其余四轮均低于参考。

### 判定

1. 候选最终值没有高于 `28.2235 dB`，五次均值也没有高于 `27.7003 dB`，两个预注册条件均未满足。
2. 带符号 moment 确实可计算、可反向传播且成本很低，但当前 5 维固定解析条件没有转化为验证 PSNR 收益，不能仅凭结构合理性写成论文有效创新。
3. 停止该候选：不创建、不运行 30 epoch 或 1,350 epoch 配置，不执行正式全倍率测试，也不更换 `moment_scale`、moment 数量或 residual gate 追参。
4. 不降低或事后修改准入阈值，不以“只差 `0.0056 dB`”作为进入长训的理由。
5. 本结果属于分析用探针数据，只保留在本进展记录中，不进入论文正式结果表。
6. 该结果与多频带、渲染对齐等候选共同说明：在已经较强的几何-外观 + query residual 框架上继续添加轻量附加条件，收益大多停留在随机波动量级。下一步应先重新评估论文主线与现有有效证据，不继续无门槛堆叠新模块。

## 2026-08-02：启动尺度条件的空间跨深度路由候选

### 方向与失败模式对应关系

- 新模型注册名：`gaussian-splatter-face-scale-depth-router`；新编码器注册名：`edsr-face-scale-depth-router`。
- 此前 geometry guidance、render alignment、multiband detail 和 signed moment 都是在已有主干旁增加输入或修正，最终收益没有超过门禁。本候选不再叠加旁路，而是直接替换普通几何-外观模型的静态 `concat + convolution` appearance fusion。
- 静态融合对所有空间位置和目标倍率使用相同处理；新路由器根据浅层/中层/深层特征差异生成逐像素三路 logits，并用目标倍率的 4 维解析编码生成三路偏置。
- softmax 保证三路权重逐像素和为 1。权重减去均匀先验 `1/3` 后再融合，因此三路无偏好时路由残差严格为零，输出退化为原 deep feature；这避免用大的初始化扰动制造表面差异。
- geometry probability、Gaussian primitive、rasterizer、方向频率 query residual、decoder、loss、数据和优化器协议保持不变。

跨层注意力、层级选择和倍率条件均属于已有研究主题。本项目不复制 ACLA、CiaoSR、MSIT 的注意力实现、局部 ensemble、token 化、损失或代码。候选创新边界只限定为：在当前人脸任意尺度 GaussianSR 几何-外观职责框架中，以零中心概率守恒的尺度-空间三深度路由替换静态 appearance fusion。只有通过效果门禁后才可作为论文候选创新。

### 本地结构验证

- Python 静态编译通过。
- 与普通 L1 几何-外观 5 epoch 配置的协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、数据、Gaussian 模型参数、decoder 和优化器协议一致，仅编码器融合机制不同。
- 参数量为 `1,945,066`；普通几何-外观模型为 `2,024,100`，减少 `79,034`，约 `3.90%`。该候选不是通过增加容量换取收益。
- CPU smoke 使用 `16x16` 输入、512 queries、x1.5/x4/x8；三个训练态输出及 x4 评估态输出均为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- 三种倍率的路由概率守恒误差均不超过 `1.19e-7`，geometry probability 误差不超过 `3.58e-7`。
- 初始三路平均使用率都接近 `1/3`，空间标准差约 `3.30e-4` 至 `3.63e-4`，说明初始化接近无偏但不是空间常数。
- x1.5 与 x8 的倍率路由 logits 最大差异为 `0.00206697`，确认倍率条件实际进入路由。
- 零中心修正后的 appearance residual 平均幅值约 `2.74e-5` 至 `3.66e-5`；修正前直接凸组合曾达到约 `0.044`，因初始化扰动过大已在本地淘汰，未打包、未上服务器。
- content router 梯度约 `9.95e-6` 至 `1.70e-5`，scale router 梯度约 `9.04e-6` 至 `1.52e-5`；geometry、appearance gate 和 frequency residual 路径也均获得非零有限梯度。
- 本机 NumPy 2.x 与旧 PyTorch 扩展会产生 ABI warning，但进程退出码为 0，所有前向、反向、守恒和评估态断言均完成。服务器 CUDA smoke 仍是正式准入依据。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_scale_depth_router_probe5_patch_20260802.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_scale_depth_router_probe5_patch_20260802.zip`。
- 大小：`6,366` bytes。
- SHA-256：`2c2d0ff1bbd8898b4f2778223b8f55eeaf65b8cd7b1fbd6d543e0214f2e34794`。
- ZIP 只包含模型注册、新编码器、新模型、独立 smoke test 和 5 epoch 配置共 5 个必要文件；ZIP 受 `.gitignore` 排除，不提交 GitHub。

通过 XFTP 上传 ZIP 到服务器项目根目录后，第一阶段只执行哈希校验、备份、解压、编译、协议校验和 CUDA smoke：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=2c2d0ff1bbd8898b4f2778223b8f55eeaf65b8cd7b1fbd6d543e0214f2e34794
ACTUAL=$(sha256sum GaussianSR_face_scale_depth_router_probe5_patch_20260802.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_scale_depth_router_20260802
  cp models/__init__.py \
    server_backups/pre_scale_depth_router_20260802/models_init.py

  unzip -o GaussianSR_face_scale_depth_router_probe5_patch_20260802.zip

  python -m py_compile \
    models/edsr_scale_depth_router.py \
    models/gaussian_face_scale_depth_router.py \
    models/__init__.py \
    smoke_test_face_scale_depth_router.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_scale_depth_router_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-scale-depth-router \
    --expected-encoder-b edsr-face-scale-depth-router

  python smoke_test_face_scale_depth_router.py \
    --config configs/train/face/probe_face_gaussian_scale_depth_router_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须输出 `CONFIG VALIDATION PASSED`、非零 `endpoint scale-logit gap` 和 `SMOKE TEST PASSED`，且 content/scale router 梯度均非零有限，才允许启动 5 epoch。效果门禁继续使用普通几何-外观参考：最终 PSNR 高于 `28.2235 dB`，五次验证均值高于 `27.7003 dB`，两个条件必须同时满足。

## 2026-08-02：尺度条件空间跨深度路由通过服务器 smoke 准入

- 补丁成功解压，模型注册、新编码器、新模型、smoke test 和 5 epoch 配置均写入预期路径。
- Python 静态编译无输出，四个目标文件通过。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型、候选模型和候选编码器正确。
- 服务器 CUDA smoke 参数量为 `1,945,066`，与本地一致，比普通几何-外观参考减少 `79,034` 个参数。
- x1.5/x4/x8 的 geometry probability 最大误差不超过 `2.38e-7`，路由概率守恒误差均不超过 `1.19e-7`。
- 三种倍率的平均路由使用率都接近 `[1/3, 1/3, 1/3]`，空间标准差为 `3.47e-4` 至 `3.82e-4`，初始化接近无偏且具有空间变化。
- x1.5 与 x8 的 `endpoint scale-logit gap` 为 `0.00206697`，倍率条件实际改变路由 logits。
- 零中心 appearance residual 平均幅值为 `2.90e-5` 至 `3.84e-5`，保持保守初始化。
- content router 梯度为 `1.030e-5`、`1.700e-5`、`1.517e-5`；scale router 梯度为 `9.144e-6`、`1.564e-5`、`1.391e-5`；geometry、appearance gate 和 frequency residual 路径也均为非零有限梯度。
- 三个训练态输出及 x4 评估态输出均为 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- `torch.meshgrid` 信息为已知兼容性 warning，不影响准入。

服务器 smoke 准入条件全部满足，只启动候选 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=2c2d0ff1bbd8898b4f2778223b8f55eeaf65b8cd7b1fbd6d543e0214f2e34794
ACTUAL=$(sha256sum GaussianSR_face_scale_depth_router_probe5_patch_20260802.zip | awk '{print $1}')
RUN=save/face_gaussian_scale_depth_router_seed1_probe5
OUT=results/face_scale_depth_router_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: patch SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: scale-depth router probe5 path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_scale_depth_router_seed1.yaml \
    --name face_gaussian_scale_depth_router_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_scale_depth_router_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_depth_router_probe5/train_console.log \
  || true
```

效果门禁保持预注册条件不变：Epoch 5 必须高于 `28.2235 dB`，五次验证均值必须高于 `27.7003 dB`，两个条件必须同时满足。未通过时只记录失败，不单独提交或推送，也不通过调 routing dimension、初始化标准差或 gate 追参。

## 2026-08-03：修复尺度跨深度路由的分块验证接口

### 首次训练中断与根因

- 首次运行名：`face_gaussian_scale_depth_router_seed1_probe5`；输出目录：`results/face_scale_depth_router_probe5`。
- 进程 PID `1560296` 以 `Exit 1` 结束。数据加载、模型构建和 Epoch 1 训练均已开始，随后在首次验证进入 `test.py::batched_predict` 时中断，尚未产生任何验证 PSNR，因此不能判定候选效果。
- 完整异常为 `ValueError: scale is required for scale-depth routing.`。公共分块评估接口会先调用 `model.gen_feat(inp)`，随后在若干 `model.query_rgb(..., scale, ...)` 分块中复用特征；候选原实现要求 `gen_feat` 立即收到 scale，与该公共契约不兼容。
- 这不是 CUDA OOM、NaN、多卡分组或路由数值问题，而是单卡直接 forward smoke 没有覆盖分块验证路径。原 smoke 测试范围不足是本次漏检的直接原因。
- 保留失败目录 `save/face_gaussian_scale_depth_router_seed1_probe5` 和 `results/face_scale_depth_router_probe5`，不删除、不覆盖、不从中续训。

### 修复与本地回归

- 只修改候选模型和独立 smoke test，不修改公共 `test.py`、训练器、配置、模型参数或路由数学。
- `gen_feat(inp)` 在无 scale 的分块评估入口缓存输入并清空旧特征；第一次 `query_rgb(..., scale, ...)` 获得倍率后执行一次尺度条件编码；同一图像、同一倍率的后续 query 分块复用缓存特征。
- 普通训练 forward 仍通过 `gen_feat(inp, scale)` 立即编码，行为不变；若后续 query 的 scale 发生变化，缓存会安全重建，避免复用错误倍率的特征。
- smoke 新增项目原生 `batched_predict` 回归；本地轻量环境仅在缺少 `tqdm` 时使用逐行等价调用，服务器完整环境直接导入原生函数。
- Python 静态编译通过。x1.5/x4/x8 原有概率守恒、倍率差异和梯度断言继续通过，参数量保持 `1,945,066`。
- x4 普通评估与分块评估输出均为 `(1, 512, 3)`，最大差异为 `7.45e-09`，低于 `1e-6` 断言阈值；最终输出 `SMOKE TEST PASSED`。

### 服务器修复补丁

- 文件：`GaussianSR_face_scale_depth_router_evalfix_patch_20260803.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_scale_depth_router_evalfix_patch_20260803.zip`。
- 大小：`3,664` bytes。
- SHA-256：`dfb6e644f0a0cbd9905feeb21a73c3b72adff49b511fce1952da083b40573e5b`。
- ZIP 只包含修复后的候选模型和增强后的 smoke test 共 2 个文件；受 `.gitignore` 排除，不提交 GitHub。

通过 XFTP 上传修复 ZIP 到服务器项目根目录后，先备份现有文件，再解压、编译并重新运行完整 CUDA smoke：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=dfb6e644f0a0cbd9905feeb21a73c3b72adff49b511fce1952da083b40573e5b
ACTUAL=$(sha256sum GaussianSR_face_scale_depth_router_evalfix_patch_20260803.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: eval-fix SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_scale_depth_router_evalfix_20260803
  cp models/gaussian_face_scale_depth_router.py \
    server_backups/pre_scale_depth_router_evalfix_20260803/
  cp smoke_test_face_scale_depth_router.py \
    server_backups/pre_scale_depth_router_evalfix_20260803/

  unzip -o GaussianSR_face_scale_depth_router_evalfix_patch_20260803.zip

  python -m py_compile \
    models/gaussian_face_scale_depth_router.py \
    smoke_test_face_scale_depth_router.py

  python smoke_test_face_scale_depth_router.py \
    --config configs/train/face/probe_face_gaussian_scale_depth_router_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须新增输出 `batched evaluation output: (1, 512, 3), max_error=...`，且最大误差不超过 `1e-6`、最终输出 `SMOKE TEST PASSED`，才能使用新的运行名重新开始 5 epoch。修复 smoke 通过前不提供重启命令。

## 2026-08-03：尺度跨深度路由评估修复通过服务器回归

- 修复补丁成功解压，模型和 smoke test 写入预期路径；Python 静态编译无输出。
- 服务器 CUDA smoke 参数量保持 `1,945,066`，x1.5/x4/x8 的概率守恒、空间路由、倍率差异和各分支梯度结果与修复前一致。
- `endpoint scale-logit gap` 仍为 `0.00206697`。
- 新增的原生分块评估路径成功运行：普通评估与分块评估输出均为 `(1, 512, 3)`，最大差异为 `7.45e-09`，显著低于 `1e-6` 阈值。
- 最终输出 `SMOKE TEST PASSED`，确认 `test.py::batched_predict` 接口兼容问题已关闭。
- 首次失败目录继续保留；新的 5 epoch 运行从头初始化，不恢复或覆盖首次运行。

只启动修复后的 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=dfb6e644f0a0cbd9905feeb21a73c3b72adff49b511fce1952da083b40573e5b
ACTUAL=$(sha256sum GaussianSR_face_scale_depth_router_evalfix_patch_20260803.zip | awk '{print $1}')
RUN=save/face_gaussian_scale_depth_router_seed1_probe5_evalfix
OUT=results/face_scale_depth_router_probe5_evalfix

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: eval-fix SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: scale-depth router evalfix path already exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_scale_depth_router_seed1.yaml \
    --name face_gaussian_scale_depth_router_seed1_probe5_evalfix \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_scale_depth_router_seed1_probe5_evalfix/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_scale_depth_router_probe5_evalfix/train_console.log \
  || true
```

候选仍与普通几何-外观参考比较：Epoch 5 高于 `28.2235 dB` 且五次验证均值高于 `27.7003 dB` 才通过。接口修复不改变效果门禁。

## 2026-08-03：尺度条件空间跨深度路由未通过 5 epoch 效果准入

### 训练结果

- 有效运行名：`face_gaussian_scale_depth_router_seed1_probe5_evalfix`；该运行从头初始化，与首次接口中断目录完全分离。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0859` 降至 `0.0594`，过程稳定。

| Epoch | 几何-外观参考 | 尺度跨深度路由 | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0365 | +0.0185 |
| 2 | 27.4557 | 27.4307 | -0.0250 |
| 3 | 27.7460 | 27.7846 | +0.0386 |
| 4 | 28.0584 | 27.9830 | -0.0754 |
| 5 | 28.2235 | 28.1122 | -0.1113 |

候选五次验证均值为 `27.6694 dB`，参考为 `27.7003 dB`，均值降低 `-0.0309 dB`。Epoch 1 和 Epoch 3 的短暂优势没有保持，后两轮差距明显扩大。

### 判定

1. Epoch 5 和五次均值两个预注册条件都未满足，候选明确未通过准入。
2. 路由概率、倍率条件和梯度均已证明实际工作，失败不能归因于模块未参与训练；结果说明三深度零中心凸路由替换静态卷积融合后，降低了后期 appearance 表征能力。
3. 参数减少 `3.90%` 是效率优势，但不能抵消 `0.1113 dB` 的最终 PSNR 下降，不能作为论文有效创新保留。
4. 停止该候选：不创建 30 epoch 或正式配置，不调 routing dimension、初始化标准差、scale encoding 或 appearance gate，不进行事后追参。
5. 本探针只记录在本地进展 Markdown，不单独提交或推送，不进入论文正式结果表。
6. encoder appearance fusion 路线至此停止。下一候选必须作用于当前主连续查询解码器的明确瓶颈，而不是再次修改 encoder 融合或添加特征旁路。

## 2026-08-03：启动 Gaussian metric local ensemble 候选

### 问题与设计

- 新模型注册名：`gaussian-splatter-face-metric-ensemble`；编码器仍为 `edsr-face-geometry-appearance`。
- 当前主 decoder 在 Gaussian rasterization 后对 coefficient/frequency feature 使用 nearest grid sampling，一个 lattice feature 独立负责对应区域内的连续 query。该离散邻域规则没有利用模型已经学习到的 Gaussian covariance。
- 新候选对 query 周围四个 LR lattice 邻域分别执行原 Fourier decoder，再以各邻域 learned `sigma_x`、`sigma_y`、`rho` 定义的 Mahalanobis distance 生成四路 softmax 权重并融合。
- learned Gaussian geometry 因而同时决定 rasterization kernel 和 decoder neighborhood metric；encoder、geometry probability、Gaussian rasterization、方向频率 RGB residual、loss、数据及优化器不变。
- 候选不增加任何参数，参数量与普通几何-外观参考同为 `2,024,100`。代价是每个 query 的 decoder 从 1 次增加到 4 次，服务器 smoke 和 5 epoch 时间需单独记录。

四邻域 local ensemble、Mahalanobis distance 和 covariance weighting 都是已有思想。本项目不复制 LIIF/CiaoSR 的面积权重、attention、局部 ensemble 实现、坐标编码、损失或代码。候选创新边界只限定为：使用当前 GaussianSR 自身可学习 covariance 构造连续 Fourier query 的多邻域度量，使 Gaussian geometry 从 rasterization 延伸到 implicit decoder 的邻域选择。只有通过效果门禁后才可进入论文候选。

### 共享基类安全改动

- 在 `GaussianSplatter` 中把原 Section 8 nearest query 代码原样抽取为可覆盖的 `decode_queries` hook；所有现有模型默认调用该实现，数学和参数不变。
- 新候选只覆盖 `decode_queries`，不复制整段 Gaussian rasterization，降低共享逻辑分叉风险。
- smoke 逐算子复现抽取前的 nearest decoder，并与共享基类默认 hook 对比，最大误差为 `0.00e+00`，确认重构没有改变现有模型默认输出。

### 本地验证

- Python 静态编译通过。
- 配置协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、编码器、Gaussian 参数、decoder、数据和优化器协议与普通几何-外观参考一致，仅模型注册名和 query aggregation 不同。
- CPU smoke 使用 `16x16` 输入、512 queries、x1.5/x4/x8；三种倍率及 x4 普通/分块评估均输出 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- 四邻域权重和误差不超过 `1.19e-7`。各倍率平均使用率明显非均匀，权重标准差为 `0.0582` 至 `0.1111`，说明 covariance metric 实际参与融合而非退化成固定平均。
- ensemble entropy 约 `1.1884` 至 `1.2534`，低于四路均匀分布的 `log(4)=1.3863`，同时未塌缩为单邻域。
- geometry gradient 约 `2.83e-6` 至 `9.90e-6`，Gaussian covariance gradient 约 `4.04e-6` 至 `1.13e-5`，frequency residual gradient 约 `0.061`；均非零有限。
- 普通评估与项目分块评估最大输出差异为 `7.45e-09`，低于 `1e-6` 阈值。
- 本机 NumPy 2.x 与旧 PyTorch 扩展会产生 ABI warning，但进程退出码为 0，全部结构、梯度、守恒、默认回归和分块评估断言均完成。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_gaussian_metric_ensemble_probe5_patch_20260803.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_gaussian_metric_ensemble_probe5_patch_20260803.zip`。
- 大小：`11,683` bytes。
- SHA-256：`b1264ae95eb81a7d076d1798eb4c468c4c4f8a58385292a7c69bc57723852eef`。
- ZIP 包含模型注册、默认 decoder hook 重构、新候选、独立 smoke 和 5 epoch 配置共 5 个必要文件；受 `.gitignore` 排除，不提交 GitHub。

通过 XFTP 上传到服务器项目根目录后，第一阶段只执行哈希校验、共享文件备份、解压、编译、协议校验和 CUDA smoke：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=b1264ae95eb81a7d076d1798eb4c468c4c4f8a58385292a7c69bc57723852eef
ACTUAL=$(sha256sum GaussianSR_face_gaussian_metric_ensemble_probe5_patch_20260803.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: metric-ensemble SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_gaussian_metric_ensemble_20260803
  cp models/__init__.py \
    server_backups/pre_gaussian_metric_ensemble_20260803/models_init.py
  cp models/gaussian.py \
    server_backups/pre_gaussian_metric_ensemble_20260803/gaussian.py

  unzip -o GaussianSR_face_gaussian_metric_ensemble_probe5_patch_20260803.zip

  python -m py_compile \
    models/gaussian.py \
    models/gaussian_face_metric_ensemble.py \
    models/__init__.py \
    smoke_test_face_gaussian_metric_ensemble.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_metric_ensemble_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-metric-ensemble

  python smoke_test_face_gaussian_metric_ensemble.py \
    --config configs/train/face/probe_face_gaussian_metric_ensemble_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时输出 `default decoder refactor error: 0.00e+00`、非均匀四路 usage、非零 covariance gradient、分块评估误差不超过 `1e-6` 和 `SMOKE TEST PASSED`，才允许启动 5 epoch。效果门禁仍为 Epoch 5 高于 `28.2235 dB` 且五次验证均值高于 `27.7003 dB`。

## 2026-08-03：Gaussian metric local ensemble 通过服务器 smoke 准入

- 补丁成功解压，模型注册、共享 decoder hook、新候选、smoke test 和 5 epoch 配置均写入预期路径；Python 静态编译无输出。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型和候选模型正确。
- 服务器 CUDA smoke 参数量为 `2,024,100`，与普通几何-外观参考及本地结果完全一致。
- 旧 nearest decoder 与共享基类默认 hook 的输出误差为 `0.00e+00`，确认共享重构未改变已有模型默认行为。
- x1.5/x4/x8 的四邻域权重和误差均不超过 `1.19e-7`；权重标准差为 `0.0588` 至 `0.1130`，entropy 为 `1.1606` 至 `1.2517`，权重既非均匀固定也未单邻域塌缩。
- geometry gradient 分别为 `2.965e-6`、`5.822e-5`、`3.128e-5`；Gaussian covariance gradient 分别为 `5.397e-6`、`1.255e-4`、`4.886e-5`；frequency residual gradient 约 `0.061`，均非零有限。
- 三个训练态输出及 x4 普通/分块评估输出均为 `(1, 512, 3)`；普通与分块评估最大差异为 `7.45e-09`。
- 最终输出 `SMOKE TEST PASSED`；`torch.meshgrid` 信息为已知兼容性 warning，不影响准入。

只启动候选 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=b1264ae95eb81a7d076d1798eb4c468c4c4f8a58385292a7c69bc57723852eef
ACTUAL=$(sha256sum GaussianSR_face_gaussian_metric_ensemble_probe5_patch_20260803.zip | awk '{print $1}')
RUN=save/face_gaussian_metric_ensemble_seed1_probe5
OUT=results/face_gaussian_metric_ensemble_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: metric-ensemble SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: Gaussian metric ensemble probe5 path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_metric_ensemble_seed1.yaml \
    --name face_gaussian_metric_ensemble_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_metric_ensemble_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_gaussian_metric_ensemble_probe5/train_console.log \
  || true
```

效果门禁保持不变：Epoch 5 高于 `28.2235 dB` 且五次验证均值高于 `27.7003 dB` 才通过。由于每个 query 执行四次 decoder，记录每轮耗时；若 OOM 或耗时不可接受，只记录工程失败，不缩小 batch 或改协议制造不可比结果。

## 2026-08-03：Gaussian metric local ensemble 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_metric_ensemble_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0956` 降至 `0.0638`，训练稳定但始终高于普通几何-外观参考。

| Epoch | 几何-外观参考 | Gaussian metric ensemble | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 26.5205 | -0.4975 |
| 2 | 27.4557 | 27.0058 | -0.4499 |
| 3 | 27.7460 | 27.2719 | -0.4741 |
| 4 | 28.0584 | 27.5912 | -0.4672 |
| 5 | 28.2235 | 27.7837 | -0.4398 |

候选五次验证均值为 `27.2346 dB`，参考为 `27.7003 dB`，均值降低 `-0.4657 dB`。五轮全部显著落后，不属于随机波动。

### 效率与判定

- 候选每轮约 `4.0-4.1m`，参考约 `3.1-3.2m`，耗时增加约 `25%-29%`；参数量虽不增加，但计算成本明显上升。
- covariance 权重、geometry/covariance 梯度和分块评估均已验证有效，失败不是模块未参与训练或接口错误。
- 强制融合四个分别解码的 Fourier prediction 破坏了原 nearest decoder 的局部职责，learned rasterization covariance 不能直接当作 decoder prediction compatibility metric。
- 停止该候选：不创建 30 epoch 或正式配置，不添加回退 gate、temperature、面积权重或 attention 对失败 ensemble 追参。
- 本结果只记录在本地 Markdown，不单独提交或推送，不进入论文正式表。
- 连续 query neighborhood ensemble 路线停止。下一步检查 Gaussian 参数库选择本身的训练/评估不一致：训练使用 dense soft Gumbel distribution，评估使用 hard argmax；候选必须改变 Gaussian allocation，而不是继续修改 appearance 或 decoder。

## 2026-08-03：启动 support-adaptive sparse Gaussian allocation 候选

### 问题与创新边界

- 新模型注册名：`gaussian-splatter-face-sparse-allocation`；新编码器注册名：`edsr-face-sparse-gaussian-allocation`。
- 当前 geometry head 在训练阶段对 100 个 Gaussian 参数原型使用 dense stochastic Gumbel distribution，评估阶段切换为 hard argmax one-hot；支持集大小和随机性存在 train/eval mismatch。
- 新候选对每个位置的 geometry logits 做 probability simplex sparse projection，产生含精确零值、位置自适应支持大小的 Gaussian allocation；训练和评估使用同一确定性规则。
- 不使用固定 top-k，不增加参数；`sigma_x`、`sigma_y`、`opacity`、`rho` 仍由 allocation 加权，Gaussian rasterization、appearance fusion、Fourier query decoder、frequency residual、loss、数据和优化器不变。

Sparsemax 是 Martins 与 Astudillo 的已有方法，稀疏 routing 和 adaptive allocation 也不是原创概念。本项目不复制 QuADA-GS 的不规则拓扑、pointer convolution、资源预算或代码。候选创新边界只限定为：在人脸任意尺度 GaussianSR 的可学习参数原型库中，用训练/推理一致的 support-adaptive sparse allocation 消除原 dense-stochastic/hard-single mismatch。只有效果通过门禁后才可作为论文候选。

### 本地验证

- Python 静态编译通过。
- 配置协议校验返回 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、Gaussian 模型参数、decoder、数据及优化器与普通几何-外观参考一致，仅 encoder 的 geometry allocation rule 不同。
- 参数量为 `2,024,100`，与普通几何-外观参考完全相同。
- 同一 synthetic logits 在 training/evaluation mode 下的 allocation 最大误差为 `0.00e+00`，确认分配规则一致且无 Gumbel sampling。
- CPU smoke 使用 `16x16` 输入、512 queries、x1.5/x4/x8；三种倍率和 x4 重复/分块评估均输出 `(1, 512, 3)`，最终输出 `SMOKE TEST PASSED`。
- 三种倍率 probability sum 最大误差不超过 `3.58e-7`；约 `54.8%-55.1%` 的分配概率为精确零值。
- 每个位置平均支持约 `44.9-45.2/100` 个原型，最小 `36-37`、最大 `54-58`、空间标准差约 `3.43-3.62`，支持大小确实随位置自适应而非固定 top-k。
- geometry gradient 约 `1.24e-6` 至 `4.78e-5`，Gaussian parameter bank gradient 约 `3.77e-8` 至 `2.20e-6`，appearance 和 frequency residual 梯度也均非零有限。
- x4 两次完整评估最大差异为 `0.00e+00`，普通与分块评估最大差异为 `7.45e-09`。
- 本机 NumPy 2.x 与旧 PyTorch 扩展的 ABI warning 不影响退出码和断言；服务器 CUDA smoke 仍是正式准入依据。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_sparse_gaussian_allocation_probe5_patch_20260803.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_sparse_gaussian_allocation_probe5_patch_20260803.zip`。
- 大小：`5,908` bytes。
- SHA-256：`2ebcf452dc913f19434f064173a0f631c494edffd0b4e950d383fec430a44f69`。
- ZIP 包含模型注册、新编码器、新模型、独立 smoke 和 5 epoch 配置共 5 个必要文件；受 `.gitignore` 排除，不提交 GitHub。

通过 XFTP 上传到服务器项目根目录后，第一阶段只执行哈希校验、注册文件备份、解压、编译、协议校验和 CUDA smoke：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=2ebcf452dc913f19434f064173a0f631c494edffd0b4e950d383fec430a44f69
ACTUAL=$(sha256sum GaussianSR_face_sparse_gaussian_allocation_probe5_patch_20260803.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: sparse-allocation SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_sparse_gaussian_allocation_20260803
  cp models/__init__.py \
    server_backups/pre_sparse_gaussian_allocation_20260803/models_init.py

  unzip -o GaussianSR_face_sparse_gaussian_allocation_probe5_patch_20260803.zip

  python -m py_compile \
    models/edsr_sparse_gaussian_allocation.py \
    models/gaussian_face_sparse_allocation.py \
    models/__init__.py \
    smoke_test_face_sparse_gaussian_allocation.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_sparse_allocation_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-sparse-allocation \
    --expected-encoder-b edsr-face-sparse-gaussian-allocation

  python smoke_test_face_sparse_gaussian_allocation.py \
    --config configs/train/face/probe_face_gaussian_sparse_allocation_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时输出 `train/eval allocation error: 0.00e+00`、非零 zero fraction、可变 support 范围、非零 Gaussian bank gradient、重复评估误差 0、分块误差不超过 `1e-6` 和 `SMOKE TEST PASSED`，才允许启动 5 epoch。效果门禁保持 Epoch 5 高于 `28.2235 dB` 且五次均值高于 `27.7003 dB`。

## 2026-08-03：support-adaptive sparse Gaussian allocation 通过服务器 smoke 准入

- 补丁成功解压，模型注册、新编码器、新模型、smoke test 和 5 epoch 配置均写入预期路径；Python 静态编译无输出。
- 配置校验输出 `CONFIG VALIDATION PASSED`：seed 1、5 epoch、batch size 12、参考模型、候选模型和候选编码器正确。
- 服务器 CUDA smoke 参数量为 `2,024,100`，与普通几何-外观参考和本地结果完全一致。
- 同一 logits 的 train/eval allocation 最大误差为 `0.00e+00`，消除了随机 Gumbel/argmax 切换。
- x1.5/x4/x8 的 probability sum 最大误差均不超过 `4.77e-7`，精确零概率比例为 `55.01%`、`55.46%`、`55.12%`。
- 平均支持大小为 `44.99`、`44.54`、`44.88/100`，各位置范围分别为 `37-56`、`34-52`、`34-53`，空间标准差为 `3.31-3.51`，支持集实际随位置变化。
- geometry gradient 为 `3.505e-5`、`7.320e-6`、`1.745e-6`；Gaussian bank gradient 为 `1.570e-6`、`3.232e-7`、`4.953e-8`；appearance 和 frequency residual 梯度也均非零有限。
- 三种训练态和 x4 评估态输出均为 `(1, 512, 3)`；重复完整评估误差 `0.00e+00`，普通/分块评估误差 `7.45e-09`。
- 最终输出 `SMOKE TEST PASSED`；`torch.meshgrid` 为已知兼容性 warning，不影响准入。

只启动候选 5 epoch 探针：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=2ebcf452dc913f19434f064173a0f631c494edffd0b4e950d383fec430a44f69
ACTUAL=$(sha256sum GaussianSR_face_sparse_gaussian_allocation_probe5_patch_20260803.zip | awk '{print $1}')
RUN=save/face_gaussian_sparse_allocation_seed1_probe5
OUT=results/face_sparse_gaussian_allocation_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: sparse-allocation SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: sparse Gaussian allocation probe5 path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_sparse_allocation_seed1.yaml \
    --name face_gaussian_sparse_allocation_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_sparse_allocation_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_sparse_gaussian_allocation_probe5/train_console.log \
  || true
```

效果门禁不变：Epoch 5 高于 `28.2235 dB` 且五次验证均值高于 `27.7003 dB` 才通过。若失败，只记录结果，不调 allocation temperature 或改成固定 top-k 追参。

## 2026-08-03：support-adaptive sparse Gaussian allocation 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_sparse_allocation_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0866` 降至 `0.0594`，过程稳定。

| Epoch | 几何-外观参考 | 稀疏 allocation | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0061 | -0.0119 |
| 2 | 27.4557 | 27.4791 | +0.0234 |
| 3 | 27.7460 | 27.7496 | +0.0036 |
| 4 | 28.0584 | 28.0453 | -0.0131 |
| 5 | 28.2235 | 28.1865 | -0.0370 |

候选五次验证均值为 `27.6933 dB`，参考为 `27.7003 dB`，均值降低 `-0.0070 dB`。Epoch 2、3 的小幅优势在后两轮反转。

### 判定

1. Epoch 5 和五次均值两个预注册条件均未满足，不能因差距较小而进入 30 epoch。
2. 稀疏支持、train/eval consistency 和 Gaussian bank 梯度都已验证有效；失败说明消除 allocation mismatch 本身没有提升当前阶段的最终重建能力。
3. 停止该候选：不调 allocation temperature、不改固定 top-k、不组合其他 gate 或 loss 追参。
4. 本结果只记录在本地 Markdown，不单独提交或推送，不进入论文正式表。
5. Gaussian allocation 路线停止。下一步检查 renderer 对 feature 的聚合语义：当前 Gaussian 加权和后执行 `[0,1]` hard clamp，但 EDSR feature 是有符号 latent，且未按局部 Gaussian coverage 归一化。

## 2026-08-03：sign-preserving opacity-normalized Gaussian aggregation 本地结构检查未通过

### 动机与实现边界

- 当前 renderer 对有正有负的 EDSR latent feature 做 opacity × Gaussian kernel 加权求和，随后 hard clamp 到 `[0,1]`；这会删除负特征，并使幅值随局部 Gaussian overlap 改变。
- 候选注册名为 `gaussian-splatter-face-normalized-aggregation`，encoder 仍为 `edsr-face-geometry-appearance`。候选用非负 opacity × Gaussian kernel 计算 coverage，再用 signed feature numerator 除以 coverage，使有效位置的归一化权重和为 1；不增加参数，不改变 decoder、frequency residual、loss、数据或训练协议。
- 在 `GaussianSplatter` 中加入共享 aggregation hook，默认实现严格保留原 sum-and-clamp 行为；候选只覆盖该 hook。
- normalized convolution、coverage normalization 与 partition-of-unity aggregation 均为已有思想，不能宣称为本项目原创。若其有效，创新边界也只能限定为在人脸任意尺度 GaussianSR latent renderer 中的具体应用；本次检查未通过，因此不进入论文候选。

### 本地验证

- Python 静态编译通过；配置协议校验返回 `CONFIG VALIDATION PASSED`，seed 1、5 epoch、batch size 12、参考模型和候选模型均正确。
- 参数量为 `2,024,100`，与几何-外观参考完全一致。合成张量上共享默认 hook 与原 sum-and-clamp 公式最大误差为 `0.00e+00`。
- CPU smoke 使用 `16×16` 输入、512 queries、×1.5/×4/×8。三种倍率的归一化权重和误差分别为 `1.79e-7`、`1.19e-7`、`0.00e+00`；coverage 均有限且存在有效支持。
- normalized latent 在三种倍率均同时保留正值和负值，整体范围约为 `[-1.506, 1.231]`，没有幅值爆炸。
- x4 重复完整评估误差为 `0.00e+00`，普通与分块评估最大误差为 `7.45e-09`；接口与确定性均正常，形式 smoke 输出 `SMOKE TEST PASSED`。
- ×1.5 与 ×4 的 geometry gradient 分别为 `2.469e-7`、`1.756e-7`，Gaussian bank gradient 分别为 `4.540e-7`、`4.078e-7`；但 ×8 时二者骤降至 `1.161e-13` 和 `1.716e-13`。appearance 与 frequency residual 梯度仍正常，说明消失集中在被归一化的 Gaussian geometry 路径。

### 判定

1. 候选未通过研究准入。高倍率下 Gaussian 支持彼此分离、单核主导时，分子与 coverage 中相同的 kernel/opacity 因子相消，输出近似退化为单点 feature，因而对 covariance 和 opacity 几乎不敏感。
2. 这不是简单的 epsilon、初始化或数值稳定问题，而是完全 coverage normalization 的结构性质；继续调阈值或增加回退 gate 会变成对失败方向追参，且削弱原始动机。
3. 不生成服务器 ZIP，不运行 CUDA smoke 或 5 epoch，不创建 30 epoch 正式配置，不提交或 push。当前实验代码和证据仅保留在本地工作区与 Markdown，模型不加入全局注册表。
4. renderer 全量归一化路线停止。后续方向应同时保留 signed latent 与 Gaussian coverage/geometry 的可学习影响，不能再采用会在单核区域严格消掉 kernel 参数的比值形式。

## 2026-08-03：启动 symmetric signed-density Gaussian aggregation 候选

### 问题与创新边界

- 新模型注册名为 `gaussian-splatter-face-signed-density-aggregation`，encoder 仍为 `edsr-face-geometry-appearance`，参数量、decoder、frequency residual、loss、数据和训练协议不变。
- 候选不再除以 coverage。它把 signed latent 按正部和负部分解，分别累加 opacity × Gaussian kernel 加权密度，再对两路使用稳定形式 `1-exp(-density)` 做软饱和，最终以正响应减负响应。
- 输出严格保持奇对称并限制在 `[-1,1]`；零附近近似原 signed Gaussian 加权和，overlap 较大时平滑饱和。由于 kernel/opacity 不在分子分母中相消，单核区域仍保留对 Gaussian geometry 的梯度。
- signed positive/negative decomposition、Beer-Lambert 型指数响应和软饱和都不是原创概念，也未复制其他论文模块或代码。只有 5/30 epoch 门禁通过后，才可把创新边界限定为：在人脸任意尺度 GaussianSR latent renderer 中，用对称双极 Gaussian density response 替代会截断负值的单边 sum-and-clamp。

### 本地结构验证

- Python 静态编译和固定协议配置校验均通过；seed 1、5 epoch、batch size 12、参考模型及候选模型正确。
- 参数量为 `2,024,100`，无新增可学习参数。共享默认 aggregation hook 与原公式误差为 `0.00e+00`；候选对 `feature` 与 `-feature` 的奇对称误差为 `0.00e+00`。
- CPU smoke 使用 `16×16` 输入、512 queries、×1.5/×4/×8。聚合特征范围依次为 `[-0.767, 0.608]`、`[-0.482, 0.553]`、`[-0.554, 0.461]`，正负两路 density 均非零，绝对值大于 `0.95` 的饱和比例均为 0。
- geometry gradient 为 `6.255e-7`、`1.605e-7`、`5.592e-8`；Gaussian bank gradient 为 `1.305e-6`、`3.896e-7`、`1.164e-7`。三种倍率均明显高于 `1e-10` 下限，×8 没有 normalized aggregation 的梯度相消。
- appearance gradient 约为 `2e-4`，frequency residual gradient 约为 `0.061`；所有预测、density、特征和梯度均有限。
- x4 重复完整评估误差为 `0.00e+00`，普通与分块评估最大误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`。
- 本机 NumPy 2.x 与旧 PyTorch 扩展的 ABI warning 不影响退出码和断言；服务器 CUDA smoke 仍是正式准入依据。

### 当前判定

该候选通过本地结构准入，可以生成服务器补丁，但尚未获得效果证据。服务器第一阶段只允许哈希校验、备份、解压、静态编译、配置校验和 CUDA smoke；在 CUDA smoke 返回全部梯度、对称性、分块一致性及 `SMOKE TEST PASSED` 前，不启动 5 epoch。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_signed_density_aggregation_probe5_patch_20260803.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_signed_density_aggregation_probe5_patch_20260803.zip`。
- 大小：`11,151` bytes。
- SHA-256：`9b5a1c899a5798b673770a62dcfc05ed7e1f54bd3efa645320b48b255d43d4ea`。
- ZIP 仅包含共享 renderer hook、模型注册、新候选、独立 smoke 和 5 epoch 配置共 5 个必要文件；失败的 normalized aggregation 模块、数据、日志、结果和 checkpoint 均未包含。

上传到服务器项目根目录后，只执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=9b5a1c899a5798b673770a62dcfc05ed7e1f54bd3efa645320b48b255d43d4ea
ACTUAL=$(sha256sum GaussianSR_face_signed_density_aggregation_probe5_patch_20260803.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: signed-density SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_signed_density_aggregation_20260803
  cp models/__init__.py \
    server_backups/pre_signed_density_aggregation_20260803/models_init.py
  cp models/gaussian.py \
    server_backups/pre_signed_density_aggregation_20260803/gaussian.py

  unzip -o GaussianSR_face_signed_density_aggregation_probe5_patch_20260803.zip

  python -m py_compile \
    models/gaussian.py \
    models/gaussian_face_signed_density_aggregation.py \
    models/__init__.py \
    smoke_test_face_signed_density_aggregation.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_signed_density_aggregation_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-signed-density-aggregation

  python smoke_test_face_signed_density_aggregation.py \
    --config configs/train/face/probe_face_gaussian_signed_density_aggregation_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时满足：默认 aggregation 误差为 0、signed symmetry 误差不超过 `1e-7`、三倍率正负 density 均激活、输出有界、geometry/bank gradient 均高于 `1e-10`、重复与分块评估误差不超过 `1e-6`，并最终输出 `SMOKE TEST PASSED`。在回传这些结果前不启动训练。

## 2026-08-03：symmetric signed-density Gaussian aggregation 通过服务器 smoke 准入

- 补丁哈希校验通过并成功解压；Python 静态编译无报错，配置校验输出 `CONFIG VALIDATION PASSED`，固定协议保持 seed 1、5 epoch、batch size 12。
- 服务器 CUDA 参数量为 `2,024,100`，与参考和本地结果一致；共享默认 aggregation 与原公式误差为 `0.00e+00`，signed symmetry error 为 `0.00e+00`。
- ×1.5/×4/×8 的正 density 均值为 `0.10534/0.01299/0.00358`，负 density 均值为 `0.12796/0.01627/0.00423`，两路在所有倍率均实际激活。
- 聚合 feature 范围依次为 `[-0.71712, 0.76869]`、`[-0.67098, 0.54909]`、`[-0.52632, 0.59059]`；饱和比例均为 0，输出有界且保留正负信息。
- geometry gradient 为 `2.897e-7/1.019e-7/6.085e-8`，Gaussian bank gradient 为 `6.229e-7/2.694e-7/1.961e-7`；高倍率没有发生 normalized aggregation 的梯度相消。
- appearance gradient 约为 `2e-4`，frequency residual gradient 约为 `0.061`；所有梯度均有限非零。
- x4 重复评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`。`torch.meshgrid` 仅为已知兼容性 warning。

该候选通过服务器结构准入，允许启动唯一一次固定协议 5 epoch probe。效果门禁保持不变：Epoch 5 必须高于 `28.2235 dB`，且五轮验证均值必须高于 `27.7003 dB`；否则停止，不调整饱和函数、density scale、gate 或 loss 追参。

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=9b5a1c899a5798b673770a62dcfc05ed7e1f54bd3efa645320b48b255d43d4ea
ACTUAL=$(sha256sum GaussianSR_face_signed_density_aggregation_probe5_patch_20260803.zip | awk '{print $1}')
RUN=save/face_gaussian_signed_density_aggregation_seed1_probe5
OUT=results/face_signed_density_aggregation_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: signed-density SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: signed-density probe5 path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_signed_density_aggregation_seed1.yaml \
    --name face_gaussian_signed_density_aggregation_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_signed_density_aggregation_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_signed_density_aggregation_probe5/train_console.log \
  || true
```

## 2026-08-03：symmetric signed-density Gaussian aggregation 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_signed_density_aggregation_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0866` 降至 `0.0593`，每轮约 `3.1-3.3m`，训练过程和计算成本稳定。

| Epoch | 几何-外观参考 | signed-density aggregation | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0167 | -0.0013 |
| 2 | 27.4557 | 27.4726 | +0.0169 |
| 3 | 27.7460 | 27.8174 | +0.0714 |
| 4 | 28.0584 | 28.0669 | +0.0085 |
| 5 | 28.2235 | 28.2205 | -0.0030 |

候选五次验证均值为 `27.7188 dB`，参考为 `27.7003 dB`，均值提高 `+0.0185 dB`；但 Epoch 5 比参考低 `0.0030 dB`。

### 判定

1. 五轮均值门槛通过，但 Epoch 5 严格大于 `28.2235 dB` 的门槛未通过；预注册规则要求两项同时成立，因此整体判定失败，不进入 30 epoch。
2. 候选的 signed feature、Gaussian geometry/bank 梯度和分块接口均已验证有效，且中期 Epoch 2-4 有小幅收益；失败不是模块未参与训练，而是最终重建能力没有稳定超过参考。
3. 不因 `0.0030 dB` 的微小差距调整 density scale、指数响应、gate、loss 或随机种子，也不重复运行同协议挑选随机波动。
4. 本结果只记录在本地 Markdown，不单独提交或 push，不进入论文正式结果表。
5. signed-density aggregation 路线停止。下一方向不得继续修改该饱和函数追参，应转向与 renderer 聚合算子不同的独立问题。

## 2026-08-03：启动 pixel-footprint integrated Fourier query 候选

### 问题、已有工作与边界

- 新模型注册名为 `gaussian-splatter-face-footprint-integrated-fourier`，encoder 仍为 `edsr-face-geometry-appearance`，Gaussian renderer、frequency residual、loss、数据和训练协议不变。
- 当前 Fourier query 在目标像素中心计算 learned local sinusoidal basis，并让 `cell` 进入 learned phase，但没有按目标像素覆盖面积解析积分。候选把每个二维 learned frequency 在矩形 pixel footprint 上的积分写成两个轴向 `sinc` 响应的乘积，再同时调制 cosine/sine basis。
- 当 footprint 趋近 0 时响应严格为 1，退化为点采样；目标像素覆盖越大，对不能在像素内稳定表示的频率衰减越强。该改动不增加参数，并保留原 nearest lattice 局部职责。
- 检索确认 [Enhancing Multi-Scale Implicit Learning in Image Super-Resolution with Integrated Positional Encoding](https://arxiv.org/abs/2112.05756) 已将像素面积上的频率积分用于任意尺度 SR，[LTE](https://arxiv.org/abs/2111.08918) 已使用 learned local Fourier texture。候选代码由当前 decoder 公式独立推导，没有读取或复制上述实现，但 integrated positional encoding、`sinc` footprint response 和 local Fourier SR 都不能宣称原创。
- 若最终有效，它只能作为已有抗混叠原则在 GaussianSR learned local frequency 上的适配型支撑模块；论文表述必须引用上述工作，不能把它作为唯一或核心原创贡献。

### 本地结构验证

- Python 静态编译和固定协议配置校验通过；seed 1、5 epoch、batch size 12、参考模型及候选模型正确。
- 参数量为 `2,024,100`，无新增参数。共享默认 decoder hook 与原 nearest Fourier decoder 误差为 `0.00e+00`。
- 解析测试的 zero-footprint error 为 `0.00e+00`；固定 frequency 下，小 footprint 与大 footprint 的平均响应分别为 `0.987215` 和 `0.810569`，面积响应方向正确。
- CPU smoke 使用 `16×16` 输入、512 queries、×1.5/×4/×8。实际平均 attenuation 为 `0.940999/0.990711/0.997467`，相对 1 的平均作用量为 `0.059001/0.009289/0.002533`，符合倍率越低、目标像素 footprint 越大、抗混叠越强的预期。
- 三倍率 geometry gradient 为 `6.544e-7/1.204e-7/3.725e-8`，Gaussian bank gradient 为 `1.394e-6/2.562e-7/9.793e-8`，integrated Fourier gradient 为 `1.195e-3/7.273e-4/8.604e-4`；appearance 与 residual 梯度也均有限非零。
- x4 重复完整评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`。
- 本地结构准入通过，但尚无 CUDA 或效果证据；本机 NumPy ABI 与 `torch.meshgrid` warning 不影响断言和退出码。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_footprint_integrated_fourier_probe5_patch_20260803.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_footprint_integrated_fourier_probe5_patch_20260803.zip`。
- 大小：`6,059` bytes。
- SHA-256：`493c1a0581a0212043023ca8d9ba3640df335f2d24c67466abaec17705175aaa`。
- ZIP 仅包含模型注册、新候选、独立 smoke 和 5 epoch 配置 4 个文件；不包含失败模块、数据、日志、结果或 checkpoint。

上传到服务器项目根目录后，第一阶段只执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=493c1a0581a0212043023ca8d9ba3640df335f2d24c67466abaec17705175aaa
ACTUAL=$(sha256sum GaussianSR_face_footprint_integrated_fourier_probe5_patch_20260803.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: footprint-Fourier SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_footprint_integrated_fourier_20260803
  cp models/__init__.py \
    server_backups/pre_footprint_integrated_fourier_20260803/models_init.py

  unzip -o GaussianSR_face_footprint_integrated_fourier_probe5_patch_20260803.zip

  python -m py_compile \
    models/gaussian_face_footprint_integrated_fourier.py \
    models/__init__.py \
    smoke_test_face_footprint_integrated_fourier.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_footprint_integrated_fourier_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-footprint-integrated-fourier

  python smoke_test_face_footprint_integrated_fourier.py \
    --config configs/train/face/probe_face_gaussian_footprint_integrated_fourier_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时输出：默认 decoder error 0、zero-footprint error 0、small attenuation 大于 large attenuation、三倍率 attenuation 有效、全部分支梯度有限非零、重复/分块误差不超过 `1e-6` 和 `SMOKE TEST PASSED`。在这些证据回传前不启动 5 epoch。

## 2026-08-04：pixel-footprint integrated Fourier query 通过服务器 smoke 准入

- 补丁哈希校验通过并成功解压；Python 静态编译无报错，配置校验输出 `CONFIG VALIDATION PASSED`，固定协议保持 seed 1、5 epoch、batch size 12。
- 服务器 CUDA 参数量为 `2,024,100`，与参考和本地结果一致；默认 decoder refactor error 与 zero-footprint error 均为 `0.00e+00`。
- 固定 frequency 的解析测试中，小 footprint 和大 footprint 响应分别为 `0.987215` 与 `0.810570`，像素面积越大、频率响应越弱，方向正确。
- ×1.5/×4/×8 的平均 attenuation 为 `0.937992/0.991276/0.997716`，平均作用量为 `0.062008/0.008724/0.002284`；低倍率具有更强的 pixel-footprint 抗混叠响应，机制没有退化为恒等映射。
- 三倍率 geometry gradient 为 `2.161e-7/1.412e-7/2.607e-8`，Gaussian bank gradient 为 `4.668e-7/3.583e-7/8.959e-8`，integrated Fourier gradient 为 `1.225e-3/7.075e-4/8.858e-4`；appearance 与 residual 梯度也均有限非零。
- x4 重复评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`。`torch.meshgrid` 仅为已知兼容性 warning。

该候选通过服务器结构准入，允许启动唯一一次固定协议 5 epoch probe。效果门禁保持不变：Epoch 5 必须高于 `28.2235 dB`，且五轮验证均值必须高于 `27.7003 dB`；否则停止，不调整 `sinc` 尺度、cell 定义、frequency 温度、gate、loss 或随机种子追参。

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=493c1a0581a0212043023ca8d9ba3640df335f2d24c67466abaec17705175aaa
ACTUAL=$(sha256sum GaussianSR_face_footprint_integrated_fourier_probe5_patch_20260803.zip | awk '{print $1}')
RUN=save/face_gaussian_footprint_integrated_fourier_seed1_probe5
OUT=results/face_footprint_integrated_fourier_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: footprint-Fourier SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: footprint-Fourier probe5 path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_footprint_integrated_fourier_seed1.yaml \
    --name face_gaussian_footprint_integrated_fourier_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_footprint_integrated_fourier_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_footprint_integrated_fourier_probe5/train_console.log \
  || true
```

## 2026-08-04：pixel-footprint integrated Fourier query 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_footprint_integrated_fourier_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0860` 降至 `0.0593`，每轮约 `3.3-3.4m`，过程稳定。

| Epoch | 几何-外观参考 | footprint-integrated Fourier | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0446 | +0.0266 |
| 2 | 27.4557 | 27.4754 | +0.0197 |
| 3 | 27.7460 | 27.7862 | +0.0402 |
| 4 | 28.0584 | 28.0676 | +0.0092 |
| 5 | 28.2235 | 28.2234 | -0.0001 |

候选五次验证均值为 `27.7194 dB`，参考为 `27.7003 dB`，均值提高 `+0.0191 dB`；但 Epoch 5 比严格门槛低 `0.0001 dB`。

### 判定

1. 五轮均值门槛通过，但 Epoch 5 严格大于 `28.2235 dB` 的条件未通过，因此整体判定失败，不进入 30 epoch。
2. `0.0001 dB` 已低于四位日志能支持的可靠区分尺度，只能视为数值持平；不得查询未记录隐藏精度、改变舍入规则或以容差替代预注册门槛。
3. 解析 attenuation、Fourier 梯度、Gaussian 梯度和分块接口均已验证有效，中前期五轮全部不低于或接近参考；失败说明已有 IPE 原则的适配没有形成可确认的最终优势。
4. 不调整 `sinc` 尺度、cell 半径、frequency 温度、gate、loss 或随机种子，也不重复运行同协议挑选波动。
5. 本结果只记录在本地 Markdown，不单独提交或 push，不进入论文正式结果表；pixel-footprint/IPE 路线停止。

## 2026-08-04：启动 per-patch Gaussian geometry isolation 候选

### 问题与实验边界

- 当前基础 `weighted_gaussian_parameters()` 会把展开后的全部图像和局部块沿 batch/patch 维求平均，再让所有块共享同一组 `sigma_x/sigma_y/opacity/rho`。这会删除 geometry encoder 已经预测出的局部差异，并在多 GPU batch 内产生跨样本耦合。
- 项目早期模型 B v1/v2 曾同时引入 per-patch renderer 与 frequency/scale geometry modulator，因此其失败无法隔离“去除跨块平均”本身的贡献。当前候选第一次在最强的 `geometry-appearance + frequency residual` 参考上只改变这一项。
- 新模型注册名为 `gaussian-splatter-face-per-patch-geometry`。每个展开块独立用自身 geometry probabilities 加权同一个 Gaussian 参数库；不添加调制器、参数、loss、数据或训练技巧，decoder 和 RGB residual 保持不变。
- per-patch geometry、局部 Gaussian 参数和去除跨样本平均都不是原创概念，也与项目早期模型 B 的局部 renderer 有继承关系。该实验是干净的机制消融和实现契约修正，不能单独作为论文创新点；若有效，只能作为当前几何-外观职责结构的必要 renderer 支撑。

### 本地结构验证

- Python 静态编译和固定协议配置校验通过；seed 1、5 epoch、batch size 12、参考模型及候选模型正确。
- 参数量为 `2,024,100`，与参考完全一致，无新增可学习参数。
- 三个合成 patch 分别使用不同 one-hot Gaussian prototype 时，local parameter isolation error 为 `0.00e+00`；重排 patch 后参数只发生对应重排，permutation error 为 `0.00e+00`。
- CPU smoke 使用 `16×16` 输入、512 queries、×1.5/×4/×8。每张输入形成 9 个独立 patch，真实 `sigma_x/sigma_y` 跨块标准差约为 `0.287-0.319`，证明 geometry probabilities 中存在此前会被平均掉的局部差异。
- opacity 参数库初始值近常数，rho 参数库初始值全零，因此初始 opacity variation 约 `3.1e-7`、rho variation 为 0；这是参数库初始化属性，不影响 sigma 的局部隔离结论，训练梯度仍覆盖完整 Gaussian bank。
- 三倍率 geometry gradient 为 `1.125e-6/2.263e-7/8.685e-8`，Gaussian bank gradient 为 `2.926e-6/4.827e-7/2.789e-7`；appearance 与 frequency residual 梯度也均有限非零。
- x4 重复完整评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`。
- 本地 CPU smoke 耗时高于共享参数 renderer，服务器 CUDA smoke 需要同时观察结构正确性、显存和运行时间，尚不能直接启动训练。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_per_patch_geometry_probe5_patch_20260804.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_per_patch_geometry_probe5_patch_20260804.zip`。
- 大小：`5,595` bytes。
- SHA-256：`289c2d9dee40e92e00fb0a88c27595bbb4acd408b9483444e4f0bd6d1dab6e0c`。
- ZIP 仅包含模型注册、新候选、独立 smoke 和 5 epoch 配置 4 个文件；复用仓库已有 `gaussian_local_freq_scale.py` 中经过验证的 per-patch rasterization，不包含数据、日志、结果或 checkpoint。

上传到服务器项目根目录后，第一阶段只执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=289c2d9dee40e92e00fb0a88c27595bbb4acd408b9483444e4f0bd6d1dab6e0c
ACTUAL=$(sha256sum GaussianSR_face_per_patch_geometry_probe5_patch_20260804.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: per-patch geometry SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_per_patch_geometry_20260804
  cp models/__init__.py \
    server_backups/pre_per_patch_geometry_20260804/models_init.py

  unzip -o GaussianSR_face_per_patch_geometry_probe5_patch_20260804.zip

  python -m py_compile \
    models/gaussian_face_per_patch_geometry.py \
    models/__init__.py \
    smoke_test_face_per_patch_geometry.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_per_patch_geometry_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-per-patch-geometry

  python smoke_test_face_per_patch_geometry.py \
    --config configs/train/face/probe_face_gaussian_per_patch_geometry_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时输出：patch isolation/permutation error 均为 0、真实 sigma patch variation 非零、四条梯度有限非零、重复/分块误差不超过 `1e-6` 和 `SMOKE TEST PASSED`。还需确认没有 OOM；这些证据回传前不启动 5 epoch。

## 2026-08-04：per-patch Gaussian geometry isolation 通过服务器 smoke 准入

- 补丁哈希校验通过并成功解压；Python 静态编译无报错，配置校验输出 `CONFIG VALIDATION PASSED`，固定协议保持 seed 1、5 epoch、batch size 12。
- 服务器 CUDA 参数量为 `2,024,100`，与参考和本地一致；patch isolation error 与 patch permutation error 均为 `0.00e+00`，确认局部参数既不跨块混合，也不依赖块排列顺序。
- ×1.5/×4/×8 均形成 9 个独立 patch。`sigma_x` variation 为 `0.3123/0.3068/0.2923`，`sigma_y` variation 为 `0.2985/0.3024/0.2787`，局部几何差异在三种倍率下均实际存在。
- opacity variation 约 `1.1e-7`，rho variation 为 0，与参数库的常数/零初始化一致；该现象不是 patch 混合，Gaussian bank 梯度仍覆盖这些参数。
- 三倍率 geometry gradient 为 `7.643e-7/2.675e-7/9.331e-8`，Gaussian bank gradient 为 `1.909e-6/5.480e-7/2.454e-7`；appearance 与 frequency residual 梯度也均有限非零。
- x4 重复评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`；没有 OOM 或异常。

该候选通过服务器结构准入，允许启动唯一一次固定协议 5 epoch probe。效果门禁保持不变：Epoch 5 必须高于 `28.2235 dB`，且五轮验证均值必须高于 `27.7003 dB`；否则停止，不增加局部 modulator、不改变 patch size、参数库、loss 或随机种子追参。

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=289c2d9dee40e92e00fb0a88c27595bbb4acd408b9483444e4f0bd6d1dab6e0c
ACTUAL=$(sha256sum GaussianSR_face_per_patch_geometry_probe5_patch_20260804.zip | awk '{print $1}')
RUN=save/face_gaussian_per_patch_geometry_seed1_probe5
OUT=results/face_per_patch_geometry_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: per-patch geometry SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: per-patch geometry probe5 path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_per_patch_geometry_seed1.yaml \
    --name face_gaussian_per_patch_geometry_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_per_patch_geometry_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_per_patch_geometry_probe5/train_console.log \
  || true
```

## 2026-08-04：per-patch Gaussian geometry isolation 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_per_patch_geometry_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0868` 降至 `0.0593`，每轮约 `3.1-3.2m`，没有产生额外的实际训练耗时负担。

| Epoch | 几何-外观参考 | per-patch geometry | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0060 | -0.0120 |
| 2 | 27.4557 | 27.4353 | -0.0204 |
| 3 | 27.7460 | 27.7682 | +0.0222 |
| 4 | 28.0584 | 28.0552 | -0.0032 |
| 5 | 28.2235 | 28.2244 | +0.0009 |

候选五次验证均值为 `27.6978 dB`，参考为 `27.7003 dB`，均值降低 `-0.0025 dB`；Epoch 5 高于参考 `0.0009 dB`。

### 判定

1. Epoch 5 门槛通过，但五轮均值严格大于 `27.7003 dB` 的条件未通过，因此整体判定失败，不进入 30 epoch。
2. patch isolation、真实 sigma variation 和 Gaussian 梯度均已确认有效，失败不是实现退化；结果说明去除跨图像/跨块平均没有改善当前强基线的整体收敛轨迹。
3. 不增加 frequency/scale local modulator、不修改 patch size、Gaussian bank、loss 或随机种子，也不与早期模型 B v1/v2 组合追参。
4. 本结果只记录在本地 Markdown，不单独提交或 push，不进入论文正式结果表；local/per-patch Gaussian geometry 路线停止。

## 2026-08-04：启动 hierarchical subpixel amplitude modulation 候选

### 问题、方法与创新边界

- 新模型注册名为 `gaussian-splatter-face-hierarchical-amplitude`，保持 geometry-appearance encoder、Gaussian renderer、nearest lattice、learned Fourier frequency/phase、RGB residual、loss 和数据协议不变。
- 当前 Fourier decoder 的 phase 随 query relative coordinate 变化，但 coefficient amplitude 在一个 nearest LR cell 内保持常数。候选从 `relative_coord + relative_cell` 构造 1/2/4 三个 dyadic level 的正余弦特征，以小型 MLP 预测每个 coefficient channel 的有界乘性残差。
- amplitude residual 限制在 `[-0.25, 0.25]`，最后一层全零初始化；初始输出逐元素严格退化为原 decoder，只有训练学到子像素层次位置相关的幅度模式后才改变 coefficient。
- [HIIF](https://arxiv.org/abs/2412.03748) 已使用 hierarchical positional encoding 与 implicit attention 增强连续 SR；多频位置编码和坐标条件调制也都是已有思想。本候选不复制 HIIF 的层次编码结构、attention、non-local feature 或代码，只参考“单层局部坐标不足以描述多尺度子像素结构”的问题判断。
- 若实验有效，边界只能表述为：在 GaussianSR 的 coefficient/frequency 职责分离 Fourier decoder 中，以零初始化 dyadic subpixel encoding 仅调制 coefficient amplitude，使 phase 与 amplitude 都能响应连续 query；不能把层次位置编码本身宣称为原创。

### 本地结构验证

- Python 静态编译和固定协议配置校验通过；seed 1、5 epoch、batch size 12、参考模型及候选模型正确。
- 参数量为 `2,041,828`，比参考增加 `17,728` 个参数，约 `0.88%`；新增参数全部位于 amplitude modulator。
- 合成 decoder 检查中 zero-init decoder error 与 zero-init amplitude error 均为 `0.00e+00`，确认新增模块初始时不改变参考行为。
- hierarchical coordinate 维度为 16，合成坐标 variation 为 `0.484279`；完整 ×1.5/×4/×8 前向的 coordinate standard deviation 为 `0.7825/0.6633/0.6496`，编码实际随 query/cell 变化。
- 初始 amplitude delta 严格为 0，但三倍率 amplitude branch gradient 为 `8.465e-5/7.014e-5/5.231e-5`，输出层从第一次更新即可学习；这不是零初始化造成的死分支。
- 三倍率 geometry gradient 为 `7.147e-7/1.040e-7/3.698e-8`，Gaussian bank gradient 为 `1.465e-6/2.264e-7/1.115e-7`；appearance 与 residual 梯度也均有限非零。
- x4 重复完整评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`。

### 服务器补丁与第一阶段操作

- 文件：`GaussianSR_face_hierarchical_amplitude_probe5_patch_20260804.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_hierarchical_amplitude_probe5_patch_20260804.zip`。
- 大小：`5,963` bytes。
- SHA-256：`cd6822dd684e06554f3ec4271c7affc34ac8b9abdbe1d116854b74a086bcbb98`。
- ZIP 仅包含模型注册、新候选、独立 smoke 和 5 epoch 配置 4 个文件，不包含数据、日志、结果或 checkpoint。

上传到服务器项目根目录后，第一阶段只执行：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=cd6822dd684e06554f3ec4271c7affc34ac8b9abdbe1d116854b74a086bcbb98
ACTUAL=$(sha256sum GaussianSR_face_hierarchical_amplitude_probe5_patch_20260804.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: hierarchical amplitude SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_hierarchical_amplitude_20260804
  cp models/__init__.py \
    server_backups/pre_hierarchical_amplitude_20260804/models_init.py

  unzip -o GaussianSR_face_hierarchical_amplitude_probe5_patch_20260804.zip

  python -m py_compile \
    models/gaussian_face_hierarchical_amplitude.py \
    models/__init__.py \
    smoke_test_face_hierarchical_amplitude.py

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_geometry_appearance_seed1.yaml \
    --model-b configs/train/face/probe_face_gaussian_hierarchical_amplitude_seed1.yaml \
    --expected-baseline-model gaussian-splatter-face-geometry-appearance \
    --expected-model-b gaussian-splatter-face-hierarchical-amplitude

  python smoke_test_face_hierarchical_amplitude.py \
    --config configs/train/face/probe_face_gaussian_hierarchical_amplitude_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

服务器必须同时输出：两个 zero-init error 均为 0、hierarchical coordinate variation 非零、三倍率 amplitude gradient 非零、其他分支梯度有限、重复/分块误差不超过 `1e-6` 和 `SMOKE TEST PASSED`。这些证据回传前不启动训练。

## 2026-08-04：hierarchical subpixel amplitude modulation 通过服务器 smoke 准入

- 补丁哈希校验通过并成功解压；Python 静态编译无报错，配置校验输出 `CONFIG VALIDATION PASSED`，固定协议保持 seed 1、5 epoch、batch size 12。
- 服务器 CUDA 参数量为 `2,041,828`，与本地一致；zero-init decoder error 和 zero-init amplitude error 均为 `0.00e+00`，新增模块初始时逐元素保持参考 decoder。
- hierarchical coordinate 维度为 16、variation 为 `0.484279`；×1.5/×4/×8 完整前向的 coordinate standard deviation 为 `0.782528/0.663326/0.649632`，层次编码实际响应 query/cell。
- 初始 amplitude max 和 mean absolute 均为 0，符合零初始化设计；对应三倍率 amplitude branch gradient 为 `8.720e-5/7.004e-5/5.213e-5`，证明输出层从第一次参数更新即可激活，不是死分支。
- 三倍率 geometry gradient 为 `2.200e-7/1.427e-7/2.609e-8`，Gaussian bank gradient 为 `4.465e-7/3.620e-7/8.967e-8`；appearance 与 frequency residual 梯度也均有限非零。
- x4 重复评估误差为 `0.00e+00`，普通与分块评估误差为 `7.45e-09`，最终输出 `SMOKE TEST PASSED`；没有 OOM 或异常。

该候选通过服务器结构准入，允许启动唯一一次固定协议 5 epoch probe。效果门禁保持不变：Epoch 5 必须高于 `28.2235 dB`，且五轮验证均值必须高于 `27.7003 dB`；否则停止，不调整 dyadic level、amplitude bound、hidden width、初始化、loss 或随机种子追参。

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=cd6822dd684e06554f3ec4271c7affc34ac8b9abdbe1d116854b74a086bcbb98
ACTUAL=$(sha256sum GaussianSR_face_hierarchical_amplitude_probe5_patch_20260804.zip | awk '{print $1}')
RUN=save/face_gaussian_hierarchical_amplitude_seed1_probe5
OUT=results/face_hierarchical_amplitude_probe5

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: hierarchical amplitude SHA-256 mismatch: $ACTUAL"
elif [ -e "$RUN" ] || [ -e "$OUT" ]; then
  echo "STOP: hierarchical amplitude probe5 path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup python -u train_gaussian.py \
    --config configs/train/face/probe_face_gaussian_hierarchical_amplitude_seed1.yaml \
    --name face_gaussian_hierarchical_amplitude_seed1_probe5 \
    --gpu 0,1,2 \
    --seed 1 \
    > "$OUT/train_console.log" 2>&1 &
  echo $! | tee "$OUT/train.pid"
fi
```

训练完成后回传：

```bash
grep -nE "epoch .*val:" \
  save/face_gaussian_hierarchical_amplitude_seed1_probe5/log.txt

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_hierarchical_amplitude_probe5/train_console.log \
  || true
```

## 2026-08-04：hierarchical subpixel amplitude modulation 未通过 5 epoch 效果准入

### 训练结果

- 运行名：`face_gaussian_hierarchical_amplitude_seed1_probe5`。
- 5 个 epoch 全部完成，进程状态为 `Done`；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 训练损失由 `0.0867` 降至 `0.0593`；每轮约 `3.3-3.6m`，比参考约 `3.1-3.2m` 略慢。

| Epoch | 几何-外观参考 | hierarchical amplitude | 差值（候选 - 参考） |
|---:|---:|---:|---:|
| 1 | 27.0180 | 27.0185 | +0.0005 |
| 2 | 27.4557 | 27.4780 | +0.0223 |
| 3 | 27.7460 | 27.7680 | +0.0220 |
| 4 | 28.0584 | 28.0492 | -0.0092 |
| 5 | 28.2235 | 28.1748 | -0.0487 |

候选五次验证均值为 `27.6977 dB`，参考为 `27.7003 dB`，均值降低 `-0.0026 dB`；Epoch 5 降低 `-0.0487 dB`。

### 判定

1. Epoch 5 和五轮均值两个预注册门槛均未通过，不进入 30 epoch。
2. zero-init、层次坐标 variation 和 amplitude branch 梯度均已验证有效；失败不是新增分支未激活，而是中期小幅收益在后两轮反转。
3. 不调整 dyadic level、amplitude bound、hidden width、初始化、loss 或随机种子，也不组合 IPE、local ensemble 或其他失败 decoder 模块追参。
4. 本结果只记录在本地 Markdown，不单独提交或 push，不进入论文正式结果表；hierarchical coordinate/amplitude 路线停止。

### 阶段性研究判断

- signed-density、pixel-footprint Fourier、per-patch geometry 和 hierarchical amplitude 四个连续候选都通过了严格结构 smoke，但效果只在 `±0.02 dB` 左右波动，且最终门槛均未同时通过。
- 这些结果说明当前 seed 1、5 epoch 筛选已经接近随机与日志精度可区分的边界。继续以相同方式增加小模块，容易对单一种子和单个最终 epoch 进行结构搜索，不足以形成可靠论文证据。
- 依据近期 INR-SR benchmark 对“复杂方法常只有边际收益、训练配置显著影响结论”的提醒，下一步不应立即继续模块轮盘。应先决定是否允许扩展固定协议，用额外种子量化现有 `geometry-appearance` 相对 baseline 的方差；在用户明确批准协议扩展前，不创建新的训练候选或启动服务器任务。

## 2026-08-04：批准 geometry-appearance 三种子配对方差验证

### 预注册协议

- 用户已明确批准把固定 seed 1 协议扩展到 seed 2、3。本轮不提出新模型，只验证现有 `gaussian-splatter-face-geometry-appearance` 相对 `gaussian-splatter` baseline 的跨种子方向一致性。
- seed 2、3 各自运行同协议 baseline 与 geometry-appearance；数据、增强、batch size 12、倍率分组、5 epoch、优化器、学习率和验证集保持一致，每对只允许模型/encoder 结构不同。
- 所有比较必须在同一个 seed 内配对计算，禁止直接比较不同 seed 的绝对 PSNR。
- 每个 seed 计算两项：Epoch 5 的 `candidate - baseline`，以及五个 epoch PSNR 均值的 `candidate - baseline`。
- 严格鲁棒性条件预注册为：seed 1、2、3 的上述两项差值全部严格大于 0。只要任一个 seed 的任一项不为正，即输出 `MULTISEED ROBUSTNESS FAILED`，不能宣称提升方向一致。
- 同时报告三种子 paired delta 的均值、样本标准差和标准误；样本仅 3 个，不把正态近似置信区间当作显著性证明。
- seed 1 已有配对轨迹：baseline 为 `26.9884/27.4608/27.7659/27.9881/28.1628`，geometry-appearance 为 `27.0180/27.4557/27.7460/28.0584/28.2235`；五轮均值差为 `+0.0271 dB`，Epoch 5 差为 `+0.0607 dB`，seed 1 两项通过。
- seed 2、3 四个任务必须全部完成后统一分析；不允许根据 seed 2 中间结果取消 seed 3，也不允许改变 seed、重复运行或选择 checkpoint。

### 配置与分析工具

- 新增 seed 2、3 的 baseline/candidate 配置共 4 份，只修改配对 seed，均已通过 `validate_face_training_configs.py`。
- 新增 `analyze_face_multiseed_probe.py`，严格解析每份日志的 Epoch 1-5，输出逐 seed paired delta、跨种子均值/样本标准差/标准误和唯一 PASS/FAIL 结论。
- 新增 `run_face_geometry_appearance_multiseed_probe.sh`，启动前检查全部 8 个 `save/results` 路径不存在，然后顺序运行 seed 2 baseline、seed 2 candidate、seed 3 baseline、seed 3 candidate；任一命令失败或日志出现 NaN/OOM/Traceback/RuntimeError 即停止。
- 本地没有 Bash 可执行文件，因此未运行 `bash -n`；服务器解压后必须先执行语法检查，通过后才能启动。Python 分析器已静态编译，日志正则单元检查通过。

### 服务器补丁与操作

- 文件：`GaussianSR_face_geometry_appearance_multiseed_probe_20260804.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_geometry_appearance_multiseed_probe_20260804.zip`。
- 大小：`5,997` bytes。
- SHA-256：`3907ef2aa41575c33f233530329e5207aa00b3eab511d089ecb49a7102b1512e`。
- ZIP 只包含 4 份配置、顺序运行器和分析器，不覆盖模型代码，不包含数据、日志、结果或 checkpoint。

上传到服务器项目根目录后执行配置安装与校验：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=3907ef2aa41575c33f233530329e5207aa00b3eab511d089ecb49a7102b1512e
ACTUAL=$(sha256sum GaussianSR_face_geometry_appearance_multiseed_probe_20260804.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: multiseed probe SHA-256 mismatch: $ACTUAL"
elif [ -e configs/train/face/probe_face_gaussian_baseline_seed2.yaml ] || \
     [ -e configs/train/face/probe_face_gaussian_geometry_appearance_seed2.yaml ] || \
     [ -e configs/train/face/probe_face_gaussian_baseline_seed3.yaml ] || \
     [ -e configs/train/face/probe_face_gaussian_geometry_appearance_seed3.yaml ] || \
     [ -e analyze_face_multiseed_probe.py ] || \
     [ -e run_face_geometry_appearance_multiseed_probe.sh ]; then
  echo "STOP: one or more multiseed patch paths already exist"
else
  unzip GaussianSR_face_geometry_appearance_multiseed_probe_20260804.zip

  python -m py_compile analyze_face_multiseed_probe.py
  bash -n run_face_geometry_appearance_multiseed_probe.sh

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_baseline_seed2.yaml \
    --model-b configs/train/face/probe_face_gaussian_geometry_appearance_seed2.yaml \
    --expected-model-b gaussian-splatter-face-geometry-appearance \
    --expected-encoder-b edsr-face-geometry-appearance

  python validate_face_training_configs.py \
    --baseline configs/train/face/probe_face_gaussian_baseline_seed3.yaml \
    --model-b configs/train/face/probe_face_gaussian_geometry_appearance_seed3.yaml \
    --expected-model-b gaussian-splatter-face-geometry-appearance \
    --expected-encoder-b edsr-face-geometry-appearance
fi
```

以上全部通过后，启动顺序运行器：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

OUT=results/face_geometry_appearance_multiseed_probe

if [ -e "$OUT" ]; then
  echo "STOP: multiseed orchestrator path exists; do not overwrite"
else
  mkdir -p "$OUT"
  nohup bash run_face_geometry_appearance_multiseed_probe.sh \
    > "$OUT/orchestrator.log" 2>&1 &
  echo $! | tee "$OUT/orchestrator.pid"
fi
```

全部完成后检查并分析：

```bash
tail -n 120 results/face_geometry_appearance_multiseed_probe/orchestrator.log

grep -inE "nan|out of memory|traceback|runtimeerror" \
  results/face_multiseed_baseline_seed2_probe5/train_console.log \
  results/face_multiseed_geometry_appearance_seed2_probe5/train_console.log \
  results/face_multiseed_baseline_seed3_probe5/train_console.log \
  results/face_multiseed_geometry_appearance_seed3_probe5/train_console.log \
  || true

python analyze_face_multiseed_probe.py \
  --pair 1 \
    save/face_gaussian_baseline_seed1_probe30_scale_moe_compare/log.txt \
    save/face_gaussian_geometry_appearance_seed1_probe5/log.txt \
  --pair 2 \
    save/face_gaussian_baseline_seed2_multiseed_probe5/log.txt \
    save/face_gaussian_geometry_appearance_seed2_multiseed_probe5/log.txt \
  --pair 3 \
    save/face_gaussian_baseline_seed3_multiseed_probe5/log.txt \
    save/face_gaussian_geometry_appearance_seed3_multiseed_probe5/log.txt
```

## 2026-08-05：geometry-appearance 三种子配对鲁棒性检验未通过

### 运行完整性

- 顺序运行器完整执行了 seed 2 baseline、seed 2 geometry-appearance、seed 3 baseline 和 seed 3 geometry-appearance 四个任务，最终输出 `MULTISEED PROBES COMPLETED`。
- 四个任务均完成 5 个 epoch；异常检索没有输出，未出现 NaN、OOM、Traceback 或 RuntimeError。
- 本次分析严格使用预注册的同 seed 配对比较，没有取消任务、重复运行、选择性替换 checkpoint 或修改训练协议。
- 服务器分析器已实际执行，标准输出通过 `tee` 保存为 `results/face_geometry_appearance_multiseed_probe/analysis.log`；末行明确输出 `MULTISEED ROBUSTNESS FAILED`，其逐 seed 差值、跨种子样本标准差和标准误均与下表一致。

### 逐种子配对结果

| Seed | Baseline 五轮 PSNR | Geometry-appearance 五轮 PSNR | 五轮均值差（候选 - baseline） | Epoch 5 差（候选 - baseline） | 两项均为正 |
|---:|---|---|---:|---:|---|
| 1 | 26.9884 / 27.4608 / 27.7659 / 27.9881 / 28.1628 | 27.0180 / 27.4557 / 27.7460 / 28.0584 / 28.2235 | +0.02712 dB | +0.0607 dB | 是 |
| 2 | 26.8654 / 27.5099 / 27.8064 / 28.0477 / 28.1882 | 26.9342 / 27.4534 / 27.7954 / 27.9645 / 28.1501 | -0.02400 dB | -0.0381 dB | 否 |
| 3 | 26.9460 / 27.4794 / 27.6879 / 27.9884 / 28.1684 | 27.0437 / 27.4804 / 27.6898 / 27.9817 / 28.2224 | +0.02958 dB | +0.0540 dB | 是 |

seed 2 在五轮均值和 Epoch 5 两项上均发生方向反转，因此即使 seed 1、3 为正，也已经触发预注册失败条件。

### 跨种子配对统计

| 配对指标 | 三种子平均差 | 样本标准差 | 标准误 |
|---|---:|---:|---:|
| 五轮 PSNR 均值差 | +0.0109 dB | 0.0302493 dB | 0.0174644 dB |
| Epoch 5 PSNR 差 | +0.0255333 dB | 0.0552098 dB | 0.0318754 dB |

两个指标的跨种子平均差虽然为正，但样本标准差和标准误均大于平均效应，且样本仅有 3 个。该结果不能覆盖 seed 2 的符号反转，也不能作为统计显著或稳健提升的证据。

### 预注册判定与停止条件

1. 严格条件要求 seed 1、2、3 的五轮均值差和 Epoch 5 差全部严格大于 0；seed 2 两项均为负，因此唯一判定为 `MULTISEED ROBUSTNESS FAILED`。
2. 不能宣称 geometry-appearance 相对 baseline 具有跨种子方向一致、稳健或显著的 PSNR 提升。seed 1 的 `+0.0607 dB` 最终值属于对随机种子敏感的表观收益，而不是足以单独支撑论文结论的证据。
3. 不启动 seed 2、3 的 30 epoch 训练，也不通过增加种子、重复运行、选择 checkpoint 或改变门槛追求通过。
4. 停止依据 seed 1 的细小 PSNR 差继续筛选 renderer、query、encoder 或 routing 模块。此前连续模块候选的 `0.00-0.02 dB` 级波动与本次跨种子方差处于同一量级，不能据此区分真实结构收益。
5. 本次属于严格验证后的否定性研究结论，只更新本地 Markdown；不提交、不 push，也不写入论文正式改进结果表。

### 阶段结论

当前架构搜索目标“在相同 CelebA 五轮探针协议下稳定提高验证 PSNR”未获得可靠候选。若继续研究，应先更换研究目标或证据层级，例如转向完整正式协议下的多指标机制分析、效率/参数量收益或可解释性消融，并为新目标重新预注册评价标准；不能继续沿用 seed 1 五轮 PSNR 小数点后两位作为模块选择器。

## 2026-08-05：论文驱动的 reflection-equivariant Gaussian geometry 候选

### 论文依据与方向筛选

- 在实现前定向阅读了 AAAI 2025 GaussianSR 与 DPI、CVPR 2025 HIIF、TPAMI 2025 Rotation Equivariant ASISR，以及 2025 ARASFSR、2026 SwinIFS、YOLO heatmap FSR 和 INR-ASSR empirical benchmark 的方法与消融；另核对 Local Gaussian Ensemble、CMANet、FreqFormer 和 AMINet。
- 排除扩散 prior、Mamba、完整 Transformer fusion、Local Gaussian Ensemble、再次添加 Fourier/坐标旁路和直接 landmark heatmap：它们分别与固定协议不匹配、变量过多、和已失败实验重合，或已有工作重合度过高。
- 选定“训练期 reflection-equivariant anisotropic Gaussian geometry”作为新候选：不移植 TPAMI 论文的 group convolution/steerable INR，只借鉴其几何等变原则；利用 GaussianSR 的显式 covariance 参数提出可独立测量的结构假设。
- 完整文献来源、逐篇证据、排除理由和原创边界见 `文献调研_任意尺度SR热点_20260802.md` 的 2026-08-05 章节。

### 实现范围

新增文件：

```text
models/gaussian_face_reflection_equivariant.py
smoke_test_face_reflection_equivariant.py
evaluate_face_reflection_equivariance.py
analyze_face_reflection_equivariant_probe.py
run_face_reflection_equivariant_multiseed_probe.sh
configs/train/face/probe_face_gaussian_reflection_equivariant_seed1.yaml
configs/train/face/probe_face_gaussian_reflection_equivariant_seed2.yaml
configs/train/face/probe_face_gaussian_reflection_equivariant_seed3.yaml
```

修改文件：

```text
models/__init__.py
train_gaussian.py
```

- 新 encoder 仅把原 EDSR segmentation head 的 Gumbel 采样前 raw logits 暴露给辅助目标，主输出行为与 `edsr-baseline` 相同。
- 对 raw logits 做 temperature-1 softmax，再用原 Gaussian parameter bank 得到 `covariance_xx=sigma_x^2`、`covariance_yy=sigma_y^2`、`covariance_xy=rho*sigma_x*sigma_y` 和 opacity 的逐位置期望。
- 原输入与水平镜像输入的 moment map 对齐后，`xx/yy/opacity` 保持同号，`xy` 按 covariance 反射规律变号；四项归一化 L1 平均后乘固定权重 `0.01`。
- 训练多一次 encoder 前向；评估分支不计算辅助目标，不增加模型参数或推理计算。
- `train_gaussian.py` 只新增 reflection diagnostics 的 loss 聚合与日志，不改变普通 baseline 和现有 MoE 分支。

### 本地验证

- Python 静态编译通过；`git diff --check` 无空白错误。
- baseline 与候选参数量均为 `1,838,159`，state dict 均为 93 个张量；相同 seed 下全部初始化张量最大误差为 `0.00e+00`。
- 小尺寸 CPU 完整训练前向中，候选主预测与 baseline 最大误差为 `0.00e+00`；初始 reflection raw loss 为 `0.00840623`，加权辅助项为 `8.40623e-05`。
- 单独反传辅助项后，segmentation head 梯度绝对和为 `0.17403`，`sigma_x/sigma_y` 梯度绝对和为 `1.5326e-4/1.5039e-4`；辅助目标已实际连接 geometry encoder 与 Gaussian bank。
- eval 模式候选与 baseline 输出最大误差为 `0.00e+00`，输出形状为 `(1, 16, 3)`。
- 本地 Python 缺少 PyYAML、Pillow 和 tensorboardX，因此完整 YAML smoke、配置校验与 held-out 数据评估留在服务器 GS 环境执行；本地使用未落盘 stub 只验证模型计算，不修改项目依赖。

### 预注册三种子门槛

seed 1/2/3 候选必须全部运行，禁止看完 seed 1 后取消其他任务。严格通过要求每个 seed 同时满足：五轮 PSNR 均值差 `> 0`、Epoch 5 PSNR 差 `> 0`、500 张 held-out val reflection error 相对对应 baseline 降低至少 20%。任一 seed 任一项失败即停止，不修改权重、temperature、normalization、随机种子或 checkpoint 追参，也不进入 30 epoch。

本轮目前只完成论文调研、实现和本地计算检查，尚未通过服务器 CUDA smoke，不能称为研究进展，不 commit、不 push。

### 服务器补丁与第一阶段准入

- 文件：`GaussianSR_face_reflection_equivariant_multiseed_patch_20260805.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_reflection_equivariant_multiseed_patch_20260805.zip`。
- 大小：`19,228` bytes。
- SHA-256：`41d512c8b10cd2f053a9c74a393b73e6c6dc3b77685a41675a2a6e0c8b1008ab`。
- ZIP 共 10 个文件，只包含模型注册、候选实现、训练日志接入、smoke、held-out evaluator、严格分析器、顺序运行器和三种子配置；不包含数据、checkpoint、日志或结果。

第一阶段仅允许安装、静态检查、三份配置校验和 seed 1 CUDA smoke；收到完整 `SMOKE TEST PASSED` 前不启动任何 5 epoch 任务：

```bash
cd /root/userfolder_new/20260527GaussiSR/GaussianSR-main

EXPECTED=41d512c8b10cd2f053a9c74a393b73e6c6dc3b77685a41675a2a6e0c8b1008ab
ACTUAL=$(sha256sum GaussianSR_face_reflection_equivariant_multiseed_patch_20260805.zip | awk '{print $1}')

if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "STOP: reflection-equivariant patch SHA-256 mismatch: $ACTUAL"
else
  mkdir -p server_backups/pre_reflection_equivariant_20260805
  cp models/__init__.py train_gaussian.py \
    server_backups/pre_reflection_equivariant_20260805/

  unzip -o GaussianSR_face_reflection_equivariant_multiseed_patch_20260805.zip

  python -m py_compile \
    models/gaussian_face_reflection_equivariant.py \
    models/__init__.py \
    train_gaussian.py \
    smoke_test_face_reflection_equivariant.py \
    evaluate_face_reflection_equivariance.py \
    analyze_face_reflection_equivariant_probe.py
  bash -n run_face_reflection_equivariant_multiseed_probe.sh

  for SEED in 1 2 3; do
    python validate_face_training_configs.py \
      --baseline "configs/train/face/probe_face_gaussian_baseline_seed${SEED}.yaml" \
      --model-b "configs/train/face/probe_face_gaussian_reflection_equivariant_seed${SEED}.yaml" \
      --expected-baseline-model gaussian-splatter \
      --expected-model-b gaussian-splatter-face-reflection-equivariant \
      --expected-encoder-b edsr-face-reflection-equivariant
  done

  python smoke_test_face_reflection_equivariant.py \
    --config configs/train/face/probe_face_gaussian_reflection_equivariant_seed1.yaml \
    --device cuda \
    --gpu 0 \
    --scales 1.5 4 8 \
    --seed 1
fi
```

smoke 必须同时确认参数量 `1,838,159`、shared initialization 与 analytic reflection error 为 0、三倍率初始主预测与 baseline 完全一致、reflection raw/aux 有限非零、encoder 与 Gaussian bank 梯度非零、eval baseline/repeat/batched error 不超过 `1e-6`，最后输出 `SMOKE TEST PASSED`。

## 2026-08-05：reflection-equivariant Gaussian geometry 通过服务器 smoke 准入

- 补丁哈希分支成功执行并解压 10 个预期文件；Python 静态编译和 Bash 语法检查没有报错。
- seed 1/2/3 三份候选配置均输出 `CONFIG VALIDATION PASSED`，确认 epoch 5、batch size 12、对应 seed、`gaussian-splatter` baseline、候选注册名和 encoder 注册名正确。
- 服务器 CUDA 模型参数量为 `1,838,159`；shared initialization error 与 analytic reflection error 均为 `0.00e+00`。
- ×1.5/×4/×8 的候选初始主预测与 baseline 最大误差均为 `0.00e+00`，证明辅助目标接入前没有改变受控预测变量。
- 三倍率 reflection raw loss 为 `0.008797/0.008491/0.008491`，加权 aux 为 `8.8e-5/8.5e-5/8.5e-5`；encoder 梯度为 `6.708e-4/7.379e-4/7.379e-4`，Gaussian bank 梯度为 `1.788e-5/1.990e-5/1.990e-5`，均有限非零。
- 初始 `covariance_xy` 和 opacity 误差为 0 属于预期：共享 baseline 初始化中全部 `rho=0`、全部 opacity 相同，因此这两项初始严格不区分类别；重建训练更新 Gaussian bank 后，两项即可参与约束。`xx/yy` 初始已非零，确保辅助分支从第一次更新起激活。
- eval 输出形状为 `(1, 512, 3)`；相对 baseline、重复评估和分块评估误差为 `0/0/7.45e-09`，最终输出 `SMOKE TEST PASSED`。

候选通过结构与训练接口准入。下一阶段必须通过顺序运行器一次性完成 seed 1、2、3 三个 5 epoch candidate probe；不得读取 seed 1 后取消后续 seed，也不得改变预注册超参数。

## 2026-08-06：reflection-equivariant Gaussian geometry 三种子联合准入未通过

### 服务器源码恢复与运行完整性

- 首次启动前，服务器上的 `train_gaussian.py` 被意外截断为 0 字节，随后发现 `models/__init__.py` 也不再导出 `models.make`；两次失败均发生在 epoch 训练前，失败路径已归档，没有作为实验结果使用。
- 服务器工作目录不含 `.git`，因此不能直接执行 Git 回滚。最终从已推送的 GitHub 提交 `4d0f59b` 构造 clean-base 快照，再只叠加 10 个 reflection 文件。完整快照为 `GaussianSR_cleanbase_4d0f59b_reflection_snapshot_20260805.zip`，SHA-256 为 `05d741cfca9d5083569b4cd383a572f9c3d62fae85abbf14a3eb3fd3f8fcca84`。
- 恢复后 `train_gaussian.py` 与 `models/__init__.py` 哈希、Python 静态编译、Bash 语法、`models.make`、配置验证和 CUDA smoke 全部通过。三种子顺序运行器最终输出 `REFLECTION-EQUIVARIANT MULTISEED PROBES COMPLETED`；异常扫描为空。

### Baseline 配对修正

- 原计划误把 seed 1 的 `face_gaussian_baseline_seed1_probe30_scale_moe_compare` 当作五轮 checkpoint 来源，但该目录只保存 epoch 10/20/30，没有 epoch 5 checkpoint。
- `probe30` 前五轮 PSNR 为 `26.9884/27.4608/27.7659/27.9881/28.1628`，而有真实 epoch-5 checkpoint 的 `face_gaussian_baseline_seed1_probe_full5` 为 `26.9929/27.4621/27.7418/27.9767/28.1793`，两条轨迹不相同，禁止拼接日志和 checkpoint。
- 最终 seed 1 使用 `face_gaussian_baseline_seed1_probe_full5` 的 `config.yaml`、`log.txt` 和 `epoch-5.pth` 一一配对。旧版验证器确认它与 seed 1 candidate 的数据、batch、epoch、优化器和共享模型字段协议一致。seed 2/3 同样使用各自 multiseed baseline 的保存配置、日志和 epoch-5 checkpoint。

### 三种子 PSNR 结果

| Seed | Baseline 五轮 PSNR | Candidate 五轮 PSNR | 五轮均值差 | Epoch 5 差 |
|---:|---|---|---:|---:|
| 1 | 26.9929 / 27.4621 / 27.7418 / 27.9767 / 28.1793 | 26.9667 / 27.4610 / 27.7677 / 27.9837 / 28.1622 | -0.0023 dB | -0.0171 dB |
| 2 | 26.8654 / 27.5099 / 27.8064 / 28.0477 / 28.1882 | 27.0302 / 27.4777 / 27.8049 / 28.0061 / 28.1221 | +0.0047 dB | -0.0661 dB |
| 3 | 26.9460 / 27.4794 / 27.6879 / 27.9884 / 28.1684 | 26.9813 / 27.5040 / 27.6965 / 27.9283 / 28.1630 | +0.0006 dB | -0.0054 dB |

- 三种子五轮均值差的 pooled mean 为 `+0.0010 dB`，样本标准差 `0.0035 dB`，标准误 `0.0020 dB`。
- 三种子 Epoch 5 差的 pooled mean 为 `-0.0295 dB`，样本标准差 `0.0322 dB`，标准误 `0.0186 dB`。
- seed 1 的五轮均值为负，且三个 seed 的 Epoch 5 差全部为负，因此 PSNR 两项预注册条件已经失败。

### 500 张 held-out reflection 评估

| Seed | Baseline error | Candidate error | 相对降低 | 20% 门槛 |
|---:|---:|---:|---:|---|
| 1 | 0.50534210 | 0.00025067 | 99.95% | 通过 |
| 2 | 0.50561898 | 0.00023274 | 99.95% | 通过 |
| 3 | 0.50794484 | 0.00024253 | 99.95% | 通过 |

- 三个 seed 的 paired delta 分别为 `-0.50509143/-0.50538623/-0.50770230`，对应标准误为 `0.00003322/0.00003605/0.00005870`；reflection reduction 的跨种子均值为 `99.95%`。
- candidate 的 `covariance_xx/yy/xy/opacity` error 均降至约 `1e-4` 或更低，说明固定权重 `0.01` 的训练期约束确实把 soft Gaussian moment 推向了镜像一致。
- 该机制指标不能解释为重建质量、身份保持或感知质量提升。baseline 总误差主要由接近 `2.0` 的 `covariance_xy` 项主导；当前评估没有同时约束 `xy` moment 的非零幅值，因此压低 `rho` 或交叉协方差幅值可能形成近似平凡解。虽然 `xx/yy` 也显著改善，但仍没有转化为 PSNR 收益。

### 预注册判定

最终分析器输出 `REFLECTION-EQUIVARIANT PROBE FAILED`。独立机制门槛在三个 seed 上都远超要求，但联合准入要求每个 seed 同时满足五轮均值差 `> 0`、Epoch 5 差 `> 0` 和 reflection error 至少降低 20%；实际没有任何 seed 同时满足三项。

该方向不进入 30 epoch，不调整 loss weight、temperature、normalization floor、反射轴、随机种子或 checkpoint，不把 held-out 指标改善包装成 SR 性能进展。本结果只记录在本地 Markdown，不 commit、不 push，也不进入论文正式改进结果表。

## 2026-08-06：RBS 启发的 latent tangent splatting 训练前预注册

### 文献复核与问题选择

reflection 结果证明内部机制指标可以显著改善却不提高重建质量，因此本轮不再添加与 RGB 输出间接相关的一致性 loss。训练前重新复核了 GaussianSR（AAAI 2025）、GSASR（ICCV 2025）、ContinuousSR（2025）、QuADA-GS（2026）、Resonant Brane Splatting（2026）、2DGH（IEEE TVCG 2026）、CasArbi（2025）、Functional Tensor Decomposition ASR（IEEE TMM 2026）和 Mamba Gaussian ASR（IEEE SPL 2025）。

QuADA-GS 的动态 densification/routing 与已失败的 sparse allocation、scale-depth router 高度重叠；Mamba/Transformer 会同时改变容量和计算；diffusion/CasArbi 与当前 bicubic-L1-PSNR 协议不匹配。最终只借鉴 RBS 经受控消融支持的一个问题判断：零阶 Gaussian 是平滑 primitive，primitive 内部的带符号空间变化可提高表达能力。RBS 在 DIV2K x4 上报告 degree 0/2/3/5/7 的 PSNR 为 `31.95/31.97/32.04/32.09/32.08 dB`，同时指出过高阶模式会留下残余噪声，因此本轮只验证一阶项。

### 最小受控实现

- 从 GitHub clean base `4d0f59b` 建立独立 worktree `GaussianSR-clean-latent-tangent-4d0f59b`，不叠加主目录中的失败实验。
- 保持 EDSR encoder、100 类 Gaussian parameter bank、Gaussian envelope/covariance/opacity、patch folding、Fourier decoder、L1 loss、数据和五轮协议不变。
- 在原 8 维 Gaussian-splatted latent 上增加一个零初始化 `Conv2d(8, 16, 1)`，为每个 primitive 的 8 个 latent channel 预测 x/y 一阶斜率，共新增 144 个参数。
- primitive 内部 appearance 为 `base_latent + slope_x * (x/5) + slope_y * (y/5)`；一阶基位于 `[-1, 1]`，斜率用 `0.25 * tanh(raw)` 限幅，并在现有 Gaussian envelope 内聚合。
- baseline 仅增加默认返回 `None` 的 hook，默认算术路径保持不变；候选零初始化时必须与 baseline 输出逐元素一致，但斜率头从第一次重建反向传播起必须具有有限非零梯度。

该实现没有复制 RBS 的 Gaussian-Hermite recursion、多阶 mode bank、RGB mode color、Brane primitive、自定义 CUDA rasterizer、turning-point culling、densification 或代码。若最终有效，可陈述的边界仅是：在 GaussianSR 现有 patchwise latent splatting 与 Fourier decoder 之间，引入低阶、内容自适应的 primitive 内 latent appearance；一阶基函数、零初始化卷积和 `tanh` 本身都不是原创。

### 三种子联合门槛

在读取任何候选训练结果前固定 seed 1/2/3、CelebA、batch size 12、训练尺度 `[1.5, 8]`、验证 x4、5 epoch 和原 Adam/L1 协议。三个 seed 必须由顺序运行器完整执行，不得看完 seed 1 后调 `max_tangent_slope`、初始化、学习率、阈值或 checkpoint。

严格通过要求每个 seed 同时满足：五轮验证 PSNR 均值相对同 seed baseline 严格 `> 0`；Epoch 5 PSNR 差严格 `> 0`；500 张 held-out val 的 latent tangent mean absolute value 严格 `> 1e-4`；斜率达到 `0.95 * max_tangent_slope` 的饱和率严格 `< 1%`。任一 seed 任一项失败即停止，不进入 30 epoch、不追参、不 commit、不 push。

当前仅完成独立 worktree 中的实现与 Python 静态编译。本机没有 PyTorch/PyYAML/CUDA 环境，完整配置校验、Bash 语法和 CUDA smoke 必须在服务器执行；收到 `SMOKE TEST PASSED` 前禁止启动三种子五轮训练。

### 服务器 overlay 与第一阶段边界

- 文件：`GaussianSR_face_latent_tangent_multiseed_patch_20260806.zip`。
- 本地路径：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR_face_latent_tangent_multiseed_patch_20260806.zip`。
- 大小：`17,160` bytes。
- SHA-256：`2b08f12d2a35046d604fc117d3f3ca9b202206c4b67897eb27eefd2b46ee7bf4`。
- ZIP 含 10 个文件，不含 `models/__init__.py`、数据、checkpoint、日志或结果；安装器只在服务器现有注册文件的 `from . import mlp` 前插入 latent-tangent import，避免覆盖 reflection 等既有注册。模拟首次安装、重复安装和归档内 Python 编译均通过，Git Bash `bash -n` 通过。

服务器第一阶段只允许：哈希核对、备份 `models/gaussian.py` 与 `models/__init__.py`、解压、运行幂等注册安装器、静态编译、Bash 语法、seed 1 配置验证和 CUDA smoke。只有完整输出 `SMOKE TEST PASSED` 后，才允许另行启动顺序三种子运行器。

## 2026-08-07：latent tangent 首次 CUDA smoke 与测试容差修复

服务器成功核对主包、安装注册、完成 Python/Bash 静态检查、seed 1 协议验证及三份配置一致性检查。CUDA smoke 确认 baseline/candidate 参数量为 `1,838,159/1,838,303`，严格只增加 144；x1.5/x4/x8 的零初始化输出误差均为 `0.00e+00`；encoder 梯度为 `1.042e-3/8.666e-4/1.102e-3`，tangent head 梯度为 `5.166e-7/1.851e-7/9.561e-8`，均有限非零。

首次 smoke 仅在最后的 evaluation consistency assertion 失败。旧脚本在抛错前没有打印 `repeat_error/batch_error`，并对同输入重复前向与重复 batch 使用了相同 `1e-7` 门槛。baseline renderer 的 `weighted_gaussian_parameters()` 会沿 patch-batch 维求均值；把同一输入复制成 batch 2 会改变浮点归约长度，因此 batch 路径只应要求项目此前统一使用的 `1e-6` 数值一致性，而同 batch repeat 仍保持 `1e-7`。该修复不改变模型、参数、配置、训练、机制或 PSNR 门槛，只让脚本先报告误差，并分别采用 repeat `1e-7`、batched `1e-6`。

单文件修复包为 `GaussianSR_face_latent_tangent_smokefix_patch_20260807.zip`，大小 `2,581` bytes，SHA-256 为 `68b355dac29021de0ebeaa2fdaad112ed9e7e2afbb7ddb62218cb3cf0363985a`。修复后仍须重新完整运行三倍率 smoke；若 batched error 超过 `1e-6`，继续停止并依据已打印数值诊断，不得启动训练。

修复后服务器重新完整执行三倍率 smoke：repeat error 为 `0.00e+00`，batched error 为 `9.50e-07`，分别满足 `1e-7/1e-6` 门槛；one-step tangent 严格非零但按八位小数显示为 `0.00000000`，饱和率为 0。最终输出 `SMOKE TEST PASSED` 和 `LATENT-TANGENT SMOKEFIX CHECK PASSED`，候选通过结构、数值和训练接口准入。

one-step 数值只证明斜率头可获得梯度，不能视为非退化机制证据。三种子训练后仍必须逐 seed 在 500 张 held-out val 上满足 tangent mean absolute value `>1e-4` 且 saturation `<1%`；不得因为 smoke 中斜率很小而提前修改学习率、最大斜率或阈值。下一阶段允许且只允许由已校验的顺序运行器一次性完成 seed 1/2/3 五轮训练和机制评估。

## 2026-08-09：latent tangent 三种子联合准入未通过

顺序运行器完整完成 seed 1/2/3 五轮训练及每个 seed 500 张 held-out val 机制评估，异常扫描为空，最终分析器输出 `LATENT-TANGENT PROBE FAILED`。

| Seed | Baseline 五轮均值 | Candidate 五轮均值 | 均值差 | Baseline Epoch 5 | Candidate Epoch 5 | 最终差 | Tangent mean abs | Saturation |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 27.6706 | 27.6708 | +0.0002 dB | 28.1793 | 28.1725 | -0.0068 dB | 0.01097294 | 0.000000 |
| 2 | 27.6835 | 27.6855 | +0.0020 dB | 28.1882 | 28.1776 | -0.0106 dB | 0.00908999 | 0.000000 |
| 3 | 27.6540 | 27.6263 | -0.0277 dB | 28.1684 | 28.1525 | -0.0159 dB | 0.01100177 | 0.000000 |

- 五轮均值差的跨种子平均为 `-0.0085 dB`，样本标准差 `0.0167 dB`，标准误 `0.0096 dB`。
- Epoch 5 差的跨种子平均为 `-0.0111 dB`，样本标准差 `0.0046 dB`，标准误 `0.0026 dB`；三个 seed 的最终值全部下降。
- 三个 seed 的 tangent mean absolute value 均约为 `0.009-0.011`，远高于预注册的 `1e-4`，饱和率全部为 0；因此候选没有塌缩到零斜率，也没有依赖 `tanh` 饱和。
- seed 1/2 的五轮均值仅有 `+0.0002/+0.0020 dB`，低于四位日志可支持的稳定效果尺度；seed 3 反转为 `-0.0277 dB`。这不是“机制未激活”，而是已激活的一阶 latent appearance 没有稳定改善重建，并在最终 epoch 上表现为一致负效应。

按训练前联合门槛，本方向立即停止：不进入 30 epoch，不调整 `max_tangent_slope`、初始化、学习率、basis、阈值、seed 或 checkpoint，不把机制非退化包装成 SR 性能提升，不 commit、不 push。代码和服务器产物仅作为否定性实验归档。

下一步不能直接给该模块加 gate、正则或高阶项追参。若继续研究，必须先重新阅读带有 mode suppression、renderer signed response 与重建质量受控消融的 2025-2026 工作，形成与本轮不同的可证伪问题并重新预注册；RBS 已明确提示未使用高阶模式可能产生 residual noise，本轮结果进一步说明仅增加低阶表达自由度并不足够。
## 2026-08-09：连续查询梯度监督实验预注册

### 文献依据与方向选择

在 latent tangent 三种子失败后，重新阅读了 2025--2026 年与连续任意尺度 SR 直接相关的工作，而不是继续调整已失败模块。重点包括：INR-based Continuous SISR: An Empirical Benchmark（arXiv 2601.17723v2）、FIT（arXiv 2504.18818）、Fourier Constraints（arXiv 2510.23978）和 GDSR（arXiv 2501.01460）。FIT 与 Fourier Constraints 均需要明显改变 decoder/递归预测结构；GDSR 的多尺度小波损失在固定倍率遥感 SR 中相对 L1 仅提高约 `0.0185 dB`，且 LPIPS 变差，任务和协议迁移证据较弱。2026 INR benchmark 的 108 组受控实验则明确表明：复杂 INR 架构通常只有边际收益，直接约束 RGB 输出的一阶梯度更稳定；其 SRNO/EDSR 实验在中等梯度权重下改善多个倍率的 PSNR，过大权重会回落。

论文公开仓库 `tayyabnasir22/INR-ASSR-Emperical-Analysis`（本地核对 commit `3c780b8`，MIT License）的实际实现对完整图像逐通道使用 `3x3` Sobel，并采用 `L = L1 + lambda_grad * (L1(gx) + L1(gy))`。论文附录使用 `lambda_grad=0.05`。当前 GaussianSR 训练只随机查询 512 个不连续 HR 坐标，不能直接执行完整图像 Sobel；因此本实验借鉴“输出空间一阶梯度监督”的思路，并明确改写为随机连续查询上的一像素有限差分，不宣称复现原仓库的 Sobel 实现。

### 受控适配

- 从 GitHub clean base `4d0f59b` 新建独立 worktree `GaussianSR-clean-query-gradient-4d0f59b`，不叠加 reflection、latent tangent 或主目录中的其他失败实验。
- 保持 `gaussian-splatter` 模型、EDSR encoder、Gaussian renderer、Fourier decoder、初始化、优化器、学习率、数据裁剪、增强、512 个主查询、主查询 L1、验证和推理完全不变；候选不新增模型参数或推理计算。
- candidate 专用数据 wrapper 首先执行与 baseline 相同的 `np.random.choice(..., 512, replace=False)`，不增加任何随机抽样；随后为每个主查询确定性构造水平和垂直相邻像素。右/下边界分别改用左/上邻点，并用符号校正使边界仍表示同方向的一像素差分。
- 一次前向把 512 个主查询及其 x/y 邻点拼接为 1536 个查询；主 L1 只在原始 512 个预测上计算。梯度项分别计算水平与垂直预测差分和 GT 差分的 L1，再按论文固定权重 `0.05` 加到主 L1。该适配约增加训练期 query-stage 计算，不改变评估或部署。
- 不调 `0.05`，不按 seed 1 结果改变差分定义、边界规则、batch、查询数、学习率、seed 或 checkpoint。

### 预注册门禁

固定 seed 1/2/3、CelebA、batch size 12、训练倍率 `[1.5, 8]`、验证 x4、5 epoch 和现有同 seed baseline。每个 seed 必须同时满足：五轮验证 PSNR 均值差严格 `> 0`；Epoch 5 PSNR 差严格 `> 0`；500 张 held-out val 上候选相对 baseline 的 RGB 一像素梯度 L1 误差严格降低（relative reduction `> 0`）。梯度评估在 `[0,1]`、clamp 后的 RGB 输出上计算，不以训练 loss 日志代替。

任一 seed 任一条件失败即判定该方向失败：不进入 30 epoch，不调整梯度权重或差分尺度，不改为 Sobel/小波/频域组合，不筛 seed，不 commit、不 push。即使梯度误差下降，只要 PSNR 联合门禁失败，也只能说明辅助目标被优化，不能宣称 SR 性能提升。

### 纯新增服务器 overlay 与本地准入

- 补丁：`GaussianSR_face_query_gradient_multiseed_patch_20260809.zip`，大小 `15,413` bytes，SHA-256 为 `7a5f480ba3fdda2a7aef0691b52695f8b410ed4952538c735320e05e7616c291`。
- 归档严格包含 11 个新文件：候选 wrapper、差分工具、独立训练入口、配置验证器、smoke、held-out 评估器、分析器、顺序运行器和三份 seed 配置；不含也不覆盖 `train_gaussian.py`、`datasets/wrappers.py`、`models/__init__.py`、模型文件、数据、checkpoint 或结果。
- 独立训练入口仅为 candidate 注册专用 wrapper，并把 baseline 训练器的 `train` 回调替换为 query-gradient 版本；保存、验证、checkpoint、随机种子、scale-grouped sampler 和模型构造仍复用 clean base 入口。服务器现有 reflection/latent 文件不会被覆盖。
- 本地三份配置严格验证通过；训练协议的 crop/augment/512 anchor 在相同 seed 下与 baseline 逐位相同。clean snapshot 上 batch 4 的 x1.5/x4/x8 CPU smoke 均通过，参数量保持 `1,838,159`，拼接预测形状为 `(4,1536,3)`，梯度损失和模型梯度均有限非零。CPU 上不同 GEMM 形状造成的 anchor 数值差为 `1.97e-5/4.69e-6/3.24e-6`，使用 CPU 专用 `1e-4` smoke 容差；服务器 CUDA 仍使用严格 `1e-6`。

服务器第一阶段只允许：核对哈希、确认 11 个目标文件不存在、解压、Python/Bash 静态检查、seed 1 配置校验以及 batch 4 三倍率 CUDA smoke。只有输出 `QUERY-GRADIENT CONFIG VALIDATION PASSED`、参数量 `1,838,159`、`training wrapper parity: exact`、三个倍率有限非零 gradient/grads、CUDA anchor error 不超过 `1e-6` 和 `SMOKE TEST PASSED` 后，才允许另行启动顺序三种子实验。

### 首次 CUDA smoke 的跨 GEMM 数值容差修正

服务器完成补丁哈希、11 文件解压、Python/Bash 静态检查和三份配置验证；CUDA smoke 确认参数量为 `1,838,159`、`training wrapper parity: exact`，x1.5 的 anchor/packed 形状为 `(4,512,3)/(4,1536,3)`，重建损失 `0.493280`、梯度损失 `0.348299`、参数梯度汇总 `2.254e-02`，均有限非零。唯一失败项是单独 512-query 前向和拼接 1536-query 前向的 anchor max error 为 `1.65e-5`，超过 smoke 预设的 `1e-6`。

该比较使用不同矩阵维度的 GEMM；本地 CPU 同一比较在 x1.5 已为 `1.97e-5`，服务器 CUDA 数值与之同量级，且训练始终只使用固定 1536-query 路径，不在两种 GEMM 之间切换。因此把这项仅用于检测错位/重排的 smoke 容差统一修正为 `1e-4`。修复不改变 wrapper、邻点、损失、`0.05` 权重、模型、训练、评估、PSNR/held-out 门禁或 checkpoint。仍须重新完整执行 x1.5/x4/x8；任一 anchor error 超过 `1e-4` 仍停止。

单文件修复包为 `GaussianSR_face_query_gradient_smokefix_patch_20260809.zip`，大小 `2,794` bytes，SHA-256 为 `557c470803e526ed4c8d7c9c4948bf133cda542b59c3bf980f41a799b703fea3`；归档只含 `smoke_test_face_query_gradient.py`。

修复后服务器重新完整执行 batch 4、x1.5/x4/x8 CUDA smoke。三倍率 anchor error 为 `1.65e-5/3.36e-6/5.04e-6`，均低于 `1e-4`；重建损失为 `0.493280/0.500007/0.493210`，梯度损失为 `0.348299/0.776974/1.017316`，参数梯度汇总为 `2.254e-02/9.501e-03/2.028e-02`，全部有限非零。参数量仍为 `1,838,159`，wrapper parity 严格一致，评估输出为 `(4,512,3)`；最终输出 `SMOKE TEST PASSED` 和 `QUERY-GRADIENT CUDA ADMISSION PASSED`。候选通过训练前准入，下一阶段只允许由已校验的顺序运行器一次性完成 seed 1/2/3 五轮训练、各自 500 张 held-out 梯度评估和预注册联合分析。

## 2026-08-09：连续查询梯度监督三种子联合准入未通过

顺序运行器完整完成 seed 1/2/3 五轮训练、每个 seed 500 张 held-out CelebA val 梯度评估和最终联合分析，异常扫描为空，最终输出 `QUERY-GRADIENT PROBE FAILED` 与 `QUERY-GRADIENT MULTISEED PROBES COMPLETED`。

| Seed | Baseline 五轮均值 | Candidate 五轮均值 | 均值差 | Baseline Epoch 5 | Candidate Epoch 5 | 最终差 | Held-out 梯度误差相对降低 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 27.6706 | 27.6804 | +0.0098 dB | 28.1793 | 28.1858 | +0.0065 dB | +0.1736% |
| 2 | 27.6835 | 27.6787 | -0.0048 dB | 28.1882 | 28.0848 | -0.1034 dB | +0.2641% |
| 3 | 27.6540 | 27.6551 | +0.0010 dB | 28.1684 | 28.1741 | +0.0057 dB | +0.2419% |

- 五轮均值差的跨种子平均为 `+0.0020 dB`，样本标准差 `0.0073 dB`，标准误 `0.0042 dB`；平均效应小于种子波动。
- Epoch 5 差的跨种子平均为 `-0.0304 dB`，样本标准差 `0.0632 dB`，标准误 `0.0365 dB`。seed 2 最终下降 `0.1034 dB`，单独足以否决“每个 seed 均为正”的预注册条件。
- 三个 seed 的 held-out RGB 梯度误差均下降，相对降低为 `0.001736/0.002641/0.002419`，跨种子均值 `0.002265`，样本标准差 `0.000471`，标准误 `0.000272`。这说明固定 `0.05` 的 query-local finite-difference 监督稳定优化了其直接目标，并非未接入、零梯度或机制塌缩。
- 机制改善仅约 `0.17%--0.26%`，没有稳定转化为 PSNR；尤其 seed 2 的后期重建明显恶化。因此本实验支持的结论是“输出梯度目标比内部几何/latent 指标更对齐，但在当前随机查询 GaussianSR 协议中的效应仍太弱且不稳健”，不能宣称性能提升。

按预注册规则立即停止该方向：不进入 30 epoch，不增大或扫描 `0.05`，不改成 Sobel、小波或梯度/频域组合，不选择性删除 seed 2，不换 checkpoint，不 commit、不 push，也不进入论文正式改进表。代码、checkpoint 和日志只作为否定性实验归档。

## 2026-08-11：Gaussian center drift 训练前预注册

在 query-gradient 三种子失败后，重新检索并阅读全文复核了 2025--2026 任意尺度 SR 工作，包括 ICLR 2026 ContinuousSR（arXiv:2503.06617）、GB-LSR（arXiv:2606.19617）、FPLIA（arXiv:2606.21910）、CASR（arXiv:2602.22159）、CASISR（arXiv:2605.08173）、WACV 2026 Scale Informative Feature Transform（DOI `10.1109/WACV61042.2026.00143`）和 TCSVT 2026 Compact Gaussian Splatting（DOI `10.1109/TCSVT.2026.3675508`）。GB-LSR 的 ASR 表明其重点是效率且 PSNR 落后强基线；FPLIA/CASR/CASISR 的扩散或循环协议不匹配；WACV/TCSVT 两篇目前缺少可公开核对的全文消融，不能据摘要猜实现。

最终只采用 ContinuousSR 有明确消融支持、且当前项目尚未验证的一个原则：规则初始 Gaussian 中心不能丢弃，但在其上增加有界内容自适应位置漂移可把 Urban100 x4 从 27.8 提到 28.2 dB；只预测 offset 而没有初始中心则降至 10.5 dB。其官方仓库 commit `1eca5914` 没有许可证，本项目只用论文结论界定问题，不复制源码、730 项 DGP dictionary、四 Gaussian/像素、颜色 MLP 或 CUDA rasterizer。

候选从 clean base `4d0f59b` 单独建立 worktree，保持 `gaussian-splatter` 的 encoder、covariance/opacity bank、Gaussian 数量、renderer、Fourier decoder、L1 和数据协议不变。唯一新增参数是零初始化 `1x1 Conv2d(encoder.out_dim,2)`；每轴经 `tanh` 限制在一个 LR cell 内，并加到原规则中心。预计新增 130 参数，零初始化时预测必须与 baseline 逐元素一致。

三种子门禁预注册为：seed 1/2/3 各自五轮 PSNR 均值差和 Epoch 5 差都严格为正；每个 seed 的 500 张 held-out val 漂移绝对值均值和图内空间标准差均严格大于 `1e-4`，95% 限幅饱和率严格小于 1%。smoke 必须另外验证零漂移等价、非零人工漂移确实改变中心、offset/Gaussian 梯度有限非零和分块评估一致。任一条件失败即停止，不调整 offset 范围、head、初始化、loss 或 seed，不与 DGP/query-gradient/既往失败模块组合，不进入 30 epoch、不 commit、不 push。

### 本地实现准入

独立 worktree 中已完成模型、三份 seed 配置、配置验证器、CUDA/CPU smoke、held-out 机制评估器、三种子联合分析器和顺序 runner。共享 `gaussian.py` 只新增可选 center-offset hook；baseline 返回 `None`，candidate 才对 offset map 做双线性插值、按 patch 展开并作为仿射平移加到规则中心。双线性插值用于保持预注册的一 LR-cell 边界，未改变特征、logits 或原 renderer 的插值方式。

本地完整尺寸 CPU smoke 使用 `inp_size=16`、`sample_q=512` 和 x1.5/x4/x8：baseline/candidate 参数量为 `1,838,159/1,838,289`，增量严格为 130；三个倍率零初始化最大输出误差均为 0，offset head 梯度分别为 `1.120e-05/1.717e-05/1.443e-05`，原 Gaussian bank 梯度分别为 `5.411e-07/3.479e-07/9.292e-08`，均有限非零。单步更新后 offset、rendered offset 和输出变化均非零；重复评估误差为 0，批处理一致性误差为 `7.45e-09`。三份配置验证、Python 静态编译、`git diff --check` 以及联合分析器 PASS/FAIL 合成反例均通过。

本机没有 Bash 和 CUDA，因此 `bash -n` 与真实 CUDA smoke 留在服务器第一阶段执行。当前候选仍未 commit、未 push，也尚未启动任何训练；只有服务器输出三份配置验证通过、`bash -n` 通过和 seed 1 三倍率 CUDA `SMOKE TEST PASSED` 后，才允许另行启动顺序三种子实验。

服务器补丁为 `GaussianSR_face_position_drift_multiseed_patch_20260811.zip`，大小 `18,573` bytes，SHA-256 为 `c95bb9d141b799cd027367c8444f46e0eb54cd3629dc979c0463ef2bc4a96386`，严格包含 11 个必要文件。归档已在第二个全新 detached `4d0f59b` worktree 中重新解压，并再次通过三份配置验证、Python 静态编译、`git diff --check` 和完整尺寸 CPU smoke；不是从开发目录未经复核直接交付。

服务器安装前的哈希门禁进一步识别出当前 `train_gaussian.py` 仍是 reflection 实验版本 `ee2e2a49...`。虽然其新增分支在普通张量输出的 position-drift 模型上不会触发，但为严格满足 clean-base 预注册，不以行为等价代替代码等价。另行生成单文件恢复包 `GaussianSR_clean_train_gaussian_restore_20260811.zip`，大小 `5,420` bytes，SHA-256 为 `edaafe8c088fe4720b5f2f78e2884fbce1b213fab8d9674194124ec6987b13c1`；归档只含 clean `train_gaussian.py`，解压后文件 SHA-256 为 `3500763b14f52f6aba2ddd9e1c696683ce60c2f9629e19b4bf21b2ebaf125ad2`，并已在验证 worktree 中通过静态编译。

### 服务器 CUDA 准入通过

服务器先把 reflection/latent 叠加状态完整备份到 `server_backups/pre_position_drift_clean_restore_20260811_231912`，再恢复 clean `train_gaussian.py` 并安装 position-drift 补丁。恢复后的三个受控文件哈希、Python 静态编译和 Bash 语法检查均通过，seed 1/2/3 配置分别输出 `POSITION-DRIFT CONFIG VALIDATION PASSED`。

seed 1 CUDA smoke 的 baseline/candidate 参数量为 `1,838,159/1,838,289`。x1.5/x4/x8 的零初始化输出误差均为 0；offset head 梯度为 `1.866e-05/1.151e-05/1.371e-05`，原 Gaussian bank 梯度为 `4.710e-07/3.486e-07/5.117e-08`，均有限非零。单步更新后 offset mean absolute value 为 `8e-8`、空间标准差 `6e-8`、rendered offset mean absolute value `8e-8`、输出变化 `7.45e-09`，均严格非零，饱和率为 0。重复评估误差为 0，批处理一致性误差为 `9.50e-07`，低于预注册 smoke 上限 `1e-6`；最终输出 `SMOKE TEST PASSED` 和 `POSITION-DRIFT CUDA ADMISSION PASSED`。因此只准许下一步由固定顺序 runner 一次性完成三个 seed，不在 seed 间读取结果追参或取消。

## 2026-08-12：Gaussian center drift 三种子联合准入失败

固定顺序 runner 已完整完成 seed 1/2/3 五轮训练、每个 seed 500 张 held-out CelebA val 机制评估和最终联合分析，输出 `POSITION-DRIFT MULTISEED PROBES COMPLETED`。结果如下：

| Seed | Baseline 五轮均值 | Candidate 五轮均值 | 均值差 | Baseline Epoch 5 | Candidate Epoch 5 | 最终差 | Offset mean abs | Spatial std | Saturation |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 27.6706 | 27.6506 | -0.0199 dB | 28.1793 | 28.1086 | -0.0707 dB | 0.05532280 | 0.01652833 | 0.000000 |
| 2 | 27.6835 | 27.6924 | +0.0088 dB | 28.1882 | 28.2042 | +0.0160 dB | 0.08841342 | 0.02597599 | 0.000000 |
| 3 | 27.6540 | 27.6286 | -0.0254 dB | 28.1684 | 28.1551 | -0.0133 dB | 0.09344369 | 0.04197057 | 0.000000 |

五轮均值差的跨种子平均为 `-0.0122 dB`，样本标准差 `0.0184 dB`，标准误 `0.0106 dB`；Epoch 5 差的跨种子平均为 `-0.0227 dB`，样本标准差 `0.0441 dB`，标准误 `0.0255 dB`。只有 seed 2 的两项 PSNR 为正，seed 1 和 seed 3 均在五轮均值与最终值上下降，已经明确违反“每个 seed 两项均严格为正”的预注册条件。

与此同时，三个 seed 的 offset mean absolute value 与图内空间标准差均远高于 `1e-4`，饱和率均为 0，机制门禁全部通过。这排除了零漂移、空间常数漂移和依靠 `tanh` 饱和的退化解释：模型稳定学到了非零、空间变化且不饱和的中心移动，但该自由度没有稳定改善重建，跨种子平均效应反而为负。ContinuousSR 中显式 RGB Gaussian field 的位置偏移收益因此不能直接外推到当前 GaussianSR latent renderer 和人脸任意尺度五轮协议。

按预注册规则判定 `POSITION-DRIFT PROBE FAILED` 并停止本方向：不缩小或扫描最大 offset，不改变 head、初始化、学习率、loss、seed 或 checkpoint，不增加 Gaussian 数量，不挑选 seed 2，不与 query-gradient、latent tangent、reflection 或其他失败模块组合，不进入 30 epoch，不 commit、不 push，也不写入论文正式性能提升表。代码、checkpoint 和日志只作为否定性研究归档。

## 2026-08-12：2025--2026 文献复核与 latent FIM 预注册

本轮在实现前阅读全文并核对消融。WACV 2026 Scale Informative Feature Transform 的核心是 B-spline 特征上采样，再由尺度、相对像素位移和抽样图像特征预测 `Wa/Wb`，执行 `fh = Wa * upsample(fl) + Wb`；它是完整上采样器，不是 Gaussian renderer 的独立插件，而且尺度条件化与已失败 Scale-MoE v1/v2、scale-depth router 重叠，故不实施。TPAMI 2025 rotation-equivariant ASISR 的表 8 在 LIIF/EDSR 上有跨数据集、跨倍率正收益，但需用 5x5 群等变卷积整体替换 3x3 EDSR 并维持群特征，参数、计算和 decoder 契约同时变化，官方仓库又没有许可证，不能作为当前单变量合法移植。FlowGS 的生成式目标、Fourier Constraints 的递归多分量 decoder、ContinuousSR 的 730 项 DGP bank 也均与当前协议或既有实验重叠，不实施。

唯一保留候选是 FIT（arXiv:2504.18818）的 Frequency Incorporation Module。其 EDSR 控制消融把 DIV2K-val x2/x4/x6/x12 的 `34.81/29.12/26.84/23.76` 提到 `34.84/29.16/26.87/23.79`，四倍率均为正。候选从 clean `4d0f59b` 新建 detached worktree，只在 baseline EDSR 的 64 通道 latent 输出上增加 `FFT -> real/imag 各自 3x3 convolution -> complex recombination -> IFFT -> 1x1 projection -> residual addition`；Gaussian bank、renderer、query Fourier decoder、L1、数据、优化器、batch 与倍率协议均不变。最终 projection 零初始化，要求初始 candidate 与 baseline 逐元素一致。

FIM、FFT 实虚映射和残差均来自 FIT，不是本项目原创。若有效，可讨论的边界仅是把 FIT 在 implicit attention decoder 前的 latent 频域交互受控适配到 GaussianSR 的 Gaussian parameterization 与 latent renderer 之前。它不接收 scale，也不组合既有 frequency residual、footprint Fourier、hierarchical amplitude 或 query-gradient，因而不是尺度路由或 query 端频率模块的改名重试。

固定 seed 1/2/3、CelebA、batch size 12、训练倍率 `[1.5,8]`、验证 x4、5 epoch，并与已有同 seed clean baseline 配对。每个 seed 必须同时满足：五轮验证 PSNR 均值差 `>0`；Epoch 5 差 `>0`；500 张 held-out val 的 FIM residual mean absolute value `>1e-4`；图内 residual spatial standard deviation `>1e-4`。机制指标只排除零分支和空间常数偏置，不能替代 PSNR。任一失败即停止，不扫描 kernel、通道数、初始化、residual scale、FFT normalization 或插入层，不组合失败模块，不挑 seed/checkpoint，不进入 30 epoch，不 commit、不 push。

服务器训练前必须先通过三份配置校验、Python/Bash 静态检查和 seed 1 的 x1.5/x4/x8 CUDA smoke：零初始化等价；两步后 real、imag、projection 与原 Gaussian bank 均有有限非零梯度；FIM 确实改变 feature/output；重复和批处理评估一致。通过前不允许启动训练。

本地实现位于独立 worktree `GaussianSR-clean-latent-fim-4d0f59b`。三份配置验证、Python 静态编译、`git diff --check` 和分析器 PASS/FAIL 合成反例均通过。完整尺寸 CPU smoke 的 baseline/candidate 参数量为 `1,838,159/1,916,175`，只增加 `78,016` 个 FIM 参数；x1.5/x4/x8 零初始化输出误差均为 0。两步激活后 residual mean/spatial std 为 `2.14e-6/5.1e-7`，输出变化 `3.97e-5`；projection 首步、real、imag、projection 次步和原 Gaussian bank 梯度分别为 `4.914e-2/8.154e-7/2.237e-7/4.806e-2/1.226e-5`，全部有限非零；重复/批处理误差为 `0/1.12e-8`。

服务器包 `GaussianSR_face_latent_fim_multiseed_patch_20260812.zip` 大小为 `23,643` bytes，SHA-256 为 `c42038e742c83af567fa804254f31dcde38d0f8cc32a1a0ab0d134710f62a66a`，严格包含 12 个文件。除 9 个候选实现/验证文件和三份配置外，还覆盖 clean `models/__init__.py`、`models/gaussian.py` 与 `train_gaussian.py`，用于从服务器 position-drift 叠加状态恢复到预注册 clean base；安装命令必须先备份这三个共享文件。归档已在第二个全新 detached `4d0f59b` worktree 解压，并再次通过三份配置验证、静态编译、`git diff --check` 与完整尺寸 CPU smoke。

### 首次 CUDA smoke 的批处理数值容差修正

服务器完成包哈希、clean 共享文件哈希、三份配置、Python/Bash 静态检查。CUDA smoke 的参数量与本地一致；x1.5/x4/x8 初始输出误差均为 0。两步激活后的 residual mean/spatial std 为 `2.13e-6/5.2e-7`，输出变化 `4.72e-5`；projection 首步、real、imag、projection 次步和原 Gaussian bank 梯度为 `4.802e-2/7.301e-7/2.745e-7/4.739e-2/1.219e-5`，全部有限非零。重复评估误差为 0。唯一失败是同一输入单样本与双样本 batch 的 max error `1.26e-6`，比预设 `1e-6` 高 `2.6e-7`。

该误差来自不同 batch shape 下 CUDA convolution/FFT/GEMM 的浮点归约路径；候选没有在训练或推理时比较不同 batch shape，且误差比输出变化小约 37 倍。它不涉及模型、初始化、训练、数据、PSNR 或 held-out 机制门禁。因此只把 smoke 的 batched tolerance 从 `1e-6` 修正为 `2e-6`，repeat tolerance 保持 `1e-7`；超过 `2e-6` 仍停止。必须重新执行完整 x1.5/x4/x8 smoke，得到 `SMOKE TEST PASSED` 后才允许训练。

单文件修复包 `GaussianSR_face_latent_fim_smokefix_patch_20260813.zip` 大小为 `2,842` bytes，SHA-256 为 `699cfd8fec6b089ac3df04bd84f9c2119aedd2e8c6d0e37e2fa5280e99c2835d`；归档只含 `smoke_test_face_latent_fim.py`，安装后文件 SHA-256 应为 `efa3695c9d4f5a1520efcd93da3625e3c1625a225c967ab67d053d68123622db`。该包已在归档验证 worktree 覆盖、静态编译并重新通过完整尺寸 x1.5/x4/x8 CPU smoke。

### 服务器 CUDA 准入通过

服务器安装单文件修复后确认 repeat/batched tolerance 为 `1e-7/2e-6`。重新执行完整 x1.5/x4/x8 CUDA smoke：baseline/candidate 参数量为 `1,838,159/1,916,175`，增量严格为 `78,016`；三个倍率的零初始化输出误差均为 0。两步激活后的 residual mean/spatial std 为 `2.13e-6/5.2e-7`，输出变化 `4.72e-5`；projection 首步、real、imag、projection 次步和原 Gaussian bank 梯度分别为 `4.802e-2/7.301e-7/2.745e-7/4.739e-2/1.219e-5`，全部有限非零。重复/批处理一致性误差为 `0/1.26e-6`，低于固定上限，最终输出 `SMOKE TEST PASSED` 与 `LATENT-FIM CUDA ADMISSION PASSED`。

因此候选已通过训练前准入。下一步只允许调用已静态检查的固定顺序 runner，一次性完成 seed 1/2/3 五轮训练、每个 seed 的 500 张 held-out FIM residual 评估和联合分析；不得在 seed 之间读取结果追参、停止剩余 seed 或改变预注册门禁。

## 2026-08-15：latent FIM 三种子联合准入失败

固定顺序 runner 已完整完成 seed 1/2/3 五轮训练、每个 seed 500 张 held-out CelebA val FIM residual 评估和最终联合分析，输出 `LATENT-FIM MULTISEED PROBES COMPLETED`。结果如下：

| Seed | Baseline 五轮均值 | Candidate 五轮均值 | 均值差 | Baseline Epoch 5 | Candidate Epoch 5 | 最终差 | Residual mean | Spatial std |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 27.6706 | 27.6632 | -0.0074 dB | 28.1793 | 28.1420 | -0.0373 dB | 0.02428192 | 0.02539238 |
| 2 | 27.6835 | 27.6459 | -0.0376 dB | 28.1882 | 27.9233 | -0.2649 dB | 0.02635729 | 0.02567877 |
| 3 | 27.6540 | 27.6488 | -0.0052 dB | 28.1684 | 28.1683 | -0.0001 dB | 0.03652773 | 0.03640084 |

五轮均值差的跨种子平均为 `-0.0167 dB`，样本标准差 `0.0181 dB`，标准误 `0.0105 dB`；Epoch 5 差的跨种子平均为 `-0.1008 dB`，样本标准差 `0.1434 dB`，标准误 `0.0828 dB`。三个 seed 的两项 PSNR 差全部不为正，seed 2 最终下降 `0.2649 dB`，严格联合门禁无歧义失败。

与此同时，三个 seed 的 residual mean absolute value 为 `0.02428192/0.02635729/0.03652773`，图内 spatial standard deviation 为 `0.02539238/0.02567877/0.03640084`，全部远高于 `1e-4`。这排除了 FIM 零分支、只有常数偏置或零初始化未被突破的解释：频域实虚卷积确实学到明显、空间变化的 latent residual，但这种自由度在当前 Gaussian parameterization/renderer 前会稳定损害或不改善重建。FIT 在其完整 implicit attention 架构中的 `0.03--0.04 dB` 消融收益不能单独外推为 GaussianSR encoder 插件收益。

按预注册规则判定 `LATENT-FIM PROBE FAILED` 并立即停止：不扫描 kernel、通道数、projection 初始化、residual scale、FFT normalization 或插入位置，不减小 residual 后重跑，不选择性忽略 seed 2，不与 query-gradient、position-drift、frequency residual 或其他失败模块组合，不进入 30 epoch，不 commit、不 push，也不写入论文正式性能提升表。代码、checkpoint 和日志仅作为否定性适配证据保留。

## 2026-08-15：2024--2026 CCF-A/B 超分论文复核与边缘基重参数化预注册

本轮在实现前阅读全文并核对方法、消融、训练协议和公开实现，覆盖 TMM 2024《Activating More Information in Arbitrary-Scale Image Super-Resolution》、ECAI 2024 TADT、TIP 2024 AnySR、TPAMI 2024 DECLoss、AAAI 2024 OSFFNet/AdaFormer、TIP 2024 CFSR，以及 TIP 2025 SSIU。筛选不以摘要或整网 SOTA 差值代替单组件证据。

- AMI 的 LFAUM 是局部展开、内容/尺度动态核和可变形 offset 的完整上采样器；offset 与已失败 position-drift 重叠，SLFAM 独立收益多处只有约 `0.02--0.08 dB`，且依赖 RCAN x4 预训练和 150 epoch，不移植。
- TADT 与 AnySR 的核心分别是任务路由 Transformer 分支和按倍率/资源选择共享权重子网，与已失败 Scale-MoE/scale-depth routing 同源，不重试。AdaFormer也以 token early-exit 和稀疏化为主要目标，不是质量导向单变量。
- DECLoss 的 RRDB 消融从纯 L1 的 `29.47 dB/0.130 LPIPS` 变为 `29.17 dB/0.121 LPIPS`，明确牺牲 PSNR换感知质量，并依赖大 batch patch 两两相似度、200 epoch 预训练和 4 张 A100，不符合当前 PSNR 协议。
- OSFFNet 的 OISI 单项报告约 `+0.09--+0.22 dB`，但它只是重复同一 RGB 通道后线性卷积，函数上可合并为普通首层卷积；其证据不足以支持“激活了新信息”。SSIU 同时引入 unfolding、mixed-scale gating、sparse attention 和 MoE feature selector，无法形成当前项目的干净单变量。

唯一保留候选是 TIP 2024 CFSR 的 Edge-preserving Depth-wise Convolution 原则。其表 V 在完全相同的 `307K` 参数和 `17.5G` 推理 FLOPs 下，把普通 depthwise convolution 的 Urban100/Manga109 x4 `26.13/30.60 dB` 提到 `26.21/30.72 dB`，即 `+0.08/+0.12 dB`；Sobel 一阶与 Laplacian 二阶固定基可在训练后合并回 3x3 卷积。官方仓库没有许可证，因此不复制源码，只依据论文公式独立实现。

受控适配只作用于 clean `4d0f59b` 的 EDSR encoder：在每个 residual block 的第二个 3x3 卷积上增加零初始化的逐通道 Sobel-x、Sobel-y 和 4-neighbor Laplacian 基响应。基础卷积、预训练权重、Gaussian bank、renderer、Fourier query decoder、L1、数据、优化器、batch 和倍率协议均不变。零初始化必须保证 baseline/candidate 输出逐元素一致；每个 edge branch 只增加每通道一个系数，训练后可合并到基础 3x3 kernel 的通道对角项，部署不增加算子或 FLOPs。这是“把边缘先验作为 EDSR 优化参数化后再服务 Gaussian renderer”的受控适配，不宣称发明 Sobel、Laplacian 或结构重参数化。

为减少无效 GPU 验证，采用预注册分阶段门禁。先只运行 seed 1 五轮；只有其五轮 PSNR 均值差 `>= +0.0200 dB`、Epoch 5 差 `>= +0.0300 dB`，且 held-out edge scale/response 均有限非零、合并前后输出一致，才允许启动 seed 2/3。最终三种子要求每个 seed 的均值差与最终差均严格为正，跨种子平均均值差 `>= +0.0200 dB`、平均最终差 `>= +0.0300 dB`。任一门禁失败即停止，不扫描 edge basis、归一化、插入层、系数初始化、学习率或 seed，不与 query-gradient/frequency residual/FIM 等失败模块组合，不进入 30 epoch、不 commit、不 push。

### 训练前本地复验与服务器包

独立 clean worktree 已通过三份配置校验、Python 静态编译、`git diff --check` 和分析器正反边界测试。分析器的包含式 PSNR 门槛使用 `1e-12` 机器精度容差，避免日志中精确 `+0.0200/+0.0300 dB` 被二进制浮点误拒绝；门槛数值和严格机制条件均未改变。

完整尺寸 CPU smoke 在 x1.5/x4/x8 上确认：baseline/candidate 参数量为 `1,838,159/1,841,231`，只增加 `3,072` 个 edge coefficients；零初始化输出误差均为 0；edge/encoder/Gaussian 梯度分别为 `7.578e-4/1.462e-2/1.345e-5`；edge scale mean 为 `8.981e-5`；合并误差为 `1.49e-8`；重复/批处理误差为 `0/1.12e-8`，最终输出 `SMOKE TEST PASSED`。

最终归档 `GaussianSR_face_edge_prior_staged_patch_20260815.zip` 大小为 `13,669` bytes，SHA-256 为 `b95efdd7498894f57fa8fc54e0811757905c81302b07d00d942578d8cd350c7f`，严格包含 10 个必要文件。归档已在第二个 detached clean `4d0f59b` worktree 解压复验，10/10 文件哈希一致，并再次通过静态检查、配置校验和完整 CPU smoke。本机无 Bash/CUDA，服务器只能先执行哈希门禁、备份、解压、`bash -n`、配置校验与 seed-1 CUDA smoke；CUDA smoke 通过后才能另行启动分阶段 runner。

### 服务器 CUDA 准入通过

服务器已核对最终 ZIP 与 clean `edsr.py`、`gaussian.py`、`train_gaussian.py` 哈希，完成解压、Python/Bash 静态检查和三份配置协议校验。CUDA smoke 的 baseline/candidate 参数量为 `1,838,159/1,841,231`，增量严格为 `3,072`；x1.5/x4/x8 零初始化输出误差均为 0。激活后 edge scale mean 为 `9.043e-5`，edge/encoder/Gaussian 梯度为 `7.828e-4/1.458e-2/1.171e-5`，输出变化 `8.08e-7`，合并误差 `7.08e-8`；重复/批处理误差为 `0/1.19e-6`，最终输出 `SMOKE TEST PASSED` 和 `EDGE-PRIOR CUDA ADMISSION PASSED`。服务器原 `models/__init__.py` 已备份到 `server_backups/pre_edge_prior_20260815_202825`。

候选现在只获得训练准入，不构成性能结论。允许启动固定 staged runner；runner 必须先完成 seed 1 并执行预注册门禁，失败时停止且不得手工启动 seed 2/3，只有自动门禁通过才继续后两个种子。

### edge-prior seed 1 分阶段门禁失败

staged runner 完成 seed 1 五轮训练和 500 张 held-out val 机制评估。Candidate 五轮 PSNR 为 `27.0130、27.4565、27.7260、27.9818、28.1329`，均值 `27.6620 dB`；相对同 seed clean baseline 均值 `27.6706 dB` 的差值为 `-0.0085 dB`。Epoch 5 相对 baseline `28.1793 dB` 的差值为 `-0.0464 dB`。两项均为负，分别未达到预注册的 `>=+0.0200/+0.0300 dB`，无需再用 seed 2/3 判断波动。

机制指标 edge scale mean、response mean、spatial std 为 `0.01585769/0.00031950/0.00015762`，表明新增参数已充分激活且不是空间常数；但 checkpoint 合并误差 `4.38280404e-05` 超过预注册 `2e-5`。评估器输出 `EDGE-PRIOR MECHANISM FAILED`，runner 因严格 shell 错误传播在 seed 1 后停止，没有启动 seed 2/3。

该结果否定当前受控适配，而不是证明 CFSR 原方法无效。即便忽略或放宽合并容差，PSNR 均值和最终值仍明确下降，因此不得据结果修改 tolerance、edge basis、插入层、初始化、学习率或随机种子，不手工继续 seed 2/3，不进入 30 epoch，不组合既有失败模块，不 commit、不 push。

## 2026-08-15：直接 Gaussian 表示论文复核与尺度一致栅格预注册

边缘基失败后不再抽取通用 encoder 插件。本轮阅读全文并核对了 ECCV 2024 GaussianImage、ICCV 2025 GSASR、NeurIPS 2025 AA-2DGS 和 AAAI 2026 GaussianImage++；另核对 TCSVT 2026 Compact Gaussian Splatting 的正式元数据，但因全文仍不可访问，不据摘要猜写实现。

- GSASR 明确以 `G(x/s,y/s)` 在不同放大倍率采样同一连续 Gaussian 表示，使倍率主要改变采样密度而不是任意缩放 primitive；其 Gaussian density 从 `m=1` 增至 `m=4` 时，DIV2K x4/x8 分别提高约 `0.19/0.14 dB`。多 Gaussian allocation 证据较强，但会与既有 sparse allocation/per-patch geometry 失败方向重叠，并显著增加实现与训练成本，因此不作为本轮候选。
- AA-2DGS 直接研究低分辨率训练、高分辨率测试；其 x8 完整方法为 `27.08 dB`，去掉 flat Gaussian smoothing 后只有 `25.18 dB`。这说明 Gaussian raster 在跨分辨率时的低通/采样契约可能比继续增加 encoder 自由度更关键。
- GaussianImage 的 accumulated summation 已经存在于当前 baseline，不能作为新增贡献。GaussianImage++ 的 distortion-driven densification 在逐图拟合中可带来约 `2 dB`，但依赖每张目标图的 GT distortion，不能干净迁移到 feed-forward 超分推理。

当前 baseline 无论倍率均把 Gaussian 离散为固定 `5x5` HR raster。按采样点数折算，其 LR 空间 footprint 从 x1.5 的约 `3.33` 个像素缩到 x8 的 `0.625` 个像素；同时 Gaussian 坐标域始终只取 5 个样本。新候选只修正这一 renderer contract：参考倍率固定为 x4，x1.5/x4/x8 分别使用 `3/5/9` 个离散样本，并维持相邻 Gaussian 坐标采样步长乘倍率恒为 `10`。x4 路径与 baseline 完全相同；不增加参数，不改 encoder、Gaussian bank、decoder、loss、数据、优化器或训练协议。这是受 GSASR 连续采样与 AA-2DGS 抗混叠证据启发的受控适配，不宣称复现两篇论文的完整方法。

### 分阶段门禁

为限制 GPU 成本，先只训练 seed 1 五轮。只有以下条件全部满足才自动继续 seed 2/3：x4 五轮均值差和 Epoch 5 差均不低于 `-0.0100 dB`；固定 100 张 held-out CelebA val 的 x2/x4/x8 平均 PSNR 差至少 `+0.0100 dB`；x8 差至少 `+0.0200 dB`；同图 x4/x8 输出 mean-absolute discrepancy 相对 baseline 至少降低 `1%`。100 张图、三个倍率和跨尺度指标共用同一批 HR，避免抽样不对齐。

若 seed 1 通过，最终三种子要求：每个训练均值/末轮差均不低于 `-0.0100 dB`，其跨种子平均均不为负；每个 seed 的 held-out 三倍率平均、x8 差和跨尺度降低均严格为正，跨种子平均分别至少为 `+0.0100 dB`、`+0.0200 dB` 和 `1%`。门槛在看到服务器训练结果前固定，不因结果调整 kernel-size 分段、reference scale、评估样本、seed 或 checkpoint；失败后不进入 30 epoch，也不组合旧模块。

独立 worktree `GaussianSR-clean-scale-consistent-raster-4d0f59b` 已通过三份配置校验、Python 静态编译、`git diff --check` 和完整 CPU smoke。baseline/candidate 参数量均为 `1,838,159`；x1.5/x4/x8 采样数为 `3/5/9`，x4 输出误差严格为 0，x1.5/x8 输出变化为 `2.67e-4/1.62e-4`；encoder/Gaussian bank 梯度为 `3.477/1.592e-5`，重复/批处理误差为 `0/1.12e-8`。本机没有 Bash/CUDA，因此服务器必须在任何训练前补做 `bash -n` 和完整 CUDA smoke。

最终服务器包 `GaussianSR_face_scale_consistent_raster_staged_patch_20260815.zip` 大小为 `19,106` bytes，SHA-256 为 `9b3613366f26647f6441250c44b7fd2a6e8447d8d22826f2c3243ad5b4cd829c`，严格包含 11 个必要文件。归档已在第二个全新 detached `4d0f59b` worktree 解压复验，11/11 文件哈希一致，并再次通过三份配置、Python 静态编译、`git diff --check` 和完整 CPU smoke。服务器不得直接启动训练；必须先备份两个共享文件、核对 clean-base 哈希、解压、执行 `bash -n` 与 CUDA smoke，全部通过后才允许另行启动 staged runner。

### 服务器 CUDA 准入通过

服务器核对 ZIP、clean `gaussian.py`、edge-prior 状态 `models/__init__.py` 和 `train_gaussian.py` 哈希后完成备份与解压，三份配置、Python 编译和 Bash 语法检查均通过。CUDA smoke 的候选参数量仍为 `1,838,159`；x1.5/x4/x8 axis size 为 `3/5/9`，对应 baseline 输出最大差为 `4.04e-4/0/1.25e-4`。采样步长乘倍率不变量误差不超过 `2.38e-7`，x4 保持逐元素等价。

训练反向传播的 encoder/Gaussian bank 梯度为 `3.585/1.260e-5`，均有限非零；重复/双样本批处理误差为 `0/8.83e-7`，低于固定 `1e-7/2e-6` 上限。最终输出 `SMOKE TEST PASSED` 和 `SCALE-CONSISTENT RASTER CUDA ADMISSION PASSED`，备份位于 `server_backups/pre_scale_consistent_raster_20260815_225705`。候选现只获得 staged seed-1 训练准入，不构成性能结论。

### scale-consistent raster seed 1 分阶段门禁失败

staged runner 完成 seed 1 五轮训练和固定 100 张同源 held-out CelebA val 的 x2/x4/x8 联合评估。Candidate 五轮 x4 PSNR 为 `26.9940/27.4188/27.7331/27.9634/28.1413`，均值 `27.6501 dB`；相对同 seed clean baseline 的五轮均值 `27.6706 dB` 下降 `0.0204 dB`，Epoch 5 相对 `28.1793 dB` 下降 `0.0380 dB`。两项均低于预注册允许的 `-0.0100 dB` floor。

held-out x2/x4/x8 的 baseline PSNR 为 `34.08013660/28.04536727/23.67010000 dB`，candidate 为 `34.05337768/28.00699228/23.65454441 dB`，差值分别为 `-0.02675892/-0.03837499/-0.01555559 dB`，三倍率平均差 `-0.02689650 dB`。baseline/candidate 的同图 x4-x8 mean-absolute discrepancy 为 `0.03378091/0.03382780`，相对降低为 `-0.00138806`，即误差反而增加约 `0.1388%`。因此训练、各倍率重建和目标跨尺度机制五类指标全部为负。

分析器输出 `SCALE-CONSISTENT RASTER SEED1 SCREEN FAILED`，runner 随即输出 `SCALE-CONSISTENT RASTER STAGED PROBE STOPPED AFTER SEED 1`，未启动 seed 2/3。该结果排除了“固定 5x5 HR raster 导致的尺度 footprint 收缩可由当前 3/5/9 离散轴直接修复”的适配假设；不能据 GSASR/AA-2DGS 原论文结果继续扫描采样点分段、步长、reference scale 或 kernel extent，也不能放宽训练 floor、减少 held-out 样本或只挑 x8。方向停止，不进入 30 epoch、不组合失败模块、不 commit、不 push。

## 2026-08-15：固定 covariance low-pass 的免训练预筛选

离散 raster 失败后重新逐式核对 AA-2DGS 与 GaussianImage++。AA-2DGS 的 world-space flat smoothing 并不是修改采样点数或直接做 pixel-area integral，而是依据多训练视角对每个 primitive 估计最大采样率，再在其局部平面使用 `V_eff = V + sigma_smooth^2 I`，同时按尺度乘积比例修正 opacity。当前单图 feed-forward GaussianSR 没有相机、深度或 per-primitive 多视角 sampling rate，不能忠实计算该 `sigma_smooth`，因此不再用其固定超参数臆造 scale mapping。

GaussianImage++ 给出了可直接迁移的 2D screen-space 形式 `G'(x) = exp(-1/2 (x-mu)^T (Sigma+sI)^-1 (x-mu))`，并明确记录 GaussianImage 使用常数 `s=0.5`。其内容自适应 filter 收益与 progressive densification 共同作用，不能据整套结果直接承诺 feed-forward SR 收益；因此本轮只把文献常数 `0.5` 作为唯一候选，不扫描 `s`，并先对已有 seed-1 clean baseline checkpoint 做无需训练的反事实筛选。

候选在 clean `4d0f59b` 增加默认恒等 `filter_covariance()` hook；baseline 公式逐元素不变，无参数子类只执行 `Sigma' = Sigma + 0.5I`。checkpoint screen 把同一份 baseline state dict 同时加载到原模型和候选，encoder、decoder、Gaussian bank 权重逐元素一致，不允许训练或重新初始化影响比较。固定 100 张 held-out CelebA val 以同一 HR 分别构造 x2/x4/x8，且复用预测计算 x4-x8 discrepancy。

预筛选门槛在结果前固定：每个倍率 PSNR 差均不得低于 `-0.005 dB`，三倍率平均至少 `+0.005 dB`，x8 至少 `+0.010 dB`，x4-x8 discrepancy 相对降低至少 `0.5%`。任一失败立即终止，不制作训练 runner、不扫描 filter variance、不尝试 scale-dependent `s`；只有四项全部通过才讨论 seed-1 五轮训练。

本地配置校验、Python 编译和 `git diff --check` 通过。完整 CPU smoke 确认 baseline/candidate 参数量均为 `1,838,159`，合成 covariance 公式误差为 0；x1.5/x4/x8 输出作用量为 `8.61e-5/2.05e-5/1.42e-5`，前向 diagonal delta 舍入误差最大 `2.38e-7`；encoder/Gaussian bank 梯度为 `3.479/4.655e-6`，repeat/batched error 为 `0/1.49e-8`。这些只证明固定 filter 正确接入，不构成效果证据。

免训练筛选包 `GaussianSR_face_covariance_lowpass_checkpoint_screen_20260815.zip` 大小为 `13,489` bytes，SHA-256 为 `2bd227fb75fa83243c30a3691c40f91c989f657a74f00836ac299a4511622902`，严格包含 7 个文件。第二个 detached clean `4d0f59b` worktree 解压后确认 7/7 文件哈希一致，并再次通过配置、Python 编译、`git diff --check` 和完整 CPU smoke。包中没有训练 runner，也不会创建 candidate checkpoint；服务器命令只允许 CUDA smoke 和已有 baseline checkpoint 的 held-out screen。

### covariance low-pass checkpoint screen 未通过

服务器完成哈希、配置、Python 编译与 CUDA smoke。参数量保持 `1,838,159`，公式误差为 0；x1.5/x4/x8 随机输入输出作用量为 `7.20e-5/2.19e-5/1.63e-5`，encoder/Gaussian bank 梯度为 `3.587/4.741e-6`，repeat/batched error 为 `0/8.68e-7`。这确认 `Sigma+0.5I` 已正确接入且可反向传播。

固定 100 张 held-out 图的 checkpoint screen 使用逐元素相同权重。x2 baseline/candidate 为 `34.08013660/34.08025695 dB`，差 `+0.00012035 dB`；x4 为 `28.04536727/28.04530859 dB`，差 `-0.00005868 dB`；x8 为 `23.67010000/23.67007144 dB`，差 `-0.00002856 dB`。三倍率平均差仅 `+0.00001104 dB`，比 `+0.005 dB` 门槛低约 453 倍。x4-x8 discrepancy 从 `0.03378091` 增至 `0.03378356`，相对降低为 `-0.00007861`，即略微恶化。

评估器输出 `COVARIANCE-LOWPASS CHECKPOINT SCREEN FAILED` 与 `CHECKPOINT SCREEN REJECTED; NO TRAINING WILL BE RUN`。因此不制作训练配置或 runner，不扫描 `s`，不改为 scale-dependent filter，不借“可能需要训练适配”绕过预筛选，也不运行任何 seed。该低成本门禁避免了至少一次五轮训练；代码与日志只作为否定性证据保留。服务器备份位于 `server_backups/pre_covariance_lowpass_20260815_232634`。

## 2026-08-15：CVPR 2025 HIIF 分层位置编码的轻量迁移预注册

在继续试验前检索并阅读全文核对了 CVPR 2025（CCF-A）论文 HIIF: Hierarchical Encoding based Implicit Image Function for Continuous Super-resolution，DOI `10.1109/CVPR52734.2025.00219`，arXiv `2412.03748`。本地全文为 `literature_cache_20260816_hiif/HIIF_CVPR_2025.pdf`，SHA-256 `1353ae63fc815973e8d153a9279f3e8ddfcb3ea57d2f6df1bf0ba9043a6b1bd8`。论文完整方法还包含 progressive multi-scale MLP 和 multi-head linear attention；这些组件参数与变量过多，本轮不整体复现。

采用该方向的原因是其层次位置编码有独立、跨倍率且量级明确的消融。HIIF Table 4 在 EDSR + Urban100 上，去掉 hierarchy 的 v1-H 为 `32.56/28.47/26.38/23.91/22.59/21.00 dB`，完整 HIIF 在 x2/x3/x4/x6/x8/x12 为 `32.69/28.59/26.51/23.99/22.72/21.09 dB`，对应 `+0.13/+0.12/+0.13/+0.08/+0.13/+0.09 dB`。这比此前约 `0.02 dB` 的通用 encoder 插件证据更强，也直接作用于任意尺度 implicit decoder。

当前受控适配只迁移论文公式 `floor(local_coord * S^(l+1)) mod S`。GaussianSR 已计算的 LR 单元内 `rel_coord` 被平移并裁剪到 `[0,1)`，固定 `S=2`，按现有四层 MLP 生成四级二进制坐标；每级通过独立 `Linear(2,256)` 在对应 hidden linear 之后、GELU 之前渐进注入。四个适配器全部零初始化，新增参数严格为 `3,072`；encoder、Gaussian bank、renderer、learned Fourier query feature、损失、数据和优化协议均不变。这是受控轻量适配，不声称复现 HIIF 的 attention 或完整网络。

成本门禁在服务器结果前固定。seed 1 先只训练 epoch 1，并与同协议 baseline epoch-1 `26.9929 dB` 比较；candidate 必须达到至少 `27.0129 dB`，即 `+0.0200 dB`，否则 runner 自动停止，不消耗 epoch 2--5。只有通过后才检查 checkpoint 内 seed、epoch、optimizer、scheduler、全局 RNG 与 `train_sampler` RNG 状态，并在不同保存目录从同一 checkpoint 续训 epoch 2--5。seed-1 最终还要求五轮训练均值差和 epoch-5 差均至少 `+0.0200 dB`，固定 100 张 held-out CelebA val 的 x2/x4/x8 每项均为正且平均至少 `+0.0200 dB`；未通过则不扩展 seed 2/3，不降门槛、不扫 hierarchy base/level、注入层或初始化。

实现位于独立 clean `4d0f59b` worktree `GaussianSR-clean-hiif-hiercoord-4d0f59b`。Python 静态编译、配置审计和 `git diff --check` 通过。CPU smoke 的 baseline/candidate 参数量为 `1,838,159/1,841,231`；共享参数误差严格为 0，新增参数最大绝对值严格为 0；x1.5/x4/x8 初始输出误差为 `7.45e-09/7.45e-09/3.73e-09`，低于固定 `1e-7` 数值等价线。一次更新的 adapter/Gaussian bank/encoder 梯度为 `2.230e-1/9.675e-7/6.262e-3`，输出平均变化 `2.787e-2`；repeat/batched error 为 `0/5.59e-09`。本机没有 Bash/CUDA，因此归档安装后仍必须先通过服务器 `bash -n` 和完整 x1.5/x4/x8 CUDA smoke，才允许启动单 epoch runner。

最终归档 `GaussianSR_face_hiif_hiercoord_staged_patch_20260815.zip` 大小 `20,274` bytes，SHA-256 `dc67e7a5e3253468924c981aa9406c4c6ce3465503785acbd3815cc1fb6534f8`，严格包含 12 个模型、配置、验证、分析与 runner 文件。第二个全新 detached `4d0f59b` worktree 解压后确认 12/12 文件 SHA-256 与开发 worktree 一致，并重新通过静态编译、配置审计、`git diff --check`、三倍率 CPU smoke 以及分析器 PASS/FAIL 合成测试。当前没有 commit、push 或训练结果。

### 服务器 CUDA 准入通过

服务器从 covariance-lowpass 状态完成受控备份与安装，备份位于 `server_backups/pre_hiif_hiercoord_20260816_123447`。ZIP、安装前后共享文件、clean `train_gaussian.py` 哈希均通过；Python 编译、Bash 语法检查和配置审计输出通过。CUDA smoke 的 baseline/candidate 参数量为 `1,838,159/1,841,231`，新增 `3,072`；共享参数误差与 adapter 初值最大值均为 0，x1.5/x4/x8 初始输出误差也均为 0。一次更新的 adapter/Gaussian bank/encoder 梯度为 `2.261e-1/5.544e-7/6.747e-3`，输出平均变化 `2.770e-2`；repeat/batched error 为 `0/7.45e-09`。最终输出 `SMOKE TEST PASSED` 与 `HIIF-HIERCOORD CUDA ADMISSION PASSED`，因此仅获得 seed-1 epoch-1 staged runner 的启动资格。

## 2026-09-04：ICCV 2025 IM-LUT 复核与特征插值混合预注册

HIIF 的 seed-1 epoch-1 门禁仍在服务器运行；本节是后续独立候选的预注册，不改变、追参或提前终止 HIIF。重新检索 2025--2026 CCF-B 以上任意尺度/通用超分工作后，排除了已记录的 GSASR、rotation-equivariant ASISR、diffusion prior、位置漂移、频域插件、尺度路由、边缘重参数化和层次坐标等方向。最终保留 ICCV 2025（CCF-A）[IM-LUT](https://arxiv.org/abs/2507.09923) 的插值混合问题设定。

IM-LUT 用 LR 内容预测 nearest、bilinear、bicubic、Lanczos 等插值的逐像素权重，以正弦尺度编码产生全局倍率调制，再把对应方法上采样后的图像和权重相乘求和，最后用局部 Refiner 修复残余失真。其 Table 4 的 Set5 消融从 reconstruction-only 的 x2/x3/x4 `35.81/31.95/29.85 dB`，逐步加入 guide、scale modulator 与 Refiner 后达到 `36.48/32.64/30.46 dB`；加入 Refiner 的对应阶段提高 `0.22/0.40/0.41 dB`，完整组合继续提高 `0.22/0.11/0.07 dB`。该证据来自同一框架受控消融，但完整 IM-LUT 的绝对质量仍低于 LIIF/MetaSR，不能把论文增益直接外推到 GaussianSR。官方仓库 `SejinPark-CV/IM-LUT` 截至本次复核只有 README 和“Code Details Updating”，因此本项目没有复制其源码，而是依据论文式 (1)--(5)、NLC 组合和结构图独立实现。

### 受控适配与非重复边界

新候选注册为 `gaussian-splatter-face-imlut-feature-mix`。Gaussian splatting 的前 8 个 latent 通道、100 项 Gaussian bank、covariance/opacity、rasterizer、Fourier query、四层 MLP、L1、数据和优化协议全部不变；只把原固定 bicubic 的后 56 通道 bypass 改为一个 `NLCFeatureMixer`，并在 64 通道拼接后增加一个 depthwise-local + pointwise 的 `LocalFeatureRefiner`：

1. LR RGB 经 5x5 有效感受野的轻量卷积预测三路内容权重；16 组二进制正弦尺度编码经小型 MLP 生成三路正倍率调制。
2. nearest、bilinear、bicubic 分别上采样 bypass feature 和对应权重，HR 权重重新归一化后混合。
3. mix gate 从严格 0 开始，Refiner 最终投影也严格为 0，因此 candidate 初始化逐元素退化为原 bicubic baseline；训练后两部分才能分别激活。
4. 该候选不是既有 query-wise RGB frequency residual：它在 Fourier `coef/freq` 生成之前替换 latent bypass 的固定插值规则；也不是 scale-consistent Gaussian raster，因为 Gaussian 前 8 通道及其采样轴完全不变；不是 latent FIM，因为没有 FFT 或 encoder residual。

新增参数为 `6,279`，约占 baseline `1,838,159` 参数的 `0.342%`。共享 `models/gaussian.py` 只新增默认执行原 bicubic 的 `compose_raster_features` hook，baseline 行为未改变；候选功能量严格限定为 NLC mixing 与 local Refiner 两个模块。

### 与用户目标一致的宽松分阶段判定

不再要求每个 seed、每个倍率都严格提高。seed 1 先完整运行五轮；只有五轮均值差不低于 `-0.010 dB`、末轮差不低于 `-0.020 dB`、100 张 held-out x2/x4/x8 平均差严格为正、任一倍率不低于 `-0.030 dB`，并且 mix gate、权重空间变化、mixed response 与 Refiner response 全部真实激活，才继续 seed 2/3。

最终三种子允许一个 seed 或个别倍率小幅下降，但必须同时满足：跨种子训练五轮均值、末轮和 held-out 三倍率平均差都严格为正；这三类聚合指标各至少 2/3 seed 为正；任一单项质量差不低于 `-0.050 dB`；三个 seed 的两项机制均激活。该规则直接对应“总体略优、允许偶尔下降”，且在看到任何服务器结果前固定。

### 本地实现与交付状态

实现位于 detached clean worktree `GaussianSR-clean-imlut-feature-mix-4d0f59b`。三份配置校验、Python 静态编译、`git diff --check`、分析器 PASS/FAIL 合成边界和完整 x1.5/x4/x8 CPU smoke 均通过。正式 smoke 使用与训练每卡一致的 batch size 4；baseline/candidate 参数量为 `1,838,159/1,844,438`，三倍率初始输出误差均为 0，NLC 潜在 latent 差为 `0.066265/0.056123/0.056834`，权重和误差不超过 `1.19e-7`。两步后 gate、weight predictor、scale modulator、Refiner、Gaussian bank 与 encoder 梯度均有限非零，repeat/batched error 为 `0/1.49e-8`。

交付包 `GaussianSR_face_imlut_feature_mix_staged_patch_20260904.zip` 大小 `21,538` bytes，SHA-256 为 `feb5b9f58918b160b641a3abbd10565670620918c3d19cb5e4b9c726c75c5247`，严格包含 11 个必要文件。新的 detached clean `4d0f59b` worktree 已确认 11/11 哈希一致，并再次通过三配置、静态检查和 batch-4 完整 CPU smoke。本机无 CUDA/完整数据栈，且服务器 HIIF 尚未给出终态；因此目前只完成候选开发与归档，不安装、不启动训练，也不把它写成已提升 baseline 的结论。HIIF 终止后必须先读取服务器三个共享文件的新哈希，再生成备份、安装和 CUDA 准入命令。

为避免手工粘贴安装命令出错，另生成独立脚本 `install_face_imlut_feature_mix_after_hiif_20260904.sh`，大小 `6,244` bytes，SHA-256 `57022767af7b41d5a9010393b85937cbd5c96a28f9d12404389818a0822e5269`。脚本先要求 HIIF 终态标记，再识别精确 HIIF 状态或旧候选刷新状态，检查共享依赖和 ZIP 哈希、拒绝覆盖训练输出、备份原文件、核对 11 个安装后文件哈希，并执行 Python 编译、runner `bash -n`、三配置和 batch-4 CUDA smoke；只有全部通过后才写入与当前 ZIP 哈希绑定的 CUDA admission stamp。

另生成 `launch_face_imlut_feature_mix_staged_probe_20260904.sh`，大小 `2,469` bytes，SHA-256 `69a1fd82449386703584dc9226f8ab1975f24bbed25c8b6d74d2d7a04aefa050`。启动脚本要求有效 admission stamp，复核 candidate 共享文件、模型和 runner 哈希，拒绝任何已有 summary/save/result/orchestrator 路径，再将 runner 输出重定向到独立平面日志，写入 PID，并在两秒后确认进程仍存活。两个脚本均已由本机 Git Bash 完整通过 `bash -n`；安装脚本本身不会自动启动训练。

## 2026-09-20：正式结果转入论文写作阶段

基于已完成的 CelebA/Helen 八倍率正式统一测试和 2026-09-19 paired audit，研究阶段暂时收束，开始论文初稿整理。新建 `paper_writing_record_20260920.md`，集中记录创新点、数据集与协议、可直接复制的正文/附录表格、消融边界和行文大纲。

当前论文候选固定为“方向高频查询残差 + 固定边缘感知加权 L1”。正式结果支持“总体有效、优势集中于中倍率和部分非整数倍率”的限定性结论，不支持“所有倍率、所有数据集和所有指标全面领先”。正文建议突出 ×2、×2.5、×3.5、×4，附录保留 ×1.5、×2、×2.5、×3.5、×4、×5.5、×7.5、×8 的完整结果。

已记录的焦点倍率 paired audit 均值变化为：CelebA PSNR-Y `+0.004754`、SSIM-Y `+0.000161`、LPIPS `-0.000464`；Helen PSNR-Y `+0.027651`、SSIM-Y `+0.000350`、LPIPS `-0.000253`。这些统计结果与正式八倍率绝对值表一致，bootstrap 区间仅作为统计补充，不替代逐倍率主表。

本记录不把 5 epoch 探针、失败方向或未统一协议的外部结果写成正式论文结果。下一步为服务器正式文件 SHA-256 冻结、定性可视化和论文初稿，不再启动新的结构搜索或 1,350 epoch 训练。
