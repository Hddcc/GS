# GaussianSR 人脸超分项目进展记录

本文件是代码版本、数据协议、训练运行、测试指标、创新方向和研究结论的持续记录。它记录实验事实，不等同于论文定稿。

## 强制记录规则

1. 每次出现新进展，都必须在同一次工作和 Git 提交中更新本文件。
2. 新进展包括代码、配置、数据集协议、训练命令、测试命令、指标、异常、诊断、创新方向、结论和下一步的变化。
3. 每条记录至少包含日期、分支、目标、改动文件、实验协议、命令或运行名、结果、结论和下一步。
4. 正式实验、短程探针、准入测试和失败方案必须明确区分，不能混写。
5. 原始 CSV、逐图 CSV、配置、日志和 checkpoint 保持在服务器或本地原位置；Markdown 只记录路径、摘要和校验状态。
6. `main` 只保存稳定可复现版本。新方案使用 `experiment/<topic>` 分支，修复使用 `fix/<topic>` 分支，文档整理使用 `docs/<topic>` 分支。
7. 未通过准入标准的方案不得合并为最终模型，也不得提前写成论文最终创新。
8. 禁止把数据集、checkpoint、训练输出、凭据、缓存和传输压缩包提交到 GitHub。

## 2026-07-31：建立 Git 版本管理与正式实验保底快照

### 版本目标

- Git 分支：`main`。
- Git 提交：首次代码快照，提交完成后以仓库日志为准。
- 远端目标：`https://github.com/Hddcc/GS.git`。
- 保存当前 GaussianSR 人脸超分代码、配置和实验文档。
- 当前正式候选仅作为保底版本，不认定为最终创新模型。
- 后续新方案在独立实验分支开发，避免覆盖可复现 Baseline 和正式候选。

### 数据与统一协议

| 数据集划分 | 数量 | 用途 |
|---|---:|---|
| CelebA train | 18,000 | 训练 |
| CelebA val | 500 | 选择最佳 checkpoint |
| CelebA test | 1,000 | 同数据集正式测试 |
| Helen test | 50 | 跨数据集正式测试 |

- 训练倍率范围：`[1.5, 8]`。
- 正式测试倍率：`1.5、2、2.5、3.5、4、5.5、7.5、8`。
- 正式训练：1,350 epoch，batch size 12，GPU `0,1,2`，seed 1，确定性训练，倍率分组采样，从头随机初始化。
- 指标：PSNR-Y、SSIM-Y、LPIPS-Alex。

### 已完成的正式运行

| 角色 | 运行名 | 状态 |
|---|---|---|
| Baseline | `face_gaussian_baseline_seed1_formal_v2protocol` | 1,350 epoch 与八倍率测试完成 |
| 保底候选 | `face_gaussian_frequency_residual_edge_seed1_formal_v2protocol` | 1,350 epoch 与八倍率测试完成 |

保底候选由“方向高频查询残差”和 `edge_weight: 0.25` 的边缘加权 L1 组成。该组合已经完整训练和测试，但两部分均不是最终确定的论文创新点。

### 正式结果摘要

| 数据集 | 平均 ΔPSNR | 平均 ΔSSIM | 平均 ΔLPIPS | PSNR/SSIM/LPIPS 改善倍率数 |
|---|---:|---:|---:|---:|
| CelebA | +0.0080 dB | +0.000044 | -0.000302 | 7 / 6 / 7 |
| Helen | +0.0206 dB | +0.000380 | +0.000027 | 6 / 8 / 5 |

结果小幅但总体有效，主要改善中高倍率；Helen 的 LPIPS 表现混合。该结果足以作为毕业论文保底对照，但创新性和提升幅度尚不足以直接确定为最终方法。

详细逐倍率结果见 `毕业论文第四章实验与方法设计.md` 和本地实验数据总账。服务器正式文件位于：

```text
results/face_formal_v2protocol
save/face_gaussian_baseline_seed1_formal_v2protocol
save/face_gaussian_frequency_residual_edge_seed1_formal_v2protocol
```

### 当前冻结状态

- 正式配置和日志已复制到 `results/face_formal_v2protocol/frozen`。
- `formal_baseline_manifest.sha256` 已完整核验，Baseline 的 `epoch-best.pth`、`epoch-last.pth`、`config.yaml` 和 `log.txt` 均为 `OK`。
- `formal_training_manifest.sha256` 已完整核验，Baseline、正式候选的 checkpoint/config/log 以及 `test_face_metrics.py` 均为 `OK`。

### 下一创新方向（尚未实现）

暂定研究“人脸结构先验驱动的尺度条件局部专家融合”：训练阶段使用 68 点关键点生成软人脸区域监督，由 LR 特征预测结构图，并根据局部结构、频率和倍率连续路由纹理、轮廓和平滑区域专家。该方向必须先完成近两年文献与代码查重，确认创新性后才能改代码。

### 下一步

1. 完成正式文件 SHA-256 核验并把结果补充到本文件。
2. 调研人脸结构先验、尺度条件专家和任意尺度人脸超分的近两年工作。
3. 确定一个新方案后创建独立实验分支，不直接修改 `main`。
4. 先进行同协议短程准入实验，通过后才启动正式训练。

## 2026-09-19：尺度条件边缘损失探针实现

### 版本与目标

- Git 分支：`experiment/face-scale-conditioned-edge-loss`。
- 起点：正式复核快照 `ade78d6`，不修改 `main` 和已冻结正式结果。
- 目标：检验边缘加权 L1 的强度是否应随连续放大倍率轻度变化，针对正式结果中低倍率混合、中倍率改善的现象做最小控制变量实验。

### 代码改动

- `datasets/wrappers.py` 新增 `effective_edge_weight_strength`。
- `sr-implicit-downsampled` 支持 `edge_weight_mode: fixed|scale-conditioned`。
- 尺度条件模式采用 `0.25 * (scale / 4.0)^0.5`，空间权重图仍逐样本归一化到均值 1，不改变整体 L1 数值尺度。
- 新增 `test_scale_conditioned_edge_weight.py`，覆盖倍率变化、均值归一化、非法参数和有限值检查。
- 新增 `configs/train/face/probe_face_gaussian_frequency_residual_scale_edge_seed1.yaml`，沿用正式候选的频率残差结构和 5 epoch、seed 1 探针协议。

### 当前状态

- 本地机制测试待运行。
- 服务器探针尚未运行；首轮只运行 5 epoch seed 1，使用与正式候选相同的数据、倍率范围 `[1.5, 8]`、batch size 12 和 x4 验证。
- 通过标准：训练/验证日志正常、机制测试通过，并观察 x4 及后续非整数倍率是否相对固定权重候选改善；未通过则停止，不启动 1,350 epoch 正式训练。

### 单包交付

- 已生成 `face_scale_conditioned_edge_probe_bundle_v2_20260919`，仅包含可运行脚本、配置、改动后的 `datasets/wrappers.py` 和机制测试。
- 包内不含 Markdown、数据集、checkpoint 或结果；服务器执行入口为 `install_and_launch_face_scale_conditioned_edge_probe_20260919.sh`。
- 入口默认使用物理 GPU 4，可通过 `PHYSICAL_GPU=3` 覆盖；训练日志写入 `results/face_scale_conditioned_edge_probe_orchestrator.log`。
- v1 在 Windows 打包时 `PAYLOAD_SHA256` 被转换为 CRLF，服务器校验把 `\r` 误识别为文件名字符，因此未进入安装和训练。v2 通过 `*SHA256` 属性强制此类无扩展名清单为 LF，并在安装器校验前兼容清理 CRLF。

### 探针结果与结论

- 服务器已完成 `face_gaussian_frequency_residual_scale_edge_seed1_probe5` 的 5 epoch、seed 1 训练和 CelebA 500 张图评价。
- 评价倍率为 `2、2.5、3.5、4`，对照为同等 5 epoch、seed 1 的 `face_gaussian_frequency_residual_edge_seed1_probe_full5`，两者均使用频率残差模型。
- 尺度条件版本相对固定 `edge_weight: 0.25` 的 PSNR-Y 差值依次为：`-0.091289、-0.121764、-0.082303、-0.059066 dB`；SSIM-Y 差值依次为 `-0.000459、-0.000801、-0.001148、-0.001255`。
- 四个目标倍率全部退化，判定该尺度条件边缘权重方案失败；不继续 Helen 评价，不启动 1,350 epoch 正式训练，不合并到 `main`。
- 原正式候选 `face_gaussian_frequency_residual_edge_seed1_formal_v2protocol` 保持冻结，当前论文保底结果不受此探针影响。

## 2026-09-19：失败方向后的下一步消融计划

- 当前分支已与远端同步，最新提交为 `fdf01fa`。
- 由于尺度条件边缘损失在 CelebA x2、x2.5、x3.5、x4 全部退化，下一步不继续新增训练结构，也不启动新的长程训练。
- 先使用已有 5 epoch checkpoint 做模块消融：`frequency residual only` 对比 `frequency residual + fixed edge-aware L1`，倍率为 `2、2.5、3.5、4`，数据协议和评价脚本保持一致。
- 该消融用于确认论文中频率残差和边缘损失的独立贡献；若频率残差单独不稳定，则保留组合方案作为保底；若单独更好，则重新审查固定边缘损失是否应从论文主方法中移出。
- 消融完成前不修改 `main`，不重跑 1,350 epoch，不把失败的尺度条件方案写入论文最终方法。
