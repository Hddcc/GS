# 建议 D：盲人脸 SPD 退化补偿研究复核

日期：2026-09-13  
复核对象：`GaussianSR创新方向候选_20260906.md` 中“建议 D：方向 5 + 方向 2”  
结论状态：**拒绝按原公式直接实施；仅允许先做免训练 oracle 准入实验。**

## 1. 结论摘要

原建议 D 不应直接进入 5-epoch probe，更不能直接进入完整训练。核心原因不是工程风险，而是建议中的关键方程

```text
Sigma_render' = Sigma_geo + Sigma_blur / r^2
```

尚未得到引用论文支持，并且存在两个基础问题：

1. 对高斯核而言，协方差相加描述的是卷积后进一步扩散。超分复原是逆问题，正号相加不天然等于“退化补偿”；至少必须先通过正号、负号和尺度映射的 oracle 对照。
2. 当前 GaussianSR 的渲染协方差定义在固定 `[-5, 5]` 的 5x5 核采样坐标中，不是 HR 或 LR 像素坐标。退化核的像素协方差不能未经校准直接相加。

WACV 2024《Best of Both Worlds》确实证明了盲退化表征对通用任意尺度 SR 有价值，但它实际采用的是：

- 隐式退化向量调制 EDSR 特征；
- 显式估计 LR 有效退化核，只在训练时用于 LR 空间 cycle consistency；
- 推理时移除显式 kernel estimator。

它没有把模糊核协方差加到 Gaussian renderer，也不是人脸超分论文。WACV 也不在 CCF 当前人工智能 A/B/C 推荐列表中，因此不能作为“2024--2026 CCF-A/B 直接迁移”的合格来源。

当前最小安全下一步是：在固定的盲退化 held-out manifest 和同一个 baseline checkpoint 上，先做无需训练的 oracle 符号、倍率、单位映射筛选。只有某一 oracle 变体在各退化桶和倍率上稳定优于不补偿基线，才允许训练轻量退化条件分支。若所有 oracle 变体均不提升，建议 D 到此终止。

## 2. 原建议逐条复核

原建议位于：

- 本地：`D:\个人资料\项目\任意尺度\GaussianSR-main\GaussianSR创新方向候选_20260906.md`，第 678--694 行。

| 原建议中的判断 | 复核结论 | 依据 |
| --- | --- | --- |
| 盲设定下提升会是 `0.3--1 dB` | **不能预注册为预期收益。** WACV 在其特定通用图像、未知 blur/downsampler 分布上报告正收益，但没有 GaussianSR、人脸数据或多 seed 证据；任务改变本身也会令 clean baseline 发生 domain shift。 | WACV 正文 PDF p.6--8；Table 1/2。 |
| 模糊核和渲染核是同一个 SPD 对象，可直接做 `Sigma_geo + Sigma_blur/r^2` | **无文献支持，量纲未闭合，符号也未证明。** WACV 通过 Fourier least-squares 得到 LR 有效核 `k_l`，不是简单采用 `Sigma_h/r^2`；ContinuousSR 和 GSASR 也没有该加法。 | WACV 正文 p.3、p.5、p.7；GaussianSR `models/gaussian.py` 第 237--314 行。 |
| 方向 5 依赖已失败的连续 SPD 方向 2 | **不能直接复用。** 已有 clean probe 三个 held-out 倍率全部下降，且协方差出现边界聚集。 | `GaussianSR-clean-continuous-spd-4d0f59b\项目进展记录.md` 第 1677--1683 行。 |
| “盲 + 人脸 + Gaussian ASSR”交集为空，因此方向大概率新 | **检索空白不等于新颖性。** WACV 已把 blind ASSR、隐式/显式双退化表征和 LR cycle 作为核心贡献；高斯卷积的协方差相加本身是经典性质。 | WACV 正文 p.2--6；官方仓库固定提交见下。 |
| 盲 ASSR “已被申请专利（US 12518344）” | **表述需纠正并交法务核查。** `US 12,518,344 B2` 是授权号，不应继续写“仅已申请”。本次只确认 USPTO 官方 PDF，未完成 claim chart，不能据此断言建议 D 落入或避开具体权利要求。 | USPTO 官方 PDF URL 与本地归档见第 8 节。 |

## 3. WACV 2024 实际方法

论文：Shao-Yu Weng 等，*Best of Both Worlds: Learning Arbitrary-Scale Blind Super-Resolution via Dual Degradation Representations and Cycle-Consistency*，WACV 2024，pp.1547--1556。

### 3.1 退化模型

正文 p.3 将常用退化写成

```text
y = (x * k_h) down_s + n
```

主体实验取 `n = 0`，并改写为 LR 空间形式 `y = x down_s * k_l`。正文 p.5 的 Eq.4 通过 Fourier-domain least-squares 从 clean downsample 与 blurred downsample 的关系求 LR 有效核 `k_l`。正文 p.7/Fig.4 还显示：同一 HR 核在不同倍率下得到的有效 LR 核不同，直接估计 HR 核会产生跨倍率不一致。

因此，论文并没有给出 `Sigma_l = Sigma_h / r^2` 的工程规则，更没有给出把该项加进 SR 渲染 covariance 的规则。

### 3.2 隐式与显式退化表征

- 隐式分支：正文 p.4。对 LR 图像取 Haar 高频三子带并拼成 9 通道，使用 SimSiam 风格目标学习 256 维退化表征。
- 显式分支：正文 p.5。由隐式向量生成多级线性滤波器，并卷积合成为 21x21 LR kernel。
- SR 调制：正文 p.5。隐式退化向量经两层 FC 和 sigmoid 后，对 EDSR residual 特征做逐通道调制。代码见固定提交的 [`models/edsr_mod.py` 第 31--68 行](https://github.com/vivian210223/arbitrary-scale-blind-SR/blob/85493094990b3744e4176d94131a413893e62063/models/edsr_mod.py#L31-L68)。
- Cycle：正文 p.5--6，Eq.5--7。模型同时生成 LR 尺寸结果，用估计的 `k_l` 重退化并与输入 LR 做 L1；代码见 [`models/controller.py` 第 47--77 行](https://github.com/vivian210223/arbitrary-scale-blind-SR/blob/85493094990b3744e4176d94131a413893e62063/models/controller.py#L47-L77) 和 [`train_SR.py` 第 136--143 行](https://github.com/vivian210223/arbitrary-scale-blind-SR/blob/85493094990b3744e4176d94131a413893e62063/train_SR.py#L136-L143)。
- 推理：正文 p.6 明确移除显式 kernel estimator；保留隐式 encoder 与 SR 模块。

### 3.3 受控收益证据及其边界

正文 p.8/Table 2 的 DIV2K-val x3 消融为：

| implicit | cycle | PSNR | 相对 neither |
| --- | --- | ---: | ---: |
| 否 | 否 | 29.5949 | 0 |
| 否 | 是 | 29.6253 | +0.0304 |
| 是 | 否 | 29.6870 | +0.0921 |
| 是 | 是 | 29.8102 | +0.2153 |

这说明退化条件化和 cycle 在论文任务上有正信号，但证据只有单数据集、单倍率、未报告 seed 方差。它不能推出“Gaussian 人脸盲 ASSR 必然提升 0.3--1 dB”，更不能支持 SPD covariance 加法。

### 3.4 训练成本

论文正文 p.6 与官方配置显示：

- DIV2K 800 张 HR；LR patch 48x48；训练倍率 `U(1,4)`；
- anisotropic Gaussian，角度 `U(0, pi)`，下采样随机选 bicubic/bilinear/area，主体为 noise-free；
- 退化表征阶段约 1500 epochs、batch 128；
- SR 阶段约 1000 epochs、batch 16。

官方配置：[`configs/degradation.yaml` 第 1--57 行](https://github.com/vivian210223/arbitrary-scale-blind-SR/blob/85493094990b3744e4176d94131a413893e62063/configs/degradation.yaml#L1-L57)、[`configs/train-div2k/train_SR.yaml` 第 1--49 行](https://github.com/vivian210223/arbitrary-scale-blind-SR/blob/85493094990b3744e4176d94131a413893e62063/configs/train-div2k/train_SR.yaml#L1-L49)。这不是一个适合直接塞进当前 5-epoch seed-1 筛选的轻量改动。

## 4. 与当前 GaussianSR 的接口冲突

当前干净实验基线：`D:\个人资料\项目\任意尺度\GaussianSR-clean-multi-gaussian-cfdd7fc`，commit `b5e0126`。

### 4.1 协方差单位不同

`models\gaussian.py` 第 237--274 行在固定核网格上计算 Gaussian：

- `sigma_x/sigma_y/rho` 组合成 2x2 covariance；
- Gaussian 在固定 `[-5,5]` 采样坐标上求值；
- 之后还经历 patch padding、`affine_grid/grid_sample` 平移、`Fold` 和 bicubic resize（第 280--316 行）。

这套 covariance 描述的是 renderer 内部离散核坐标，不是 HR blur kernel 或 LR effective kernel 的像素二阶矩。任何 `T(Sigma_blur)` 都必须通过 impulse response 校准映射到该坐标，不能只除以倍率平方。

### 4.2 当前数据没有退化标签

`datasets\wrappers.py` 第 148--160 行只用 `resize_fn` 在线 bicubic 下采样；样本返回也没有 kernel、角度、blur strength 或 degradation id。若建立 blind benchmark，需要新的 wrapper 和固定 manifest，而不是在现有验证集上在线随机生成退化。

此外，旋转、水平翻转和垂直翻转必须同步变换 covariance；多 worker DataLoader 必须使用显式 generator 或预生成 manifest，不能依赖全局 Python `random`。

### 4.3 连续 SPD 旧实验已失败

本地记录：`D:\个人资料\项目\任意尺度\GaussianSR-clean-continuous-spd-4d0f59b\项目进展记录.md` 第 1677--1683 行。

- seed-1 held-out x2/x4/x8：`-0.1452/-0.0263/-0.0242 dB`；
- 三倍率均值：`-0.0652 dB`；
- covariance offset 和 off-diagonal 已明显激活，并非机制未工作；
- 对角尺度饱和率超过预注册上限，出现边界聚集；
- staged screen 已拒绝，不能把它当作稳定底座再叠加 blur covariance。

## 5. ContinuousSR 与 GSASR 不能补上该证据

### ContinuousSR

- 本地论文：`D:\个人资料\项目\任意尺度\literature_cache_20260811\2503.06617.pdf`。
- 本地官方代码：`D:\个人资料\项目\任意尺度\literature_cache_20260811\ContinuousSR_code`，commit `1eca5914d4b36cf78f250822b9ad2e6a6dc88a12`。
- 论文 p.5 的 DGP covariance weighting 是渲染 covariance dictionary；p.6--7 的 LR 仍由 DF2K HR 通过 bicubic 生成；p.8 将 rain/noise 作为未来扩展。
- 代码 `models\gaussian.py` 第 224--258 行预测渲染 covariance 并随输出尺度调整，没有估计输入退化核，也没有 blur/render covariance 补偿加法。
- 仓库采用 CC BY-NC 4.0，只允许非商业使用；其训练代码也未公开，不能直接作为本项目完整训练实现。

### GSASR

- 本地论文：`D:\个人资料\项目\任意尺度\literature_cache_20260811\2501.06838.pdf`。
- 本地代码：`D:\个人资料\项目\任意尺度\literature_cache_20260905_gsasr_code`，commit `9d2eb64a51303ce7a22fd672197185488b38e10a`。
- GSASR 研究的是 Gaussian arbitrary-scale rendering，并未提供 blind degradation estimator 或 `Sigma_render +/- Sigma_blur` 的受控证据。

所以，这两篇工作可以支持“Gaussian covariance 是可学习渲染参数”，不能支持“输入模糊 covariance 应以正号加入渲染 covariance”。

## 6. 唯一建议的下一步：oracle sign/scale/unit admission

此阶段不训练 degradation encoder，不改主干，不跑 seed 2/3，也不复现 WACV 的 1500+1000 epoch 两阶段流程。

### 6.1 固定数据

- CelebA held-out 100 张，固定图像顺序。
- 固定 `scale in {1.5, 2, 4, 8}`。
- 先只使用 noise-free anisotropic Gaussian + bicubic；kernel size 21。
- blur 至少覆盖 mild/medium 两档，角度覆盖 `0/45/90` 度。
- 同一 HR、kernel、scale 只生成一次 LR，并保存 manifest、RNG seed 和文件 hash；所有候选逐样本配对。
- 每个样本保留 GT HR kernel covariance `Sigma_h`、按 WACV Eq.4 求得的 LR effective kernel `k_l`，以及 `k_l` 归一化二阶矩 `Sigma_l`。

### 6.2 在同一个 baseline checkpoint 上比较

| 变体 | 定义 |
| --- | --- |
| `A0` | 不修改 renderer covariance。 |
| `A+` | `Sigma_g + alpha * T(Sigma_h / r^2)`。 |
| `A-` | PSD-safe subtraction，并对最小特征值做 clamp。 |
| `ALR+` | `Sigma_g + alpha * T(Sigma_l)`。 |
| `ALR-` | 使用 `Sigma_l` 的 PSD-safe subtraction。 |

`alpha in {0.25, 0.5, 1}`。`T` 必须通过 GaussianSR renderer 的 impulse calibration 得到，记录矩阵方向、坐标缩放和归一化规则；不允许把 `/r^2` 当成已经完成单位换算。

### 6.3 预注册门禁

主指标仍用当前项目 RGB PSNR 协议，不与 WACV 的 Y-channel 数字混比。逐 `scale x blur strength x anisotropy` 分桶，并报告 paired mean、sample std 和每桶样本数。

通过条件：

1. 至少一个 oracle 变体相对 `A0` 的总体 paired mean PSNR 为正；
2. mild 和 medium blur 两档均为正，不能由 clean/bicubic 样本掩盖；
3. x2/x4/x8 均不为负，x1.5 单独报告；
4. covariance 全程有限且 SPD，clamp/saturation 比例低于预注册上限；
5. 结论需在 Helen test 50 张上做一次只读外域复核。

若所有符号、`alpha` 和 `Sigma_h/Sigma_l` 变体均未通过，立即停止建议 D。不要训练退化编码器，也不要事后换门限。

## 7. oracle 通过后的公平训练设计

只有第 6 节通过后，才允许实现最小条件分支。首选不是硬编码 covariance 加法，而是预测/条件化 LR effective degradation embedding 或 `k_l`，并限制为一个 encoder 加一个 Gaussian 参数调制接口。

必须从头训练并保持以下三组严格公平：

| 组别 | 作用 |
| --- | --- |
| `B-clean` | 原 bicubic-only GaussianSR，仅量化 domain shift，不作为主要 blind 对手。 |
| `B-blind` | 原 GaussianSR 在同一 blind mixture 上从头训练，不提供 GT kernel。 |
| `C-condition` | 加最小 degradation conditioning，在同一 mixture、初始化和训练预算下训练。 |

可增加 oracle-GT-degradation 上限，但不能把 oracle 结果写成 blind 结果。测试必须复用完全相同的 manifest，并分桶报告 CelebA 与 Helen；clean bicubic 子集单独报告，防止 blind gain 以 clean collapse 为代价。至少跑 3 seeds 均值和标准差，同时报告参数量、FLOPs、GPU-hours 与推理延迟。

## 8. 复现、许可与专利边界

### WACV 代码复现风险

官方仓库固定提交：`85493094990b3744e4176d94131a413893e62063`，URL：<https://github.com/vivian210223/arbitrary-scale-blind-SR>。

核对发现：

- 论文写随机奇数 kernel size 7--21、eigenvalue `U(0.2,4)`；trainer 无参调用的代码默认值实际为 kernel size 21、`U(0.6,5)`，见 [`datasets/blur.py` 第 231--245 行](https://github.com/vivian210223/arbitrary-scale-blind-SR/blob/85493094990b3744e4176d94131a413893e62063/datasets/blur.py#L231-L245)。
- supplement 的 SR 学习率为 `0.001`，官方配置实际为 `1e-4`。
- `test.py` 调用 PSNR 时未传 scale，`utils.calc_psnr` 因而使用默认 shave 1；不能直接拿其论文数字校准本项目 RGB 指标。
- Python/NumPy 随机源未全部固定，原仓不是严格多 seed 可复现协议。

### 许可

WACV 官方仓库根目录没有 `LICENSE/COPYING`，GitHub 元数据为 `license: null`。因此不得复制其代码、修改后分发或放入本项目交付包。若后续通过 oracle，应依据论文方程 clean-room 独立实现，并保留引用。

### 专利

- USPTO 官方 PDF：<https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/12518344>。
- 本地：`D:\个人资料\项目\任意尺度\tmp\blind_spd_research_20260913\US12518344B2.pdf`。
- 本次只确认授权文献存在，未完成权利要求逐项映射。商业化或专利撰写前，需要法务对“隐式/显式退化表征 + SR 调制 + LR cycle”整体组合做独立 FTO 检索。

创新表述不得使用“首次 blind ASSR”或仅凭检索空白声称“首次 SPD 同构”。如实验最终成立，可讨论的项目特定贡献应限定为：**经过坐标 impulse calibration、符号 oracle 验证和盲基线公平训练验证的 GaussianSR 退化条件化几何机制。**

## 9. 一手来源与本地校验

1. WACV 2024 CVF 论文页：<https://openaccess.thecvf.com/content/WACV2024/html/Weng_Best_of_Both_Worlds_Learning_Arbitrary-Scale_Blind_Super-Resolution_via_Dual_WACV_2024_paper.html>。
2. WACV 正文 PDF：<https://openaccess.thecvf.com/content/WACV2024/papers/Weng_Best_of_Both_Worlds_Learning_Arbitrary-Scale_Blind_Super-Resolution_via_Dual_WACV_2024_paper.pdf>。本地：`D:\个人资料\项目\任意尺度\tmp\blind_spd_research_20260913\wacv2024.pdf`；SHA-256：`5F7258FECDECDA9AF1D44CCEB074443911F4BD23ED3398321115599877A7A98F`。
3. WACV 补充材料：<https://openaccess.thecvf.com/content/WACV2024/supplemental/Weng_Best_of_Both_WACV_2024_supplemental.pdf>。
4. WACV 官方代码：<https://github.com/vivian210223/arbitrary-scale-blind-SR/tree/85493094990b3744e4176d94131a413893e62063>。
5. CCF 人工智能推荐目录：<https://www.ccf.org.cn/Academic_Evaluation/AI/>；该页列出 CVPR/ICCV/ECCV 等会议，未列 WACV。
6. ContinuousSR 论文：<https://arxiv.org/abs/2503.06617>。本地 PDF SHA-256：`9E6BD947CD25C52C52481A0AA4C45CC091CFEDBDB094B0192E8F7A77377C9293`。
7. GSASR 论文：<https://arxiv.org/abs/2501.06838>。本地 PDF SHA-256：`1A3734AEC741FF76CAC936B5C5EBE7C3AF5AFFCA355E3A7F9F603072FCCE5A4B`。
8. USPTO `US 12,518,344 B2` 官方 PDF：见第 8 节；本地 SHA-256：`0268557361DD24261A3A5FE1D0B035D1A73E06F8CF31B45AA60B2A3423506337`。

## 10. 最终决策

**拒绝原建议 D 的直接实现。** 现阶段不创建完整 blind degradation encoder，不复用已失败的 continuous-SPD 分支，不启动新的 5-epoch 训练，也不将 WACV 代码纳入交付包。

**保留一个低成本、可证伪的入口：第 6 节 oracle admission。** 它首先回答三个决定性问题：补偿符号是否正确、倍率变换是否正确、退化 covariance 与 renderer covariance 的单位是否可校准。只有这三点同时得到实证支持，建议 D 才有资格进入下一轮代码实现。
