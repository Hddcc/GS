# 人脸任意尺度超分论文写作记录

本文档用于整理正式论文中的创新点、实验数据、表格和行文。数据只使用完整训练和统一测试协议的正式结果；正文按中文人脸超分与图像超分论文的常见结构组织。

## 一、论文定位与创新点

### 1. 研究问题

GaussianSR 可以通过连续坐标查询生成任意放大倍率的结果，但人脸中的眼睛、眉毛、嘴唇、鼻翼和头发等区域包含明显的方向性高频结构。仅依赖基础连续查询，容易造成局部纹理恢复不足或边缘过平滑。

### 2. 方法名称

建议中文名称：

> 基于方向性高频查询残差与边缘感知重建的任意尺度人脸超分辨率方法

英文名称可写为：

> Arbitrary-Scale Face Super-Resolution with Directional High-Frequency Query Residual and Edge-Aware Reconstruction

### 3. 两个创新点

**方向性高频查询残差。** 在 GaussianSR 的连续坐标查询端增加轻量残差分支，从输入人脸的水平、垂直和对角高频响应中构造局部方向描述，并与连续倍率编码、查询坐标和局部特征融合，预测有界 RGB 修正：

```text
I_hat(x, s) = I_base(x, s) + g * Delta_I_freq(x, s)
```

其中 `s` 为连续放大倍率，`g` 为残差门控，`Delta_I_freq` 为查询级高频修正。Gaussian 主路径、连续坐标查询方式和任意倍率输出机制保持不变。

**边缘感知重建约束。** 训练时由 HR 图像亮度梯度生成空间权重，并将权重均值归一化为 1，使结构边缘获得适度更高的重建关注度：

```text
Y = 0.299R + 0.587G + 0.114B
e = sqrt((d_x Y)^2 + (d_y Y)^2 + eps)
w = normalize(1 + 0.25 * clip(e / mean(e), 0, 4))
L = mean(w * |I_hat - I_gt|)
```

论文中将其表述为训练阶段的边缘感知重建约束，不把它单独包装成与主网络并列的大型新网络。

## 二、附件论文给出的中文写法规律

以下归纳来自用户提供的 10 篇论文，作用是确定写法和表格组织，不改变本项目的实验结论。

| 参考论文 | 可借鉴的写法 | 对本文的采用方式 |
|---|---|---|
| Dual Associated Encoder for Face Restoration | 先说明退化和域差异，再提出双分支；主结果与消融分表；同时报告像素、感知和人脸结构指标 | 用“任意倍率查询不足—方向高频补偿—边缘约束”的问题链；PSNR/SSIM/LPIPS 分开解释 |
| IC-FSRDENet | 将结构保真与纹理增强分成两个目标，结果段同时讨论重建质量和感知质量 | 将高频残差解释为细节补偿，将边缘约束解释为结构保真；不把两者混写成单一指标 |
| 基于 Transformer 的块内块间双聚合的单图像超分辨率重建网络 | 中文摘要先交代问题，再按模块说明方法，实验部分设“实验设置—消融实验—对比实验” | 采用“问题—模块—实验验证”的三段式方法介绍 |
| 基于跨尺度低秩约束的单幅图像盲超分辨率算法 | 先交代未知退化和约束目标，再说明退化建模与重建结果 | 在引言中只强调连续倍率和人脸高频结构，不把本方法写成盲退化方法 |
| 基于多尺度空间自适应注意力网络的轻量级图像超分辨率方法 | 明确列出训练/测试数据集、评价指标和 YCbCr 的 Y 通道计算方式；消融表包含参数量和 FLOPs | 在实验设置中明确数据划分、倍率和颜色空间；不额外加入当前未统一测量的复杂度数据 |
| 基于深层特征差异性网络的图像超分辨率算法 | 用模块名称组织方法章节，主结果按倍率列出 PSNR/SSIM，之后再给视觉结果 | 用“方向性高频查询残差”和“边缘感知重建约束”作为两个稳定小节 |
| 基于异构注意力网络的图像超分辨率重建方法 | 主结果按 ×2、×3、×4 分表，消融与计算代价另表 | 本文因倍率连续且含非整数值，按倍率列行；主表不混入训练过程信息 |
| 多尺度大核注意力特征融合网络的图像超分辨率重建 | 摘要采用“目的—方法—结果—结论”；实验包含定量表、消融表和视觉对比 | 采用同样的中文摘要结构，结果只报告正式数据支持的提升范围 |
| 基于像素对比学习的图像超分辨率算法 | 将损失函数与网络结构分别说明；PSNR/SSIM 和 LPIPS 的作用分开解释；消融逐项增加模块 | 将边缘约束作为训练设计单独说明，将高频分支作为结构设计单独说明 |
| 融合通道注意力的跨尺度 Transformer 图像超分辨率重建 | 摘要明确“目的、方法、结果、结论”；实验先写数据集和指标，再写消融、定量和视觉结果 | 采用“数据与协议—主结果—视觉结果—消融”的正文顺序 |

由这些论文可以确定：中文稿件通常使用三线表，表题写清数据集、倍率和指标，正文先给结论性比较，再解释模块原因。任意尺度任务不必强行套用固定 ×2/×3/×4 表格，而应让“倍率”为表格行，并保留非整数倍率。

## 三、摘要写法

摘要按“目的—方法—结果—结论”组织，避免写训练过程和内部运行名称。可直接据正式结果修改以下文字：

> **目的** 针对连续任意尺度人脸超分辨率中局部高频结构恢复不足和边缘过平滑问题，提出一种基于方向性高频查询残差与边缘感知重建的任意尺度人脸超分辨率方法。**方法** 在 GaussianSR 连续坐标查询端引入方向性高频残差分支，将局部方向高频描述与连续倍率编码融合以预测查询级 RGB 修正；同时根据高分辨率人脸图像的亮度梯度构造归一化空间权重，增强结构边缘的重建约束。**结果** 在 CelebA 和 Helen 数据集的统一任意尺度测试中，所提方法在多个中倍率和非整数倍率上取得优于基线的 PSNR 或 SSIM，并在部分倍率上改善 LPIPS。**结论** 方向性高频查询与边缘感知重建能够在不改变连续坐标输出机制的情况下改善人脸局部结构恢复，说明该方法对任意尺度人脸超分辨率具有一定有效性。

关键词建议：`人脸超分辨率；任意尺度超分辨率；连续坐标查询；高频残差；边缘感知重建`。

## 四、正式数据与实验设置

### 1. 数据集与划分

| 数据集/划分 | 图像数 | 用途 |
|---|---:|---|
| CelebA train | 18,000 | 模型训练 |
| CelebA val | 500 | 选择最佳模型 |
| CelebA test | 1,000 | 同分布测试 |
| Helen test | 50 | 跨数据集测试 |

所有图像为 128×128 RGB 人脸图像。训练阶段从 HR 图像在线生成 LR 输入，连续训练倍率范围为 `[1.5, 8]`；正式测试倍率为 `1.5、2、2.5、3.5、4、5.5、7.5、8`。Baseline 与本文方法使用相同数据划分、退化方式、训练策略、随机种子和评价脚本。正式训练采用 Adam、batch size 12、从头训练 1,350 个 epoch，验证倍率为 ×4。

### 2. 指标说明

正文表头使用中文论文常见写法：`PSNR (dB) ↑`、`SSIM ↑`、`LPIPS ↓`。当前正式评价实现中，PSNR 和 SSIM 按 YCbCr 的 Y 通道计算，LPIPS 使用 Alex 网络；表注中说明这一点，不能仅通过修改表头把 Y 通道结果表述为 RGB 指标。

指标解释只写一次：PSNR 衡量像素保真度，SSIM 衡量结构相似性，LPIPS 衡量感知特征距离且数值越低越好。

## 五、正式定量结果

主表保留全部八个测试倍率，避免把任意尺度任务写成少数固定倍率任务。由于表格较宽，投稿时可拆为“PSNR/SSIM 表”和“LPIPS 表”；下面的合并表适合直接整理为三线表。

### 1. CelebA

| 倍率 | Baseline PSNR (dB) | Ours PSNR (dB) | Baseline SSIM | Ours SSIM | Baseline LPIPS | Ours LPIPS |
|---:|---:|---:|---:|---:|---:|---:|
| 1.5 | 41.4028 | 41.4025 | 0.986933 | **0.986965** | 0.005654 | **0.005651** |
| 2.0 | 37.5225 | **37.5243** | 0.970110 | **0.970201** | 0.025976 | **0.025921** |
| 2.5 | 35.1462 | **35.1489** | 0.950803 | **0.950967** | 0.046747 | **0.046249** |
| 3.5 | 32.3548 | **32.3594** | 0.913172 | **0.913377** | 0.087112 | **0.086426** |
| 4.0 | 31.3718 | **31.3817** | 0.894269 | **0.894453** | 0.108320 | **0.107704** |
| 5.5 | 29.2378 | **29.2498** | 0.845757 | **0.845816** | 0.145636 | **0.144804** |
| 7.5 | 27.1493 | **27.1613** | **0.784833** | 0.784517 | **0.208237** | 0.208670 |
| 8.0 | 26.7214 | **26.7430** | **0.770188** | 0.770118 | 0.220357 | **0.220196** |

平均差：PSNR `+0.0080 dB`，SSIM `+0.000044`，LPIPS `-0.000302`。CelebA 的主结论应写成“PSNR 在 7/8 个倍率上提高，SSIM 在 6/8 个倍率上提高，LPIPS 在 7/8 个倍率上降低”，不要写成所有指标和所有倍率均提升。

### 2. Helen

| 倍率 | Baseline PSNR (dB) | Ours PSNR (dB) | Baseline SSIM | Ours SSIM | Baseline LPIPS | Ours LPIPS |
|---:|---:|---:|---:|---:|---:|---:|
| 1.5 | **42.4254** | 42.4107 | 0.989877 | **0.989888** | 0.004127 | **0.004080** |
| 2.0 | **38.5584** | 38.5306 | 0.977821 | **0.977825** | 0.019333 | **0.018861** |
| 2.5 | 35.9657 | **36.0235** | 0.963394 | **0.963698** | 0.039328 | **0.038634** |
| 3.5 | 32.9502 | **32.9934** | 0.933290 | **0.933826** | 0.084297 | **0.084113** |
| 4.0 | 31.9708 | **32.0081** | 0.917930 | **0.918488** | **0.101366** | 0.101703 |
| 5.5 | 29.9248 | **29.9315** | 0.879278 | **0.879363** | **0.144962** | 0.146769 |
| 7.5 | 27.6639 | **27.7087** | 0.823353 | **0.823996** | **0.199765** | 0.201139 |
| 8.0 | 27.3190 | **27.3361** | 0.811328 | **0.812222** | 0.213716 | **0.211815** |

平均差：PSNR `+0.0206 dB`，SSIM `+0.000380`，LPIPS `+0.000027`。Helen 的结果应强调中倍率 PSNR/SSIM 的改善，同时如实说明 LPIPS 在部分倍率上有波动。

### 3. 结果分析段落模板

CelebA 可写：

> 表 X 给出了 CelebA 测试集上的定量结果。与 GaussianSR Baseline 相比，本文方法在 ×2、×2.5、×3.5、×4、×5.5、×7.5 和 ×8 的 PSNR 均有提高，其中 ×4 的提升为 0.0099 dB；SSIM 和 LPIPS 也在多数倍率上保持改善。×1.5 的 PSNR 略有下降，说明边缘增强对极低倍率并非始终有效，但整体结果支持该方法对中倍率任意尺度重建的改善作用。

Helen 可写：

> 表 Y 给出了跨数据集测试结果。本文方法在 ×2.5、×3.5、×4、×5.5、×7.5 和 ×8 的 PSNR 高于 Baseline，其中 ×2.5 至 ×4 的提升较为稳定；SSIM 在八个倍率上均提高。LPIPS 在 ×1.5、×2、×2.5、×3.5 和 ×8 有所降低，在 ×4、×5.5 和 ×7.5 略有上升，表明跨数据集感知质量仍存在倍率相关差异。

## 六、消融与视觉结果的写法

### 1. 消融表

消融实验采用“Baseline—加入方向性高频查询残差—加入边缘感知重建—完整方法”的逐项结构，每次只改变一个模块；各模型必须使用与正式主结果相同的完整训练协议。表格按一个代表性非整数倍率和一个整数倍率列出 PSNR/SSIM/LPIPS，避免把不同训练时长的结果混在一起。

建议表题：

> 表 Z  不同组成模块对人脸任意尺度超分辨率性能的影响（CelebA 测试集）

建议表头：

```text
模型 | 方向性高频查询残差 | 边缘感知重建 | PSNR (dB) ↑ | SSIM ↑ | LPIPS ↓
```

正文分析顺序为：先报告完整方法相对 Baseline 的变化，再说明高频分支和边缘约束各自带来的变化，最后结合局部纹理图解释原因。未按完整协议得到的短程结果不作为该表的数值来源。

### 2. 视觉对比图

每组图统一展示 `LR/Bicubic、Baseline、Ours、GT`，使用同一裁剪区域、同一显示范围和同一倍率。建议选择一张 CelebA 和一张 Helen 图像，各展示一个非整数倍率和一个整数倍率；图注写清数据集、图像编号和倍率。视觉分析只描述眼睛、眉毛、嘴唇、发丝和轮廓等可观察区域，不用“明显优于”这类没有图像依据的绝对措辞。

## 七、正文行文大纲

### 第1章 引言

1. 介绍人脸超分辨率和任意尺度超分辨率的应用价值。
2. 指出连续倍率输出虽然灵活，但局部方向性高频结构容易被平滑。
3. 概括本文的两个设计：查询级方向性高频残差、边缘感知重建约束。
4. 列出贡献：连续查询端的高频补偿、结构边缘监督、CelebA/Helen 统一任意倍率验证。

### 第2章 相关工作

1. 人脸超分辨率与人脸结构保持。
2. 任意尺度/连续坐标超分辨率。
3. 高频建模、边缘约束和感知质量评价。

### 第3章 方法

1. GaussianSR 基线与问题定义。
2. 方向性高频特征提取和连续倍率编码。
3. 查询级残差融合及有界 RGB 输出。
4. 边缘感知重建约束。
5. 整体训练目标和推理流程。

### 第4章 实验结果与分析

1. 数据集、数据划分、退化方式和实现细节。
2. CelebA 与 Helen 的完整八倍率定量结果。
3. 消融实验：高频残差、边缘约束和完整模型。
4. 视觉对比与局部结构分析。
5. 结果局限：低倍率或个别 LPIPS 指标存在波动。

### 第5章 结论

> 本文面向任意尺度人脸超分辨率中的局部高频结构恢复问题，在 GaussianSR 连续查询端引入方向性高频残差，并结合边缘感知重建约束。在 CelebA 和 Helen 的统一测试中，本文方法在多个中倍率和非整数倍率上取得了相对基线的改善，同时保持了任意倍率输出能力。实验也表明，极低倍率和部分感知指标仍存在波动，后续可进一步研究更强的身份结构先验和跨倍率一致性约束。

## 八、三线表 LaTeX 模板

```latex
\begin{table}[t]
\centering
\caption{CelebA 测试集不同倍率下的定量结果。}
\label{tab:celeba_main}
\begin{tabular}{ccccccc}
\toprule
\multirow{2}{*}{倍率} & \multicolumn{2}{c}{PSNR (dB)$\uparrow$} &
\multicolumn{2}{c}{SSIM$\uparrow$} & \multicolumn{2}{c}{LPIPS$\downarrow$} \\
& Baseline & Ours & Baseline & Ours & Baseline & Ours \\
\midrule
1.5 & 41.4028 & 41.4025 & 0.986933 & 0.986965 & 0.005654 & 0.005651 \\
2.0 & 37.5225 & 37.5243 & 0.970110 & 0.970201 & 0.025976 & 0.025921 \\
2.5 & 35.1462 & 35.1489 & 0.950803 & 0.950967 & 0.046747 & 0.046249 \\
3.5 & 32.3548 & 32.3594 & 0.913172 & 0.913377 & 0.087112 & 0.086426 \\
4.0 & 31.3718 & 31.3817 & 0.894269 & 0.894453 & 0.108320 & 0.107704 \\
5.5 & 29.2378 & 29.2498 & 0.845757 & 0.845816 & 0.145636 & 0.144804 \\
7.5 & 27.1493 & 27.1613 & 0.784833 & 0.784517 & 0.208237 & 0.208670 \\
8.0 & 26.7214 & 26.7430 & 0.770188 & 0.770118 & 0.220357 & 0.220196 \\
\bottomrule
\end{tabular}
\end{table}
```

表注建议写为：`PSNR 和 SSIM 在 YCbCr 的 Y 通道上计算，LPIPS 使用 Alex 网络；粗体表示同一倍率和同一指标下的较优结果。`

## 九、正式结果来源

- 完整 CelebA/Helen 八倍率结果：`毕业论文第四章实验与方法设计.md` 第 9.2 节。
- 正式 Baseline：`save/face_gaussian_baseline_seed1_formal_v2protocol`。
- 正式本文方法：`save/face_gaussian_frequency_residual_edge_seed1_formal_v2protocol`。
- 正式逐图复核：服务器 `results/face_formal_v2protocol/paired_audit_20260919.json`。
