# 任意尺度超分辨率与 Gaussian 表达热点调研（2026-08-02）

## 调研目的

本次调研用于选择下一版可落地的毕业论文创新点。筛选标准为：

1. 与人脸任意尺度 2D GaussianSR 直接相关。
2. 不照抄他人模块、代码、公式组合或实验结论，只提炼可迁移的问题定义与设计原则。
3. 不引入新的标注、外部预训练权重、定制 CUDA rasterizer 或复杂训练流程。
4. 能以 smoke → 5 epoch → 30 epoch 的现有低成本协议验证。

以下资料来自 arXiv 页面与摘要。2026 年条目属于较新的预印本，未默认视为已经同行评审；论文写作时必须再次核实正式发表信息。

## 热点方向与取舍

| 方向 | 代表工作 | 可参考思想 | 当前项目判断 |
|---|---|---|---|
| 自适应 Gaussian 资源分配 | [QuADA-GS](https://arxiv.org/abs/2606.29400) | 根据 LR 局部复杂度分配 Gaussian/特征预算，避免所有位置均匀处理 | 很新且与项目高度相关，但需要不规则拓扑、预算路由、层级 pointer convolution，工程和实验成本过高，暂不采用 |
| 高阶 Gaussian 表达 | [Resonant Brane Splatting](https://arxiv.org/abs/2606.29453) | 普通 Gaussian 是平滑低通 primitive；带符号高阶局部模式更适合边缘、对比和纹理 | 问题判断最适合当前瓶颈；不复制 Brane、Gaussian-Hermite primitive、新 rasterizer 或 culling，只参考“给低通 Gaussian 补充带符号局部模式”的原则 |
| 直接预测连续 Gaussian | [GSASR](https://arxiv.org/abs/2501.06838)、[ContinuousSR](https://arxiv.org/abs/2503.06617) | 从 LR 特征直接预测 image-conditioned Gaussian，并进行 scale-aware 连续渲染 | 需要替换当前 GaussianSR 主渲染流程和 CUDA 实现，工作量接近重做 baseline，不适合作为当前增量创新 |
| 可缩放状态空间模型 | [S3Mamba](https://arxiv.org/abs/2411.11906)、[EAMamba](https://arxiv.org/abs/2506.22246) | 用状态空间模型增强长程依赖，并让状态转移或扫描感知目标倍率 | 是恢复领域热点，但现有环境没有成熟 Mamba 依赖；手写 selective scan 风险高，额外消融也多，暂不采用 |
| 频率/小波 Transformer | [FIT](https://arxiv.org/abs/2504.18818)、[LIWT](https://arxiv.org/abs/2411.06442) | 在空间与频率域联合建模，利用高频先验恢复纹理 | 热点明确，但本项目的 edge loss、多频带 appearance 注入已连续失败；继续扩展 FFT/DWT 会重复路线并增加复杂度 |
| 多尺度隐式 Transformer | [MSIT](https://arxiv.org/abs/2403.06536) | 让 latent code 和注意力显式适配多倍率 | 本项目已测试 scale conditioning、scale MoE 与 curriculum，收益不足；不继续做倍率模块堆叠 |
| 连续隐式注意力 | [CiaoSR](https://arxiv.org/abs/2212.04362) | 学习局部 ensemble 权重，并以 scale-aware attention 引入更大范围信息 | 可作为备选，但当前 GaussianSR 已有 Gaussian 聚合和 Fourier query decoder；先测试更贴近 Gaussian 表达缺陷的方案 |
| 跨层/稀疏注意力 | [ACLA](https://arxiv.org/abs/2203.03619)、[LCoA](https://arxiv.org/abs/2404.04922)、[HSPA](https://arxiv.org/abs/2305.15768) | 自适应选择跨层或高相似度特征，减少无关信息聚合 | 可用于未来替换静态 appearance fusion，但模块与消融较多，不作为本轮首选 |

## 选定方向：带符号局部 Gaussian moment query basis

### 问题判断

GaussianSR 的平滑 Gaussian kernel 擅长连续聚合，但局部边缘和曲率需要依赖多 primitive 叠加及后续解码。此前直接向 appearance 分支注入 edge/frequency 特征会在后期降低 PSNR，说明问题不宜继续放在 LR appearance fusion 中解决。

### 本项目改法

保持普通几何-外观编码器、Gaussian kernel、rasterization 和方向频率 residual 不变，只在连续 query residual MLP 的输入中增加 5 个固定解析局部 moment：

- 水平与垂直一阶带符号 moment，用于表达局部方向变化。
- 水平、垂直与交叉二阶带符号 moment，用于表达曲率、角点和细纹理变化。
- 所有 moment 使用局部 Gaussian envelope 限制作用范围，并与 relative coordinate、cell 和 scale feature 一起输入 residual MLP。

该方案不创建新的 Gaussian primitive，不修改 splatting kernel，不实现 RBS 的 Gaussian-Hermite mode、Brane rasterizer 或 turning-point culling，也不使用其代码。它只是把“低通 Gaussian 需要带符号局部表达”的问题判断转化为现有 query decoder 的轻量解析条件。

### 预期成本与判定

- 只增加 residual MLP 第一层的 5 个输入维度，预计新增约 320 个参数。
- 不增加训练损失、数据或服务器依赖。
- 先以普通 L1 几何-外观模型作为同协议参考运行 5 epoch。
- 最终 PSNR 必须超过 `28.2235 dB`，5 次均值必须超过 `27.7003 dB`；两个条件同时满足才进入 30 epoch。

## 论文使用边界

论文可以把热点调研用于相关工作和设计动机，但不能宣称 Gaussian-Hermite、高阶 Gaussian、Mamba、频率 Transformer、注意力或自适应 Gaussian 分配是本项目原创。若候选最终有效，本项目创新只表述为：在人脸任意尺度 GaussianSR 的现有连续 query residual 中，引入轻量带符号局部 Gaussian moment 条件，以补充平滑 Gaussian 聚合对局部方向与曲率表达的不足。
