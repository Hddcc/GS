# 建议 F：完整 GSASR 迁移与零训练门禁复核

日期：2026-09-15  
候选来源：ICCV 2025（CCF-A）《Generalized and Efficient 2D Gaussian Splatting for Arbitrary-scale Super-Resolution》  
当前状态：只完成一手来源复核和零训练门禁设计；尚未安装官方环境、下载权重或运行 CelebA 评测，不属于实验结果。

## 1. 当前失败结果与归因边界

### 1.1 IAFM IDM+ARM 的明确结论

建议 E 的 seed-1、5 epoch 探针已正常完成，机制门禁通过但质量门禁失败：

| 指标 | 结果 |
|---|---:|
| 五轮训练 PSNR delta 均值 | `-0.0222 dB` |
| 最后一轮 PSNR delta | `-0.0328 dB` |
| x2 / x4 / x8 held-out delta | `-0.0915 / -0.0291 / -0.0156 dB` |
| held-out 三倍率均值 | `-0.0454 dB` |
| IDM spatial std | `0.019958` |
| 四个 ARM 最小 raw / scaled response | `0.010740 / 0.001074` |
| candidate-baseline output response | `0.004361` |

这说明失败不是分支未激活、无梯度或实现退化为恒等映射，而是 soft IDM+ARM 在当前人脸任意尺度 GaussianSR 协议下确实改变了特征，却没有改善配对重建质量，且 x2 退化最明显。因此停止 entropy weight、ARM 数量、残差倍率和训练长度扫描是合理的。

### 1.2 既有失败方向的共同模式

此前 scale-consistent raster、IMLUT feature mix、CATANet-IASA、HCSA、PromptSR-GAPL、continuous-SPD、static-LSS、MSConvStar、`m=4` multi-primitive 和 IAFM 均完成了各自的机制检查。多数分支的响应、梯度、注意力/路由统计或几何参数均非零，但 x2/x4/x8 聚合仍为负。典型 held-out mean 分别约为：

| 方向 | held-out mean delta | 关键现象 |
|---|---:|---|
| scale-consistent raster | `-0.0269 dB` | 跨倍率差异未下降 |
| IMLUT feature mix | `-0.0118 dB` | 混合与 refiner 均有响应 |
| CATANet-IASA | `-0.0305 dB` | x2 为 `-0.0765 dB` |
| HCSA | `-0.0919 dB` | x2 为 `-0.2054 dB` |
| PromptSR-GAPL | `-0.0917 dB` | x2 为 `-0.2315 dB` |
| continuous-SPD | `-0.0652 dB` | 协方差改变量大，但质量下降 |
| static-LSS | `-0.0297 dB` | 连续位移很小，取整后全为零 |
| MSConvStar | `-0.0754 dB` | 多尺度分支活跃但三倍率全降 |
| multi-primitive `m=4` | `-0.0054 dB` | 机制活跃且最接近持平，仍未转正 |
| IAFM IDM+ARM | `-0.0454 dB` | 信息分支活跃，x2 下降最大 |

这些结果共同否定的是“在原 GaussianSR 主体上追加小型残差、注意力、局部位移或单项几何调制，并用短程随机初始化探针期待稳定增益”的路线。它们不能推出所有 Transformer、所有多高斯表示或所有连续 rasterizer 都无效。

下一步不应继续更换小插件，而应先验证一套在同 EDSR 主干下、公开结果显著优于 GaussianSR 的完整高斯表示与渲染系统是否能跨域迁移到人脸数据。

## 2. 为什么完整 GSASR 尚未被既有实验否定

项目此前的 `m=4`、center-drift、SPD 和 raster 实验均保留了原 GaussianSR 的主要表示链路：像素/特征位置上的 latent Gaussian、有限 Gaussian bank、latent feature rasterization 以及 Fourier/MLP RGB decoder。它们只改变高斯数量、中心、协方差、opacity 或局部采样规则中的一部分。

GSASR 改写的是从编码特征到输出图像的整条后半段：

1. 每个 LR feature window 使用可学习 Gaussian embeddings，并通过图像条件 cross-attention 形成 content-conditioned Gaussians。
2. Gaussian embeddings 之间进行 shifted-window self-attention，并显式注入逆倍率条件。
3. 五个解耦头直接预测 `sigma`、`rho`、`alpha`、RGB 和位置 offset，不再先 rasterize latent feature 后交给 Fourier decoder 查询 RGB。
4. 高斯中心 `mu=p+o` 可随输入内容移动，所有高斯属性由统一 decoder 联合预测。
5. 最终高斯密度为 `m=16`，并使用作者实现的连续、可微 CUDA rasterizer。

因此：

- `m=4` multi-primitive 只证明“旧表示链路中增加四个 primitive”未带来正聚合收益，不等价于 GSASR 的 content-conditioned `m=16` 直接 RGB Gaussians。
- center-drift 只在旧 latent 表示上增加中心偏移，没有同时替换属性生成器、Gaussian interaction 和 RGB 合成方式。
- continuous-SPD 只放开旧 primitive 的协方差/opacity；其协方差偏移过大以及 clamp/saturation 问题不能代表 GSASR 的端到端属性约束。
- scale-consistent raster 只调整旧 renderer 的核/参考倍率，不具备 GSASR 的连续 sampling interval、直接 RGB 合成和 CUDA 并行 rasterization。

补充材料 Table 12 也表明，GSASR 完整系统内部的 `m=4` 已经有效，但 `m=16` 更好：

| `m` | DIV2K x2 PSNR | x4 PSNR | x8 PSNR |
|---:|---:|---:|---:|
| 1 | 36.36 | 30.61 | 27.00 |
| 4 | 36.54 | 30.80 | 27.14 |
| 9 | 36.55 | 30.80 | 27.16 |
| 16 | **36.65** | **30.89** | **27.22** |

这里的 `m=4` 与项目旧 multi-primitive 的名字相似但计算图不同，不能互相替代或直接比较。

## 3. 一手定量证据

### 3.1 主文 Table 1：同 EDSR、同 DIV2K 的 GaussianSR 对照

主文明确说明所有方法使用相同 EDSR-baseline encoder。按 Y channel PSNR：

| 倍率 | GaussianSR | GSASR | delta |
|---:|---:|---:|---:|
| x2 | 36.10 | 36.65 | `+0.55 dB` |
| x4 | 30.46 | 30.89 | `+0.43 dB` |
| x8 | 26.76 | 27.22 | `+0.46 dB` |
| x16 | 23.80 | 24.38 | `+0.58 dB` |
| x24 | 22.41 | 22.90 | `+0.49 dB` |
| x30 | 21.75 | 22.19 | `+0.44 dB` |

这些差值远大于本项目多个短程插件的波动幅度，并且从训练范围内倍率延伸到高倍率仍保持正值。论文训练倍率为 `[1,4]`，x8 及以上属于倍率外泛化。

### 3.2 补充材料 Table 2：结构密集图像上的结果

同 EDSR、Urban100、Y channel PSNR：

| 倍率 | GaussianSR | GSASR | delta |
|---:|---:|---:|---:|
| x2 | 32.22 | 33.27 | `+1.05 dB` |
| x4 | 26.19 | 27.01 | `+0.82 dB` |
| x8 | 22.36 | 23.09 | `+0.73 dB` |
| x12 | 20.68 | 21.39 | `+0.71 dB` |

Manga109 同表的 GaussianSR→GSASR 为 x2 `38.55→39.06`、x4 `30.54→31.16`、x8 `24.26→25.09`。该证据支持其对重复结构、边缘和纹理密集区域的优势，但不能直接保证在人脸平滑区域上仍有同等增益。

### 3.3 补充材料 Table 8：与本项目更接近的 RGB 指标

当前项目 held-out 使用 RGB PSNR，因此 RGB 表比论文主表的 Y channel 更接近目标协议。同 EDSR 的 RGB PSNR 对照如下：

| 数据集/倍率 | GaussianSR | GSASR | delta |
|---|---:|---:|---:|
| DIV2K x2 | 34.61 | 35.13 | `+0.52 dB` |
| DIV2K x4 | 29.01 | 29.43 | `+0.42 dB` |
| DIV2K x8 | 25.29 | 25.76 | `+0.47 dB` |
| Urban100 x2 | 30.59 | 31.61 | `+1.02 dB` |
| Urban100 x4 | 24.68 | 25.48 | `+0.80 dB` |
| Urban100 x8 | 20.90 | 21.63 | `+0.73 dB` |

### 3.4 主文 Table 2：完整流水线成本

在 720×720 输出、单张 NVIDIA A100、包含 encoder/decoder/rendering 的完整流程中：

| 倍率 | 方法 | PSNR | 时间 | GPU memory |
|---:|---|---:|---:|---:|
| x2 | GaussianSR | 未用于本轮结论 | 754 ms | 5200 MB |
| x2 | GSASR | 未用于本轮结论 | 1573 ms | 13367 MB |
| x4 | GaussianSR | 31.49 | 686 ms | 5048 MB |
| x4 | GSASR | 32.01 | 543 ms | 3420 MB |
| x8 | GaussianSR | 27.77 | 666 ms | 5012 MB |
| x8 | GSASR | 28.25 | 195 ms | 1051 MB |

GSASR 的效率优势不是所有倍率都成立：x2 明显更慢且显存更高，x4/x8 才同时改善时间和显存。这些成本数字依赖 A100、作者 CUDA 实现、720×720 输入协议和论文版本，不能直接当作本服务器 GPU 的预计耗时或显存。

## 4. 架构公式与官方代码定位

### 4.1 直接 RGB Gaussian 表示

主文 Eq.1 定义单个 Gaussian 对位置 `(x,y)` 的 RGB 贡献：

```text
G(x,y) = alpha * c * f(x,y)
```

其中 `c` 是归一化 peak RGB，`alpha` 是 opacity；Eq.2 的 `f(x,y)` 是由 `sigma_x`、`sigma_y`、相关系数 `rho` 和中心 `mu=(mu_x,mu_y)` 决定的二维高斯。Eq.3 将 LR 图像写为所有 Gaussians 的直接求和：

```text
I_LR(x,y) = sum_i G_i(x,y)
```

Eq.4 通过倍率 `s` 改变连续域采样位置：

```text
I_SR(x,y;s) = sum_i G_i(x/s, y/s)
```

这与原 GaussianSR 先生成连续 latent feature、再用查询 decoder 预测 RGB 的链路不同。

### 4.2 图像条件 Gaussian decoder

主文 Eq.5 使用 Gaussian embedding 作为 query、EDSR feature 作为 key/value：

```text
Attention(Q,K,V) = SoftMax(QK^T / sqrt(d) + B) V
```

官方论文版本配置与代码对应如下：

| 项目 | 官方值 | 定位 |
|---|---:|---|
| EDSR feature channels | 64 | `TrainTestGSASR/options/train/paper/train_GSASR_EDSR-Baseline_paper_bicubic_x1_4.yml` |
| Gaussian embedding dim | 180 | 同上；`basicsr/archs/fea2gs_arch.py:392-417` |
| attention heads | 6 | 同上；`fea2gs_arch.py:392-429` |
| LR feature window | 12 | 同上 |
| seed embeddings/window | 144 | `fea2gs_arch.py:392-411` |
| condition injection | 1 block × 2 layers | 配置及 `fea2gs_arch.py:419-425` |
| Gaussian interaction | 6 blocks × 6 layers | 配置及 `fea2gs_arch.py:427-429` 后续定义 |
| scale condition | `1/scale` 经两层 MLP | `fea2gs_arch.py:480-484, 519-525` |
| density expansion | 两次 ×2 PixelShuffle | `fea2gs_arch.py:486-490, 549-552` |
| 五个属性输出 | sigma/rho/alpha/RGB/mean | `fea2gs_arch.py:558-574` |
| 自由中心 | predicted mean + reference position | `fea2gs_arch.py:564-571` |

官方 rasterizer 的 Python 接口位于：

```text
TrainTestGSASR/basicsr/utils/gaussian_splatting.py:158-210
```

其中 sampling step 为 `default_step_size/final_scale`，并对 `sigma_x/sigma_y/rho/alpha/RGB/coords` 做显式范围映射；实际 CUDA extension 由仓库根目录 `setup_gscuda.py` 编译，源文件位于 `utils/gs_cuda_dmax/gswrapper.cpp` 和 `utils/gs_cuda_dmax/gs.cu`。

### 4.3 官方训练事实

- DIV2K 训练集，LR patch `48×48`。
- 连续倍率随机采样 `[1.0,4.0]`。
- L1 loss，Adam，初始学习率 `2e-4`，warm-up 2,000 iterations。
- 4 张 A100、总 batch 64、500,000 iterations。
- x8 及更高倍率结果来自训练倍率范围外泛化。

因此不能把“随机初始化后只跑 5 epoch”作为完整 GSASR 的有效否决实验；本项目 5 epoch 约 7,500 optimizer steps，与官方 500,000 iterations 相差约两个数量级。

## 5. 唯一下一方向

建议 F 是当前唯一进入下一门禁的方向：

> 使用作者发布的论文版 EDSR GSASR 完整 checkpoint 和未改动推理链路，先在本项目固定的 100-face x2/x4/x8 held-out manifest 上做零训练跨域准入。只有通过后，才讨论将完整 Gaussian decoder、直接 RGB Gaussians 和 CUDA rasterizer迁移到人脸项目。

当前不并行开发 ATD、Mamba、扩散、另一种注意力、另一种 SPD、更多 `m` 或 IAFM v2。ATD-light 的固定 x2/x4 结果虽强，但缺少单模型任意倍率的直接证据；LWay 面向未知真实退化，与当前同分布 bicubic 协议不匹配。GSASR 是与现有 GaussianSR 最直接、同 EDSR 对照最完整、且可以先零训练证伪的候选。

## 6. 零训练 100-face 准入流程

### 6.1 固定输入和参考

1. 复用现有 100 张 CelebA held-out manifest，图像顺序、HR 文件、x2/x4/x8 LR 生成方式和随机种子全部冻结。
2. 参考模型固定为当前 face baseline：`save/face_gaussian_baseline_seed1_probe_full5/epoch-5.pth` 及其保存配置。
3. 候选固定为官方 `GSASR_paper/EDSR` checkpoint，不进行人脸 fine-tune、参数融合、TTA、self-ensemble 或后处理。
4. 使用作者论文版本推理代码，不使用 Enhanced 版本的 ROPE、Flash Attention、AMP 训练技巧或 DF2K checkpoint。
5. 每个方法从同一 LR tensor/文件读取输入；候选输出裁剪到精确 HR 尺寸后再评测。

### 6.2 指标和可复现检查

- 主门禁使用项目现有 RGB PSNR 计算，不切换为论文 Y channel + shave protocol。
- 输出逐图 CSV，包含 image id、scale、baseline PSNR、candidate PSNR 和 delta。
- 检查 x2/x4/x8 输出尺寸准确、值有限、无空图或异常 clamp。
- 对固定样本和三个倍率各重复推理两次，要求输出最大误差不超过 `1e-6`。
- 记录 checkpoint SHA-256、官方代码 commit、环境版本、CUDA extension 编译日志、物理 GPU 和总耗时。
- 可额外记录 SSIM/LPIPS，但它们不参与本轮预注册主门禁，不能替代 PSNR 失败。

### 6.3 预注册质量门槛

在同一 100 张、同一倍率、同一 RGB evaluator 上：

```text
mean(delta_x2, delta_x4, delta_x8) > 0
delta_x2 >= -0.030 dB
delta_x4 >= -0.030 dB
delta_x8 >= -0.030 dB
```

这里每个 `delta_xs` 是该倍率 100 张图像 PSNR 的配对均值差。不得按最好 checkpoint、最好图像子集或最好倍率重新选择门槛。

该门禁刻意保留小幅单倍率容忍，但要求三倍率聚合严格为正。它只回答“官方完整系统是否具有值得迁移的人脸跨域信号”，不回答“GSASR 架构在人脸公平训练下是否优于当前模型”。

## 7. 门禁后的唯一动作

### 7.1 若零训练门禁失败

- 立即记录 `GSASR ZERO-TRAIN ADMISSION FAILED` 并停止建议 F。
- 不移植 decoder/rasterizer，不做人脸 fine-tune，不扫描 `m=1/4/9/16`、window、raster ratio 或 checkpoint。
- 不以外部预训练数据不同为理由反复放宽门槛，也不把个别倍率或少数图像的正值包装成提升。
- 返回论文保底候选，重新讨论研究问题，而不是继续插件试错。

### 7.2 若零训练门禁通过

- 先记录 `GSASR ZERO-TRAIN ADMISSION PASSED`；该状态仍不是正式论文结果。
- 在新的 `experiment/face-full-gsasr` 分支完整移植 paper-version EDSR + Fea2GS + CUDA rasterizer，保留官方模块边界和属性约束，不先做 face-specific 小改动。
- 第一阶段使用官方 EDSR GSASR checkpoint warm-start，运行单 seed 的人脸适配探针；不得随机初始化后用 5 epoch 直接否决。
- 在决定正式训练前另行预注册公平 A/B：相同 CelebA 数据、相同 optimizer steps、batch、倍率分布、损失、随机种子与评测协议。外部预训练 candidate 与从头训练 baseline 的结果只能作为工程候选，不能作为架构公平消融。
- 完整迁移先证明可复现官方输出、CUDA/Python rasterizer 一致性、属性范围、梯度和 x2/x4/x8 尺寸，再启动训练。

## 8. 公平性与结论边界

1. 官方 GSASR checkpoint 在 DIV2K 上训练 500,000 iterations；当前 face baseline 是 CelebA 5-epoch checkpoint。零训练比较同时混入架构、训练数据和训练量差异，只能做低成本迁移门禁。
2. 论文主表使用 Y-channel PSNR/SSIM，并按倍率 crop border；本项目门禁使用现有 RGB evaluator。两套绝对数值不可横向比较。
3. 论文训练倍率 `[1,4]`，本项目训练范围 `[1.5,8]`；官方 x8 是 OOD 泛化证据，但不保证 CelebA x8。
4. 论文报告单次模型，没有跨 seed 方差；本项目仍需后续 seed 2/3 和正式 CelebA/Helen 评测才能形成论文结论。
5. 720×720 时间和显存来自 A100，不能外推到当前服务器具体 GPU。
6. 零训练门禁的正结果不能写成“本项目方法提升”；负结果也只是否定当前 checkpoint 的跨域迁移价值，不是对 GSASR 论文结果的反驳。

## 9. 创新性边界

GSASR 的下列内容均属于 ICCV 2025 已公开方法，不能作为本项目原创创新：

- image-conditioned Gaussian embeddings；
- condition injection cross-attention；
- Gaussian interaction blocks；
- inverse-scale conditioning；
- 直接预测 sigma/rho/alpha/RGB/free position；
- `m=16` Gaussian density；
- scale-aware differentiable CUDA rasterization。

完整迁移若通过，只能成为新的强基线或研究平台。后续真正的人脸创新必须建立在清晰的新问题、独立设计和公平消融上，并明确区别于原论文；在出现证据前不预先命名或声称创新点。

## 10. 许可、依赖与服务器环境边界

### 10.1 许可

- 官方 GSASR 仓库根许可证为 Apache License 2.0。
- 本地固定仓库 commit：`9d2eb64a51303ce7a22fd672197185488b38e10a`。
- 本地 `LICENSE` SHA-256：`BF12F8423686999382F195B2D7BD0780BA8271A672ECE47C7BFBE5A20C66AD6B`。
- 若复制或修改官方代码，必须保留许可证、版权/归属说明；修改文件需明确标注变更，并检查/保留仓库中的 NOTICE 或第三方归属信息。
- 论文版依赖 BasicSR 和自定义 CUDA extension；每个第三方依赖仍需遵守其自身许可证。根 Apache-2.0 不能自动覆盖外部依赖、数据集或模型权重的独立条款。

### 10.2 环境和二进制依赖

官方 README 指定 Python 3.10、PyTorch 2.0.1、torchvision 0.15.2、CUDA 11.8，并称 CUDA 11.0-12.4 已测试。当前服务器 GS 环境此前为 Python 3.9 且 PyTorch/torchvision 较旧，不能假设 custom CUDA extension 可直接 ABI 兼容。

零训练门禁应优先建立独立 conda 环境，不覆盖现有 `GS` 环境。必须先执行：

1. `gscuda` 编译准入；
2. 官方 checkpoint 完整性校验；
3. 官方单图 x2/x4/x8 smoke；
4. CUDA 与 Python rasterizer 小输入数值一致性检查；
5. 再运行 100-face 推理。

官方模型权重体积和 CUDA 编译产物不得提交到项目 Git；传输时记录本地与服务器 SHA-256。

## 11. 官方来源与本地固定副本

### 11.1 官方论文与补充材料

- CVF 论文页：<https://openaccess.thecvf.com/content/ICCV2025/html/Chen_Generalized_and_Efficient_2D_Gaussian_Splatting_for_Arbitrary-scale_Super-Resolution_ICCV_2025_paper.html>
- CVF 主文 PDF：<https://openaccess.thecvf.com/content/ICCV2025/papers/Chen_Generalized_and_Efficient_2D_Gaussian_Splatting_for_Arbitrary-scale_Super-Resolution_ICCV_2025_paper.pdf>
- CVF 补充材料 PDF：<https://openaccess.thecvf.com/content/ICCV2025/supplemental/Chen_Generalized_and_Efficient_ICCV_2025_supplemental.pdf>
- arXiv：<https://arxiv.org/abs/2501.06838>

### 11.2 官方代码与权重

- 官方仓库：<https://github.com/ChrisDud0257/GSASR>
- 官方模型主页：<https://huggingface.co/mutou0308/GSASR>
- 论文版 EDSR checkpoint：<https://huggingface.co/mutou0308/GSASR/tree/main/GSASR_paper/EDSR>
- 论文版 EDSR Google Drive 备份：<https://drive.google.com/drive/folders/1rSnM1HOBaI6TpfJ0XkXhHZcjjRnS95Sb?usp=sharing>

### 11.3 本地固定材料

```text
论文正文与补充材料固定副本：
D:\个人资料\项目\任意尺度\literature_cache_20260811\2501.06838.pdf
SHA-256:
1A3734AEC741FF76CAC936B5C5EBE7C3AF5AFFCA355E3A7F9F603072FCCE5A4B

官方代码缓存：
D:\个人资料\项目\任意尺度\literature_cache_20260905_gsasr_code
commit:
9d2eb64a51303ce7a22fd672197185488b38e10a
```

## 12. 当前决定

建议 E 的 IAFM soft path 正式停止。建议 F 不立即开发或训练新的模型，第一步且唯一一步是完整官方 GSASR 的 100-face、x2/x4/x8 零训练准入。该门禁失败则停止；通过后才创建完整迁移分支和公平训练协议。
